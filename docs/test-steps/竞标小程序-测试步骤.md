# 竞标小程序模块测试步骤

> **说明：** 本模块继承 MiniBaseCase，属于小程序测试，操作均为 API 调用而非 UI 操作。

## TestBiddingCamera - 竞拍

### test_0bidding_offer - [竞拍]暗拍-出价

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DD2', 'DD4'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DD2', 'DD4']`
步骤2：等待到下一个5分钟整点 `self.wait_until_next_five_minute()`
步骤3：API调用 `self.case.bidding_offer()` 暗拍出价
步骤4：获取缓存的物品编号和出价 `cached('articlesNo')`, `cached('bidPrice')`

断言：调用方法：`self.pc.bidding_camera_assert(headers='camera', data='a', autoBidPrice=obj_2, articlesNo=obj)`

---

### test_modify_the_price - [竞拍]暗拍-修改价格

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DD2', 'DD4', '@BA2'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DD2', 'DD4', '@BA2']`
步骤2：等待默认时间 `self.wait_default()`
步骤3：API调用 `self.case.modify_the_price()` 修改暗拍价格
步骤4：获取缓存的物品编号和出价 `cached('articlesNo')`, `cached('bidPrice')`

断言：调用方法：`self.pc.bidding_camera_assert(headers='camera', data='a', autoBidPrice=obj_2, articlesNo=obj)`

---

### test_direct_auction_bidding - [竞拍]直拍-出价

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2']`
步骤2：等待到下一个5分钟整点 `self.wait_until_next_five_minute()`
步骤3：API调用 `self.case.direct_auction_bidding()` 直拍出价
步骤4：获取缓存的物品编号和出价 `cached('articlesNo')`, `cached('bidPrice')`

断言：调用方法：`self.pc.bidding_camera_assert(headers='camera', autoBidPrice=obj_2, articlesNo=obj)`

---

### test_direct_auction_price_change - [竞拍]直拍-改价

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1']`
步骤2：等待默认时间 `self.wait_default()`
步骤3：API调用 `self.case.direct_auction_price_change()` 修改直拍价格
步骤4：获取缓存的物品编号和出价 `cached('articlesNo')`, `cached('bidPrice')`

断言：调用方法：`self.pc.bidding_camera_assert(headers='camera', autoBidPrice=obj_2, articlesNo=obj)`

---

## TestBiddingMy - 我的

### test_straight_shot_change_to_self_pickup - [待发货]直拍-改自提

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1']`
步骤2：等待5分钟 `self.wait_for_five_minutes()`
步骤3：API调用 `self.case.straight_shot_change_to_self_pickup()` 直拍改自提
步骤4：获取缓存的物品编号 `cached('articlesNo')`

断言：调用方法：`self.pc.bidding_my_assert(headers='camera', i=3, articlesNo=obj)`

---

### test_direct_shot_change_to_mailing - [待收货]直拍-改邮寄

> **注意：** 此测试被 `@unittest.skip('没按最新的数据排序，测不了')` 跳过

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1']`
步骤2：API调用 `self.case.direct_shot_change_to_mailing()` 直拍改邮寄
步骤3：获取缓存的物品编号 `cached('articlesNo')`

断言：调用方法：`self.pc.bidding_my_assert(headers='camera', i=2, articlesNo=obj)`

---

### test_direct_shot_confirm_receipt - [待收货]直拍-确认收货

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1']`
步骤2：API调用 `self.case.direct_shot_confirm_receipt()` 确认收货
步骤3：获取缓存的物品编号 `cached('articlesNo')`

断言：调用方法：`self.pc.bidding_my_assert(headers='camera', i=4, articlesNo=obj)`

---

### test_direct_shooting_apply_for_after_sales - [已收货]直拍-申请售后

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1']`
步骤2：API调用 `self.case.direct_shooting_apply_for_after_sales()` 申请售后

断言：调用方法：`self.pc.camera_after_sales_order_assert(headers='camera', applyTime='now', afterDesc='售后', afterStatusStr='线上审核', tradeCategoryStr='直拍')`

---

### test_direct_auction_receives_the_difference - [可补差]直拍-接收补差

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF1']`
步骤2：API调用 `self.case.direct_auction_receives_the_difference()` 接收补差
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.bidding_my_assert(data='c', i=[5], headers='camera', id=obj, afterStatus=5)`

---

### test_direct_shot_refuses_to_make_up_for_the_difference - [可补差]直拍-拒绝补差

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF1']`
步骤2：API调用 `self.case.direct_shot_refuses_to_make_up_for_the_difference()` 拒绝补差
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.bidding_my_assert(data='c', i=[7], headers='camera', id=obj, afterStatus=7)`

---

### test_direct_shooting_ends_after_sales - [可补差]直拍-结束售后

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF1']`
步骤2：API调用 `self.case.direct_shooting_ends_after_sales()` 结束售后
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.bidding_my_assert(i=4, headers='camera', id=obj, articlesStatus=4)`

---

### test_direct_shot_go_shipping - [待寄回]直拍-去发货-预约上门

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF2'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF2']`
步骤2：API调用 `self.case.direct_shot_go_shipping()` 去发货（预约上门方式）
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.fulfillment_camera_after_sales_order_assert(id=obj, afterStatus=10, buyerReturnModeStr='预约上门', afterStatusStr='待接收', tradeCategoryStr='直拍')`

---

### test_direct_shooting_to_ship_logistics - [待寄回]直拍-去发货-自叫物流

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF2'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF2']`
步骤2：API调用 `self.case.direct_shooting_to_ship_logistics()` 去发货（自叫物流方式）
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.fulfillment_camera_after_sales_order_assert(id=obj, afterStatus=10, buyerReturnModeStr='自叫物流', afterStatusStr='待接收', tradeCategoryStr='直拍')`

---

### test_direct_shot_to_ship_by_yourself - [待寄回]直拍-去发货-自行送货

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF2'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF2']`
步骤2：API调用 `self.case.direct_shot_to_ship_by_yourself()` 去发货（自行送货方式）
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.fulfillment_camera_after_sales_order_assert(id=obj, afterStatus=10, buyerReturnModeStr='自行送货', afterStatusStr='待接收', tradeCategoryStr='直拍')`

---

### test_direct_shot_re_inspection_receive_the_spread - [可补差]-直拍-复检审核-接收补差

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF2', '@BB4', 'CF3', 'CB7', 'CB8', 'CF4'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF2', '@BB4', 'CF3', 'CB7', 'CB8', 'CF4']`
步骤2：API调用 `self.case.direct_shot_re_inspection_receive_the_spread()` 复检审核后接收补差
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.bidding_my_assert(data='c', i=[5], headers='camera', id=obj, afterStatus=5)`

---

### test_direct_shot_re_inspection_rejection_price - [可补差]-直拍-复检审核-拒绝补差

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF2', '@BB4', 'CF3', 'CB7', 'CB8', 'CF4'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF2', '@BB4', 'CF3', 'CB7', 'CB8', 'CF4']`
步骤2：API调用 `self.case.direct_shot_re_inspection_rejection_price()` 复检审核后拒绝补差
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.bidding_my_assert(data='c', i=[7], headers='camera', id=obj, afterStatus=7)`

---

### test_direct_shooting_re_inspection_ends_after_sales - [可补差]-直拍-复检审核-结束售后

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5']`
步骤2：API调用 `self.case.direct_shooting_re_inspection_ends_after_sales()` 复检审核后结束售后
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.bidding_my_assert(data='c', i=[9], headers='camera', id=obj, afterStatus=9)`

---

### test_straight_shot_i_want_to_appeal - [待申诉]-直拍-我要申诉

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5']`
步骤2：API调用 `self.case.straight_shot_i_want_to_appeal()` 发起申诉
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.bidding_my_assert(data='c', i=[4], headers='camera', id=obj, afterStatus=4)`

---

### test_pending_appeal_and_end_after_sales - [待申诉]-直拍-结束售后

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5']`
步骤2：API调用 `self.case.pending_appeal_and_end_after_sales()` 待申诉状态结束售后
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.bidding_my_assert(data='c', i=[4], headers='camera', id=obj, afterStatus=4)`

---

### test_direct_platform_review_receive_the_spread - [可补差]-直拍-平台申诉通过-接收补差

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5', 'DC2'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5', 'DC2']`
步骤2：API调用 `self.case.direct_platform_review_receive_the_spread()` 平台申诉通过后接收补差
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.bidding_my_assert(data='c', i=[5], headers='camera', id=obj, afterStatus=5)`

---

### test_direct_platform_review_no_rejection_price - [可补差]-直拍-平台申诉通过-拒绝补差

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5', 'DC2'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5', 'DC2']`
步骤2：API调用 `self.case.direct_platform_review_no_rejection_price()` 平台申诉通过后拒绝补差
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.bidding_my_assert(data='c', i=[7], headers='camera', id=obj, afterStatus=7)`

---

### test_direct_platform_review_ends_after_sales - [可补差]-直拍-平台申诉通过-结束售后

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5', 'DC2'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5', 'DC2']`
步骤2：API调用 `self.case.direct_platform_review_ends_after_sales()` 平台申诉通过后结束售后
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.bidding_my_assert(data='c', i=[9], headers='camera', id=obj, afterStatus=9)`

---

### test_direct_platform_review_go_shipping - [待寄回]-直拍-平台申诉通过-去发货-预约上门

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5', 'DC1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5', 'DC1']`
步骤2：API调用 `self.case.direct_platform_review_go_shipping()` 平台申诉通过后去发货（预约上门方式）
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.fulfillment_camera_after_sales_order_assert(id=obj, afterStatus=10, buyerReturnModeStr='预约上门', afterStatusStr='待接收', tradeCategoryStr='直拍')`

---

### test_direct_platform_review_self_proclaimed_logistics - [待寄回]-直拍-平台申诉通过-去发货-自叫物流

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5', 'DC1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5', 'DC1']`
步骤2：API调用 `self.case.direct_platform_review_self_proclaimed_logistics()` 平台申诉通过后去发货（自叫物流方式）
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.fulfillment_camera_after_sales_order_assert(id=obj, afterStatus=10, buyerReturnModeStr='自叫物流', afterStatusStr='待接收', tradeCategoryStr='直拍')`

---

### test_direct_platform_review_mail_it_yourself - [待寄回]-直拍-平台申诉通过-去发货-自行送货

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5', 'DC1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5', 'DC1']`
步骤2：API调用 `self.case.direct_platform_review_mail_it_yourself()` 平台申诉通过后去发货（自行送货方式）
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.fulfillment_camera_after_sales_order_assert(id=obj, afterStatus=10, buyerReturnModeStr='自行送货', afterStatusStr='待接收', tradeCategoryStr='直拍')`
