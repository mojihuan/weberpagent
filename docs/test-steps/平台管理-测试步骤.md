# 平台管理模块测试步骤

## TestPlatformAuctionProductManage - 平台管理|虚拟库存|上拍商品管理

### test_0select_the_session_to_add_product_confirm - [可上拍物品tab]选择场次-暗拍卖场-添加商品上架

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DD2'])`

**登录方式：** `login='super'`

步骤1：登录系统（super 账号）
步骤2：导航到上拍商品管理页面
步骤3：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DD2']`
步骤4：调用 `case.select_the_session_to_add_product_confirm()` 选择暗拍卖场场次添加商品上架

断言：调用方法：`self.pc.platform_auction_product_manage_assert(headers='super', i=2, updateTime='now', statusStr='销售中', auctionMarketSession='暗拍')`

---

### test_direct_auction_add_items_to_the_shelves - [可上拍物品tab]-选择场次-直拍卖场-添加商品上架

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1']`
步骤2：调用 `case.direct_auction_add_items_to_the_shelves()` 选择直拍卖场场次添加商品上架

断言：调用方法：`self.pc.platform_auction_product_manage_assert(headers='super', i=2, updateTime='now', statusStr='销售中', auctionMarketSession='直拍')`

---

### test_guaranteed_sale_cancellation_of_sale - [可上拍物品tab]-取消销售

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4']`
步骤2：调用 `case.guaranteed_sale_cancellation_of_sale()` 取消保卖销售
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.auction_my_assert(j=1, i=2, id=obj)`

---

### test_secret_shooting_modification_sessions - [可上拍物品tab]-修改场次-暗拍修改场次

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DD2', 'DF1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DD2', 'DF1']`
步骤2：调用 `case.secret_shooting_modification_sessions()` 暗拍修改场次

断言：调用方法：`self.pc.platform_auction_product_manage_assert(headers='super', i=2, updateTime='now', statusStr='销售中', auctionMarketSession='暗拍')`

---

### test_cancel_the_auction - [可上拍物品tab]-取消上拍

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DD2', 'DF1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DD2', 'DF1']`
步骤2：调用 `case.cancel_the_auction()` 取消上拍

断言：调用方法：`self.pc.platform_auction_product_manage_assert(headers='super', i=1, updateTime='now')`

---

## TestPlatformItemsToBeSpecified - 平台管理|运营中心|待指定物品

### test_0designated_recyclers - [指定回收商]

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB4'])`

**登录方式：** `login='platform'`

步骤1：登录系统（platform 账号）
步骤2：导航到待指定物品页面
步骤3：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB4']`
步骤4：调用 `case.designated_recyclers()` 指定回收商

断言：调用方法：`self.pc.platform_items_to_be_specified_assert(headers='platform', assignTime='now')`

---

### test_modify_the_recycler - [修改回收商]

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB4'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB4']`
步骤2：调用 `case.modify_the_recycler()` 修改回收商

断言：调用方法：`self.pc.platform_items_to_be_specified_assert(headers='platform', assignTime='now')`

---

## TestPlatformListOfDarkAuctionHouses - 平台管理|卖场管理|暗拍卖场列表

### test_0every_day_on_a_expired_phone - [创建卖场]指定日期-场次频率每天-无保证金-手机-保存并上架

**前置条件：** 无额外前置数据

**登录方式：** `login='super'`

步骤1：登录系统（super 账号）
步骤2：导航到暗拍卖场列表页面
步骤3：调用 `case.every_day_on_a_expired_phone()` 创建暗拍卖场（每天一场，无保证金，手机品类，保存并上架）

断言：调用方法：`self.pc.platform_list_of_dark_auction_houses_assert(headers='super', createTime='now', statusStr='已上架')`

---

### test_price_priority_for_close_proximity - [创建卖场]暗拍-仅保存

**前置条件：** 无额外前置数据（复用登录状态）

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：调用 `case.price_priority_for_close_proximity()` 创建暗拍卖场（仅保存，不上架）

断言：调用方法：`self.pc.platform_list_of_dark_auction_houses_assert(i=2, headers='super', createTime='now', statusStr='待上架')`

---

### test_create_five_minute_auction_session - [创建卖场]指定5分钟一场-保存并上架

**前置条件：** 无额外前置数据（复用登录状态）

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：调用 `case.create_five_minute_auction_session()` 创建5分钟一场的卖场并上架

断言：调用方法：`self.pc.platform_list_of_dark_auction_houses_assert(headers='super', createTime='now', statusStr='已上架')`

---

### test_modify_the_store_event_time - [已下架tab]暗拍卖场-编辑-保存并上架

**前置条件：** `self.pre.operations(data=['DD2', 'DD3'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['DD2', 'DD3']`
步骤2：调用 `case.modify_the_store_event_time()` 编辑已下架的暗拍卖场并保存上架

断言：调用方法：`self.pc.platform_list_of_dark_auction_houses_assert(headers='super', updateTime='now', statusStr='已上架')`

---

### test_save_and_list_is_modified_to_save_only - [已下架tab]暗拍卖场-重新上架

**前置条件：** `self.pre.operations(data=['DD2', 'DD3'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['DD2', 'DD3']`
步骤2：调用 `case.save_and_list_is_modified_to_save_only()` 已下架卖场重新上架

断言：调用方法：`self.pc.platform_list_of_dark_auction_houses_assert(headers='super', updateTime='now', statusStr='已上架')`

---

### test_listed_status_removed_from_the_store - [下架]已上架状态-下架卖场

**前置条件：** `self.pre.operations(data=['DD1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['DD1']`
步骤2：调用 `case.listed_status_removed_from_the_store()` 已上架卖场下架

断言：调用方法：`self.pc.platform_list_of_dark_auction_houses_assert(headers='super', createTime='now', statusStr='已下架')`

---

### test_add_goods_mobile_phones - [查看场次详情]-已上架状态-添加商品-手机

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DD2'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DD2']`
步骤2：调用 `case.add_goods_mobile_phones()` 查看场次详情并添加手机商品

断言：调用方法：`self.pc.platform_list_of_dark_auction_houses_assert(headers='super', data='a', totalMum=1)`

---

### test_turn_off_the_open_view_of_the_product - [查看场次详情]关闭开放查看商品

**前置条件：** `self.pre_platform.every_day_on_a_specified_date_phone()`（平台级别前置准备）

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行平台级前置准备 `every_day_on_a_specified_date_phone()`
步骤2：调用 `case.turn_off_the_open_view_of_the_product()` 关闭开放查看商品

> **注意：** 此测试无断言

---

### test_pending_status_listed - [待上架tab]-上架

**前置条件：** `self.pre.operations(data=['DD5'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['DD5']`
步骤2：调用 `case.pending_status_listed()` 待上架卖场上架

断言：调用方法：`self.pc.platform_list_of_dark_auction_houses_assert(headers='super', statusStr='已上架', updateTime='now')`

---

### test_pending_status_edit_save_and_list - [待上架tab]-编辑-保存并上架

**前置条件：** `self.pre.operations(data=['DD5'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['DD5']`
步骤2：调用 `case.pending_status_edit_save_and_list()` 编辑待上架卖场并保存上架

断言：调用方法：`self.pc.platform_list_of_dark_auction_houses_assert(headers='super', statusStr='已上架', updateTime='now')`

---

### test_pending_status_deleted - [待上架tab]-删除

**前置条件：** `self.pre.operations(data=['DD5'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['DD5']`
步骤2：调用 `case.pending_status_deleted()` 删除待上架卖场

> **注意：** 此测试无断言

---

### test_delisted_edit_save_and_list - [已下架tab]-编辑-保存并上架

**前置条件：** `self.pre.operations(data=['DD2', 'DD3'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['DD2', 'DD3']`
步骤2：调用 `case.delisted_edit_save_and_list()` 编辑已下架卖场并保存上架

断言：调用方法：`self.pc.platform_list_of_dark_auction_houses_assert(headers='super', statusStr='已上架', updateTime='now')`

---

### test_multi_select_show_listed_to_be_listed - [调整广告位]多选-显示已上架和待上架-保存并更新

**前置条件：** 无额外前置数据（复用登录状态）

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：调用 `case.multi_select_show_listed_to_be_listed()` 多选调整广告位（已上架和待上架）

> **注意：** 此测试无断言

---

### test_multi_select_show_listed_pending_removed - [调整广告位]多选-显示已上架和待上架和已下架-保存并更新

**前置条件：** 无额外前置数据（复用登录状态）

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：调用 `case.multi_select_show_listed_pending_removed()` 多选调整广告位（已上架、待上架和已下架）

> **注意：** 此测试无断言

---

### test_view_logs - [查看日志]

**前置条件：** 无额外前置数据（复用登录状态）

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：调用 `case.view_logs()` 查看日志

> **注意：** 此测试无断言

---

### test_derived - [导出]

**前置条件：** 无额外前置数据（复用登录状态）

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：调用 `case.derived()` 导出数据

> **注意：** 此测试无断言

---

## TestPlatformListOfDirectAuctionHouses - 平台管理|卖场管理|直拍卖场列表

### test_0zhi_every_day_on_a_specified_date_phone - [创建卖场]指定日期-场次频率每天-手机-保存并上架

**前置条件：** 无额外前置数据

**登录方式：** `login='super'`

步骤1：登录系统（super 账号）
步骤2：导航到直拍卖场列表页面
步骤3：调用 `case.zhi_every_day_on_a_specified_date_phone()` 创建直拍卖场（每天一场，手机品类，保存并上架）

断言：调用方法：`self.pc.platform_list_of_direct_auction_houses_assert(headers='super', createTime='now', statusStr='已上架')`

---

### test_zhi_price_priority_for_close_proximity - [创建卖场]仅保存

**前置条件：** 无额外前置数据（复用登录状态）

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：调用 `case.zhi_price_priority_for_close_proximity()` 创建直拍卖场（仅保存）

断言：调用方法：`self.pc.platform_list_of_direct_auction_houses_assert(headers='super', createTime='now', statusStr='待上架')`

---

### test_zhi_modify_the_store_event_time - [已下架tab]-编辑-保存并上架

**前置条件：** `self.pre.operations(data=['DB1', 'DB4'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['DB1', 'DB4']`
步骤2：调用 `case.zhi_modify_the_store_event_time()` 编辑已下架的直拍卖场并保存上架

断言：调用方法：`self.pc.platform_list_of_direct_auction_houses_assert(headers='super', updateTime='now', statusStr='已上架')`

---

### test_zhi_listed_status_removed_from_the_store - [已上架tab]-下架

**前置条件：** `self.pre.operations(data=['DB1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['DB1']`
步骤2：调用 `case.zhi_listed_status_removed_from_the_store()` 已上架直拍卖场下架

断言：调用方法：`self.pc.platform_list_of_direct_auction_houses_assert(headers='super', updateTime='now', statusStr='已下架')`

---

### test_zhi_add_goods_mobile_phones - [查看场次详情]已上架状态-添加商品-手机

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1']`
步骤2：调用 `case.zhi_add_goods_mobile_phones()` 查看场次详情并添加手机商品

断言：调用方法：`self.pc.platform_list_of_direct_auction_houses_assert(headers='super', data='a', totalMum=1)` 和 `self.pc.platform_list_of_direct_auction_houses_assert(headers='super', data='b', updateTime='now', createTime='now')`

---

### test_zhi_turn_off_the_open_view_of_the_product - [查看场次详情]关闭开放查看商品

**前置条件：** `self.pre.operations(data=['DB1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['DB1']`
步骤2：调用 `case.zhi_turn_off_the_open_view_of_the_product()` 关闭开放查看商品

> **注意：** 此测试无断言

---

### test_zhi_multi_select_show_listed_to_be_listed - [调整广告位]多选-显示已上架和待上架-保存并更新

**前置条件：** 无额外前置数据（复用登录状态）

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：调用 `case.zhi_multi_select_show_listed_to_be_listed()` 多选调整广告位

> **注意：** 此测试无断言

---

### test_zhi_multi_select_show_listed_pending_removed - [调整广告位]多选-显示已上架和待上架和已下架-保存并更新

**前置条件：** 无额外前置数据（复用登录状态）

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：调用 `case.zhi_multi_select_show_listed_pending_removed()` 多选调整广告位

> **注意：** 此测试无断言

---

### test_zhi_view_logs - [查看日志]

**前置条件：** 无额外前置数据（复用登录状态）

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：调用 `case.zhi_view_logs()` 查看日志

> **注意：** 此测试无断言

---

### test_zhi_derived - [导出]

**前置条件：** 无额外前置数据（复用登录状态）

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：调用 `case.zhi_derived()` 导出数据

> **注意：** 此测试无断言

---

## TestPlatformMessageReleaseList - 平台管理|消息管理|消息发布列表

### test_0platform_refuse - [审核]-平台审核拒绝

**前置条件：** `self.pre.operations(data=['JA2'])`

**登录方式：** `login='platform'`

步骤1：登录系统（platform 账号）
步骤2：导航到消息发布列表页面
步骤3：执行前置数据准备 `['JA2']`
步骤4：调用 `case.platform_refuse()` 平台审核拒绝

断言：调用方法：`self.pc.message_release_list_assert(publishStateDesc='待发布', internalAuditStateDesc='审核通过', platformAuditStateDesc='审核不通过', createTime='now')`

---

### test_platform_approved - [审核]-平台审核通过

**前置条件：** `self.pre.operations(data=['JA2'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['JA2']`
步骤2：调用 `case.platform_approved()` 平台审核通过

断言：调用方法：`self.pc.message_release_list_assert(publishStateDesc='已发布', internalAuditStateDesc='审核通过', platformAuditStateDesc='审核通过', createTime='now')`

---

### test_platform_back - [审核]-平台撤回

**前置条件：** `self.pre.operations(data=['JA2', 'DE1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['JA2', 'DE1']`
步骤2：调用 `case.platform_back()` 平台撤回

断言：调用方法：`self.pc.message_release_list_assert(publishStateDesc='已撤回', internalAuditStateDesc='审核通过', platformAuditStateDesc='审核通过', createTime='now')`

---

## TestPlatformProductReview - 平台管理|同售管理|商品审核

> **注意：** 此类仅包含 `get_instantiation` 方法，无具体测试方法。

---

## TestPlatformPurseCenter - 平台管理|订单管理|订单审核

### test_0platform_approval - [审核]-快递易-对公充值-平台审核通过

**前置条件：** `self.pre.operations(data=['KA1'])`

**登录方式：** `login='platform'`

步骤1：登录系统（platform 账号）
步骤2：导航到订单审核页面
步骤3：执行前置数据准备 `['KA1']`
步骤4：调用 `case.platform_approval()` 平台审核通过

断言：调用方法：`self.pc.platform_order_review_assert(headers='platform', orderBusinessTypeStr='钱包充值', auditStatusStr='已通过', createTime='now')`

---

### test_platform_audit_rejection - [审核]-快递易-对公充值-平台审核拒绝

**前置条件：** `self.pre.operations(data=['KA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['KA1']`
步骤2：调用 `case.platform_audit_rejection()` 平台审核拒绝

断言：调用方法：`self.pc.platform_order_review_assert(headers='platform', orderBusinessTypeStr='钱包充值', auditStatusStr='未通过', createTime='now')`

---

## TestGrievanceManage - 平台管理|壹准拍机|售后管理|申诉管理

### test_straight_shot_through_make_up_the_difference - [待处理]-直拍-审核通过-仅补差

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5'])`

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5']`
步骤2：调用 `case.straight_shot_through_make_up_the_difference()` 直拍审核通过仅补差

断言：调用方法：`self.pc.platform_grievance_manage_assert(i=1, headers='platform', statusStr='申诉成功', auditTime='now')` 和 `self.pc.platform_yz_auction_assert(i=[5], headers='platform', afterStatusStr='补差成功', firstAppealAuditTime='now')`

---

### test_direct_shooting_through_priority_supplementation - [待处理]-直拍-审核通过-优先补差

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5'])`

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5']`
步骤2：调用 `case.direct_shooting_through_priority_supplementation()` 直拍审核通过优先补差
步骤3：获取缓存的售后订单号 `cached('afterOrderNo')`

断言：调用方法：`self.pc.platform_grievance_manage_assert(i=1, headers='platform', statusStr='申诉成功', auditTime='now')` 和 `self.pc.bidding_my_assert(data='c', i=[6], headers='camera', orderNo=obj, afterStatus=6)`

---

### test_direct_approved_refund - [待处理]-直拍-审核通过-退货退款

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5'])`

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4', 'DB1', 'DB2', '@BA1', 'CE1', '@BB1', '@BB2', 'CF5', '@BB5']`
步骤2：调用 `case.direct_approved_refund()` 直拍审核通过退货退款
步骤3：获取缓存的售后订单号 `cached('afterOrderNo')`

断言：调用方法：`self.pc.platform_grievance_manage_assert(i=1, headers='platform', statusStr='申诉成功', auditTime='now')` 和 `self.pc.bidding_my_assert(data='c', i=[7], headers='camera', orderNo=obj, afterStatus=7)`
