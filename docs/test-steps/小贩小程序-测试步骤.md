# 小贩小程序模块测试步骤

> **说明：** 本模块继承 MiniBaseCase，属于小程序测试，操作均为 API 调用而非 UI 操作。

## TestTraffickerHelp - 首页|帮卖

### test_0execute_one_click_help - [一键帮卖]-从库存添加-我要帮卖

**前置条件：** `self.pre.operations(data=['FA1', 'GA1'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1']`
步骤2：API调用 `self.case.execute_one_click_help()` 一键帮卖从库存添加

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待发货', updateTime='now')`

---

### test_guaranteed_sale_buyout - [一键帮卖]-从库存添加-我要保卖买断

**前置条件：** `self.pre.operations(data=['FA1', 'GA1'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1']`
步骤2：API调用 `self.case.guaranteed_sale_buyout()` 保卖买断

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待发货', updateTime='now')`

---

### test_guaranteed_sales_and_profit_sharing - [一键帮卖]-从库存添加-我要保卖分润

**前置条件：** `self.pre.operations(data=['FA1', 'GA1'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1']`
步骤2：API调用 `self.case.guaranteed_sales_and_profit_sharing()` 保卖分润

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待发货', updateTime='now')`

---

### test_0express_delivery_is_easy_to_ship - [发货]快递易发货

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4']`
步骤2：API调用 `self.case.express_delivery_is_easy_to_ship()` 快递易发货

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待收货', updateTime='now')`

---

### test_self_mailing - [发货]自行邮寄发货

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4']`
步骤2：API调用 `self.case.self_mailing()` 自行邮寄发货

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待收货', updateTime='now')`

---

### test_deliver_it_yourself - [发货]自己送发货

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4']`
步骤2：API调用 `self.case.deliver_it_yourself()` 自己送发货

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待收货', updateTime='now')`

---

### test_quality_inspection_retirement - [待质检]-申请退机

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1']`
步骤2：API调用 `self.case.quality_inspection_retirement()` 待质检申请退机

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待退机', updateTime='now')`

---

### test_pending_confirmation_request_to_withdraw - [待确认]-申请退机

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA9', 'GA6', 'HA1', 'GB1'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1', 'GA3', 'GA9', 'GA6', 'HA1', 'GB1']`
步骤2：API调用 `self.case.pending_confirmation_request_to_withdraw()` 待确认申请退机

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待退机', updateTime='now')`

---

### test_negotiate_a_refund_request - [待议价]-申请退机

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA9', 'GA6', 'HA1', 'GB1', 'GA12'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1', 'GA3', 'GA9', 'GA6', 'HA1', 'GB1', 'GA12']`
步骤2：API调用 `self.case.negotiate_a_refund_request()` 待议价申请退机

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待退机', updateTime='now')`

---

### test_for_sale_request_to_withdraw - [待议价]-申请退机

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GB1'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GB1']`
步骤2：API调用 `self.case.for_sale_request_to_withdraw()` 待售出申请退机

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待退机', updateTime='now')`

---

### test_pending_negotiation_confirm_the_guaranteed_sale - [待议价]-确认保卖

> **注意：** 此测试方法无实际测试逻辑（空方法体）

---

### test_negotiation_confirm_the_guarantee_of_sale - [待确认]-申请议价

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA9', 'GA6', 'HA1', 'GB1'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1', 'GA3', 'GA9', 'GA6', 'HA1', 'GB1']`
步骤2：API调用 `self.case.negotiation_confirm_the_guarantee_of_sale()` 待确认申请议价

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待议价', updateTime='now')`

---

### test_pending_departure_cancel_the_departure - [待退机]-取消退机

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA9', 'GA6', 'HA1', 'GB1'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1', 'GA3', 'GA9', 'GA6', 'HA1', 'GB1']`
步骤2：API调用 `self.case.pending_departure_cancel_the_departure()` 取消退机

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待确认', updateTime='now')`

---

### test_manual_sign_in_during_exit - [退机中]-手动签收

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8', 'GB2'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8', 'GB2']`
步骤2：API调用 `self.case.manual_sign_in_during_exit()` 退机中手动签收

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='已退机', updateTime='now')`

---

### test_logistics_signing_during_retirement - [退机中]-物流签收入库

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8', 'GB3'])`

步骤1：API调用前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8', 'GB3']`
步骤2：API调用 `self.case.logistics_signing_during_retirement()` 退机中物流签收入库

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='退机中', updateTime='now')`

---

## TestTraffickerInventory - 首页|库存

### test_0individual_item_counting - [流程]新建盘点-提交盘点-完成盘点

**前置条件：** 无额外前置数据

步骤1：API调用 `self.case.individual_item_counting()` 新建盘点并完成

断言：调用方法：`self.pc.inventory_count_assert(updateTime='now', stockNo='PD')`

---

### test_0handover_for_quality_inspection - [流程]移交物品-移交质检

**前置条件：** `self.pre.operations(data=['FA1'])`

步骤1：API调用前置数据准备 `['FA1']`
步骤2：API调用 `self.case.handover_for_quality_inspection()` 移交质检

断言：调用方法：`self.pc.inventory_handover_record_assert(statusStr='已接收', createTime='now')`

---

### test_handover_for_maintenance - [流程]移交物品-移交维修

**前置条件：** `self.pre.operations(data=['FA1'])`

步骤1：API调用前置数据准备 `['FA1']`
步骤2：API调用 `self.case.handover_for_maintenance()` 移交维修

断言：调用方法：`self.pc.inventory_handover_record_assert(statusStr='已接收', createTime='now')`

---

### test_transfer_for_repair - [流程]移交物品-移交送修

**前置条件：** `self.pre.operations(data=['FA1'])`

步骤1：API调用前置数据准备 `['FA1']`
步骤2：API调用 `self.case.transfer_for_repair()` 移运送修

断言：调用方法：`self.pc.inventory_handover_record_assert(statusStr='已接收', createTime='now')`

---

### test_transfer_for_sales - [流程]移交物品-移交销售

**前置条件：** `self.pre.operations(data=['FA1'])`

步骤1：API调用前置数据准备 `['FA1']`
步骤2：API调用 `self.case.transfer_for_sales()` 移交销售

断言：调用方法：`self.pc.inventory_handover_record_assert(statusStr='已接收', createTime='now')`

---

### test_transfer_for_purchasing_after_sales - [流程]移交物品-移交采购售后

**前置条件：** `self.pre.operations(data=['FA1'])`

步骤1：API调用前置数据准备 `['FA1']`
步骤2：API调用 `self.case.transfer_for_purchasing_after_sales()` 移交采购售后

断言：调用方法：`self.pc.inventory_handover_record_assert(statusStr='已接收', createTime='now')`

---

### test_transfer_for_stock - [流程]移交物品-移交库存

**前置条件：** `self.pre.operations(data=['FA1'])`

步骤1：API调用前置数据准备 `['FA1']`
步骤2：API调用 `self.case.transfer_for_stock()` 移交库存

断言：调用方法：`self.pc.inventory_handover_record_assert(statusStr='已接收', createTime='now')`

---

### test_0sign_in_warehouse - [流程]签收入库

**前置条件：** `self.pre.operations(data=['FA2'])`

步骤1：API调用前置数据准备 `['FA2']`
步骤2：API调用 `self.case.sign_in_warehouse()` 签收入库

断言：调用方法：`self.pc.inventory_logistics_list_assert(statusStr='已分拣', sortationTime='now')`

---

### test_sign_in_warehouse_in_quality - [流程]签收入库-移交质检

**前置条件：** `self.pre.operations(data=['FA2'])`

步骤1：API调用前置数据准备 `['FA2']`
步骤2：API调用 `self.case.sign_in_warehouse_in_quality()` 签收入库并移交质检

断言：调用方法：`self.pc.inventory_logistics_list_assert(statusStr='已分拣', sortationTime='now')`

---

### test_sign_in_warehouse_in_repair - [流程]签收入库-移交维修

**前置条件：** `self.pre.operations(data=['FA2'])`

步骤1：API调用前置数据准备 `['FA2']`
步骤2：API调用 `self.case.sign_in_warehouse_in_repair()` 签收入库并移交维修

断言：调用方法：`self.pc.inventory_logistics_list_assert(statusStr='已分拣', sortationTime='now')`

---

### test_sign_in_warehouse_in_send - [流程]签收入库-移交送修

**前置条件：** `self.pre.operations(data=['FA2'])`

步骤1：API调用前置数据准备 `['FA2']`
步骤2：API调用 `self.case.sign_in_warehouse_in_send()` 签收入库并移交送修

断言：调用方法：`self.pc.inventory_logistics_list_assert(statusStr='已分拣', sortationTime='now')`

---

### test_sign_in_warehouse_in_sell - [流程]签收入库-移交销售

**前置条件：** `self.pre.operations(data=['FA2'])`

步骤1：API调用前置数据准备 `['FA2']`
步骤2：API调用 `self.case.sign_in_warehouse_in_sell()` 签收入库并移交销售

断言：调用方法：`self.pc.inventory_logistics_list_assert(statusStr='已分拣', sortationTime='now')`

---

### test_sign_in_warehouse_in_purchase - [流程]签收入库-移交采购

**前置条件：** `self.pre.operations(data=['FA2'])`

步骤1：API调用前置数据准备 `['FA2']`
步骤2：API调用 `self.case.sign