# 保卖模块测试步骤

## TestGuaranteeOrderManage - 保卖管理|订单列表

### test_0quick_guarantee_item_submission - [快速保卖]-输入物品编号-提交

**前置条件：** `self.pre.operations(data=['FA1'])`

**登录方式：** `login='main'`

步骤1：点击密码登录
步骤2：输入账号 Y59800075
步骤3：输入密码 Aa123456
步骤4：点击登录
步骤5：导航到保卖订单列表页面
步骤6：执行前置数据准备 `['FA1']`
步骤7：调用 `case.quick_guarantee_item_submission()` 快速保卖提交物品

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='待发货')`

---

### test_quick_guarantee_item_submission_delivery - [快速保卖]-输入物品编号-提交并发货-快递sf

**前置条件：** `self.pre.operations(data=['FA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1']`
步骤2：调用 `case.quick_guarantee_item_submission_delivery()` 快速保卖提交并发货（快递sf）

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='待收货')`

---

### test_quick_guarantee_item_submission_delivery_jd - [快速保卖]-输入物品编号-提交并发货-快递jd

**前置条件：** `self.pre.operations(data=['FA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1']`
步骤2：调用 `case.quick_guarantee_item_submission_delivery_jd()` 快速保卖提交并发货（快递jd）

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='待收货')`

---

### test_quick_guarantee_item_submission_delivery_self_mail - [快速保卖]-输入物品编号-提交并发货-快递自行邮寄

**前置条件：** `self.pre.operations(data=['FA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1']`
步骤2：调用 `case.quick_guarantee_item_submission_delivery_self_mail()` 快速保卖提交并发货（自行邮寄）

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='待收货')`

---

### test_fast_item_shipping_by_yourself - [快速保卖]-输入物品编号-提交并发货-自己送货

**前置条件：** `self.pre.operations(data=['FA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1']`
步骤2：调用 `case.fast_item_shipping_by_yourself()` 快速保卖提交并发货（自己送货）

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='待取件')`

---

### test_ship_now_by_express_sf - [立即发货]-快递sf

**前置条件：** `self.pre.operations(data=['FA1', 'EA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'EA1']`
步骤2：调用 `case.ship_now_by_express_sf()` 立即发货（快递sf）

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='待取件')`

---

### test_ship_now_by_express_jd - [立即发货]-快递jd

**前置条件：** `self.pre.operations(data=['FA1', 'EA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'EA1']`
步骤2：调用 `case.ship_now_by_express_jd()` 立即发货（快递jd）

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='待收货')`

---

### test_ship_now_by_express_self_mail - [立即发货]-快递自行邮寄

**前置条件：** `self.pre.operations(data=['FA1', 'EA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'EA1']`
步骤2：调用 `case.ship_now_by_express_self_mail()` 立即发货（自行邮寄）

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='待收货')`

---

### test_ship_now_by_express_send_it_yourself - [立即发货]-自己送货

**前置条件：** `self.pre.operations(data=['FA1', 'EA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'EA1']`
步骤2：调用 `case.ship_now_by_express_send_it_yourself()` 立即发货（自己送货）

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='待收货')`

---

### test_quick_guarantee_cancel_the_order - [取消订单]-取消订单

**前置条件：** `self.pre.operations(data=['FA1', 'EA1', 'EA2'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'EA1', 'EA2']`
步骤2：调用 `case.quick_guarantee_cancel_the_order()` 取消保卖订单

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='已取消')`

---

### test_fast_guarantee_reshipment - [重新发货]-自己送改快递-重新发货

**前置条件：** `self.pre.operations(data=['FA1', 'EA1', 'EA2'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'EA1', 'EA2']`
步骤2：调用 `case.fast_guarantee_reshipment()` 自己送改快递重新发货

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='待取件')`

---

## TestGuaranteeReturnsManage - 保卖管理|退货管理

### test_0cancel_returns_by_mail - [待退货tab]-邮寄退货-取消退货

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB2'])`

**登录方式：** `login='main'`

步骤1：点击密码登录
步骤2：输入账号 Y59800075
步骤3：输入密码 Aa123456
步骤4：点击登录
步骤5：导航到保卖退货管理页面
步骤6：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB2']`
步骤7：调用 `case.cancel_returns_by_mail()` 取消邮寄退货

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='已收货')`

---

### test_change_the_return_method_by_mail - [待退货tab]-邮寄退货-更改退货方式

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB2'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB2']`
步骤2：调用 `case.change_the_return_method_by_mail()` 更改邮寄退货方式

断言：调用方法：`self.pc.guarantee_returns_manage_assert(applyTime='now', logisticsTypeStr='快递', i=1)`

---

### test_self_lifted_cancel_a_return - [待取货tab]-自提退货-取消退货

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB3'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB3']`
步骤2：调用 `case.self_lifted_cancel_a_return()` 取消自提退货

> **注意：** 此测试的断言被注释，标记为 todo 测试环境无法测试

---

### test_change_the_return_method - [待取货tab]-自提退货-更改退货方式

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB3'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB3']`
步骤2：调用 `case.change_the_return_method()` 更改自提退货方式

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='已收货')`

---

### test_confirm_receipt - [退货已出库tab]-确认收货

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB2', 'CC1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB2', 'CC1']`
步骤2：调用 `case.confirm_receipt()` 确认退货收货

断言：调用方法：`self.pc.guarantee_order_manage_assert(placeOrderTime='now', statusDesc='已收货')`

---

## TestGuaranteeGoodsManage - 保卖管理|商品管理

### test_0return_method_express - [退货]-快递

> **注意：** 此测试被 `@unittest.skip('')` 跳过，缺 API 用例

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

**登录方式：** `login='main'`

步骤1：点击密码登录
步骤2：输入账号 Y59800075
步骤3：输入密码 Aa123456
步骤4：点击登录
步骤5：导航到保卖商品管理页面
步骤6：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤7：调用 `case.return_method_express()` 快递退货
步骤8：获取缓存的ID `cached('id')`

> **注意：** 此测试的断言被注释，标记为 todo 测试环境无法测试

---

### test_return_method_self_lifted - [退货]-自提

> **注意：** 此测试被 `@unittest.skip('')` 跳过，缺 API 用例

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：调用 `case.return_method_self_lifted()` 自提退货

> **注意：** 此测试的断言被注释，标记为 todo 测试环境无法测试

---

### test_sell_bid_reference_price - [销售]-竞价拍机-今日参考价

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：调用 `case.sell_bid_reference_price()` 竞价拍机按今日参考价销售
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.guarantee_goods_manage_assert(id=obj, statusStr='销售中', saleTypeStr='竞价拍机')`

---

### test_sell_bid_self_pricing - [销售]-竞价拍机-自主定价

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：调用 `case.sell_bid_self_pricing()` 竞价拍机自主定价销售
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.guarantee_goods_manage_assertt(id=obj, statusStr='销售中', saleTypeStr='竞价拍机')`

---

### test_sell_bid_platform_pricing - [销售]竞价拍机-平台定价

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：调用 `case.sell_bid_platform_pricing()` 竞价拍机平台定价销售
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.guarantee_goods_manage_assert(id=obj, statusStr='待平台确认', saleTypeStr='竞价拍机')`

---

### test_sell_same_sale_reference_price - [销售]-第三方同售-今日参考价

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：调用 `case.sell_same_sale_reference_price()` 第三方同售按今日参考价销售
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.guarantee_goods_manage_assert(id=obj, statusStr='销售中', saleTypeStr='同售')`

---

### test_sell_self_same_sale_pricing - [销售]-第三方同售-自主定价

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：调用 `case.sell_self_same_sale_pricing()` 第三方同售自主定价销售
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.guarantee_goods_manage_assert(id=obj, statusStr='销售中', saleTypeStr='同售')`

---

### test_sell_same_sale_platform_pricing - [销售]-第三方同售-平台定价

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：调用 `case.sell_same_sale_platform_pricing()` 第三方同售平台定价销售
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.guarantee_goods_manage_assert(id=obj, statusStr='待平台确认', saleTypeStr='同售')`

---

### test_cancel_the_sale - [取消销售]-取消销售

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4']`
步骤2：调用 `case.cancel_the_sale()` 取消销售
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.guarantee.goods_list(id=obj, statusStr='待销售', saleTypeStr='竞价拍机')`

---

### test_update_price - [改价]-改价

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4']`
步骤2：调用 `case.update_price()` 改价操作
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.guarantee.goods_list(id=obj, signTime='now', statusStr='销售中', saleTypeStr='竞价拍机')`
