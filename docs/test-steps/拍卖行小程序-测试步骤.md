# 拍卖行小程序模块测试步骤

> **说明：** 本模块继承 MiniBaseCase，属于小程序测试，操作均为 API 调用而非 UI 操作。

## TestAuctionIndex - 首页

### test_0fast_submit_new_order - [快速发货]-创建订单

**前置条件：** 无额外前置数据

步骤1：API调用 `self.case.fast_submit_new_order()` 快速发货创建订单

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=1, statusDesc='待发货', placeOrderTime='now')`

---

### test_large_quantity_fast_submit_new_order - [快速发货]-多数量-创建订单

**前置条件：** 无额外前置数据

步骤1：API调用 `self.case.fast_submit_new_order()` 快速发货多数量创建订单

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=1, statusDesc='待发货', placeOrderTime='now')`

---

### test_max_quantity_fast_submit_new_order - [快速发货]-数量最大值-创建订单

**前置条件：** 无额外前置数据

步骤1：API调用 `self.case.max_quantity_fast_submit_new_order()` 快速发货数量最大值创建订单

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=1, statusDesc='待发货', placeOrderTime='now')`

---

### test_auto_submit_new_order - [精确发货]自动估价-创建订单

**前置条件：** 无额外前置数据

步骤1：API调用 `self.case.auto_submit_new_order()` 精确发货自动估价创建订单

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=1, statusDesc='待发货', placeOrderTime='now')`

---

### test_quality_inspection_service_create_an_order - [质检服务]创建订单

**前置条件：** 无额外前置数据

步骤1：API调用 `self.case.quality_inspection_service_create_an_order()` 质检服务创建订单

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=1, statusDesc='待发货', placeOrderTime='now')`

---

## TestAuctionMy - 我的

### test_0sales_out_warehouse - [待销售]-今日参考价-销售物品

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：API调用 `self.case.sales_out_warehouse()` 按今日参考价销售物品

断言：调用方法：`self.pc.platform_virtual_inventory_list_assert(headers='super', i=3, statusStr='销售中', signTime='now')`

---

### test_self_pricing_selling_items - [待销售]-自主定价-销售物品

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：API调用 `self.case.self_pricing_selling_items()` 自主定价销售物品

断言：调用方法：`self.pc.platform_virtual_inventory_list_assert(headers='super', i=3, statusStr='销售中', signTime='now')`

---

### test_together_xy_sell_items - [待销售]-平台定价-销售物品

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：API调用 `self.case.together_xy_sell_items()` 平台定价销售物品

断言：调用方法：`self.pc.platform_virtual_inventory_list_assert(headers='super', i=5, statusStr='待平台确认', signTime='now')`

---

### test_sales_re_inspection - [待销售]-复检

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：API调用 `self.case.sales_re_inspection()` 提交复检申请

断言：调用方法：`self.pc.fulfillment_quality_manage_assert(data='b', statusStr='待审核', applyTime='now')`

---

### test_express_easy_return - [待销售]-快递退货

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：API调用 `self.case.express_easy_return()` 快递方式退货

断言：调用方法：`self.pc.fulfillment_returns_manage_assert(data='a', i=1, logisticsTypeStr='快递', applyTime='now')`

---

### test_self_pickup_returns - [待销售]-自提退货

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6']`
步骤2：API调用 `self.case.self_pickup_returns()` 自提方式退货

断言：调用方法：`self.pc.fulfillment_returns_manage_assert(data='a', i=2, logisticsTypeStr='自提', applyTime='now')`

---

### test_sell_no_quality_check_return - [待销售]-无需质检-退货

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB4', '@AB8'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB4', '@AB8']`
步骤2：API调用 `self.case.sell_no_quality_check_return()` 无需质检直接退货

断言：调用方法：`self.pc.fulfillment_returns_manage_assert(data='a', i=1, logisticsTypeStr='快递', applyTime='now')`

---

### test_cancel_the_sale - [销售中]-取消销售

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4']`
步骤2：API调用 `self.case.cancel_the_sale()` 取消销售
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.auction_my_assert(j=1, i=2, id=obj)`

---

### test_price_change_during_sales - [销售中]-改价

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB4']`
步骤2：API调用 `self.case.case.price_change_during_sales()` 销售中改价
步骤3：获取缓存的ID和价格 `cached('id')`, `cached('upbeatPrice')`

断言：调用方法：`self.pc.auction_my_assert(j=1, i=3, id=obj, upbeatPrice=obj_2)`

---

### test_platform_cancel_the_sale - [待平台确认]-取消销售

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB4'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB4']`
步骤2：API调用 `self.case.platform_cancel_the_sale()` 平台确认阶段取消销售
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.auction_my_assert(j=1, i=2, id=obj)`

---

### test_change_the_return_method - [退货中]-自提改快递-更改退货方式

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB3'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB3']`
步骤2：API调用 `self.case.change_the_return_method()` 将自提退货改为快递退货

断言：调用方法：`self.pc.fulfillment_returns_manage_assert(data='a', i=1, logisticsTypeStr='快递', applyTime='now')`

---

### test_cancel_the_return - [退货中]-取消退货

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB3'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB3']`
步骤2：API调用 `self.case.cancel_the_return()` 取消退货

断言：调用方法：`self.pc.fulfillment_returns_manage_assert(data='a', i=5, updateTime='now')`

---

### test_confirm_receipt - [退货已出库]-确认收货

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB2', 'CC1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB6', '@AB2', 'CC1']`
步骤2：API调用 `self.case.confirm_receipt()` 确认退货收货

断言：调用方法：`self.pc.fulfillment_returns_manage_assert(data='b', i=4, outboundTime='now')`

---

### test_offer_confirms_sale - [报价确认]-确认销售

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5', 'CA2', 'CB3', 'CB4', 'CD1', 'DA1'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5', 'CA2', 'CB3', 'CB4', 'CD1', 'DA1']`
步骤2：API调用 `self.case.offer_confirms_sale()` 报价确认销售

断言：调用方法：`self.pc.platform_virtual_inventory_list_assert(headers='super', signTime='now', statusStr='销售待出库')`

---

### test_shipped_immediately_sf - [待发货]-立即发货-顺丰快递邮寄发货

**前置条件：** `self.pre.operations(data=['@AA1'])`

步骤1：API调用前置数据准备 `['@AA1']`
步骤2：API调用 `self.case.shipped_immediately_sf()` 顺丰快递邮寄发货

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=2, placeOrderTime='now', businessTypeDesc='壹准保卖', statusDesc='待取件', expressTypeDesc='快递邮寄')`

---

### test_shipped_immediately_own - [待发货]-立即发货-自行邮寄-顺丰发货

**前置条件：** `self.pre.operations(data=['@AA1'])`

步骤1：API调用前置数据准备 `['@AA1']`
步骤2：API调用 `self.case.shipped_immediately_own()` 自行邮寄顺丰发货

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=3, placeOrderTime='now', businessTypeDesc='壹准保卖', statusDesc='待收货', expressTypeDesc='自己送货/自取')`

---

### test_deliver_it_yourself - [待发货]-立即发货-自己送货

**前置条件：** `self.pre.operations(data=['@AA1'])`

步骤1：API调用前置数据准备 `['@AA1']`
步骤2：API调用 `self.case.deliver_it_yourself()` 自己送货发货

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=3, placeOrderTime='now', businessTypeDesc='壹准保卖', statusDesc='待收货', expressTypeDesc='自己送货/自取')`

---

### test_shipped_immediately_sf_the_next_day - [待发货]-立即发货-顺丰快递邮寄-次日发货

**前置条件：** `self.pre.operations(data=['@AA1'])`

步骤1：API调用前置数据准备 `['@AA1']`
步骤2：API调用 `self.case.shipped_immediately_sf_the_next_day()` 顺丰快递邮寄次日发货

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=2, placeOrderTime='now', businessTypeDesc='壹准保卖', statusDesc='待取件', expressTypeDesc='快递邮寄')`

---

### test_shipped_immediately_no_shipping_address - [待发货]-立即发货-顺丰快递邮寄-无收货地址

**前置条件：** `self.pre.operations(data=['@AA1'])`

步骤1：API调用前置数据准备 `['@AA1']`
步骤2：API调用 `self.case.shipped_immediately_no_shipping_address()` 无收货地址时顺丰快递邮寄发货

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=2, placeOrderTime='now', businessTypeDesc='壹准保卖', statusDesc='待取件', expressTypeDesc='快递邮寄')`

---

### test_cancel_the_order - [待发货]-取消订单

**前置条件：** `self.pre.operations(data=['@AA1'])`

步骤1：API调用前置数据准备 `['@AA1']`
步骤2：API调用 `self.case.cancel_the_order()` 取消订单

断言：调用方法：`self.pc.fulfillment_order_manage_assert(data='b', i=6, placeOrderTime='now', businessTypeDesc='壹准保卖', statusDesc='已取消')`

---

### test_pending_pickup_cancel_the_order - [待取件]-取消订单

**前置条件：** `self.pre.operations(data=['@AA1', '@AB6'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB6']`
步骤2：API调用 `self.case.pending_pickup_cancel_the_order()` 待取件状态取消订单
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.auction_my_assert(data='b', i=6, id=obj)`

---

### test_pending_receipt_cancel_the_order - [待收货]-取消订单

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5']`
步骤2：API调用 `self.case.pending_receipt_cancel_the_order()` 待收货状态取消订单
步骤3：获取缓存的ID `cached('id')`

断言：调用方法：`self.pc.auction_my_assert(data='b', i=6, id=obj)`

---

### test_pending_receipt_of_reshipment_by_sf_logistics - [待收货]-重新发货-顺丰快递

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5']`
步骤2：API调用 `self.case.pending_receipt_of_reshipment_by_sf_logistics()` 重新发货（顺丰快递方式）

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=2, placeOrderTime='now', businessTypeDesc='壹准保卖', statusDesc='待取件', expressTypeDesc='快递邮寄')`

---

### test_pending_receipt_reshipment_self_mailing - [待收货]-重新发货-自行邮寄

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5']`
步骤2：API调用 `self.case.pending_receipt_reshipment_self_mailing()` 重新发货（自行邮寄方式）

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=3, placeOrderTime='now', businessTypeDesc='壹准保卖', statusDesc='待收货', expressTypeDesc='自己送货/自取')`

---

### test_pending_receipt_reshipment_deliver_it_yourself - [待收货]-重新发货-自己送

**前置条件：** `self.pre.operations(data=['@AA1', '@AB5'])`

步骤1：API调用前置数据准备 `['@AA1', '@AB5']`
步骤2：API调用 `self.case.pending_receipt_reshipment_deliver_it_yourself()` 重新发货（自己送方式）

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=3, placeOrderTime='now', businessTypeDesc='壹准保卖', statusDesc='待收货', expressTypeDesc='自己送货/自取')`

---

### test_0quality_shipped_immediately_sf - [待发货]-质检服务-立即发货-顺丰快递邮寄发货

**前置条件：** `self.pre.operations(data=['@AA2'])`

步骤1：API调用前置数据准备 `['@AA2']`
步骤2：API调用 `self.case.quality_shipped_immediately_sf()` 质检服务顺丰快递邮寄发货

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=2, placeOrderTime='now', businessTypeDesc='质检服务', statusDesc='待取件', expressTypeDesc='快递邮寄')`

---

### test_quality_shipped_immediately_own - [待发货]-质检服务-立即发货-自行邮寄-顺丰发货

**前置条件：** `self.pre.operations(data=['@AA2'])`

步骤1：API调用前置数据准备 `['@AA2']`
步骤2：API调用 `self.case.quality_shipped_immediately_own()` 质检服务自行邮寄顺丰发货

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=3, placeOrderTime='now', businessTypeDesc='质检服务', statusDesc='待收货', expressTypeDesc='自己送货/自取')`

---

### test_quality_deliver_it_yourself - [待发货]-质检服务-立即发货-自己送货

**前置条件：** `self.pre.operations(data=['@AA2'])`

步骤1：API调用前置数据准备 `['@AA2']`
步骤2：API调用 `self.case.quality_deliver_it_yourself()` 质检服务自己送货发货

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=3, placeOrderTime='now', businessTypeDesc='质检服务', statusDesc='待收货', expressTypeDesc='自己送货/自取')`

---

### test_quality_shipped_immediately_sf_the_next_day - [待发货]-质检服务-立即发货-顺丰快递邮寄-次日发货

**前置条件：** `self.pre.operations(data=['@AA2'])`

步骤1：API调用前置数据准备 `['@AA2']`
步骤2：API调用 `self.case.quality_shipped_immediately_sf_the_next_day()` 质检服务顺丰快递次日发货

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=2, placeOrderTime='now', businessTypeDesc='质检服务', statusDesc='待取件', expressTypeDesc='快递邮寄')`

---

### test_quality_shipped_immediately_no_address - [待发货]-质检服务-立即发货-顺丰快递邮寄-无发货地址

**前置条件：** `self.pre.operations(data=['@AA2'])`

步骤1：API调用前置数据准备 `['@AA2']`
步骤2：API调用 `self.case.quality_shipped_immediately_no_address()` 质检服务无发货地址时发货

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=3, placeOrderTime='now', businessTypeDesc='质检服务', statusDesc='自己送货/自取', expressTypeDesc='快递邮寄')`

---

### test_quality_shipped_immediately_no_shipping_address - [待发货]-质检服务-立即发货-顺丰快递邮寄-无收货地址

**前置条件：** `self.pre.operations(data=['@AA2'])`

步骤1：API调用前置数据准备 `['@AA2']`
步骤2：API调用 `self.case.quality_shipped_immediately_no_shipping_address()` 质检服务无收货地址时发货

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=2, placeOrderTime='now', businessTypeDesc='质检服务', statusDesc='待取件', expressTypeDesc='快递邮寄')`

---

### test_quality_cancel_the_order - [待发货]-质检服务-取消订单

**前置条件：** `self.pre.operations(data=['@AA2'])`

步骤1：API调用前置数据准备 `['@AA2']`
步骤2：API调用 `self.case.quality_cancel_the_order()` 质检服务取消订单

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=6, placeOrderTime='now', businessTypeDesc='质检服务', statusDesc='已取消')`

---

### test_quality_pending_pickup_cancel_the_order - [待取件]-质检服务-取消订单

**前置条件：** `self.pre.operations(data=['@AA2', '@AB7'])`

步骤1：API调用前置数据准备 `['@AA2', '@AB7']`
步骤2：API调用 `self.case.quality_pending_pickup_cancel_the_order()` 待取件状态质检服务取消订单

断言：调用方法：`self.fulfillment.order_manage(data='b', i=6, receiptNum=0, placeOrderTime='now', businessTypeDesc='质检服务', statusDesc='已取消')`

---

### test_quality_pending_receipt_cancel_the_order - [待收货]-质检服务-取消订单

**前置条件：** `self.pre.operations(data=['@AA2', '@AB1'])`

步骤1：API调用前置数据准备 `['@AA2', '@AB1']`
步骤2：API调用 `self.case.quality_pending_receipt_cancel_the_order()` 待收货状态质检服务取消订单

断言：调用方法：`self.fulfillment.order_manage(data='b', i=6, receiptNum=0, placeOrderTime='now', businessTypeDesc='质检服务', statusDesc='已取消')`

---

### test_quality_pending_receipt_of_reshipment_by_sf_logistics - [待收货]-质检服务-重新发货-顺丰快递

**前置条件：** `self.pre.operations(data=['@AA2', '@AB1'])`

步骤1：API调用前置数据准备 `['@AA2', '@AB1']`
步骤2：API调用 `self.case.quality_pending_receipt_of_reshipment_by_sf_logistics()` 质检服务重新发货（顺丰快递）

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=3, placeOrderTime='now', businessTypeDesc='质检服务', statusDesc='待收货', expressTypeDesc='自己送货')`

---

### test_quality_pending_receipt_reshipment_self_mailing - [待收货]-质检服务-重新发货-自行邮寄

**前置条件：** `self.pre.operations(data=['@AA2', '@AB1'])`

步骤1：API调用前置数据准备 `['@AA2', '@AB1']`
步骤2：API调用 `self.case.quality_pending_receipt_reshipment_self_mailing()` 质检服务重新发货（自行邮寄）

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=3, placeOrderTime='now', businessTypeDesc='质检服务', statusDesc='待收货', expressTypeDesc='自己送货/自取')`

---

### test_quality_pending_receipt_reshipment_deliver_it_yourself - [待收货]-质检服务-重新发货-自己送

**前置条件：** `self.pre.operations(data=['@AA2', '@AB1'])`

步骤1：API调用前置数据准备 `['@AA2', '@AB1']`
步骤2：API调用 `self.case.quality_pending_receipt_reshipment_deliver_it_yourself()` 质检服务重新发货（自己送）

断言：调用方法：`self.pc.fulfillment_order_manage_assert(i=3, placeOrderTime='now', businessTypeDesc='质检服务', statusDesc='待收货', expressTypeDesc='自己送货/自取')`

---

### test_quality_inspection_completed_express_return - [质检完成]-质检服务-快递退货

**前置条件：** `self.pre.operations(data=['@AA2', '@AB1', 'CA1', 'CB1', 'CB2'])`

步骤1：API调用前置数据准备 `['@AA2', '@AB1', 'CA1', 'CB1', 'CB2']`
步骤2：API调用 `self.case.quality_inspection_completed_express_return()` 质检完成后快递退货

断言：调用方法：`self.pc.fulfillment_returns_manage_assert(data='a', i=1, logisticsTypeStr='快递', applyTime='now')`

---

### test_quality_self_pickup_returns - [质检完成]-质检服务-自提退货

**前置条件：** `self.pre.operations(data=['@AA2', '@AB1', 'CA1', 'CB1', 'CB2'])`

步骤1：API调用前置数据准备 `['@AA2', '@AB1', 'CA1', 'CB1', 'CB2']`
步骤2：API调用 `self.case.quality_self_pickup_returns()` 质检完成后自提退货

断言：调用方法：`self.pc.fulfillment_returns_manage_assert(data='a', i=2, logisticsTypeStr='自提', applyTime='now')`

---

### test_quality_sales_re_inspection - [质检完成]-质检服务-复检

**前置条件：** `self.pre.operations(data=['@AA2', '@AB1', 'CA1', 'CB1', 'CB2'])`

步骤1：API调用前置数据准备 `['@AA2', '@AB1', 'CA1', 'CB1', 'CB2']`
步骤2：API调用 `self.case.quality_sales_re_inspection()` 质检完成后提交复检

断言：调用方法：`self.pc.fulfillment_quality_manage_assert(data='b', statusStr='待审核', applyTime='now')`
