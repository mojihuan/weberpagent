# 帮卖模块测试步骤

## TestHelpGenerateOrder - 帮卖管理|帮卖上架列表

### test_0self_dispatch - [批次列表tab]-去发货-自行邮寄发货成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4'])`

**登录方式：** `login='main'`

步骤1：点击密码登录
步骤2：输入账号 Y59800075
步骤3：输入密码 Aa123456
步骤4：点击登录
步骤5：导航到帮卖上架列表页面
步骤6：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4']`
步骤7：调用 `case.self_dispatch()` 自行邮寄发货

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待收货', updateTime='now')`

---

### test_sf_express_delivery_is_easy - [批次列表tab]-去发货-快递易发货成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4']`
步骤2：调用 `case.sf_express_delivery_is_easy()` 快递易发货

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待收货', updateTime='now')`

---

### test_send_it_yourself - [订单列表tab]-去发货-自己送发货成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4']`
步骤2：调用 `case.send_it_yourself()` 自己送发货

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待收货', updateTime='now')`

---

### test_cancel_order - [订单列表tab]-取消订单-取消成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4']`
步骤2：调用 `case.cancel_order()` 取消订单

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='已取消', updateTime='now')`

---

### test_manual_signature - [订单列表tab]-来货去退机-手动签收成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8', 'GB2'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8', 'GB2']`
步骤2：调用 `case.manual_signature()` 手动签收

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='已退机', updateTime='now')`

---

### test_logistics_signature - [订单列表tab]-来货去退机-物流签收成功

> **注意：** 此测试被 `@unittest.skip('')` 跳过，todo 物流签收入库后订单状态没改变

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8', 'GB3'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8', 'GB3']`
步骤2：调用 `case.logistics_signature()` 物流签收

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='已退机', updateTime='now')`

---

### test_confirmation_bond - [订单列表tab]-确认保卖-保卖买断订单保卖成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA9', 'GA6', 'HA1', 'GB1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA9', 'GA6', 'HA1', 'GB1']`
步骤2：调用 `case.confirmation_bond()` 确认保卖买断

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='已结算', updateTime='now')`

---

### test_profit_sharing_was_successful - [订单列表tab]-确认保卖-保卖分润订单保卖成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA10', 'GA6', 'HA1', 'GB1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA10', 'GA6', 'HA1', 'GB1']`
步骤2：调用 `case.profit_sharing_was_successful()` 确认保卖分润

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待售出', updateTime='now')`

---

### test_apply_for_bargaining - [订单列表tab]-申请议价-保卖买断-议价成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA9', 'GA6', 'HA1', 'GB1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA9', 'GA6', 'HA1', 'GB1']`
步骤2：调用 `case.apply_for_bargaining()` 申请议价

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待议价', updateTime='now')`

---

### test_apply_for_cancellation - [订单列表tab]-申请退机-退机成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1']`
步骤2：调用 `case.apply_for_cancellation()` 申请退机

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待退机', updateTime='now')`

---

### test_new_help_order - [批次列表tab]-发起帮卖-添加-生成帮卖订单成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1']`
步骤2：调用 `case.new_help_order()` 发起帮卖添加物品生成订单

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待发货', updateTime='now')`

---

### test_new_guaranteed_purchase_order - [批次列表tab]-发起帮卖-我要保卖-保卖买断创建订单成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1']`
步骤2：调用 `case.new_guaranteed_purchase_order()` 保卖买断创建订单

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待发货', updateTime='now')`

---

### test_new_profit_sharing_order - [批次列表tab]-发起帮卖-我要保卖-保卖分润创建订单成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1']`
步骤2：调用 `case.new_profit_sharing_order()` 保卖分润创建订单

断言：调用方法：`self.pc.help_generate_order_assert(orderStateStr='待发货', updateTime='now')`

---

### test_add_item - [发起帮卖]-下单添加物品成功

**前置条件：** `self.pre.operations(data=['FA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1']`
步骤2：调用 `case.add_item()` 下单添加物品

断言：调用方法：`self.pc.help_generate_order_assert(data='b', createTime='now')`

后续操作：`self.pre.operations(data=['GA2'])`

---

### test_place_an_order_to_add_items_delete_the_item - [发起帮卖]-下单添加物品-删除商品

**前置条件：** `self.pre.operations(data=['FA1', 'GA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1']`
步骤2：调用 `case.place_an_order_to_add_items_delete_the_item()` 下单添加物品后删除商品

> **注意：** 此测试无断言

---

## TestHelpSellTheListOfGoods - 帮卖管理|帮卖来货列表

### test_0the_flow_of_goods_is_signed - [批次列表tab]-帮卖发货-物流签收成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA6'])`

**登录方式：** `login='vice'`

步骤1：登录系统（vice 账号）
步骤2：导航到帮卖来货列表页面
步骤3：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA6']`
步骤4：调用 `case.the_flow_of_goods_is_signed()` 物流签收

断言：调用方法：`self.pc.help_sell_list_of_goods_assert(headers='vice', orderStateStr='待质检', updateTime='now')`

---

### test_order_logistics_signature - [订单列表tab]-帮卖发货-物流签收成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA6'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA6']`
步骤2：调用 `case.order_logistics_signature()` 物流签收

断言：调用方法：`self.pc.help_sell_list_of_goods_assert(headers='vice', orderStateStr='待质检', updateTime='now')`

---

### test_order_manual_signature - [订单列表tab]-帮卖发货-手动签收成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA6'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA6']`
步骤2：调用 `case.order_manual_signature()` 手动签收

断言：调用方法：`self.pc.help_sell_list_of_goods_assert(headers='vice', orderStateStr='待质检', updateTime='now')`

---

### test_manual_settlement - [订单列表tab]-手动结算-结算成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GB1', 'HB1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GB1', 'HB1']`
步骤2：调用 `case.manual_settlement()` 手动结算

断言：调用方法：`self.pc.help_sell_list_of_goods_assert(headers='vice', orderStateStr='待结算', updateTime='now')`

---

### test_de_check - [订单列表tab]-去复检-保卖分润订单-提交复检成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA10', 'GA6', 'HA1', 'GB1', 'GA12'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA10', 'GA6', 'HA1', 'GB1', 'GA12']`
步骤2：调用 `case.de_check()` 提交复检

断言：调用方法：`self.pc.help_sell_list_of_goods_assert(headers='vice', orderStateStr='待议价', updateTime='now')`

---

### test_audit_rejection - [订单列表tab]-审核-保卖分润订单-审核拒绝成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA10', 'GA6', 'HA1', 'GB1', 'GA12'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA10', 'GA6', 'HA1', 'GB1', 'GA12']`
步骤2：调用 `case.audit_rejection()` 审核拒绝

断言：调用方法：`self.pc.help_sell_list_of_goods_assert(headers='vice', orderStateStr='待确认', updateTime='now')`

---

### test_go_to_quality - [订单列表tab]-去质检-提交质检成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1']`
步骤2：调用 `case.go_to_quality()` 提交质检

断言：调用方法：`self.pc.help_sell_list_of_goods_assert(headers='vice', orderStateStr='待售出', updateTime='now')`

---

### test_express_delivery_unit_set_return_machine - [订单列表tab]-去退机-快递易-单台退-退机成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8']`
步骤2：调用 `case.express_delivery_unit_set_return_machine()` 快递易单台退机

断言：调用方法：`self.pc.help_sell_list_of_goods_assert(headers='vice', orderStateStr='退机中', updateTime='now')`

---

### test_express_delivery_same_batch_return_machine - [订单列表tab]-去退机-快递易-同批次退-退机成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8']`
步骤2：调用 `case.express_delivery_same_batch_return_machine()` 快递易同批次退机

断言：调用方法：`self.pc.help_sell_list_of_goods_assert(headers='vice', orderStateStr='退机中', updateTime='now')`

---

### test_mail_by_yourself - [订单列表tab]-去退机-自行邮寄-同下单商家退-退机成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8']`
步骤2：调用 `case.mail_by_yourself()` 自行邮寄退机

断言：调用方法：`self.pc.help_sell_list_of_goods_assert(headers='vice', orderStateStr='退机中', updateTime='now')`

---

### test_send_it_yourself_vice - [订单列表tab]-去退机-自己送-退机成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GA8']`
步骤2：调用 `case.send_it_yourself_vice()` 自己送退机

断言：调用方法：`self.pc.help_sell_list_of_goods_assert(headers='vice', orderStateStr='退机中', updateTime='now')`

---

### test_go_to_sale - [订单列表tab]-去销售成功

**前置条件：** `self.pre.operations(data=['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GB1'])`

**登录方式：** `login` 未指定（复用前一个登录状态）

步骤1：执行前置数据准备 `['FA1', 'GA1', 'GA3', 'GA4', 'GA7', 'HA1', 'GB1']`
步骤2：调用 `case.go_to_sale()` 去销售

断言：调用方法：`self.pc.help_sell_list_of_goods_assert(headers='vice', orderStateStr='待售出', updateTime='now')`

---

## TestHelpServiceConfiguration - 帮卖管理|帮卖业务配置

### test_0edit_configuration_information - [编辑]-修改配置信息

**前置条件：** 无额外前置数据

**登录方式：** `login='vice'`

步骤1：登录系统（vice 账号）
步骤2：导航到帮卖业务配置页面
步骤3：调用 `case.edit_configuration_information()` 修改配置信息

断言：调用方法：`self.pc.help_service_configuration_assert(headers='vice', sellTimeoutConfiguration=30)`
