---
phase: 111-stepcodebuffer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/step_code_buffer.py
  - backend/tests/unit/test_step_code_buffer.py
autonomous: true
requirements: [CODEGEN-01, CODEGEN-03, CODEGEN-04, VAL-01]

must_haves:
  truths:
    - "append_step() 接收 action_dict，通过 ActionTranslator 同步翻译为 TranslatedAction，创建 StepRecord 存储"
    - "_derive_wait() 对 navigate 返回 wait_for_load_state、耗时>800ms 返回实际耗时、click 返回 300ms"
    - "assemble() 将 StepRecord 展平为 list[TranslatedAction] 并委托 PlaywrightCodeGenerator.generate() 组装完整测试文件"
    - "assemble() 返回的代码通过 ast.parse 语法验证"
    - "单元测试覆盖 append_step 同步翻译、wait 推导 3 种策略、assemble 组装、空 buffer 边界"
  artifacts:
    - path: "backend/core/step_code_buffer.py"
      provides: "StepCodeBuffer 类 + StepRecord dataclass"
      exports: ["StepCodeBuffer", "StepRecord"]
      min_lines: 120
    - path: "backend/tests/unit/test_step_code_buffer.py"
      provides: "VAL-01 单元测试（同步 append + wait + assemble 部分）"
      exports: ["test_append_step_sync", "test_derive_wait_navigate", "test_derive_wait_click", "test_derive_wait_long_duration", "test_assemble_complete", "test_assemble_empty_buffer", "test_assemble_syntax_valid"]
  key_links:
    - from: "backend/core/step_code_buffer.py"
      to: "backend/core/action_translator.py"
      via: "ActionTranslator().translate(action_dict)"
      pattern: "ActionTranslator"
    - from: "backend/core/step_code_buffer.py"
      to: "backend/core/code_generator.py"
      via: "PlaywrightCodeGenerator().generate(...)"
      pattern: "PlaywrightCodeGenerator"
---

<objective>
创建 StepCodeBuffer 核心实现：StepRecord dataclass + 同步 append_step() + _derive_wait() 三策略 + assemble() 委托组装 + 基础单元测试。

Purpose: 建立逐步代码翻译缓冲区的数据结构和核心同步翻译能力，覆盖 CODEGEN-01、CODEGEN-03、CODEGEN-04 需求。
Output: step_code_buffer.py（可独立运行的 StepCodeBuffer 类）+ test_step_code_buffer.py（同步翻译和组装测试）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-stepcodebuffer/111-CONTEXT.md
@.planning/phases/111-stepcodebuffer/111-RESEARCH.md
@.planning/phases/111-stepcodebuffer/111-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/core/action_translator.py (line 20-28):
```python
@dataclass(frozen=True)
class TranslatedAction:
    """单个操作的 Playwright 翻译结果（不可变）。"""
    code: str              # 生成的 Playwright 代码行
    action_type: str       # 原始操作类型
    is_comment: bool       # True 表示代码是注释
    has_locator: bool      # True 表示使用了定位器
    locators: tuple[str, ...] = ()  # 定位器链元数据
```

From backend/core/action_translator.py (line 64):
```python
def translate(self, action: dict) -> TranslatedAction:
```

From backend/core/action_translator.py (line 106):
```python
def translate_with_llm(self, action: dict, llm_snippet: str = "") -> TranslatedAction:
```

From backend/core/action_translator.py (line 154-165):
```python
@staticmethod
def _identify_action_type(action: dict) -> str:
    for key in action:
        if key != "interacted_element":
            return key
    return "unknown"
```

From backend/core/code_generator.py (line 26-47):
```python
class PlaywrightCodeGenerator:
    def __init__(self) -> None:
        self._translator = ActionTranslator()

    def generate(
        self,
        run_id: str,
        task_name: str,
        task_id: str,
        actions: list[TranslatedAction],
        precondition_config: dict | None = None,
        assertions_config: list[dict] | None = None,
    ) -> str:
```

From backend/core/code_generator.py (line 418-424):
```python
@staticmethod
def validate_syntax(code: str) -> bool:
    try:
        ast.parse(code)
        return True
    except SyntaxError:
        return False
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 StepRecord dataclass + StepCodeBuffer 同步翻译核心</name>
  <files>backend/core/step_code_buffer.py</files>
  <read_first>
    backend/core/action_translator.py (line 20-28: TranslatedAction, line 59-105: translate)
    backend/core/code_generator.py (line 39-47: generate signature, line 418-424: validate_syntax)
    .planning/codebase/CONVENTIONS.md (frozen dataclass pattern, Chinese comments)
  </read_first>
  <behavior>
    - test_append_step_sync: 调用 buffer.append_step(action_dict) 后，内部 _records 长度为 1，record.action 是 TranslatedAction 实例，record.step_index == 0，record.wait_before 非空（如果操作是 click）
    - test_append_step_increments_index: 连续 3 次 append_step，step_index 分别为 0, 1, 2
    - test_append_multiple_steps: 3 次 append_step 后 _records 长度为 3
    - test_derive_wait_navigate: action_type="navigate" 返回 '    page.wait_for_load_state("networkidle")'
    - test_derive_wait_click: action_type="click", duration=None 返回 '    page.wait_for_timeout(300)'
    - test_derive_wait_long_duration: action_type="input", duration=1.5 返回 '    page.wait_for_timeout(1500)'
    - test_derive_wait_no_wait: action_type="scroll", duration=0.3 返回空字符串 ""
    - test_assemble_complete: append 3 个不同类型步骤后 assemble() 返回字符串包含 "def test_" 且 ast.parse 通过
    - test_assemble_empty_buffer: 空 buffer 的 assemble() 返回 ast.parse 合法的 Python（只有 header + def 无函数体）
    - test_assemble_syntax_valid: 包含 navigate + click 步骤的 assemble 结果通过 PlaywrightCodeGenerator.validate_syntax()
  </behavior>
  <action>
    创建 backend/core/step_code_buffer.py，实现以下内容：

    1. **StepRecord frozen dataclass**（per D-01）：
       ```python
       @dataclass(frozen=True)
       class StepRecord:
           action: TranslatedAction
           wait_before: str = ""
           step_index: int = 0
       ```

    2. **StepCodeBuffer 类**，构造函数：
       - `__init__(self) -> None` — 初始化 `_records: list[StepRecord] = []`，`_next_index: int = 0`，`_translator = ActionTranslator()`，`_generator = PlaywrightCodeGenerator()`
       - 注意：base_dir 和 run_id 暂不在此构造函数中（append_step_async 在 Plan 02 需要，但同步方法不需要）

    3. **append_step() 方法**（per CODEGEN-01, D-01）：
       - 签名：`def append_step(self, action_dict: dict, duration: float | None = None) -> None`
       - 内部调用 `self._translator.translate(action_dict)` 获取 TranslatedAction
       - 调用 `self._derive_wait(action_type, duration)` 获取 wait_before 字符串
       - action_type 通过 `ActionTranslator._identify_action_type(action_dict)` 获取（调用静态方法，与 code_generator.py line 206 模式一致）
       - 创建 StepRecord(action=translated, wait_before=wait_code, step_index=self._next_index)
       - 追加到 `self._records`，递增 `self._next_index`

    4. **_derive_wait() 方法**（per CODEGEN-03, D-04）：
       - 签名：`def _derive_wait(self, action_type: str, duration: float | None = None) -> str`
       - 三种策略（优先级从高到低）：
         a. action_type == "navigate" → `'    page.wait_for_load_state("networkidle")'`
         b. duration is not None and duration > 0.8 → `f"    page.wait_for_timeout({int(duration * 1000)})"`
         c. action_type == "click" → `"    page.wait_for_timeout(300)"`
         d. 其他 → `""`（空字符串，无等待）
       - 注意：navigate 优先级最高，无论 duration 如何都返回 wait_for_load_state

    5. **assemble() 方法**（per CODEGEN-04, D-05, D-06）：
       - 签名：
         ```python
         def assemble(
             self,
             run_id: str,
             task_name: str,
             task_id: str,
             precondition_config: dict | None = None,
             assertions_config: list[dict] | None = None,
         ) -> str:
         ```
       - 遍历 self._records，展平为 list[TranslatedAction]：
         - 如果 record.wait_before 非空，创建一个 TranslatedAction(code=record.wait_before, action_type="wait", is_comment=False, has_locator=False) 插入到主操作之前（per D-06）
         - 然后追加 record.action
       - 调用 `self._generator.generate(run_id, task_name, task_id, flat_actions, precondition_config, assertions_config)` 返回结果
       - assemble() 返回 generate() 的返回值，不做额外处理

    6. **records 属性**：
       - `@property def records(self) -> list[StepRecord]: return list(self._records)` — 返回副本，保持不可变性

    7. **文件头部**：
       - docstring: `"""StepCodeBuffer -- 逐步累积翻译结果，组装完整 Playwright 测试文件。"""`
       - imports: dataclass, logging, ActionTranslator, TranslatedAction, PlaywrightCodeGenerator
       - logger = logging.getLogger(__name__)
       - 中文注释（per CONVENTIONS.md）

    不要在 Plan 01 中添加 append_step_async 或任何 LLMHealer 相关代码。Plan 02 负责 CODEGEN-02。
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_step_code_buffer.py -v -k "append_step_sync or append_step_increments or append_multiple or derive_wait or assemble" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - backend/core/step_code_buffer.py 存在且包含 `class StepRecord`
    - backend/core/step_code_buffer.py 包含 `@dataclass(frozen=True)` 在 StepRecord 上方
    - backend/core/step_code_buffer.py 包含 `class StepCodeBuffer`
    - backend/core/step_code_buffer.py 包含 `def append_step(self, action_dict: dict, duration: float | None = None)`
    - backend/core/step_code_buffer.py 包含 `def _derive_wait(self, action_type: str, duration: float | None = None)`
    - backend/core/step_code_buffer.py 包含 `def assemble(self, run_id: str, task_name: str, task_id: str, precondition_config: dict | None = None, assertions_config: list[dict] | None = None)`
    - backend/core/step_code_buffer.py 包含 `self._translator.translate(action_dict)`
    - backend/core/step_code_buffer.py 包含 `self._generator.generate(`
    - backend/core/step_code_buffer.py 包含 `page.wait_for_load_state("networkidle")`
    - backend/core/step_code_buffer.py 包含 `page.wait_for_timeout(300)`
    - backend/tests/unit/test_step_code_buffer.py 存在
    - `uv run pytest backend/tests/unit/test_step_code_buffer.py -v` 全部 PASSED
  </acceptance_criteria>
  <done>
    StepCodeBuffer.append_step() 接收 action_dict，通过 ActionTranslator.translate() 同步翻译为 TranslatedAction，创建 StepRecord 存储翻译结果 + wait_before + step_index。_derive_wait() 对 navigate 返回 wait_for_load_state、耗时>800ms 返回实际耗时、click 返回 300ms。assemble() 展平 StepRecord 为 TranslatedAction 列表并委托 PlaywrightCodeGenerator.generate() 组装。单元测试覆盖 append 同步翻译、wait 推导 3 种策略、assemble 组装、空 buffer 边界、语法验证。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 StepCodeBuffer 单元测试（VAL-01 同步部分）</name>
  <files>backend/tests/unit/test_step_code_buffer.py</files>
  <read_first>
    backend/core/step_code_buffer.py (刚创建的实现)
    backend/core/action_translator.py (line 20-28: TranslatedAction 构造方式)
    backend/tests/unit/test_code_api.py (测试 fixture 和 mock 模式参考)
  </read_first>
  <behavior>
    - test_append_step_sync: action_dict={"click": {"index": 5}, "interacted_element": mock_elem} → append_step 后 buffer.records 长度 1，record.step_index==0，record.action 是 TranslatedAction 实例
    - test_append_step_increments_index: 3 次 append_step → step_index 分别 0, 1, 2
    - test_append_step_navigate_generates_wait: action_dict={"navigate": {"url": "https://example.com"}} → record.wait_before 包含 "wait_for_load_state"
    - test_derive_wait_navigate: _derive_wait("navigate", None) == '    page.wait_for_load_state("networkidle")'
    - test_derive_wait_click: _derive_wait("click", None) == '    page.wait_for_timeout(300)'
    - test_derive_wait_long_duration: _derive_wait("input", 1.5) == '    page.wait_for_timeout(1500)'
    - test_derive_wait_no_wait: _derive_wait("scroll", 0.3) == ""
    - test_derive_wait_navigate_ignores_duration: _derive_wait("navigate", 2.0) 仍然返回 wait_for_load_state（navigate 优先级最高）
    - test_assemble_complete: 3 个不同步骤后 assemble 返回包含 "def test_" 的字符串，ast.parse 通过
    - test_assemble_empty_buffer: 空 buffer assemble 返回 ast.parse 合法的 Python
    - test_assemble_includes_waits: navigate 步骤后 assemble 返回的代码包含 "wait_for_load_state"
    - test_assemble_syntax_valid: 多步骤 assemble 结果通过 PlaywrightCodeGenerator.validate_syntax()
    - test_records_returns_copy: buffer.records 的修改不影响内部 _records（不可变性）
  </behavior>
  <action>
    创建 backend/tests/unit/test_step_code_buffer.py，包含以下测试：

    **Fixtures:**
    - `buffer()` — 返回 StepCodeBuffer() 实例
    - `mock_elem` — Mock 对象，模拟 interacted_element，具有 attributes={}, node_name="BUTTON", x_path="//button", ax_name="提交"
    - `click_action` — dict: {"click": {"index": 5}, "interacted_element": mock_elem}
    - `navigate_action` — dict: {"navigate": {"url": "https://example.com"}}
    - `input_action` — dict: {"input": {"text": "hello", "index": 3}, "interacted_element": mock_elem}
    - `scroll_action` — dict: {"scroll": {"direction": "down", "amount": 500}}

    **append_step 同步翻译测试:**
    - test_append_step_sync: 验证 append_step(click_action) 后 records 长度 1，record.step_index==0，record.action.code 非空，record.action 是 TranslatedAction 实例
    - test_append_step_increments_index: 3 次 append_step(click_action) → records[i].step_index == i
    - test_append_step_navigate_generates_wait: append_step(navigate_action) → record.wait_before 包含 "wait_for_load_state"

    **_derive_wait 策略测试（通过 buffer 内部方法测试）:**
    - 直接调用 buffer._derive_wait() 验证返回值：
      - test_derive_wait_navigate: buffer._derive_wait("navigate", None) == '    page.wait_for_load_state("networkidle")'
      - test_derive_wait_click: buffer._derive_wait("click", None) == '    page.wait_for_timeout(300)'
      - test_derive_wait_long_duration: buffer._derive_wait("input", 1.5) == '    page.wait_for_timeout(1500)'
      - test_derive_wait_no_wait: buffer._derive_wait("scroll", 0.3) == ""
      - test_derive_wait_navigate_ignores_duration: buffer._derive_wait("navigate", 2.0) == '    page.wait_for_load_state("networkidle")'

    **assemble 组装测试:**
    - test_assemble_complete: buffer.append_step(navigate_action), append_step(click_action), append_step(input_action) → assemble("run1", "测试任务", "task1") 返回包含 "def test_" 的字符串 → ast.parse(result) 不抛异常
    - test_assemble_empty_buffer: 空 buffer → assemble("run1", "空任务", "task1") → ast.parse(result) 不抛异常
    - test_assemble_includes_waits: navigate 步骤后 assemble 的结果包含 "wait_for_load_state"
    - test_assemble_syntax_valid: 多步骤结果 → PlaywrightCodeGenerator.validate_syntax(result) == True

    **不可变性测试:**
    - test_records_returns_copy: buffer.append_step(click_action); records = buffer.records; records.clear() → buffer.records 仍然长度 1

    测试文件头部：
    ```python
    """StepCodeBuffer 单元测试 -- VAL-01 同步翻译、wait 推导、assemble 组装。"""
    import ast
    from unittest.mock import MagicMock

    import pytest

    from backend.core.action_translator import TranslatedAction
    from backend.core.code_generator import PlaywrightCodeGenerator
    from backend.core.step_code_buffer import StepCodeBuffer, StepRecord
    ```

    不测试 append_step_async — Plan 02 负责 CODEGEN-02 测试。
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_step_code_buffer.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/unit/test_step_code_buffer.py 存在
    - 文件包含 `def test_append_step_sync`
    - 文件包含 `def test_derive_wait_navigate`
    - 文件包含 `def test_derive_wait_click`
    - 文件包含 `def test_derive_wait_long_duration`
    - 文件包含 `def test_assemble_complete`
    - 文件包含 `def test_assemble_empty_buffer`
    - 文件包含 `def test_assemble_syntax_valid`
    - `uv run pytest backend/tests/unit/test_step_code_buffer.py -v` 全部 PASSED，0 failed
  </acceptance_criteria>
  <done>
    test_step_code_buffer.py 包含 13+ 测试用例，覆盖 append_step 同步翻译、_derive_wait 3 种策略 + navigate 优先级、assemble 组装 + 空 buffer + 语法验证、records 不可变性。全部 PASSED。
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest backend/tests/unit/test_step_code_buffer.py -v` — 全部 PASSED
2. `uv run python -c "from backend.core.step_code_buffer import StepCodeBuffer, StepRecord; print('import OK')"` — 无导入错误
3. `uv run pytest backend/tests/unit/test_step_code_buffer.py -k derive_wait -v` — wait 推导 3 策略 + navigate 优先级测试通过
4. `uv run pytest backend/tests/unit/test_step_code_buffer.py -k assemble -v` — assemble 组装 + 语法验证测试通过
</verification>

<success_criteria>
- StepCodeBuffer.append_step() 同步翻译 action_dict 为 TranslatedAction，创建 StepRecord 存储
- _derive_wait() navigate → wait_for_load_state、duration>0.8 → 实际耗时、click → 300ms
- assemble() 委托 PlaywrightCodeGenerator.generate() 组装完整测试文件
- assemble() 返回值通过 ast.parse 语法验证
- 13+ 单元测试全部 PASSED，覆盖 CODEGEN-01、CODEGEN-03、CODEGEN-04、VAL-01（同步部分）
</success_criteria>

<output>
After completion, create `.planning/phases/111-stepcodebuffer/111-01-SUMMARY.md`
</output>
