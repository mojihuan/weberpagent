---
phase: 111-stepcodebuffer
plan: 02
type: execute
wave: 2
depends_on: ["111-01"]
files_modified:
  - backend/core/step_code_buffer.py
  - backend/tests/unit/test_step_code_buffer.py
autonomous: true
requirements: [CODEGEN-02, VAL-01]

must_haves:
  truths:
    - "append_step_async() 检测弱步骤（elem=None 或 <=1 locator），即时调用 LLMHealer.heal() 修复"
    - "append_step_async() 从磁盘读取 DOM 快照文件 {base_dir}/{run_id}/dom/step_{n}.txt"
    - "修复失败时 append_step_async() fallback 到原始翻译结果"
    - "单元测试覆盖弱步骤检测（elem=None、<=1 locator）、DOM 读取、修复成功/失败、非 click/input 跳过修复"
  artifacts:
    - path: "backend/core/step_code_buffer.py"
      provides: "append_step_async() 弱步骤修复方法"
      contains: "async def append_step_async"
    - path: "backend/tests/unit/test_step_code_buffer.py"
      provides: "VAL-01 异步修复测试"
      exports: ["test_async_heals_weak_step_no_elem", "test_async_heals_weak_step_single_locator", "test_async_reads_dom_snapshot", "test_async_heal_failure_fallback"]
  key_links:
    - from: "backend/core/step_code_buffer.py"
      to: "backend/core/llm_healer.py"
      via: "LLMHealer(llm_config).heal(action_type, failed_locators, dom_content, action_params)"
      pattern: "LLMHealer"
    - from: "backend/core/step_code_buffer.py"
      to: "backend/core/locator_chain_builder.py"
      via: "LocatorChainBuilder().extract(elem, action_type)"
      pattern: "LocatorChainBuilder"
---

<objective>
为 StepCodeBuffer 添加 append_step_async() 弱步骤修复能力 + 完善单元测试覆盖 CODEGEN-02 需求。

Purpose: 实现逐步即时修复弱步骤（elem=None 或 <=1 locator），修复时 DOM 上下文最新，替代旧的批量修复模式。
Output: step_code_buffer.py 增加 append_step_async() 方法 + test_step_code_buffer.py 增加异步修复测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-stepcodebuffer/111-CONTEXT.md
@.planning/phases/111-stepcodebuffer/111-RESEARCH.md
@.planning/phases/111-stepcodebuffer/111-01-SUMMARY.md

<interfaces>
<!-- Key types from dependencies. Executor must read step_code_buffer.py first to see Plan 01 output. -->

From backend/core/llm_healer.py (line 89-107, 187-208):
```python
@dataclasses.dataclass(frozen=True)
class LLMHealResult:
    success: bool
    code_snippet: str
    raw_response: str
    locator: str
    target_snippet: str = ""
    replacement: str = ""

class LLMHealer:
    def __init__(self, llm_config: dict) -> None:
        self._llm = create_llm(llm_config)

    async def heal(
        self,
        action_type: str,
        failed_locators: tuple[str, ...],
        dom_snapshot: str,
        action_params: dict,
    ) -> LLMHealResult:
```

From backend/core/locator_chain_builder.py (line 38-56):
```python
class LocatorChainBuilder:
    def extract(self, elem: Any, action_type: str) -> list[str]:
```

From backend/core/code_generator.py (line 205-257) — _heal_weak_steps 弱步骤检测模式:
```python
# 弱步骤检测逻辑（Plan 02 必须复制此模式）
elem = action.get("interacted_element")
needs_healing = False
failed_locators: tuple[str, ...] = ()

if elem is None:
    needs_healing = True
else:
    locators = translator._chain_builder.extract(elem, action_type)
    if len(locators) <= 1:
        needs_healing = True
        failed_locators = tuple(locators)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 StepCodeBuffer 添加 append_step_async() 弱步骤修复</name>
  <files>backend/core/step_code_buffer.py</files>
  <read_first>
    backend/core/step_code_buffer.py (Plan 01 创建的现有实现)
    backend/core/llm_healer.py (line 89-107: LLMHealResult, line 187-208: heal 方法签名)
    backend/core/locator_chain_builder.py (line 45: extract 方法)
    backend/core/code_generator.py (line 205-257: _heal_weak_steps 弱步骤检测模式)
  </read_first>
  <behavior>
    - test_async_heals_weak_step_no_elem: action_dict 中 interacted_element=None → append_step_async 调用 healer.heal()，heal 返回 success=True → record.action.code 包含 LLM 修复的代码
    - test_async_heals_weak_step_single_locator: elem 只有 1 个 locator → append_step_async 调用 healer.heal()
    - test_async_no_heal_strong_step: elem 有 2+ locator → append_step_async 不调用 healer，使用原始翻译
    - test_async_reads_dom_snapshot: append_step_async 读取 DOM 文件 {base_dir}/{run_id}/dom/step_{n}.txt
    - test_async_dom_missing_fallback: DOM 文件不存在 → 不修复，使用原始翻译
    - test_async_heal_failure_fallback: healer.heal() 返回 success=False → 使用原始翻译
    - test_async_heal_exception_fallback: healer.heal() 抛异常 → 使用原始翻译，不崩溃
    - test_async_non_click_input_skip: navigate 操作 → 不检测弱步骤，直接翻译
  </behavior>
  <action>
    修改 backend/core/step_code_buffer.py，添加以下内容：

    **1. 更新 imports：**
    - 添加 `from pathlib import Path`
    - 添加 `from backend.core.llm_healer import LLMHealer`
    - 添加 `from backend.core.locator_chain_builder import LocatorChainBuilder`

    **2. 更新 __init__ 签名（保持向后兼容）：**
    ```python
    def __init__(
        self,
        *,
        base_dir: str = "",
        run_id: str = "",
        llm_config: dict | None = None,
    ) -> None:
        self._records: list[StepRecord] = []
        self._next_index: int = 0
        self._translator = ActionTranslator()
        self._generator = PlaywrightCodeGenerator()
        self._chain_builder = LocatorChainBuilder()
        self._base_dir = base_dir
        self._run_id = run_id
        self._llm_config = llm_config or {}
    ```
    注意 `*` 使所有参数 keyword-only，base_dir 和 run_id 默认空字符串保持向后兼容（Plan 01 的测试不受影响）。

    **3. 添加 _is_weak_step() 私有方法：**
    ```python
    def _is_weak_step(self, action_dict: dict, action_type: str) -> tuple[bool, tuple[str, ...]]:
        """检测弱步骤，返回 (needs_healing, failed_locators)。"""
        elem = action_dict.get("interacted_element")
        if elem is None:
            return True, ()
        locators = self._chain_builder.extract(elem, action_type)
        if len(locators) <= 1:
            return True, tuple(locators)
        return False, ()
    ```

    **4. 添加 append_step_async() 方法**（per CODEGEN-02, D-02, D-03）：
    ```python
    async def append_step_async(
        self,
        action_dict: dict,
        duration: float | None = None,
    ) -> None:
        """翻译操作并尝试修复弱步骤（elem=None 或 <=1 locator）。

        仅对 click/input 操作检测弱步骤。
        DOM 快照路径: {base_dir}/{run_id}/dom/step_{n}.txt（1-indexed）。
        修复失败时 fallback 到原始翻译结果。
        """
        action_type = ActionTranslator._identify_action_type(action_dict)
        translated = self._translator.translate(action_dict)

        # 仅 click/input 需要弱步骤检测（per code_generator.py D-07）
        if action_type in ("click", "input"):
            needs_healing, failed_locators = self._is_weak_step(action_dict, action_type)
            if needs_healing:
                # DOM 快照路径（1-indexed per Pitfall 1）
                dom_path = Path(self._base_dir) / self._run_id / "dom" / f"step_{self._next_index + 1}.txt"
                if dom_path.exists():
                    dom_content = dom_path.read_text(encoding="utf-8")
                    action_params = action_dict.get(action_type, {})
                    try:
                        healer = LLMHealer(self._llm_config)
                        result = await healer.heal(
                            action_type, failed_locators, dom_content, action_params
                        )
                        if result.success:
                            translated = self._translator.translate_with_llm(
                                action_dict, result.code_snippet
                            )
                            logger.info(
                                f"[{self._run_id}] Step {self._next_index + 1} "
                                f"LLM 修复成功"
                            )
                    except Exception as exc:
                        logger.warning(
                            f"[{self._run_id}] Step {self._next_index + 1} "
                            f"LLM 修复异常: {exc}"
                        )

        wait_before = self._derive_wait(action_type, duration)
        record = StepRecord(
            action=translated,
            wait_before=wait_before,
            step_index=self._next_index,
        )
        self._records.append(record)
        self._next_index += 1
    ```

    **不要修改已有的 append_step()、_derive_wait()、assemble() 方法签名或逻辑。**
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_step_code_buffer.py -v -k "async or heal or weak or dom" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - backend/core/step_code_buffer.py 包含 `async def append_step_async(self, action_dict: dict, duration: float | None = None)`
    - backend/core/step_code_buffer.py 包含 `def _is_weak_step(self, action_dict: dict, action_type: str)`
    - backend/core/step_code_buffer.py 包含 `from backend.core.llm_healer import LLMHealer`
    - backend/core/step_code_buffer.py 包含 `from backend.core.locator_chain_builder import LocatorChainBuilder`
    - backend/core/step_code_buffer.py 包含 `dom_path = Path(self._base_dir)`
    - backend/core/step_code_buffer.py 包含 `healer.heal(action_type, failed_locators, dom_content, action_params)`
    - backend/core/step_code_buffer.py 包含 `self._translator.translate_with_llm(action_dict, result.code_snippet)`
    - Plan 01 的现有测试仍然 PASSED（不回归）
  </acceptance_criteria>
  <done>
    append_step_async() 检测弱步骤（elem=None 或 <=1 locator），从磁盘读取 DOM 快照，调用 LLMHealer.heal() 修复。修复成功时使用 translate_with_llm 重新翻译。修复失败（异常或 success=False 或 DOM 缺失）时 fallback 到原始翻译结果。仅 click/input 操作检测弱步骤。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 append_step_async() 单元测试（VAL-01 异步部分）</name>
  <files>backend/tests/unit/test_step_code_buffer.py</files>
  <read_first>
    backend/core/step_code_buffer.py (刚更新的 append_step_async 实现)
    backend/core/llm_healer.py (line 89-107: LLMHealResult 冻结数据类，用于构造 mock 返回值)
    backend/core/code_generator.py (line 205-257: 弱步骤检测模式参考)
  </read_first>
  <behavior>
    - test_async_heals_weak_step_no_elem: elem=None → healer.heal() 被调用 → 成功修复后 record.action.code 包含 LLM 代码
    - test_async_heals_weak_step_single_locator: elem 有 1 个 locator → healer.heal() 被调用
    - test_async_no_heal_strong_step: elem 有 2+ locator → healer.heal() 不被调用
    - test_async_reads_dom_snapshot: 验证 DOM 文件路径正确 {base_dir}/{run_id}/dom/step_{n}.txt
    - test_async_dom_missing_fallback: DOM 文件不存在 → healer.heal() 不被调用，使用原始翻译
    - test_async_heal_failure_fallback: heal() 返回 LLMHealResult(success=False, ...) → 使用原始翻译
    - test_async_heal_exception_fallback: heal() 抛 RuntimeError → 使用原始翻译
    - test_async_non_click_input_skip: navigate 操作 → healer.heal() 不被调用
    - test_async_step_index_increments: 多次 append_step_async → step_index 正确递增
  </behavior>
  <action>
    在 backend/tests/unit/test_step_code_buffer.py 中添加以下测试（追加到 Plan 01 已有测试之后）：

    **新增 imports（如果还没有）：**
    ```python
    from pathlib import Path
    from unittest.mock import AsyncMock, MagicMock, patch
    import pytest
    from backend.core.llm_healer import LLMHealResult
    ```

    **新增 fixtures:**
    - `async_buffer(tmp_path)` — 返回 StepCodeBuffer(base_dir=str(tmp_path), run_id="test_run", llm_config={"model": "test"})
    - `mock_elem_weak` — Mock elem 只有 1 个 locator（设置 x_path 但无 ax_name/id/data-testid）
    - `mock_elem_strong` — Mock elem 有 2+ locator（设置 x_path + ax_name + id）
    - `click_action_no_elem` — dict: {"click": {"index": 5}, "interacted_element": None}
    - `click_action_weak` — dict: {"click": {"index": 5}, "interacted_element": mock_elem_weak}
    - `click_action_strong` — dict: {"click": {"index": 5}, "interacted_element": mock_elem_strong}
    - `navigate_action` — dict: {"navigate": {"url": "https://example.com"}}

    **弱步骤检测测试:**
    - test_async_heals_weak_step_no_elem:
      - 创建 DOM 文件 {tmp_path}/test_run/dom/step_1.txt
      - patch LLMHealer.heal 返回 LLMHealResult(success=True, code_snippet="    page.locator('.btn').click()", raw_response="", locator="")
      - await buffer.append_step_async(click_action_no_elem)
      - assert buffer.records[0].action.code 包含 "page.locator('.btn')"

    - test_async_heals_weak_step_single_locator:
      - 创建 DOM 文件
      - patch LLMHealer.heal 返回 success=True
      - await buffer.append_step_async(click_action_weak)
      - 验证 heal 被调用

    - test_async_no_heal_strong_step:
      - await buffer.append_step_async(click_action_strong)
      - 验证 LLMHealer.heal 不被调用
      - record.action.code 是 ActionTranslator 翻译的原始代码

    **DOM 快照测试:**
    - test_async_reads_dom_snapshot:
      - 创建 DOM 文件并写入内容 "<html>test</html>"
      - patch LLMHealer.heal，capture heal 的 dom_snapshot 参数
      - 验证 dom_snapshot == "<html>test</html>"

    - test_async_dom_missing_fallback:
      - 不创建 DOM 文件
      - await buffer.append_step_async(click_action_no_elem)
      - 验证 LLMHealer.heal 不被调用
      - record.action 是原始翻译

    **修复失败 fallback 测试:**
    - test_async_heal_failure_fallback:
      - 创建 DOM 文件
      - patch LLMHealer.heal 返回 LLMHealResult(success=False, code_snippet="", raw_response="error", locator="")
      - await buffer.append_step_async(click_action_no_elem)
      - record.action 是原始翻译（不是 LLM 代码）

    - test_async_heal_exception_fallback:
      - 创建 DOM 文件
      - patch LLMHealer.heal 抛 RuntimeError("LLM timeout")
      - await buffer.append_step_async(click_action_no_elem)
      - 不抛异常，record.action 是原始翻译

    **非 click/input 跳过修复:**
    - test_async_non_click_input_skip:
      - await buffer.append_step_async(navigate_action)
      - LLMHealer.heal 不被调用
      - record.action 是 navigate 翻译

    **步骤索引递增:**
    - test_async_step_index_increments:
      - await buffer.append_step_async(click_action_strong) 3 次
      - records[0].step_index == 0, records[1].step_index == 1, records[2].step_index == 2

    **测试标记:**
    所有 async 测试使用 `@pytest.mark.asyncio` 装饰器。pytest-asyncio 使用 auto mode，不需要手动标记。

    确保所有 Plan 01 的测试仍然 PASSED（不修改任何已有测试）。
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_step_code_buffer.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/unit/test_step_code_buffer.py 包含 `def test_async_heals_weak_step_no_elem`
    - backend/tests/unit/test_step_code_buffer.py 包含 `def test_async_heals_weak_step_single_locator`
    - backend/tests/unit/test_step_code_buffer.py 包含 `def test_async_no_heal_strong_step`
    - backend/tests/unit/test_step_code_buffer.py 包含 `def test_async_reads_dom_snapshot`
    - backend/tests/unit/test_step_code_buffer.py 包含 `def test_async_dom_missing_fallback`
    - backend/tests/unit/test_step_code_buffer.py 包含 `def test_async_heal_failure_fallback`
    - backend/tests/unit/test_step_code_buffer.py 包含 `def test_async_heal_exception_fallback`
    - backend/tests/unit/test_step_code_buffer.py 包含 `def test_async_non_click_input_skip`
    - `uv run pytest backend/tests/unit/test_step_code_buffer.py -v` 全部 PASSED，0 failed
  </acceptance_criteria>
  <done>
    test_step_code_buffer.py 新增 9 个异步测试，覆盖弱步骤检测（elem=None、<=1 locator、2+ locator 跳过）、DOM 读取（存在/缺失）、修复结果（成功/失败/异常）、非 click/input 跳过。Plan 01 + Plan 02 全部测试 PASSED。
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest backend/tests/unit/test_step_code_buffer.py -v` — 全部 PASSED（Plan 01 + Plan 02 合计 22+ 测试）
2. `uv run pytest backend/tests/unit/test_step_code_buffer.py -k async -v` — 异步修复测试通过
3. `uv run pytest backend/tests/unit/test_step_code_buffer.py -k heal -v` — 弱步骤修复测试通过
4. `uv run pytest backend/tests/unit/test_step_code_buffer.py -k derive_wait -v` — Plan 01 wait 推导测试不回归
5. `uv run python -c "from backend.core.step_code_buffer import StepCodeBuffer, StepRecord; print('import OK')"` — 无导入错误
</verification>

<success_criteria>
- append_step_async() 检测弱步骤（elem=None 或 <=1 locator），从磁盘读取 DOM 快照
- 弱步骤调用 LLMHealer.heal() 修复，修复成功用 translate_with_llm 重新翻译
- 修复失败/异常/DOM 缺失时 fallback 到原始翻译，不崩溃
- 仅 click/input 操作检测弱步骤
- 9 个新异步测试 + 13 个已有同步测试全部 PASSED
- CODEGEN-02 需求完全满足
</success_criteria>

<output>
After completion, create `.planning/phases/111-stepcodebuffer/111-02-SUMMARY.md`
</output>
