---
phase: 112-集成接入
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/code_generator.py
  - backend/tests/unit/test_code_generator.py
  - backend/tests/unit/test_step_code_buffer.py
autonomous: true
requirements: [INTEG-03, VAL-02]

must_haves:
  truths:
    - "code_generator.py does NOT contain generate_and_save method"
    - "code_generator.py does NOT contain _heal_weak_steps method"
    - "code_generator.py generate() method still exists and works"
    - "test_code_generator.py does NOT contain test_healing_failure_preserves_original"
    - "test_code_generator.py does NOT contain test_generate_and_save_validates_before_write"
    - "Integration test verifies buffer accumulates steps in step_callback-like context"
    - "Integration test verifies closure-captured buffer pattern (action_dict -> buffer.append_step_async)"
  artifacts:
    - path: "backend/core/code_generator.py"
      provides: "PlaywrightCodeGenerator with generate() only, no generate_and_save/_heal_weak_steps"
      min_lines: 200
    - path: "backend/tests/unit/test_step_code_buffer.py"
      provides: "TestIntegration class with VAL-02 integration tests"
      contains: "class TestIntegration"
  key_links:
    - from: "backend/tests/unit/test_step_code_buffer.py::TestIntegration"
      to: "backend/core/step_code_buffer.py"
      via: "Direct buffer.append_step_async calls with DOM files"
      pattern: "buffer\\.append_step_async"
    - from: "backend/tests/unit/test_step_code_buffer.py::TestIntegration"
      to: "backend/api/routes/runs.py on_step closure pattern"
      via: "Closure-captured buffer simulation test"
      pattern: "test_closure_captured_buffer"
---

<objective>
Remove deprecated methods from code_generator.py and add integration tests for buffer in step_callback context

Purpose: Clean up code_generator.py by removing generate_and_save and _heal_weak_steps (their logic is now in StepCodeBuffer.append_step_async). Add integration tests that simulate the step_callback context to verify buffer accumulation and assembly.
Output: Slimmed code_generator.py, updated test files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-集成接入/112-CONTEXT.md
@.planning/phases/112-集成接入/112-RESEARCH.md
@.planning/phases/111-stepcodebuffer/111-01-SUMMARY.md
@.planning/phases/111-stepcodebuffer/111-02-SUMMARY.md

<interfaces>
<!-- code_generator.py methods to DELETE -->

From backend/core/code_generator.py line 125-179 -- generate_and_save:
```python
async def generate_and_save(self, run_id, task_name, task_id, agent_history,
                            base_dir="outputs", llm_config=None,
                            precondition_config=None, assertions_config=None) -> str:
```

From backend/core/code_generator.py line 181-258 -- _heal_weak_steps:
```python
async def _heal_weak_steps(self, raw_actions, run_id, base_dir, llm_config) -> dict[int, str]:
```

<!-- code_generator.py methods to PRESERVE -->

From backend/core/code_generator.py:
```python
class PlaywrightCodeGenerator:
    def __init__(self) -> None:
        self._translator = ActionTranslator()

    def generate(self, run_id, task_name, task_id, actions,
                 precondition_config=None, assertions_config=None) -> str: ...

    def validate_syntax(self, code: str) -> bool: ...
    # plus all _build_* helper methods
```

<!-- Tests to REMOVE from test_code_generator.py -->

From backend/tests/unit/test_code_generator.py:
- test_healing_failure_preserves_original (line 159) -- calls generate_and_save
- test_generate_and_save_validates_before_write (line 311) -- calls generate_and_save

<!-- Existing test_step_code_buffer.py pattern for integration tests -->

The existing test_step_code_buffer.py has fixtures: async_buffer, mock_elem_weak, click_action_no_elem, navigate_action, etc.
Integration tests should be in a new `class TestIntegration` in the same file (per VAL-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete generate_and_save and _heal_weak_steps from code_generator.py + update tests</name>
  <files>backend/core/code_generator.py, backend/tests/unit/test_code_generator.py</files>
  <read_first>
    backend/core/code_generator.py
    backend/tests/unit/test_code_generator.py
  </read_first>
  <behavior>
    - Test: test_code_generator.py does NOT import or reference generate_and_save
    - Test: test_code_generator.py does NOT import or reference _heal_weak_steps
    - Test: All remaining tests in test_code_generator.py pass (generate(), _build_body, validate_syntax tests)
    - Test: code_generator.py generates valid code via generate() method
  </behavior>
  <action>
**Step 1: Delete two methods from backend/core/code_generator.py**

Remove the `generate_and_save` method (approximately lines 125-179) and the `_heal_weak_steps` method (approximately lines 181-258). These are the ONLY methods being removed.

Keep EVERYTHING else: `generate()`, `validate_syntax()`, `_needs_logging()`, `_sanitize_function_name()`, `_build_header()`, `_build_body()`, `_build_precondition()`, `_build_assertions()`, `_translate_url_contains()`, `_translate_text_exists()`, `_translate_no_errors()`, `_translate_element_exists()`, and the `__init__` method.

Also remove the `LLMHealer` import at the top of the file since _heal_weak_steps was the only user:
```python
# Remove this line:
from backend.core.llm_healer import LLMHealer
```

Keep the `logging` import, `ast` import, `re` import, `datetime` imports, `Path` import, `Any` import, and `ActionTranslator`/`TranslatedAction` imports.

**Step 2: Remove two tests from backend/tests/unit/test_code_generator.py**

Delete these two async test functions entirely:
- `async def test_healing_failure_preserves_original(...)` (around line 159)
- `async def test_generate_and_save_validates_before_write(...)` (around line 311)

Remove the `AsyncMock` import if it becomes unused (check if any other test uses it -- if not, remove from imports).

Remove the `os` import if it becomes unused (check -- `os.path.exists` is used in test_healing_failure_preserves_original, no other test uses os directly).

Remove the `tempfile` import if it becomes unused.

Keep all other tests: test_generate_complete_file, test_fallback_complete_file_parses, test_build_body_indent_fix, test_build_body_preserves_already_indented, test_build_body_empty_actions, test_build_body_mixed_indent_multiline, test_generate_calls_validate_syntax, test_generate_logs_warning_on_syntax_error.

**Step 3: Run tests to verify**

Run `uv run pytest backend/tests/unit/test_code_generator.py -v -x` to confirm all remaining tests pass.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/unit/test_code_generator.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - code_generator.py does NOT contain `def generate_and_save`
    - code_generator.py does NOT contain `def _heal_weak_steps`
    - code_generator.py does NOT contain `from backend.core.llm_healer import LLMHealer`
    - code_generator.py still contains `def generate(`
    - code_generator.py still contains `def validate_syntax`
    - test_code_generator.py does NOT contain `test_healing_failure_preserves_original`
    - test_code_generator.py does NOT contain `test_generate_and_save_validates_before_write`
    - `uv run pytest backend/tests/unit/test_code_generator.py -v -x` exits 0
  </acceptance_criteria>
  <done>code_generator.py has generate_and_save and _heal_weak_steps removed, generate() preserved; test_code_generator.py has 2 deprecated tests removed, remaining 8 tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TestIntegration class to test_step_code_buffer.py for VAL-02</name>
  <files>backend/tests/unit/test_step_code_buffer.py</files>
  <read_first>
    backend/tests/unit/test_step_code_buffer.py
    backend/core/step_code_buffer.py
    backend/core/action_translator.py
  </read_first>
  <behavior>
    - Test 1: buffer accumulates multiple steps in simulated step_callback context, records length matches step count
    - Test 2: buffer assemble after multi-step accumulation produces valid Python (ast.parse passes, contains def test_)
    - Test 3: weak step (elem=None) with DOM present triggers LLMHealer, heal is called
    - Test 4: weak step heal failure falls back gracefully, buffer still has the step
    - Test 5: assemble with precondition_config produces page.goto in output
    - Test 6: assemble with assertions_config produces expect() in output
    - Test 7: closure-captured buffer -- simulate on_step closure pattern where buffer is created outside closure, action_dict passed in, verify accumulation works through closure indirection (addresses key_links review concern)
  </behavior>
  <action>
Add a `class TestIntegration` at the END of backend/tests/unit/test_step_code_buffer.py with the following tests.

The class simulates the step_callback context: create buffer, create DOM files, call append_step_async multiple times, verify accumulation and assembly.

Each test should follow the existing fixture patterns in the file (MagicMock for DOM elements, tmp_path for file I/O, patch for LLMHealer). Refer to the existing test functions above TestIntegration for the exact import names (MagicMock, AsyncMock, patch) and fixture patterns.

**Test methods to create (7 total):**

1. `test_accumulates_in_callback_context(self, tmp_path)` -- Create buffer, call append_step_async 3 times with different action dicts (navigate, click with elem, input with elem). Assert len(buffer.records) == 3, verify each action_type.

2. `test_assemble_after_accumulation(self, tmp_path)` -- Create buffer, append 2 steps (navigate + input with elem), call buffer.assemble(). Assert "def test_" in content, ast.parse(content) passes.

3. `test_weak_step_triggers_healer(self, tmp_path)` -- Create buffer, write DOM file at {tmp_path}/{run_id}/dom/step_1.txt, call append_step_async with elem=None action. Mock LLMHealer to return LLMHealResult(success=True). Assert mock_instance.heal.assert_called_once().

4. `test_weak_step_heal_failure_graceful(self, tmp_path)` -- Same setup as test 3 but healer returns success=False. Assert buffer still has 1 record, record contains TranslatedAction.

5. `test_assemble_with_precondition(self, tmp_path)` -- Append navigate step, call assemble with precondition_config={"target_url": "https://erp.example.com"}. Assert "page.goto" in content, ast.parse passes.

6. `test_assemble_with_assertions(self, tmp_path)` -- Append navigate step, call assemble with assertions_config=[{"type": "text_exists", "expected": "Dashboard", "name": "..."}]. Assert "expect" in content, ast.parse passes.

7. `test_closure_captured_buffer(self, tmp_path)` -- **This tests the key_link from agent_service -> on_step -> buffer.append_step_async through closure indirection.** Create a StepCodeBuffer OUTSIDE a closure function, then define an inner async function `async def on_step(action_dict)` that captures the buffer and calls `await buffer.append_step_async(action_dict)`. Call on_step 3 times with different action dicts. Assert buffer.records has 3 entries with correct types. This validates that the closure-capture pattern works correctly, which is the core integration path from Plan 01.

Use `from unittest.mock import MagicMock, AsyncMock, patch` and `import ast` at the top of the file (these may already exist).

Run `uv run pytest backend/tests/unit/test_step_code_buffer.py -v -x` to verify all tests pass (both existing and new).
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/unit/test_step_code_buffer.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - test_step_code_buffer.py contains `class TestIntegration:`
    - test_step_code_buffer.py contains `def test_accumulates_in_callback_context`
    - test_step_code_buffer.py contains `def test_assemble_after_accumulation`
    - test_step_code_buffer.py contains `def test_weak_step_triggers_healer`
    - test_step_code_buffer.py contains `def test_weak_step_heal_failure_graceful`
    - test_step_code_buffer.py contains `def test_assemble_with_precondition`
    - test_step_code_buffer.py contains `def test_assemble_with_assertions`
    - test_step_code_buffer.py contains `def test_closure_captured_buffer`
    - `uv run pytest backend/tests/unit/test_step_code_buffer.py -v -x` exits 0 with all tests passing
  </acceptance_criteria>
  <done>TestIntegration class added with 7 integration tests covering VAL-02: accumulation, assembly, weak-step healing trigger, graceful failure, precondition config, assertions config, and closure-captured buffer pattern. All existing + new tests pass.</done>
</task>

</tasks>

<verification>
```bash
# Unit tests for both modified files
uv run pytest backend/tests/unit/test_code_generator.py backend/tests/unit/test_step_code_buffer.py -v -x

# Quick sanity check that code_generator still generates valid output
uv run pytest backend/tests/unit/test_code_generator.py::test_generate_complete_file -v

# Full unit test suite
uv run pytest backend/tests/unit/ -v --timeout=30
```
</verification>

<success_criteria>
1. code_generator.py has NO generate_and_save method and NO _heal_weak_steps method
2. code_generator.py generate() method is preserved and all its tests pass
3. test_code_generator.py has 2 deprecated tests removed (test_healing_failure_preserves_original, test_generate_and_save_validates_before_write)
4. test_step_code_buffer.py has TestIntegration class with 7 tests covering VAL-02 including closure-captured buffer pattern
5. All unit tests pass with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/112-集成接入/112-02-SUMMARY.md`
</output>
