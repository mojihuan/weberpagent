---
phase: 113-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_code_generator.py
  - backend/tests/unit/test_precondition_injection.py
  - backend/tests/unit/test_assertion_translation.py
  - backend/db/schemas.py
autonomous: true
requirements: [VAL-03]

must_haves:
  truths:
    - "No test file docstring contains generate_and_save or _heal_weak_steps references"
    - "All Pydantic models use model_config = ConfigDict(from_attributes=True) instead of class Config"
    - "No Pydantic deprecation warnings in test output"
  artifacts:
    - path: "backend/tests/unit/test_code_generator.py"
      provides: "Docstring mentions only generate(), no generate_and_save"
      contains: "generate_and_save"
      must_not_contain: "generate_and_save"
    - path: "backend/db/schemas.py"
      provides: "All Pydantic models use ConfigDict"
      contains: "model_config = ConfigDict"
  key_links:
    - from: "backend/db/schemas.py"
      to: "pydantic.ConfigDict"
      via: "import"
      pattern: "from pydantic import.*ConfigDict"
---

<objective>
Clean up stale docstring references and fix Pydantic deprecation warnings.

Purpose: Remove generate_and_save/_heal_weak_steps residual mentions from test docstrings (per D-01). Fix Pydantic class-based Config deprecation warnings across all schemas (per D-05).

Output: Clean test docstrings, modernized Pydantic config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-e2e/113-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean stale docstring references in 3 test files</name>
  <files>
    backend/tests/unit/test_code_generator.py
    backend/tests/unit/test_precondition_injection.py
    backend/tests/unit/test_assertion_translation.py
  </files>
  <read_first>
    backend/tests/unit/test_code_generator.py
    backend/tests/unit/test_precondition_injection.py
    backend/tests/unit/test_assertion_translation.py
  </read_first>
  <action>
    Update docstring/comment references in 3 test files to remove stale `generate_and_save` and `_heal_weak_steps` mentions. Per D-01, test code is already correct -- only comments are outdated.

    Specific changes per file:

    1. `backend/tests/unit/test_code_generator.py` line 4:
       - Current: `"generate_and_save and _heal_weak_steps removed (logic moved to StepCodeBuffer)."`
       - Change to: `"Tests generate(), _build_body, validate_syntax, and indent handling."`

    2. `backend/tests/unit/test_precondition_injection.py` lines 4-6:
       - Current: `"- PREC-02: generate_and_save() passes precondition_config through to generate()"`
       - Change to: `"- PREC-02: StepCodeBuffer.assemble() passes precondition_config through to generate()"`

    3. `backend/tests/unit/test_assertion_translation.py` line 5:
       - Current: `"- ASRT-02: assertions_config parameter on generate() and generate_and_save()"`
       - Change to: `"- ASRT-02: assertions_config parameter on generate() and StepCodeBuffer.assemble()"`
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && grep -n "generate_and_save\|_heal_weak_steps" backend/tests/unit/test_code_generator.py backend/tests/unit/test_precondition_injection.py backend/tests/unit/test_assertion_translation.py; echo "EXIT=$?"</automated>
    Expected: grep returns exit code 1 (no matches found)
  </verify>
  <done>
    - grep -c "generate_and_save" returns 0 for all 3 files
    - grep -c "_heal_weak_steps" returns 0 for all 3 files
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Pydantic deprecation warnings in schemas.py</name>
  <files>backend/db/schemas.py</files>
  <read_first>backend/db/schemas.py</read_first>
  <action>
    Replace all `class Config: from_attributes = True` blocks with `model_config = ConfigDict(from_attributes=True)` across ALL Pydantic models in backend/db/schemas.py. Per D-05, fix is not limited to BatchResponse -- all models with class-based Config should be updated.

    Step 1: Add `ConfigDict` to the pydantic import on line 6:
    - Current: `from pydantic import BaseModel, Field, field_validator, model_validator`
    - Change to: `from pydantic import BaseModel, ConfigDict, Field, field_validator, model_validator`

    Step 2: For each model that has `class Config: from_attributes = True`, replace:
    ```python
        class Config:
            from_attributes = True
    ```
    with:
    ```python
        model_config = ConfigDict(from_attributes=True)
    ```

    The affected models (8 total, from line grep):
    - TaskResponse (lines 92-93)
    - RunResponse (lines 122-123)
    - StepResponse (lines 157-158)
    - ReportResponse (lines 229-230)
    - AssertionResponse (lines 268-269)
    - AssertionResultResponse (lines 292-293)
    - BatchRunSummary (lines 313-314)
    - BatchResponse (lines 326-327)

    After changes, run: `cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/unit/test_db_schemas.py -v --timeout=30`
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/unit/test_db_schemas.py -v --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - schemas.py line 6 contains `ConfigDict` in the pydantic import
    - schemas.py contains zero occurrences of `class Config:`
    - schemas.py contains 8 occurrences of `model_config = ConfigDict(from_attributes=True)`
    - uv run pytest backend/tests/unit/test_db_schemas.py exits 0
  </acceptance_criteria>
  <done>
    - All 8 Pydantic models use model_config = ConfigDict(from_attributes=True)
    - No class Config: blocks remain in schemas.py
    - test_db_schemas.py passes
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "generate_and_save\|_heal_weak_steps" backend/tests/unit/test_code_generator.py backend/tests/unit/test_precondition_injection.py backend/tests/unit/test_assertion_translation.py` returns no matches
2. `grep -c "class Config:" backend/db/schemas.py` returns 0
3. `grep -c "model_config = ConfigDict" backend/db/schemas.py` returns 8
4. `uv run pytest backend/tests/unit/ --timeout=30` passes
</verification>

<success_criteria>
- Zero stale generate_and_save/_heal_weak_steps references in test docstrings
- All Pydantic models use modern ConfigDict pattern
- Unit tests pass with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/113-e2e/113-01-SUMMARY.md`
</output>
