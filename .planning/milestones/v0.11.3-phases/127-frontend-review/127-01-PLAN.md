---
phase: 127-frontend-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/127-frontend-review/127-FINDINGS.md
autonomous: true
requirements:
  - CORR-03
  - PERF-02

must_haves:
  truths:
    - "All ~60 frontend files have been scanned and assigned a risk priority (P1/P2/P3)"
    - "ESLint and TypeScript compiler checks have been run and results documented"
    - "Risk matrix with line counts and risk justification exists for every file"
    - "Pre-identified CONCERNS.md frontend issues are verified against current code"
  artifacts:
    - path: ".planning/phases/127-frontend-review/127-FINDINGS.md"
      provides: "Initial breadth scan results with risk matrix and ESLint/TS findings"
      min_lines: 100
  key_links:
    - from: "127-FINDINGS.md risk matrix"
      to: "127-02-PLAN.md P1 deep-dive targets"
      via: "P1 file list"
      pattern: "Priority.*P1"
---

<objective>
Breadth-scan all ~60 frontend files, run ESLint + TypeScript compiler checks, produce risk priority matrix, verify CONCERNS.md frontend issues, and initialize 127-FINDINGS.md.

Purpose: Establish the risk landscape for the entire frontend codebase so Plan 02 can focus deep-dive effort on the highest-risk files.
Output: 127-FINDINGS.md with tool results, risk matrix, CONCERNS.md verification, and initial quick-scan findings for low-risk files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-frontend-review/127-CONTEXT.md
@.planning/phases/127-frontend-review/127-RESEARCH.md
@.planning/phases/125-backend-core-review/125-FINDINGS.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

<interfaces>
<!-- Phase 125/126 FINDINGS.md format to replicate -->
From 125-FINDINGS.md:
- Tool Results section (ruff/mypy equivalent -> ESLint/TypeScript)
- Risk Priority Matrix table
- Quick-Scan Findings for P3 files
- CONCERNS.md Verification table
- Summary Statistics section

From 126-FINDINGS.md:
- Same structure with Security Check Matrix (adapted for frontend)
- Cross-validation with backend findings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run ESLint and TypeScript checks, then breadth-scan all files and produce risk matrix</name>
  <files>.planning/phases/127-frontend-review/127-FINDINGS.md</files>
  <read_first>
    - frontend/eslint.config.js (ESLint configuration)
    - frontend/tsconfig.json (TypeScript configuration)
    - frontend/tsconfig.app.json (TypeScript app config)
    - .planning/phases/125-backend-core-review/125-FINDINGS.md (format reference -- first 100 lines for structure)
    - .planning/codebase/CONCERNS.md (frontend issues to verify)
    - frontend/src/api/client.ts (API client, 61 lines)
    - frontend/src/hooks/useRunStream.ts (SSE hook, 215 lines)
  </read_first>
  <action>
    ## Step 1: Run tool checks

    Run these commands and capture ALL output:
    ```bash
    cd /Users/huhu/project/weberpagent/frontend && npx tsc --noEmit 2>&1
    cd /Users/huhu/project/weberpagent/frontend && npx eslint src/ 2>&1
    ```

    Document results in the "Tool Results" section of 127-FINDINGS.md:
    - **TypeScript compilation**: count errors by file. Note: RESEARCH says zero errors, verify.
    - **ESLint**: RESEARCH says 18 problems (15 errors, 3 warnings). Document each with file, line, rule ID, and message. The known issues are:
      - `no-explicit-any` in types/index.ts lines 91, 334, 339, 353 and DataMethodSelector.tsx line 92
      - `no-useless-escape` in DataMethodSelector.tsx line 161
      - `set-state-in-effect` in CodeViewerModal.tsx line 26 and RunMonitor.tsx line 26
      - `react-hooks/exhaustive-deps` warnings in AssertionSelector.tsx line 276, DataMethodSelector.tsx lines 283 and 299

    ## Step 2: Build risk priority matrix

    Use `wc -l` to get line counts for ALL 87 frontend .ts/.tsx files. Assign priority based on CONTEXT.md D-01:

    **P1 (deep line-by-line review in Plan 02):**
    - useRunStream.ts (215 lines) -- SSE core, all JSON.parse without try/catch, connection state management, unbounded timeline growth
    - DataMethodSelector.tsx (829 lines) -- largest component, complex multi-step state machine
    - TaskForm.tsx (560 lines) -- task form with child modal composition
    - AssertionSelector.tsx (546 lines) -- assertion configuration with external method calls
    - client.ts (61 lines) -- HTTP client with retry logic, error handling, all API calls depend on this

    **P2 (quick scan in Plan 03):**
    - All pages/*.tsx (8 files): Dashboard.tsx, Tasks.tsx, TaskDetail.tsx, RunList.tsx, RunMonitor.tsx, BatchProgress.tsx, Reports.tsx, ReportDetail.tsx
    - types/index.ts (456 lines) -- all TypeScript interfaces
    - StepTimeline.tsx (285 lines), TaskRow.tsx (140 lines)
    - ImportModal/*.tsx (4 files)
    - Other TaskModal/*.tsx: OperationCodeSelector.tsx (226 lines), TaskFormModal.tsx, FieldParamsEditor.tsx, JsonTreeViewer.tsx
    - Report/*.tsx (6 files)
    - RunMonitor/*.tsx (3 non-timeline files)
    - hooks/*.ts (4 non-SSE hooks): useTasks.ts, useReports.ts, useDashboard.ts, useBatchProgress.ts
    - api/*.ts (8 non-client files): tasks.ts, runs.ts, reports.ts, batches.ts, dashboard.ts, externalOperations.ts, externalDataMethods.ts, externalAssertions.ts

    **P3 (lint/type check only -- reviewed in this task):**
    - shared/*.tsx (6 files): ConfirmModal, EmptyState, ImageViewer, LoadingSpinner, Pagination, ReasoningText, StatusBadge
    - Dashboard/*.tsx (3 files): QuickStart, RecentRuns, StatCard, TrendChart
    - constants/roleLabels.ts
    - utils/reasoningParser.ts, utils/retry.ts
    - App.tsx, main.tsx, Layout.tsx, Sidebar.tsx, NavItem.tsx, Button.tsx
    - BatchProgress/*.tsx: BatchSummary.tsx, BatchTaskCard.tsx
    - TaskDetail/*.tsx: CodeExecutionStatus.tsx, ConfigPanel.tsx, RunHistory.tsx, StatsChart.tsx, TaskHeader.tsx, TaskInfo.tsx
    - TaskList/*.tsx: BatchActions.tsx, BatchExecuteDialog.tsx, TaskFilters.tsx, TaskListHeader.tsx, TaskTable.tsx
    - All index.ts barrel files

    Format the matrix as a table with columns: Priority | File | Lines | Risk Justification

    ## Step 3: Quick-scan P3 files

    For each P3 file, read the file content and check for:
    - Any `any` type usage
    - console.log statements (violates coding-style.md)
    - Missing error handling in try/catch blocks
    - Obvious bugs or anti-patterns
    - Mutation of state (should use spread operator)

    Document findings in "Quick-Scan Findings (P3 Files)" section using the same format as 125-FINDINGS.md:
    ```
    ### [P3] filename:line -- description
    - **Severity:** Critical/High/Medium/Low
    - **Category:** Correctness/Architecture/Performance/Security
    - **Description:** [specific finding]
    - **Recommendation:** [specific fix]
    ```

    ## Step 4: Verify CONCERNS.md frontend issues

    Cross-reference these CONCERNS.md entries against the actual code:
    1. "No frontend tests" -- verify no vitest/jest config exists in package.json or any test files
    2. "DataMethodSelector 829 lines" -- verify line count matches
    3. "main.tsx missing QueryClientProvider" -- check main.tsx and App.tsx for QueryClientProvider location (RESEARCH says it's in App.tsx, not main.tsx)
    4. "event_manager.py memory leak" -- check if useRunStream.ts timeline array also grows without bound (frontend mirror of backend issue)

    Document as a verification table: CONCERNS.md Entry | Status | Finding Reference

    ## Step 5: Cross-validation with backend event_manager.py

    Read backend/core/event_manager.py (the SSE publish side, already reviewed in Phase 125).
    Compare the event format `event: <type>\ndata: <json>\n\n` with what useRunStream.ts expects.

    Specifically verify:
    - Backend event types (started, step, precondition, assertion, external_assertions, finished, error) match frontend addEventListener types
    - Backend SSEStartedEvent fields (run_id, task_id, task_name) match frontend started handler expectations
    - Backend SSEStepEvent fields match frontend step handler expectations
    - Backend None sentinel handling matches frontend `if event is None: break` pattern

    Document findings in "SSE Cross-Validation" section.

    ## Step 6: Write 127-FINDINGS.md

    Create the file with all sections:
    1. Header (review date, scope, methodology)
    2. Tool Results (ESLint + TypeScript)
    3. Risk Priority Matrix (full table)
    4. CONCERNS.md Verification (table)
    5. SSE Cross-Validation (findings)
    6. Quick-Scan Findings (P3 files only)
    7. Summary Statistics (counts by severity)
  </action>
  <verify>
    <automated>test -f /Users/huhu/project/weberpagent/.planning/phases/127-frontend-review/127-FINDINGS.md && grep -c "Risk Priority Matrix" /Users/huhu/project/weberpagent/.planning/phases/127-frontend-review/127-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 127-FINDINGS.md exists and contains "## Tool Results" section
    - 127-FINDINGS.md contains "## Risk Priority Matrix" with at least 40 file entries
    - 127-FINDINGS.md contains "## CONCERNS.md Verification" table
    - 127-FINDINGS.md contains "## Quick-Scan Findings" with at least 5 P3 findings
    - 127-FINDINGS.md contains "## Summary Statistics" with severity counts
    - All 5 P1 files are listed in the risk matrix
    - ESLint results match the 18 problems expected (or document delta)
    - TypeScript results match "zero errors" expected (or document delta)
  </acceptance_criteria>
  <done>127-FINDINGS.md created with tool results, risk matrix for all ~60 files, CONCERNS.md verification, SSE cross-validation, and P3 quick-scan findings. File is ready for Plan 02 to append P1 deep-dive findings.</done>
</task>

</tasks>

<verification>
1. 127-FINDINGS.md exists and is well-structured (section headers present)
2. Risk matrix covers all ~60 frontend files
3. ESLint and TypeScript tool results are documented
4. CONCERNS.md frontend entries are verified against code
5. SSE cross-validation with backend event_manager.py is documented
</verification>

<success_criteria>
- All ~60 frontend files assigned to P1, P2, or P3 priority
- ESLint results (18 problems) and TypeScript results (zero errors) documented
- P3 files have quick-scan findings or "no significant issues" status
- CONCERNS.md frontend entries verified
- SSE cross-validation identifies any format mismatches
- 127-FINDINGS.md ready for Plan 02 to append deep-dive findings
</success_criteria>

<output>
After completion, create `.planning/phases/127-frontend-review/127-01-SUMMARY.md`
</output>
