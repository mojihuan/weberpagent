---
phase: 127-frontend-review
plan: 02
type: execute
wave: 2
depends_on:
  - 127-01
files_modified:
  - .planning/phases/127-frontend-review/127-FINDINGS.md
autonomous: true
requirements:
  - CORR-03
  - PERF-02

must_haves:
  truths:
    - "useRunStream.ts has been reviewed line-by-line for all SSE edge cases: JSON.parse without try/catch, event ordering, duplicate events, disconnect/reconnect, timeline growth"
    - "DataMethodSelector.tsx (829 lines) has been reviewed for internal state management correctness and component splitting rationality"
    - "TaskForm.tsx (560 lines) has been reviewed for component-modal composition state flow and data flow correctness"
    - "AssertionSelector.tsx (546 lines) has been reviewed for state management and external method call handling"
    - "client.ts has been reviewed for retry logic, error handling completeness, and type safety"
    - "SSE event format cross-validated with backend event_manager.py -- event types and data structures match"
  artifacts:
    - path: ".planning/phases/127-frontend-review/127-FINDINGS.md"
      provides: "P1 deep-dive findings appended to existing breadth scan results"
      contains: "Deep-Dive Findings"
  key_links:
    - from: "useRunStream.ts SSE handlers"
      to: "backend/core/event_manager.py publish format"
      via: "SSE event format cross-validation"
      pattern: "addEventListener.*step|started|finished"
    - from: "client.ts apiClient"
      to: "all api/*.ts modules"
      via: "HTTP request/response handling"
      pattern: "apiClient.*endpoint"
---

<objective>
Deep-dive review of 5 P1 frontend files: useRunStream.ts, DataMethodSelector.tsx, TaskForm.tsx, AssertionSelector.tsx, and client.ts.

Purpose: These 5 files carry the highest risk -- SSE edge cases in useRunStream, 3 oversized components with complex state management, and the HTTP client that all API calls depend on. Line-by-line review identifies correctness bugs and performance issues.
Output: P1 deep-dive findings appended to 127-FINDINGS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-frontend-review/127-CONTEXT.md
@.planning/phases/127-frontend-review/127-RESEARCH.md
@.planning/phases/127-frontend-review/127-FINDINGS.md
@.planning/phases/125-backend-core-review/125-FINDINGS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Backend SSE format (from Phase 125 findings) -->
Backend publishes SSE events via event_manager.py:
```python
# event format: "event: <type>\ndata: <json>\n\n"
# Event types: started, step, precondition, assertion, external_assertions, finished, error
# None sentinel terminates stream
```

Backend event data structures (from run_pipeline.py):
```python
SSEStartedEvent: run_id, task_id, task_name
SSEStepEvent: index, action, reasoning, screenshot_url, status, duration_ms
SSEFinishedEvent: status, total_steps, duration_ms
```

Frontend expects (from RESEARCH):
```typescript
// useRunStream.ts listeners
addEventListener('started', ...)  // expects { run_id, task_id, task_name }
addEventListener('step', ...)     // expects { index, action, reasoning, screenshot_url, status, duration_ms }
addEventListener('finished', ...) // expects { status, total_steps, duration_ms }
addEventListener('precondition', ...)
addEventListener('assertion', ...)
addEventListener('external_assertions', ...)
addEventListener('error', ...)
```

Frontend API client (from RESEARCH):
```typescript
// client.ts: apiClient<T> with retry + error toast
// RESEARCH: retry with exponential backoff (3 retries: 1s, 2s, 3s)
// Error handling via sonner toast
// ApiError class for typed errors
```

Known ESLint issues in P1 files:
- DataMethodSelector.tsx line 92: no-explicit-any
- DataMethodSelector.tsx line 161: no-useless-escape
- DataMethodSelector.tsx lines 283, 299: react-hooks/exhaustive-deps warnings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deep-dive review useRunStream.ts and client.ts</name>
  <files>.planning/phases/127-frontend-review/127-FINDINGS.md</files>
  <read_first>
    - frontend/src/hooks/useRunStream.ts (215 lines, P1 -- read entire file)
    - frontend/src/api/client.ts (61 lines, P1 -- read entire file)
    - backend/core/event_manager.py (156 lines, SSE publish side -- cross-validation)
    - backend/api/routes/runs_routes.py lines 310-330 (SSE stream endpoint -- frontend consumes this)
    - .planning/phases/127-frontend-review/127-FINDINGS.md (append findings to existing file)
  </read_first>
  <action>
    ## useRunStream.ts (215 lines) -- Line-by-line deep review per D-02

    Read the entire file. For each of the following specific areas, document findings:

    ### 1. JSON.parse without try/catch (all 7 event handlers)
    Check EVERY addEventListener callback. For each one:
    - Identify the exact line where JSON.parse(e.data) is called
    - Verify there is NO try/catch wrapping it
    - Document what happens if backend sends malformed JSON (uncaught exception kills the listener)
    - Count total unprotected JSON.parse calls (expected: 7)

    ### 2. Event ordering -- step before started
    Check if the 'step' handler correctly handles receiving a 'step' event BEFORE the 'started' event.
    - What state does the step handler access? (prev.steps, prev.timeline)
    - If started hasn't fired yet, is the run state properly initialized?
    - Document whether this is a real risk or if EventSource guarantees ordering

    ### 3. Duplicate events -- step no dedup
    Check the 'step' handler: does it deduplicate steps with the same index?
    - If backend sends two 'step' events with index 3, does the frontend show 2 entries?
    - Document the behavior

    ### 4. Connection state desync -- isConnectedRef
    Analyze the connect() function:
    - At what point is isConnectedRef.current set to true?
    - Is it set BEFORE or AFTER the EventSource fires the 'open' event?
    - If connection fails, does isConnected state correctly reflect the failure?
    - Is there an onopen handler on the EventSource?
    - Is there an onerror handler? What does it do?

    ### 5. Timeline array unbounded growth per D-02
    - Check how run.timeline grows with each event
    - Is there any cap or cleanup?
    - For a 50-step test, how many timeline entries would accumulate?
    - Document the memory impact

    ### 6. useEffect cleanup race
    - What are the useEffect dependencies for the SSE connection?
    - Is there an eslint-disable comment on the dependency array?
    - When runId changes, does the old connection properly close before the new one opens?

    ### 7. SSE cross-validation with backend event_manager.py
    Compare the frontend addEventListener types with backend event types:
    - Backend publishes: started, step, precondition, assertion, external_assertions, finished, error
    - Frontend listens for: [list all addEventListener types in useRunStream.ts]
    - Are there any event types the backend sends that the frontend doesn't listen for?
    - Are there any frontend listeners that never receive events?

    For each matching event type, compare data fields:
    - started: backend sends {run_id, task_id, task_name} -- does frontend use all fields?
    - step: backend sends {index, action, reasoning, screenshot_url, status, duration_ms} -- does frontend use all?
    - finished: backend sends {status, total_steps, duration_ms} -- does frontend use all?
    - Document any fields the frontend ignores or any fields it expects but backend doesn't send

    ### 8. set-state-in-effect check
    - RESEARCH flagged RunMonitor.tsx line 26 for set-state-in-effect
    - Check if useRunStream.ts has similar patterns (setState calls inside effects)
    - Document any cascading render issues

    ## client.ts (61 lines) -- Deep review

    Read the entire file. Check:

    ### 1. Retry logic
    - How many retries? What are the delays?
    - Does it distinguish retryable vs non-retryable errors?
    - What status codes trigger retry?
    - Is there a max retry count that prevents infinite loops?

    ### 2. Error handling
    - How are network errors (fetch throws) handled?
    - How are HTTP error responses (4xx, 5xx) handled?
    - Is there a try/catch around the fetch call?
    - What happens if the server returns non-JSON?

    ### 3. Type safety
    - Does apiClient<T> properly type the return value?
    - Is there any `any` usage?
    - Does it validate the response structure or trust the server?

    ### 4. Error toast (sonner)
    - When is the toast shown? For all errors or only some?
    - Is the error message user-friendly?
    - Could sensitive information leak through toast messages?

    ### 5. FormData handling
    - Does the client handle FormData uploads differently?
    - If so, is the Content-Type header correctly omitted for multipart?

    ## Write findings

    Append a "## Deep-Dive Findings (P1 Files)" section to 127-FINDINGS.md.
    Format each finding as:
    ```
    ### [DD-{prefix}-{NN}] filename:line -- description
    - **Severity:** Critical/High/Medium/Low
    - **Category:** Correctness/Architecture/Performance/Security
    - **Description:** [specific finding with exact line references]
    - **Recommendation:** [specific fix]
    ```
  </action>
  <verify>
    <automated>grep -c "Deep-Dive Findings" /Users/huhu/project/weberpagent/.planning/phases/127-frontend-review/127-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 127-FINDINGS.md contains "## Deep-Dive Findings (P1 Files)" section
    - At least 8 findings documented for useRunStream.ts covering all 6 D-02 areas
    - At least 3 findings documented for client.ts
    - SSE cross-validation documents exact field matching between backend and frontend
    - Each finding has Severity, Category, Description, and Recommendation
    - All JSON.parse locations in useRunStream.ts are individually documented
    - isConnectedRef state desync analysis is present
    - Timeline growth analysis quantifies entries for typical test runs
  </acceptance_criteria>
  <done>useRunStream.ts and client.ts have been reviewed line-by-line. All findings documented in 127-FINDINGS.md under "Deep-Dive Findings (P1 Files)". SSE cross-validation with backend event_manager.py is complete with field-level matching.</done>
</task>

<task type="auto">
  <name>Task 2: Deep-dive review DataMethodSelector.tsx, TaskForm.tsx, AssertionSelector.tsx</name>
  <files>.planning/phases/127-frontend-review/127-FINDINGS.md</files>
  <read_first>
    - frontend/src/components/TaskModal/DataMethodSelector.tsx (829 lines, P1 -- read entire file)
    - frontend/src/components/TaskModal/TaskForm.tsx (560 lines, P1 -- read entire file)
    - frontend/src/components/TaskModal/AssertionSelector.tsx (546 lines, P1 -- read entire file)
    - frontend/src/components/TaskModal/OperationCodeSelector.tsx (226 lines, P2 context for TaskForm composition)
    - frontend/src/types/index.ts (456 lines, type definitions used by these components)
    - .planning/phases/127-frontend-review/127-FINDINGS.md (append findings)
  </read_first>
  <action>
    ## DataMethodSelector.tsx (829 lines) -- Deep review per D-05

    Read the entire 829-line file. Focus on:

    ### 1. State management complexity
    - Count ALL useState calls. List each state variable with its type and purpose.
    - Identify state that depends on other state (derived state that should be useMemo).
    - Find any state that is set but never read (dead state).
    - Identify state that should be useRef instead of useState (values that don't affect rendering).

    ### 2. Multi-step state machine analysis
    - How many steps/pages does this component have?
    - Is the step transition logic clear? Are there unreachable steps?
    - What happens if the user navigates back -- is state preserved?
    - Are there race conditions in async operations that change step state?

    ### 3. int/float parameter handling per RESEARCH Open Question 3
    - Find the code that converts input values to int/float (expected around lines 518-525 per RESEARCH)
    - What happens when the user types a non-numeric value?
    - Does it store 0 for NaN? If so, does this confuse the user?
    - Document the exact behavior

    ### 4. Component splitting rationality per D-05
    - Identify logical sections that could be extracted into sub-components
    - Are there inline callbacks that cause re-renders (not wrapped in useCallback)?
    - Is there business logic that should be extracted into a custom hook?

    ### 5. ESLint issues verification
    - Line 92: no-explicit-any -- what is the `any` type? What should it be?
    - Line 161: no-useless-escape -- what is the regex and what's wrong with the escape?
    - Lines 283, 299: exhaustive-deps -- what dependencies are missing from useCallback/useEffect?

    ### 6. Error handling
    - How are API errors handled (data method execution failures)?
    - Is there error boundary protection?
    - What happens if externalDataMethods.ts API calls fail?

    ## TaskForm.tsx (560 lines) -- Deep review per D-05

    Read the entire 560-line file. Focus on:

    ### 1. Component-modal composition per RESEARCH Pattern 3
    - TaskForm renders OperationCodeSelector, DataMethodSelector, AssertionSelector as child modals
    - Trace the state flow: what props does TaskForm pass to each child?
    - What callbacks (onConfirm, onCancel) does each child receive?
    - When a child modal confirms, how does TaskForm update its state?

    ### 2. Form state management
    - Count ALL useState calls
    - Is form state initialized from props (edit mode) correctly?
    - When the task prop changes (e.g., editing a different task), does state reset?
    - Are there stale closure issues in callbacks that reference old state?

    ### 3. Form submission
    - What validation happens before submission?
    - How are assertion/precondition arrays serialized for the API?
    - Is there a loading state during submission that prevents double-submit?

    ### 4. Performance
    - Are there expensive computations that should be useMemo?
    - Are inline objects/arrays passed as props (causing child re-renders)?
    - Is the form re-rendering when unrelated state changes?

    ## AssertionSelector.tsx (546 lines) -- Deep review per D-05

    Read the entire 546-line file. Focus on:

    ### 1. Assertion type switching
    - How does the component handle switching between assertion types (text_exists, url_contains, element_exists, no_errors)?
    - When switching types, is previous type-specific state cleaned up?
    - Are there assertion types that have more complex state than others?

    ### 2. External assertion method handling
    - How are external assertion methods loaded and displayed?
    - What happens if the external assertions API fails?
    - Is there loading/error state for the external methods list?

    ### 3. ESLint exhaustive-deps warning at line 276
    - What useEffect/useCallback has the missing dependency?
    - What is the consequence of the missing dependency (stale closure)?

    ### 4. State management
    - Count ALL useState calls
    - Identify redundant state (values derivable from other state)
    - Find state that should be consolidated

    ## Write findings

    Append findings to the existing "## Deep-Dive Findings (P1 Files)" section in 127-FINDINGS.md.
    Use the same format:
    ```
    ### [DD-{prefix}-{NN}] filename:line -- description
    - **Severity:** Critical/High/Medium/Low
    - **Category:** Correctness/Architecture/Performance/Security
    - **Description:** [specific finding with exact line references]
    - **Recommendation:** [specific fix]
    ```

    Organize findings by file with sub-headers:
    ```
    ### DataMethodSelector.tsx (829 lines)
    [findings]

    ### TaskForm.tsx (560 lines)
    [findings]

    ### AssertionSelector.tsx (546 lines)
    [findings]
    ```
  </action>
  <verify>
    <automated>grep -c "DataMethodSelector.tsx\|TaskForm.tsx\|AssertionSelector.tsx" /Users/huhu/project/weberpagent/.planning/phases/127-frontend-review/127-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 127-FINDINGS.md contains findings for all 3 components under "Deep-Dive Findings (P1 Files)"
    - DataMethodSelector.tsx has at least 5 findings covering state complexity, int/float handling, component splitting
    - TaskForm.tsx has at least 3 findings covering modal composition state flow and form submission
    - AssertionSelector.tsx has at least 3 findings covering assertion type switching and external methods
    - Each finding has exact line references (not just "around line X")
    - State variable counts are documented for each component
    - ESLint issues (exhaustive-deps, no-explicit-any, no-useless-escape) are individually explained
  </acceptance_criteria>
  <done>DataMethodSelector.tsx, TaskForm.tsx, and AssertionSelector.tsx have been reviewed line-by-line. All findings documented in 127-FINDINGS.md. State management analysis, component splitting assessment, and ESLint issue explanations are complete.</done>
</task>

</tasks>

<verification>
1. 127-FINDINGS.md has "Deep-Dive Findings (P1 Files)" section
2. useRunStream.ts: all 6 D-02 areas documented (JSON.parse, event ordering, dedup, reconnect, timeline, cleanup race)
3. client.ts: retry logic, error handling, type safety reviewed
4. DataMethodSelector.tsx: state count, int/float handling, component splitting assessed
5. TaskForm.tsx: modal composition, form state, submission reviewed
6. AssertionSelector.tsx: type switching, external methods, ESLint warning analyzed
7. SSE cross-validation: backend event types match frontend listener types
</verification>

<success_criteria>
- All 5 P1 files have been reviewed line-by-line
- useRunStream.ts: at least 8 findings covering all D-02 areas plus SSE cross-validation
- DataMethodSelector.tsx: at least 5 findings covering state management and splitting
- TaskForm.tsx: at least 3 findings covering modal composition and form handling
- AssertionSelector.tsx: at least 3 findings covering type switching and external methods
- client.ts: at least 3 findings covering retry, error handling, type safety
- Total P1 findings: at least 22 actionable items
</success_criteria>

<output>
After completion, create `.planning/phases/127-frontend-review/127-02-SUMMARY.md`
</output>
