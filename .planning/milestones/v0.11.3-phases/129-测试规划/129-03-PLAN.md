---
phase: 129-测试规划
plan: 03
type: execute
wave: 3
depends_on:
  - 129-01
  - 129-02
files_modified:
  - .planning/phases/129-测试规划/129-FINDINGS.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "Every Critical/High frontend finding from 127-128 has a specific test scenario or documented rationale"
    - "E2E gap scenarios identify missing coverage beyond existing 7 spec files"
    - "Final statistics cover all test scenarios across backend, frontend, and E2E"
    - "Document is self-contained: future milestone can implement tests from this document alone"
  artifacts:
    - path: ".planning/phases/129-测试规划/129-FINDINGS.md"
      provides: "Complete test scenario document with frontend, E2E, and final summary"
      contains: "## Final Summary"
  key_links:
    - from: ".planning/phases/129-测试规划/129-FINDINGS.md"
      to: ".planning/phases/127-frontend-review/127-FINDINGS.md"
      via: "Frontend test scenarios reference source finding IDs"
      pattern: "127-FINDINGS\\.md.*#"
    - from: ".planning/phases/129-测试规划/129-FINDINGS.md"
      to: "e2e/tests/"
      via: "E2E gap scenarios reference existing spec files for coverage analysis"
      pattern: "e2e/tests/"
---

<objective>
展开前端组件测试场景和 E2E 缺失场景，添加最终统计总结，完成 129-FINDINGS.md。

Purpose: 补充前端和 E2E 测试场景（per D-03 后端优先后的补充），输出最终统计使里程碑成果可量化。E2E 识别缺失场景但不作为重点（per D-04）。

Output: 129-FINDINGS.md 的 "Frontend Test Scenarios"、"E2E Gap Scenarios"、和 "Final Summary" 部分。完成整个文档。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-测试规划/129-CONTEXT.md
@.planning/phases/129-测试规划/129-RESEARCH.md
@.planning/phases/129-测试规划/129-01-SUMMARY.md

<interfaces>
<!-- Pre-identified frontend/E2E test candidates from RESEARCH.md -->

Frontend Component Test Candidates (P1):
1. useRunStream JSON.parse error handling (127-DD-USE-01)
2. useRunStream unbounded array growth (127-SSE-4)
3. TaskForm stale data on mode switch (127-DD-TF-01)
4. client.ts retry toast persistence (127-DD-CLI-03)
5. DataMethodSelector empty-to-0 conversion (127-DD-DMS-01)

E2E Gap Scenarios (P2):
1. Precondition failure flow (task with bad precondition -> verify error state)
2. Assertion failure flow (task with failing assertion -> verify report)
3. Batch execution monitoring (create batch -> monitor progress -> verify all runs)
4. Stop run flow (start run -> stop -> verify agent stops -- currently broken, DD-runs-10)
5. External module integration (create task with external data method -> verify execution)

Existing E2E spec files (for gap analysis):
- e2e/tests/smoke.spec.ts
- e2e/tests/task-flow.spec.ts
- e2e/tests/assertion-flow.spec.ts
- e2e/tests/variable-substitution.spec.ts
- e2e/tests/data-method-selector.spec.ts
- e2e/tests/data-method-execution.spec.ts
- e2e/tests/full-flow.spec.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand frontend component and E2E test scenarios, write final summary</name>
  <files>.planning/phases/129-测试规划/129-FINDINGS.md</files>
  <read_first>
    - .planning/phases/129-测试规划/129-FINDINGS.md (read current state with Plan 01 analysis + Plan 02 backend scenarios)
    - .planning/phases/127-frontend-review/127-FINDINGS.md (for detailed frontend finding descriptions)
    - .planning/phases/128-代码质量审查/128-FINDINGS.md (for frontend quality findings, CP-1~CP-5 frontend mirrors)
    - .planning/phases/129-测试规划/129-RESEARCH.md (for frontend/E2E pre-identified candidates)
    - .planning/codebase/TESTING.md (for existing E2E spec file list and anti-patterns)
  </read_first>
  <action>
Read the testable frontend findings from 129-FINDINGS.md. Expand frontend component test scenarios and E2E gap scenarios. Write the final summary.

**Part A: Frontend Component Test Scenarios**

For each frontend finding classified as "frontend-component" test type, expand using the same scenario format:

```markdown
### [TS-FE-NN] Scenario Name
- **Severity:** High/Medium/Low
- **Test Type:** Frontend Component
- **Source Finding:** See 127-FINDINGS.md {finding ID} ({file}:{line})
- **Description:** {Specific test behavior}
- **Priority:** P1/P2
- **Mock requirements:** {what to mock}
- **Implementation cost:** Low/Medium/High
- **Testability:** Testable now / DEFERRED
```

Expand these frontend scenarios (minimum from RESEARCH.md):

1. **TS-FE-01** useRunStream JSON.parse error handling -- test: (1) SSE event with malformed JSON data field, verify error is caught and logged, not thrown to crash the stream. (2) Event with missing data field, verify graceful degradation. Source: 127-DD-USE-01. Note: frontend test framework not yet configured, mark implementation cost as Medium (needs vitest/testing-library setup).

2. **TS-FE-02** useRunStream unbounded array growth -- test: (1) Subscribe to long-running run with many events, verify arrays (steps, logs) do not grow unbounded. (2) Verify cleanup on unmount resets arrays. Source: 127-SSE-4; mirrors CP-1 on backend.

3. **TS-FE-03** TaskForm stale data on mode switch -- test: (1) Edit task A, switch to create mode, verify form fields are empty (not stale task A data). (2) Create task B, switch to edit task A, verify form shows task A data. Source: 127-DD-TF-01.

4. **TS-FE-04** client.ts retry toast persistence -- test: (1) Request fails, retry toast appears. (2) Retry succeeds, verify toast is dismissed (not persisting). Source: 127-DD-CLI-03.

5. **TS-FE-05** DataMethodSelector empty-to-0 conversion -- test: (1) Clear int/float input field, verify value is empty string (not 0). (2) Type number, clear it, verify empty state. Source: 127-DD-DMS-01.

Additionally, check for any other frontend findings from 127/128 classified as "frontend-component" in Plan 01 output and expand those too.

**Part B: E2E Gap Scenarios**

Section header: `## E2E Gap Scenarios`

For each E2E gap, expand using format:

```markdown
### [TS-E2E-NN] Scenario Name
- **Severity:** Medium/Low
- **Test Type:** E2E
- **Source Finding:** See {phase}-FINDINGS.md {finding ID}
- **Description:** {E2E test steps and expected outcomes}
- **Priority:** P2
- **Existing Coverage:** {which existing spec file covers related flow, if any}
- **Gap:** {what is missing from existing coverage}
```

Expand these E2E gaps:

1. **TS-E2E-01** Precondition failure flow -- create task with intentionally bad precondition code, execute run, verify error state is displayed in UI. Existing coverage: full-flow.spec.ts covers happy path only. Gap: error path for precondition failure.

2. **TS-E2E-02** Assertion failure flow -- create task with failing assertion, execute run, verify assertion failure appears in report with expected details. Existing coverage: assertion-flow.spec.ts covers assertion configuration but not failure outcomes. Gap: assertion failure result display.

3. **TS-E2E-03** Batch execution monitoring -- create batch with multiple tasks, monitor batch progress page, verify all runs complete and progress bar updates. Existing coverage: none. Gap: batch progress monitoring.

4. **TS-E2E-04** Stop run flow -- start run, click stop button during execution, verify agent stops (currently broken per 126-DD-runs-10). Existing coverage: none. Gap: stop functionality verification.

5. **TS-E2E-05** External module integration -- create task using external data method, execute, verify data method response is used correctly. Existing coverage: data-method-execution.spec.ts covers happy path. Gap: error handling when external module fails.

**Part C: Final Summary**

Add section `## Final Summary` with:

1. **Overall Statistics Table:**

| Category | Count | P0 | P1 | P2 | DEFERRED |
|----------|-------|----|----|----|----------|
| Backend Unit | {N} | {N} | {N} | {N} | {N} |
| Backend Integration | {N} | {N} | {N} | {N} | {N} |
| Frontend Component | {N} | {N} | {N} | {N} | {N} |
| E2E | {N} | {N} | {N} | {N} | {N} |
| **Total** | **{N}** | **{N}** | **{N}** | **{N}** | **{N}** |

2. **Source Phase Distribution:** Count of test scenarios derived from each source phase (125/126/127/128).

3. **Severity Distribution:** Count by severity across all scenarios.

4. **Top 10 Highest ROI Scenarios:** List TS-BE/FE/E2E IDs with brief name and ROI score.

5. **Implementation Roadmap Recommendation:**
   - Phase A (test infrastructure): Create conftest.py, fixtures, mock patterns
   - Phase B (P0 backend unit): Implement all P0 unit test scenarios
   - Phase C (P0 backend integration): Implement P0 integration scenarios (systemic patterns first)
   - Phase D (P1 frontend): Set up vitest, implement P1 frontend scenarios
   - Phase E (P2 E2E): Add missing E2E spec files

6. **Requirements Coverage:** Explicitly map TEST-01 and TEST-02 to the scenarios that satisfy them:
   - TEST-01 (missing test coverage for core business flows): List scenario IDs covering core flows
   - TEST-02 (boundary/error/race/timeout gaps): List scenario IDs covering boundary conditions
  </action>
  <verify>
    <automated>grep -c "### \[TS-FE-" .planning/phases/129-测试规划/129-FINDINGS.md && grep -c "### \[TS-E2E-" .planning/phases/129-测试规划/129-FINDINGS.md && grep -c "Final Summary" .planning/phases/129-测试规划/129-FINDINGS.md && grep -c "Implementation Roadmap" .planning/phases/129-测试规划/129-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 129-FINDINGS.md contains "## Frontend Component Test Scenarios" with at least 5 scenarios (TS-FE-01 through TS-FE-05)
    - 129-FINDINGS.md contains "## E2E Gap Scenarios" with at least 5 scenarios (TS-E2E-01 through TS-E2E-05)
    - 129-FINDINGS.md contains "## Final Summary" with overall statistics table
    - Statistics table has rows for Backend Unit, Backend Integration, Frontend Component, E2E, and Total
    - Statistics table has columns for Count, P0, P1, P2, DEFERRED
    - "Source Phase Distribution" shows counts from 125/126/127/128
    - "Top 10 Highest ROI Scenarios" lists specific scenario IDs
    - "Implementation Roadmap Recommendation" has 5 phases (A through E)
    - "Requirements Coverage" maps TEST-01 and TEST-02 to specific scenario IDs
    - Each frontend scenario references 127-FINDINGS.md or 128-FINDINGS.md
    - Each E2E scenario references existing spec files for coverage analysis
    - No code files are created or modified
  </acceptance_criteria>
  <done>Frontend component test scenarios (at least 5) and E2E gap scenarios (at least 5) are documented. The Final Summary provides complete statistics across all scenario types, source phases, and severity levels. Implementation roadmap recommendation and requirements coverage mapping are included. The 129-FINDINGS.md document is complete and self-contained.</done>
</task>

</tasks>

<verification>
- 129-FINDINGS.md contains Frontend Component Test Scenarios section (at least 5 scenarios)
- 129-FINDINGS.md contains E2E Gap Scenarios section (at least 5 scenarios)
- 129-FINDINGS.md contains Final Summary with statistics, roadmap, and requirements mapping
- Total scenario count is in reasonable range (40-120 per RESEARCH.md sanity check)
- TEST-01 and TEST-02 both mapped to specific scenario IDs
- No code changes made (per D-05)
- Document is self-contained for future milestone implementation
</verification>

<success_criteria>
- Complete 129-FINDINGS.md with backend (Plan 02), frontend, and E2E test scenarios
- Final summary with actionable statistics and implementation roadmap
- TEST-01 covered by core business flow test scenarios
- TEST-02 covered by boundary/error/race/timeout test scenarios
- Document format consistent with Phase 125-128 FINDINGS (per D-06)
- Each scenario has all required fields (per D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/129-测试规划/129-03-SUMMARY.md`
</output>
