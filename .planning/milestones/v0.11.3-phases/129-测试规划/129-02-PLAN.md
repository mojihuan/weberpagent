---
phase: 129-测试规划
plan: 02
type: execute
wave: 2
depends_on:
  - 129-01
files_modified:
  - .planning/phases/129-测试规划/129-FINDINGS.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "Every Critical/High backend finding from 125-128 has a specific test scenario or is documented as deferred"
    - "Each backend test scenario includes: name, description, finding reference, test type (unit/integration), priority, mock requirements"
    - "Backend unit test scenarios cover pure logic modules (StallDetector, schemas, excel_parser, error_utils)"
    - "Backend integration test scenarios cover cross-module paths (pipeline stages, SSE flow, event_manager lifecycle)"
    - "5 systemic patterns (CP-1~CP-5) each have at least one integration test scenario"
  artifacts:
    - path: ".planning/phases/129-测试规划/129-FINDINGS.md"
      provides: "Backend test scenarios section with unit + integration test scenario details"
      contains: "## Backend Test Scenarios"
  key_links:
    - from: ".planning/phases/129-测试规划/129-FINDINGS.md"
      to: ".planning/phases/125-backend-core-review/125-FINDINGS.md"
      via: "Each backend test scenario references source finding ID"
      pattern: "125-FINDINGS\\.md.*#"
    - from: ".planning/phases/129-测试规划/129-FINDINGS.md"
      to: ".planning/phases/126-api/126-FINDINGS.md"
      via: "API-related test scenarios reference source finding ID"
      pattern: "126-FINDINGS\\.md.*#"
---

<objective>
对后端 Critical/High 发现推导具体测试场景，包含单元测试和集成测试场景的详列，追加到 129-FINDINGS.md。

Purpose: 后端无测试保护、风险最高（per D-03）。将 Plan 01 筛选出的后端可测试发现展开为可直接实施的测试场景描述，使后续里程碑的测试实施者无需重新阅读原始 FINDINGS 即可编写测试。

Output: 129-FINDINGS.md 的 "Backend Test Scenarios" 部分 -- 按 unit/integration 分组，每个场景包含完整实施信息。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-测试规划/129-CONTEXT.md
@.planning/phases/129-测试规划/129-RESEARCH.md
@.planning/phases/129-测试规划/129-01-SUMMARY.md

<interfaces>
<!-- Key pre-identified high-ROI backend test candidates from RESEARCH.md Section "High-ROI Test Candidates" -->

Backend Unit Test Candidates (P0):
1. StallDetector.check() -- dual invocation bug (125-P1 agent_service.py:340-347)
2. assertion_service.check_element_exists -- stub always returns True (125-P2 assertion_service.py:88-110)
3. step_code_buffer._is_corrective_evaluate -- detection gap (125-P1 step_code_buffer.py:227-257)
4. code_generator._substitute_variables -- false match risk (125-P1 code_generator.py:241-242)
5. task_progress_tracker.update_from_evaluation -- loose keyword matching (125-P3 task_progress_tracker.py:149-152)
6. External assertion summary context mutation (125-Cross-4, 126-DD-pipe-04)
7. test_flow_service._shift_step_numbers -- boundary values (125-P2 test_flow_service.py:174-193)

Backend Integration Test Candidates (P0):
1. CP-1: EventManager lifecycle (publish -> subscribe -> cleanup -> verify empty)
2. CP-2: SSE error handling (broken subscriber -> publish -> verify no crash)
3. CP-4: Blocking I/O in async (save_screenshot -> verify concurrent tasks not blocked)
4. Pipeline precondition failure -> missing "started" event (125-P1 run_pipeline.py:499-500, 126-DD-pipe-03)
5. Batch execution partial creation (126-DD-batch-03)
6. Stop run does not cancel agent (126-DD-runs-10)
7. Heartbeat task leak on re-subscribe (125-P2 event_manager.py:84-85)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand backend unit test scenarios from High/Critical findings</name>
  <files>.planning/phases/129-测试规划/129-FINDINGS.md</files>
  <read_first>
    - .planning/phases/129-测试规划/129-FINDINGS.md (read Plan 01 output -- the filtered testable findings list)
    - .planning/phases/125-backend-core-review/125-FINDINGS.md (for detailed finding descriptions when expanding scenarios)
    - .planning/phases/126-api/126-FINDINGS.md (for API-related backend findings)
    - .planning/phases/128-代码质量审查/128-FINDINGS.md (for CP-1~CP-5 systemic patterns and backend quality findings)
    - .planning/phases/129-测试规划/129-RESEARCH.md (for pre-identified candidates and code examples of expected output format)
  </read_first>
  <action>
Read the testable backend findings from 129-FINDINGS.md (Plan 01 output). For each backend finding classified as "unit" test type, expand into a detailed test scenario following this exact format (per D-07):

```markdown
### [TS-BE-NN] Scenario Name
- **Severity:** High/Medium/Low (from source finding)
- **Test Type:** Unit
- **Source Finding:** See {phase}-FINDINGS.md {finding ID} ({file}:{line})
- **Description:** {Specific test behavior description with concrete inputs, expected outputs, and edge cases. Must include enough detail for implementation without re-reading source findings.}
- **Priority:** P0/P1/P2 (P0=High severity + high regression risk, P1=High severity + low risk or Medium + high risk, P2=Medium/Low)
- **Mock requirements:** None / {specific mocks needed}
- **Implementation cost:** Low/Medium/High
- **Testability:** Testable now / DEFERRED (requires fix first: {reason})
```

Unit test scenarios to expand (minimum coverage from RESEARCH.md pre-identified candidates):

1. **TS-BE-01** StallDetector double-invocation -- test check() with consecutive failures, verify intervention at correct threshold (not half). Test: (1) N failures once -> assert intervention at N, (2) N failures twice -> assert intervention at N/2 (demonstrates bug), (3) Mixed success/failure -> assert reset behavior. Source: 125-FINDINGS P1 agent_service.py:340-347, 128-FINDINGS BD-08.

2. **TS-BE-02** assertion_service.check_element_exists stub -- test that check_element_exists always returns True regardless of input. Verify stub behavior is documented. Source: 125-FINDINGS P2 assertion_service.py:88-110. Mark as DEFERRED (requires implementing actual element check logic first).

3. **TS-BE-03** step_code_buffer._is_corrective_evaluate detection gap -- test corrective evaluate detection with various DOM state changes. Source: 125-FINDINGS P1 step_code_buffer.py:227-257.

4. **TS-BE-04** code_generator._substitute_variables false match -- test variable substitution with patterns like {{var}} that could match partial strings. Source: 125-FINDINGS P1 code_generator.py:241-242.

5. **TS-BE-05** task_progress_tracker.update_from_evaluation loose keyword matching -- test keyword matching with ambiguous step descriptions. Source: 125-P3 task_progress_tracker.py:149-152.

6. **TS-BE-06** External assertion context mutation -- test that external assertion execution does not mutate the shared variable_map. Source: 125-Cross-4, 126-DD-pipe-04.

7. **TS-BE-07** test_flow_service._shift_step_numbers boundary -- test with step numbers at boundaries (0, max, negative, gaps). Source: 125-P2 test_flow_service.py:174-193.

Additionally, scan Plan 01 output for any OTHER backend findings classified as "unit" that are not in the above list and expand those too. Priority is High severity first, then Medium.

Append the expanded scenarios to 129-FINDINGS.md under a new section `## Backend Unit Test Scenarios`.
  </action>
  <verify>
    <automated>grep -c "### \[TS-BE-" .planning/phases/129-测试规划/129-FINDINGS.md && grep -c "Test Type: Unit" .planning/phases/129-测试规划/129-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 129-FINDINGS.md contains section "## Backend Unit Test Scenarios"
    - At least 7 unit test scenarios exist with IDs TS-BE-01 through TS-BE-07
    - Each scenario has all 8 required fields: name, severity, test type, source finding, description, priority, mock requirements, implementation cost
    - Each scenario's source finding references a specific FINDINGS file and finding ID
    - DEFERRED scenarios are tagged with "DEFERRED" and explain what fix is required
    - At least one scenario references 125-FINDINGS.md (backend core)
    - At least one scenario references 128-FINDINGS.md (code quality / systemic patterns)
    - No code files are created or modified
  </acceptance_criteria>
  <done>All backend findings classified as "unit" test type have been expanded into detailed test scenarios with concrete test descriptions, source references, priority ratings, and mock requirements. Each scenario can be implemented without re-reading the original FINDINGS files.</done>
</task>

<task type="auto">
  <name>Task 2: Expand backend integration test scenarios from High/Critical findings and systemic patterns</name>
  <files>.planning/phases/129-测试规划/129-FINDINGS.md</files>
  <read_first>
    - .planning/phases/129-测试规划/129-FINDINGS.md (read current state including Plan 01 output and Task 1 unit scenarios)
    - .planning/phases/125-backend-core-review/125-FINDINGS.md (for pipeline/agent/event_manager finding details)
    - .planning/phases/126-api/126-FINDINGS.md (for API integration findings: SSE, batch, run execution)
    - .planning/phases/128-代码质量审查/128-FINDINGS.md (for CP-1~CP-5 systemic pattern descriptions)
    - .planning/phases/129-测试规划/129-RESEARCH.md (for integration test examples and mock strategy)
  </read_first>
  <action>
Read the testable backend findings from 129-FINDINGS.md classified as "integration" test type. For each, expand into a detailed test scenario using the same format as Task 1.

Integration test scenarios to expand (minimum coverage from RESEARCH.md pre-identified candidates):

1. **TS-BE-20** EventManager lifecycle (CP-1) -- test: (1) Subscribe to a run, publish events, verify cleanup(run_id) removes all stored events. (2) Run full pipeline (mocked agent), verify cleanup is called in _finalize_run. (3) Verify heartbeat task is cancelled and removed after cleanup. (4) Verify re-subscribe does not leak previous heartbeat task. Source: 125-FINDINGS P2 event_manager.py:27; 128-FINDINGS CP-1.

2. **TS-BE-21** SSE error handling (CP-2) -- test: (1) Publish event with broken subscriber (raises exception), verify other subscribers still receive events. (2) Test JSON.parse with malformed event data. (3) Test event_manager.publish with queue error. Source: 126-FINDINGS SSE findings; 128-FINDINGS CP-2.

3. **TS-BE-22** Blocking I/O in async context (CP-4) -- test: (1) save_screenshot does not block event loop during concurrent requests. (2) subprocess.run in execute_run_code does not block concurrent API requests. Source: 128-FINDINGS CP-4.

4. **TS-BE-23** Pipeline precondition failure missing "started" event -- test: when precondition fails, verify "run_started" event is NOT emitted but "run_completed" with error status IS emitted. Source: 125-FINDINGS P1 run_pipeline.py:499-500, 126-DD-pipe-03.

5. **TS-BE-24** Batch execution partial creation -- test: create batch with N tasks, if one task creation fails, verify batch record and other tasks are created correctly. Source: 126-DD-batch-03.

6. **TS-BE-25** Stop run does not cancel agent -- test: start run, call stop endpoint, verify agent execution is actually cancelled (not just status change). Source: 126-DD-runs-10.

7. **TS-BE-26** Heartbeat task leak on re-subscribe -- test: subscribe to same run_id twice, verify only one heartbeat task exists after second subscribe (first is cancelled). Source: 125-P2 event_manager.py:84-85.

8. **TS-BE-27** Mutable state coupling (CP-5) -- test: (1) context mutation in external assertions does not leak to variable_map. (2) setState callbacks produce correct state. Source: 128-FINDINGS CP-5.

9. **TS-BE-28** Repository CRUD with in-memory SQLite -- test: create, read, update, delete operations for each model (Task, Run, Report, Batch). Verify UUID generation, timestamp handling, relationship integrity. Source: TESTING.md recommended additions.

10. **TS-BE-29** API route integration via TestClient -- test: each API endpoint with valid/invalid inputs. Verify response format (success/error), status codes, error messages. Source: 126-FINDINGS API findings.

Additionally, scan Plan 01 output for any OTHER backend findings classified as "integration" not in the above list and expand those.

Append expanded scenarios under `## Backend Integration Test Scenarios` in 129-FINDINGS.md.

After appending, add a summary subsection `### Backend Scenario Summary` with:
- Total unit scenarios count
- Total integration scenarios count
- Count by severity (High/Medium/Low)
- Count by priority (P0/P1/P2)
- Count of DEFERRED scenarios
- Top 5 highest ROI scenarios listed by ID
  </action>
  <verify>
    <automated>grep -c "### \[TS-BE-" .planning/phases/129-测试规划/129-FINDINGS.md && grep -c "Test Type: Integration" .planning/phases/129-测试规划/129-FINDINGS.md && grep -c "Backend Scenario Summary" .planning/phases/129-测试规划/129-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - 129-FINDINGS.md contains section "## Backend Integration Test Scenarios"
    - At least 10 integration test scenarios exist with IDs TS-BE-20+
    - Each scenario has all required fields (name, severity, test type, source finding, description, priority, mock requirements, implementation cost, testability)
    - CP-1 through CP-5 each have at least one integration test scenario
    - "### Backend Scenario Summary" subsection exists with counts by severity, priority, and testability status
    - At least one scenario references 126-FINDINGS.md (API layer)
    - At least one scenario references 128-FINDINGS.md systemic patterns
    - No code files are created or modified
  </acceptance_criteria>
  <done>All backend findings classified as "integration" have been expanded into detailed test scenarios. The systemic patterns CP-1~CP-5 each have integration test coverage. A summary subsection provides counts by severity, priority, and testability for the complete backend test scenario set (unit + integration).</done>
</task>

</tasks>

<verification>
- 129-FINDINGS.md contains "## Backend Unit Test Scenarios" with at least 7 scenarios
- 129-FINDINGS.md contains "## Backend Integration Test Scenarios" with at least 10 scenarios
- Each scenario has complete fields per D-07
- CP-1~CP-5 each covered by at least one scenario
- Backend Scenario Summary subsection has accurate counts
- No code changes made (per D-05)
</verification>

<success_criteria>
- All backend testable findings from Plan 01 are expanded into concrete test scenarios
- Each scenario is detailed enough for implementation without re-reading source FINDINGS
- Scenarios cover both pure logic (unit) and cross-module paths (integration)
- Systemic patterns have dedicated integration test scenarios
- Summary statistics provide actionable overview
</success_criteria>

<output>
After completion, create `.planning/phases/129-测试规划/129-02-SUMMARY.md`
</output>
