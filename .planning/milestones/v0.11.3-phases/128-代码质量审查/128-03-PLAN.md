---
phase: 128-代码质量审查
plan: 03
type: execute
wave: 3
depends_on:
  - 128-01
  - 128-02
files_modified:
  - .planning/phases/128-代码质量审查/128-FINDINGS.md
autonomous: true
requirements:
  - MAINT-01
  - MAINT-02
  - MAINT-03
  - ARCH-03
  - PERF-01

must_haves:
  truths:
    - "Frontend P1 files have deep-dive findings for MAINT/ARCH-03/PERF-01 quality dimensions"
    - "Cross-phase correlation analysis links backend and frontend findings as systemic patterns"
    - "Final summary statistics exist with severity/category/requirement breakdowns"
    - "Top findings list highlights the most impactful quality issues across the full stack"
  artifacts:
    - path: ".planning/phases/128-代码质量审查/128-FINDINGS.md"
      provides: "Frontend deep-dive + cross-phase correlation + final summary"
      contains: "## Frontend P1 Deep-Dive"
    - path: ".planning/phases/128-代码质量审查/128-03-SUMMARY.md"
      provides: "Final plan summary with complete statistics"
  key_links:
    - from: "Backend findings (Plan 02)"
      to: "Cross-phase correlation"
      via: "Systemic pattern identification (same issue in both layers)"
      pattern: "event_manager memory leak.*mirrors.*useRunStream unbounded"
---

<objective>
Frontend P1 deep-dive + cross-phase correlation analysis + final summary statistics.

Purpose: Complete the quality review by analyzing frontend P1 files for MAINT/ARCH-03/PERF-01 issues, then perform the cross-phase correlation analysis (per D-10) linking backend and frontend findings into systemic patterns, and produce final summary statistics.

Output: Frontend findings, cross-phase correlation, and final summary appended to 128-FINDINGS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-代码质量审查/128-CONTEXT.md
@.planning/phases/128-代码质量审查/128-01-SUMMARY.md
@.planning/phases/128-代码质量审查/128-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/127-frontend-review/127-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend P1 deep-dive + cross-phase correlation + final summary</name>
  <files>.planning/phases/128-代码质量审查/128-FINDINGS.md</files>
  <read_first>
    .planning/phases/128-代码质量审查/128-FINDINGS.md
    .planning/phases/128-代码质量审查/128-01-SUMMARY.md
    .planning/phases/128-代码质量审查/128-02-SUMMARY.md
    frontend/src/hooks/useRunStream.ts
    frontend/src/components/TaskModal/DataMethodSelector.tsx
    frontend/src/components/TaskModal/TaskForm.tsx
    frontend/src/components/TaskModal/AssertionSelector.tsx
    frontend/src/api/client.ts
    frontend/src/types/index.ts
    frontend/src/hooks/useTasks.ts
    frontend/src/hooks/useReports.ts
    frontend/src/hooks/useDashboard.ts
    frontend/src/hooks/useBatchProgress.ts
    frontend/src/main.tsx
    frontend/src/App.tsx
  </read_first>
  <action>
    Read the Plan 01 SUMMARY to get the P1 file list. Read each P1 frontend file.

    ### Part A: Frontend P1 Deep-Dive (MAINT/ARCH-03/PERF-01)

    For each P1 frontend file:

    #### MAINT-01: DRY/SOLID Violations
    1. **SRP violations in large components**:
       - DataMethodSelector.tsx (829 lines): count distinct concerns (data method selection, parameter editing, preview, state management, error handling)
       - TaskForm.tsx (560 lines): form management + child modal coordination + validation + submission
       - AssertionSelector.tsx (546 lines): assertion type switching + external method integration + parameter editing
    2. **Code duplication across hooks**:
       - useTasks, useReports, useDashboard all use identical useState+useEffect+fetch pattern (See 127-FINDINGS.md, but add MAINT-01 analysis: how many lines of duplicated fetch boilerplate exist?)
       - Error handling duplication in hooks (try/catch + toast + console.error repeated N times)
    3. **DRY violations in components**:
       - Similar modal patterns across DataMethodSelector, AssertionSelector, OperationCodeSelector
       - Repeated form field patterns that could be extracted to shared components

    #### MAINT-02: Structural Complexity
    Using ESLint complexity output from Plan 01:
    1. Components exceeding 500 lines: DataMethodSelector (829), TaskForm (560), AssertionSelector (546)
    2. Functions with high complexity: state machine logic in DataMethodSelector, form validation in TaskForm
    3. Deep nesting: multiple useState + conditional rendering creating >4 levels of nesting

    #### MAINT-03: Misleading Names (per D-04)
    1. Functions where name != behavior
    2. Inconsistent naming patterns across similar operations
    3. Props or state variables with unclear semantics

    #### ARCH-03: Cross-Cutting Consistency (frontend dimensions)
    1. **Error handling consistency**: How many different error handling patterns exist in frontend?
       - client.ts central error handling vs per-hook try/catch vs component-level error state
       - Toast notification consistency: are all errors toasted? Are some silently caught?
    2. **State management strategy**: React Query installed but unused (See 127-FINDINGS.md). Quantify: how many hooks use manual fetch when they could use React Query?
    3. **Component composition patterns**: Are page components consistent in how they compose hooks and child components?

    #### PERF-01: Resource and Rendering Issues
    1. **Unnecessary re-renders**: Components re-rendering on every parent update due to missing memoization
    2. **Resource leaks**: EventSource connections in useRunStream not properly cleaned up (See 127-FINDINGS.md, add PERF-01 analysis)
    3. **Unbounded arrays**: timeline/steps arrays in useRunStream grow without limit during long runs

    Per D-01: Reference existing Phase 127 findings with "See 127-FINDINGS.md #{id}" format.

    ### Part B: Cross-Phase Correlation Analysis (per D-10)

    Compare findings across ALL phases (125 + 126 + 127 + 128) to identify systemic patterns:

    Build correlation table:
    ```
    | Systemic Pattern | Backend Instance | Frontend Instance | Root Cause | Severity |
    ```

    Known systemic patterns to verify and document:
    1. **Memory leak pattern**: event_manager._events (backend) mirrors useRunStream arrays (frontend) -- both grow without cleanup
    2. **Error handling gap**: SSE event_generator no try/except (backend 126) mirrors JSON.parse no try/catch (frontend 127) -- both lack defensive error handling at boundaries
    3. **Dual system inconsistency**: 3 logging systems (backend) mirrors mixed state management (frontend) -- both have installed-but-not-used systems
    4. **Blocking in async context**: sync I/O in async functions (backend 125) mirrors synchronous state updates in effects (frontend 127)

    For each pattern:
    - Reference the specific finding IDs from each phase
    - Identify the root cause (if shared)
    - Assess systemic severity (higher than individual finding severity if the pattern is pervasive)

    ### Part C: Final Summary Statistics

    Produce final statistics for all of Phase 128:
    ```
    ## Final Summary Statistics

    | Metric | Value |
    |--------|-------|
    | Total files reviewed | {count} |
    | Total lines scanned | {count} |
    | Backend P1 files (deep-dive) | {count} |
    | Frontend P1 files (deep-dive) | {count} |
    | Critical issues | {count} |
    | High issues | {count} |
    | Medium issues | {count} |
    | Low issues | {count} |
    | Total actionable findings | {count} |
    | Cross-referenced existing findings | {count} |
    | New findings | {count} |

    ### Findings by Requirement
    | Requirement | New Findings | Cross-Referenced |
    |-------------|-------------|-----------------|
    | MAINT-01 | {n} | {n} |
    | MAINT-02 | {n} | {n} |
    | MAINT-03 | {n} | {n} |
    | ARCH-03 | {n} | {n} |
    | PERF-01 | {n} | {n} |

    ### Findings by Category
    | Category | Count |
    |----------|-------|
    | Maintainability | {n} |
    | Architecture | {n} |
    | Performance | {n} |

    ### Top 5 Quality Findings (by severity + systemic impact)
    1. {finding}
    2. {finding}
    3. {finding}
    4. {finding}
    5. {finding}

    ### Cross-Phase Correlation Summary
    {count} systemic patterns identified across {count} phases.
    {detail each pattern}

    ### Confirmed CONCERNS.md Quality Issues
    | CONCERNS.md Entry | 128 Finding Reference |
    |-------------------|----------------------|

    ### New Quality Issues Not in CONCERNS.md
    {list}
    ```

    ### Part D: Update STATE.md
    Append key decisions and findings from Phase 128 to .planning/STATE.md under "Accumulated Context > Decisions".
  </action>
  <acceptance_criteria>
    - 128-FINDINGS.md contains "## Frontend P1 Deep-Dive" section with at least 8 findings with FD- prefixed IDs
    - 128-FINDINGS.md contains "## Cross-Phase Correlation Analysis" with correlation table
    - 128-FINDINGS.md contains "## Final Summary Statistics" with all required metrics
    - Cross-phase correlation identifies at least 3 systemic patterns spanning backend + frontend
    - Top 5 findings list is present with severity and impact descriptions
    - Findings by requirement table covers all 5 requirements (MAINT-01, MAINT-02, MAINT-03, ARCH-03, PERF-01)
    - Existing findings properly cross-referenced (no duplication of 127-FINDINGS analysis)
    - .planning/STATE.md updated with Phase 128 decisions
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Frontend P1 Deep-Dive\|Cross-Phase Correlation\|Final Summary Statistics\|FD-\|Top 5 Quality" .planning/phases/128-代码质量审查/128-FINDINGS.md</automated>
  </verify>
  <done>
    Frontend deep-dife findings documented. Cross-phase correlation analysis complete with systemic patterns identified. Final summary statistics compiled. STATE.md updated with Phase 128 findings. 128-FINDINGS.md is complete and ready for use in Phase 129 (test planning).
  </done>
</task>

</tasks>

<verification>
- 128-FINDINGS.md is complete with all sections: Tool Results, Risk Matrix, Cross-Reference Map, Quick-Scan Findings, Backend P1 Deep-Dive (MAINT/ARCH-03/PERF-01), Frontend P1 Deep-Dive, Cross-Phase Correlation, Final Summary Statistics
- All 5 requirements have findings: MAINT-01, MAINT-02, MAINT-03, ARCH-03, PERF-01
- Cross-phase correlation links backend and frontend patterns
- No duplication of Phase 125/126/127 analysis
- STATE.md updated
</verification>

<success_criteria>
- Complete quality review output in 128-FINDINGS.md
- Quantified complexity metrics (radon + ESLint)
- Risk matrix covering full stack
- Backend deep-dive with MAINT/ARCH-03/PERF-01 analysis
- Frontend deep-dive with MAINT/ARCH-03/PERF-01 analysis
- Cross-phase correlation identifying systemic patterns
- Final statistics with severity/category/requirement breakdowns
- All requirements (MAINT-01, MAINT-02, MAINT-03, ARCH-03, PERF-01) covered
</success_criteria>

<output>
After completion, create `.planning/phases/128-代码质量审查/128-03-SUMMARY.md`
</output>
