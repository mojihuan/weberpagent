---
phase: 07-动态数据支持
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/random_generators.py
  - backend/tests/unit/test_random_generators.py
autonomous: true
requirements:
  - DYN-01
user_setup: []

must_haves:
  truths:
    - "sf_waybill() 生成以 'SF' 开头的 14 位物流单号"
    - "random_phone() 生成以 '13' 开头的 11 位手机号"
    - "random_imei() 生成以 'I' 开头的 15 位 IMEI"
    - "random_serial() 生成 8 位纯数字序列号"
  artifacts:
    - path: "backend/core/random_generators.py"
      provides: "随机数生成函数"
      exports: ["sf_waybill", "random_phone", "random_imei", "random_serial", "random_numbers"]
      min_lines: 50
    - path: "backend/tests/unit/test_random_generators.py"
      provides: "随机数生成器单元测试"
      contains: "def test_sf_waybill"
  key_links:
    - from: "backend/core/random_generators.py"
      to: "PreconditionService._setup_execution_env()"
      via: "函数导入并注入到执行环境"
      pattern: "from backend.core.random_generators import sf_waybill"
---

<objective>
创建随机数生成器模块，提供 SF 物流单号、手机号、IMEI 等随机数据生成函数。

Purpose: 让用户在前置条件中生成测试数据，避免硬编码
Output: 随机数生成函数模块 + 单元测试
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-动态数据支持/07-CONTEXT.md
@.planning/phases/07-动态数据支持/07-RESEARCH.md

<interfaces>
<!-- 现有测试模式参考 -->
From backend/tests/unit/test_precondition_service.py:
```python
# 测试模式: pytest.mark.asyncio + fixture
# 断言模式: assert result.success is True
# context 访问: service.get_context()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建随机数生成器模块</name>
  <files>backend/core/random_generators.py</files>
  <read_first>
    - /Users/huhu/project/weberpagent/backend/core/precondition_service.py (了解现有代码结构和风格)
  </read_first>
  <behavior>
    - sf_waybill() 返回 "SF" + 12位字符（共14位）
    - random_phone() 返回 "13" + 9位数字（共11位）
    - random_imei() 返回 "I" + 14位数字（共15位）
    - random_serial() 返回 8位纯数字字符串
    - random_numbers(n) 返回 n位数字字符串
  </behavior>
  <action>
创建 `backend/core/random_generators.py` 文件，实现以下函数：

1. **sf_waybill()** - SF 物流单号生成
   - 格式: "SF" + 时间戳后4位 + UUID前4位(大写) + 线程ID后4位 + 随机4位数字
   - 返回: 14位字符串，以 "SF" 开头
   - 实现:
     ```python
     import time
     import uuid
     import threading
     import random

     def sf_waybill() -> str:
         timestamp_part = str(int(time.time() * 1000000))[-4:]
         uuid_part = str(uuid.uuid4())[:4].upper()
         thread_id = str(threading.current_thread().ident or 0)[-4:]
         random_part = ''.join(random.choices('0123456789', k=4))
         return f"SF{timestamp_part}{uuid_part}{thread_id}{random_part}"
     ```

2. **random_phone()** - 11位手机号生成
   - 格式: "13" + 9位随机数字
   - 返回: 11位字符串，以 "13" 开头
   - 实现:
     ```python
     def random_phone() -> str:
         return "13" + ''.join(random.choices('0123456789', k=9))
     ```

3. **random_imei()** - 15位 IMEI 生成
   - 格式: "I" + 14位随机数字
   - 返回: 15位字符串，以 "I" 开头
   - 实现:
     ```python
     def random_imei() -> str:
         return "I" + ''.join(random.choices('0123456789', k=14))
     ```

4. **random_serial()** - 8位序列号生成
   - 格式: 8位纯数字
   - 返回: 8位字符串
   - 实现:
     ```python
     def random_serial() -> str:
         return ''.join(random.choices('0123456789', k=8))
     ```

5. **random_numbers(n: int) -> str** - 指定长度随机数字
   - 参数: n - 数字位数
   - 返回: n位纯数字字符串
   - 实现:
     ```python
     def random_numbers(n: int) -> str:
         return ''.join(random.choices('0123456789', k=n))
     ```

文件顶部添加模块文档字符串和类型提示。使用标准库模块（time, uuid, threading, random），不添加外部依赖。
</action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_random_generators.py -v</automated>
  </verify>
  <done>
    - backend/core/random_generators.py 文件存在
    - 包含 sf_waybill, random_phone, random_imei, random_serial, random_numbers 函数
    - 所有函数有类型提示
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建随机数生成器单元测试</name>
  <files>backend/tests/unit/test_random_generators.py</files>
  <read_first>
    - /Users/huhu/project/weberpagent/backend/tests/unit/test_precondition_service.py (参考测试模式)
    - /Users/huhu/project/weberpagent/backend/core/random_generators.py (被测试的模块)
  </read_first>
  <behavior>
    - test_sf_waybill_format: 验证返回值以 "SF" 开头，长度为 14
    - test_sf_waybill_uniqueness: 连续生成 100 个值，验证全部唯一
    - test_random_phone_format: 验证返回值以 "13" 开头，长度为 11，全数字
    - test_random_imei_format: 验证返回值以 "I" 开头，长度为 15，后14位全数字
    - test_random_serial_format: 验证返回值长度为 8，全数字
    - test_random_numbers_length: 验证 random_numbers(5) 返回 5 位数字
  </behavior>
  <action>
创建 `backend/tests/unit/test_random_generators.py` 文件，实现以下测试：

```python
"""随机数生成器单元测试"""

import pytest
import re
from backend.core.random_generators import (
    sf_waybill,
    random_phone,
    random_imei,
    random_serial,
    random_numbers,
)


class TestSfWaybill:
    """SF 物流单号生成测试"""

    def test_sf_waybill_format(self):
        """测试 SF 单号格式：SF 开头，共 14 位"""
        result = sf_waybill()
        assert result.startswith("SF"), f"应以 SF 开头，实际: {result}"
        assert len(result) == 14, f"长度应为 14，实际: {len(result)}"
        # SF 后面的 12 位应该是字母和数字的组合
        suffix = result[2:]
        assert suffix.isalnum(), f"后缀应为字母数字，实际: {suffix}"

    def test_sf_waybill_uniqueness(self):
        """测试 SF 单号唯一性"""
        results = [sf_waybill() for _ in range(100)]
        unique_results = set(results)
        assert len(unique_results) == 100, "100 次生成应全部唯一"


class TestRandomPhone:
    """手机号生成测试"""

    def test_random_phone_format(self):
        """测试手机号格式：13 开头，共 11 位"""
        result = random_phone()
        assert result.startswith("13"), f"应以 13 开头，实际: {result}"
        assert len(result) == 11, f"长度应为 11，实际: {len(result)}"
        assert result.isdigit(), f"应为纯数字，实际: {result}"

    def test_random_phone_uniqueness(self):
        """测试手机号唯一性"""
        results = [random_phone() for _ in range(100)]
        unique_results = set(results)
        assert len(unique_results) >= 95, "100 次生成应有至少 95 个唯一值"


class TestRandomImei:
    """IMEI 生成测试"""

    def test_random_imei_format(self):
        """测试 IMEI 格式：I 开头，共 15 位"""
        result = random_imei()
        assert result.startswith("I"), f"应以 I 开头，实际: {result}"
        assert len(result) == 15, f"长度应为 15，实际: {len(result)}"
        assert result[1:].isdigit(), f"后 14 位应为纯数字，实际: {result}"


class TestRandomSerial:
    """序列号生成测试"""

    def test_random_serial_format(self):
        """测试序列号格式：8 位纯数字"""
        result = random_serial()
        assert len(result) == 8, f"长度应为 8，实际: {len(result)}"
        assert result.isdigit(), f"应为纯数字，实际: {result}"


class TestRandomNumbers:
    """指定长度随机数字测试"""

    def test_random_numbers_length(self):
        """测试指定长度生成"""
        for n in [1, 5, 10, 20]:
            result = random_numbers(n)
            assert len(result) == n, f"长度应为 {n}，实际: {len(result)}"
            assert result.isdigit(), f"应为纯数字，实际: {result}"

    def test_random_numbers_zero(self):
        """测试长度为 0"""
        result = random_numbers(0)
        assert result == "", f"长度为 0 应返回空字符串，实际: {result}"
```

遵循现有测试模式：使用 pytest + 类组织测试，每个测试有清晰的文档字符串。
</action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_random_generators.py -v</automated>
  </verify>
  <done>
    - backend/tests/unit/test_random_generators.py 文件存在
    - 测试覆盖所有 5 个函数
    - 所有测试通过
  </done>
</task>

</tasks>

<verification>
运行随机数生成器测试套件，验证所有函数正确工作：
```bash
uv run pytest backend/tests/unit/test_random_generators.py -v
```
</verification>

<success_criteria>
1. sf_waybill() 生成 14 位 SF 物流单号 (DYN-01)
2. random_phone() 生成 11 位手机号 (DYN-01)
3. random_imei() 生成 15 位 IMEI (DYN-01)
4. random_serial() 生成 8 位序列号 (DYN-01)
5. 所有单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-动态数据支持/07-01-SUMMARY.md`
</output>
