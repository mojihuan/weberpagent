---
phase: 06-接口断言集成
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - backend/api/routes/runs.py
  - backend/core/report_service.py
  - backend/db/schemas.py
  - frontend/src/components/Report/ApiAssertionResults.tsx
  - frontend/src/types/index.ts
autonomous: true
requirements:
  - API-04
---

must_haves:
  truths:
    - 接口断言在 UI 测试完成后执行
    - 断言结果存储到 AssertionResult 表
    - 报告页面显示接口断言结果区域
    - 断言失败影响最终测试状态
  artifacts:
    - path: backend/api/routes/runs.py
      provides: 执行流程集成
      min_lines: 250
      contains: "api_assertions"
    - path: backend/core/report_service.py
      provides: 报告数据包含断言结果
      min_lines: 150
      contains: "api_assertion_results"
    - path: frontend/src/components/Report/ApiAssertionResults.tsx
      provides: 接口断言结果展示组件
      min_lines: 80
      contains: "ApiAssertionResults"
  key_links:
    - from: backend/api/routes/runs.py
      to: backend/core/api_assertion_service.py
      via: "UI 测试后执行接口断言"
      pattern: "ApiAssertionService"
    - from: backend/core/report_service.py
      to: backend/db/repository.py
      via: "获取断言结果"
      pattern: "assertion_result_repo"
    - from: frontend/src/components/Report/ApiAssertionResults.tsx
      to: frontend/src/types/index.ts
      via: "类型定义"
      pattern: "ApiAssertionResult"

---

<objective>
将接口断言集成到执行流程和报告系统中。

Purpose: 实现 UI 测试完成后执行接口断言，断言结果展示在测试报告中。
Output: 修改后的执行流程、报告服务，前端断言结果展示组件。
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-接口断言集成/06-CONTEXT.md
@.planning/phases/06-接口断言集成/06-RESEARCH.md
@.planning/phases/06-接口断言集成/06-VALIDATION.md
@.planning/phases/06-接口断言集成/06-02-PLAN.md
@.planning/phases/06-接口断言集成/06-03-PLAN.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/api/routes/runs.py:
```python
async def run_agent_background(
    run_id: str,
    task_id: str,
    task_name: str,
    task_description: str,
    max_steps: int,
    preconditions: list[str] | None = None,
    # NEW: api_assertions parameter to add
    api_assertions: list[str] | None = None,
): ...
```

From backend/core/report_service.py:
```python
class ReportService:
    async def generate_report(self, run_id: str) -> Optional[Report]: ...
    async def get_report_data(self, run_id: str) -> Optional[dict]: ...
```

From backend/db/schemas.py:
```python
class SSEPreconditionEvent(BaseModel):
    index: int
    code: str
    status: str  # running, success, failed
    error: Optional[str] = None
    duration_ms: int = 0
    variables: Optional[dict[str, Any]] = None

# NEW: Add SSEApiAssertionEvent
class SSEApiAssertionEvent(BaseModel):
    index: int
    code: str
    status: str  # running, success, failed
    error: Optional[str] = None
    duration_ms: int = 0
    field_results: Optional[list[dict]] = None
```

From backend/core/api_assertion_service.py (created in 06-02):
```python
@dataclass
class FieldAssertionResult:
    field_name: str
    expected: Any
    actual: Any
    passed: bool = False
    message: str = ""
    assertion_type: str = "exact"

@dataclass
class ApiAssertionResult:
    index: int
    code: str
    success: bool = False
    error: str | None = None
    duration_ms: int = 0
    field_results: list[FieldAssertionResult] = field(default_factory=list)

class ApiAssertionService:
    async def execute_all(self, assertions: list[str], timeout_each: float = 30.0) -> list[ApiAssertionResult]: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 SSEApiAssertionEvent schema</name>
  <files>backend/db/schemas.py</files>
  <behavior>
    - Test: SSEApiAssertionEvent 类存在
    - Test: 包含 index, code, status  error, duration_ms, field_results 字段
  </behavior>
  <action>
1. 打开 `backend/db/schemas.py`
2. 在 SSEPreconditionEvent 类之后添加 SSEApiAssertionEvent:
   ```python
   class SSEApiAssertionEvent(BaseModel):
       """SSE 接口断言事件"""
       index: int
       code: str
       status: str  # running, success, failed
       error: Optional[str] = None
       duration_ms: int = 0
       field_results: Optional[list[dict[str, Any]]] = None
   ```
3. 保存文件
  </action>
  <verify>
    <automated>grep -n "class SSEApiAssertionEvent" backend/db/schemas.py</automated>
  </verify>
  <done>SSEApiAssertionEvent schema 添加完成</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 集成接口断言到执行流程</name>
  <files>backend/api/routes/runs.py</files>
  <behavior>
    - Test: run_agent_background 函数接受 api_assertions 参数
    - Test: UI 测试后执行接口断言
    - Test: 断言结果存储到数据库
    - Test: 断言失败时整体状态为 failed
  </behavior>
  <action>
1. 打开 `backend/api/routes/runs.py`
2. 在文件顶部添加导入:
   ```python
   from backend.core.api_assertion_service import ApiAssertionService
   from backend.db.schemas import SSEApiAssertionEvent
   ```
3. 修改 `run_agent_background` 函数签名，添加 `api_assertions` 参数:
   ```python
   async def run_agent_background(
       run_id: str,
       task_id: str,
       task_name: str,
       task_description: str,
       max_steps: int,
       preconditions: list[str] | None = None,
       api_assertions: list[str] | None = None,  # 新增参数
   ):
   ```
4. 在 UI 断言评估后、生成报告前，添加接口断言执行代码（约在 line 200 之后）:
   ```python
           # === 接口断言执行（新增） ===
           if api_assertions:
               api_assertion_service = ApiAssertionService(external_module_path=external_module_path)
               api_assertion_service.context = context  # 复用前置条件上下文

               for i, code in enumerate(api_assertions):
                   if not code.strip():
                       continue

                   # 发送 api_assertion 事件（running）
                   code_display = code[:100] + "..." if len(code) > 100 else code
                   api_event = SSEApiAssertionEvent(
                       index=i,
                       code=code_display,
                       status="running",
                   )
                   await event_manager.publish(run_id, f"event: api_assertion\ndata: {api_event.model_dump_json()}\n\n")

                   # 执行接口断言
                   api_result = await api_assertion_service.execute_single(code, i)

                   # 发送 api_assertion 事件（success/failed）
                   api_event = SSEApiAssertionEvent(
                       index=i,
                       code=code_display,
                       status="success" if api_result.success else "failed",
                       error=api_result.error,
                       duration_ms=api_result.duration_ms,
                       field_results=[
                           {
                               "field_name": fr.field_name,
                               "expected": fr.expected,
                               "actual": fr.actual,
                               "passed": fr.passed,
                               "message": fr.message,
                               "assertion_type": fr.assertion_type,
                           }
                           for fr in api_result.field_results
                       ] if api_result.field_results else None,
                   )
                   await event_manager.publish(run_id, f"event: api_assertion\ndata: {api_event.model_dump_json()}\n\n")

                   # 存储断言结果到数据库
                   if api_result.success and api_result.field_results:
                       for field_result in api_result.field_results:
                           await assertion_result_repo.create(
                               run_id=run_id,
                               assertion_id=f"api_{api_result.index}_{field_result.field_name}",
                               status="pass" if field_result.passed else "fail",
                               message=field_result.message,
                               actual_value=str(field_result.actual),
                           )
                   elif not api_result.success:
                       # 执行失败的断言
                       await assertion_result_repo.create(
                           run_id=run_id,
                           assertion_id=f"api_{api_result.index}",
                           status="fail",
                           message=api_result.error or "断言执行失败",
                           actual_value="",
                       )

               # 更新最终状态
               api_results = await api_assertion_service.execute_all(
                   [a for a in api_assertions if a.strip()]
               )
               if any(not r.success for r in api_results):
                   final_status = "failed"
                   logger.info(f"[{run_id}] 接口断言存在失败，状态设为 failed")
   ```
5. 修改 `create_run` 端点，解析 api_assertions 并传递给 `run_agent_background`:
   ```python
       # 解析 api_assertions
       api_assertions = None
       if task.api_assertions:
           try:
               api_assertions = json.loads(task.api_assertions)
           except json.JSONDecodeError:
               logger.warning(f"Task {task_id} api_assertions JSON 解析失败")

       # 启动后台执行
       background_tasks.add_task(
           run_agent_background,
           run.id,
           task_id,
           task.name,
           task.description,
           task.max_steps,
           preconditions,
           api_assertions,  # 新增参数
       )
   ```
6. 保存文件
  </action>
  <verify>
    <automated>grep -n "api_assertions" backend/api/routes/runs.py | head -5</automated>
  </verify>
  <done>接口断言集成到执行流程， UI 测试后执行接口断言，断言结果存储到数据库</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 扩展报告服务包含接口断言结果</name>
  <files>backend/core/report_service.py</files>
  <behavior>
    - Test: get_report_data 返回 api_assertion_results 字段
    - Test: 报告状态考虑接口断言结果
  </behavior>
  <action>
1. 打开 `backend/core/report_service.py`
2. 在 `get_report_data` 方法中添加 api_assertion_results 分组:
   ```python
       # Get assertion results
       assertion_results = await self.assertion_result_repo.list_by_run(run_id)

       # Separate UI assertions from API assertions
       ui_assertion_results = [
           ar for ar in assertion_results
           if not ar.assertion_id.startswith("api_")
       ]
       api_assertion_results = [
           ar for ar in assertion_results
           if ar.assertion_id.startswith("api_")
       ]

       # Calculate pass rate
       pass_rate = self.calculate_pass_rate(assertion_results)
       api_pass_rate = self.calculate_pass_rate(api_assertion_results) if api_assertion_results else "N/A"

       return {
           "report": report,
           "steps": steps,
           "assertion_results": assertion_results,
           "ui_assertion_results": ui_assertion_results,
           "api_assertion_results": api_assertion_results,
           "pass_rate": pass_rate,
           "api_pass_rate": api_pass_rate,
       }
   ```
3. 添加 `calculate_pass_rate` 静态方法（如果不存在）:
   ```python
       @staticmethod
       def calculate_pass_rate(assertion_results: list) -> str:
           """Calculate assertion pass rate as formatted string.

           Args:
               assertion_results: List of AssertionResult objects

           Returns:
               Formatted string like "75% (3/4)"
           """
           if not assertion_results:
               return "N/A (0/0)"

           total = len(assertion_results)
           passed = sum(1 for ar in assertion_results if ar.status == "pass")
           percentage = int((passed / total) * 100) if total > 0 else 0

           return f"{percentage}% ({passed}/{total})"
   ```
4. 保存文件
  </action>
  <verify>
    <automated>grep -n "api_assertion_results" backend/core/report_service.py</automated>
  </verify>
  <done>报告服务返回接口断言结果，计算单独的通过率</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 创建前端接口断言结果展示组件</name>
  <files>frontend/src/components/Report/ApiAssertionResults.tsx, frontend/src/types/index.ts</files>
  <behavior>
    - Test: ApiAssertionResults 组件渲染断言结果列表
    - Test: 显示通过率
    - Test: 失败断言显示红色样式
    - Test: 通过断言显示绿色样式
  </behavior>
  <action>
1. 在 `frontend/src/types/index.ts` 中添加类型定义:
   ```typescript
   export interface ApiAssertionFieldResult {
     field_name: string
     expected: any
     actual: any
     passed: boolean
     message: string
     assertion_type: string
   }

   export interface ApiAssertionResult {
     index: number
     code: string
     status: 'running' | 'success' | 'failed'
     error?: string
     duration_ms: number
     field_results?: ApiAssertionFieldResult[]
   }
   ```
2. 创建 `frontend/src/components/Report/ApiAssertionResults.tsx`:
   ```tsx
   import { CheckCircle, XCircle, Clock } from 'lucide-react'
   import type { AssertionResult } from '../../types'

   interface ApiAssertionResultsProps {
     results: AssertionResult[]
     passRate: string
   }

   export function ApiAssertionResults({ results, passRate }: ApiAssertionResultsProps) {
     if (!results || results.length === 0) {
       return null
     }

     const allPassed = results.every(r => r.status === 'pass')

     return (
       <div className="mt-4">
         <div className="flex items-center justify-between mb-3">
           <h3 className="text-lg font-medium text-gray-900">接口断言结果</h3>
           <span className={`px-2 py-1 rounded-full text-sm font-medium ${
             allPassed ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'
           }`}>
             通过率: {passRate}
           </span>
         </div>

         <div className="space-y-2">
           {results.map((result, index) => (
             <div
               key={index}
               className={`p-4 rounded-lg border ${
                 result.status === 'pass'
                   ? 'bg-green-50 border-green-200'
                   : 'bg-red-50 border-red-200'
               }`}
             >
               <div className="flex items-start gap-3">
                 {result.status === 'pass' ? (
                   <CheckCircle className="w-5 h-5 text-green-500 flex-shrink-0" />
                 ) : (
                   <XCircle className="w-5 h-5 text-red-500 flex-shrink-0" />
                 )}
                 <div className="flex-1 min-w-0">
                   <div className="flex items-center gap-2">
                     <span className="font-mono text-sm text-gray-700">
                       断言 #{result.assertion_id}
                     </span>
                     <span className="text-xs text-gray-500 flex items-center gap-1">
                       <Clock className="w-3 h-3" />
                       {result.duration_ms || 0}ms
                     </span>
                   </div>
                   {result.message && (
                       <p className="text-sm text-gray-600 mt-1">
                           {result.message}
                       </p>
                   )}
                   {result.actual_value && (
                       <p className="text-sm text-gray-500 mt-1">
                           实际值: {result.actual_value}
                       </p>
                   )}
                 </div>
               </div>
             </div>
           ))}
         </div>
       </div>
     )
   }
   ```
3. 保存文件
  </action>
  <verify>
    <automated>grep -n "ApiAssertionResults" frontend/src/components/Report/ApiAssertionResults.tsx</automated>
  </verify>
  <done>ApiAssertionResults 组件创建完成，显示断言结果列表和通过率</done>
</task>

<task type="auto">
  <name>Task 5: 集成接口断言组件到报告页面</name>
  <files>frontend/src/pages/Report.tsx</files>
  <action>
1. 打开 `frontend/src/pages/Report.tsx`（或报告相关页面）
2. 在现有断言结果展示区域后添加接口断言结果展示:
   ```tsx
   import { ApiAssertionResults } from '../components/Report/ApiAssertionResults'

   // 在报告数据获取后
   const { report, steps, assertion_results, api_assertion_results, pass_rate, api_pass_rate } = reportData

   // 在现有 AssertionResults 组件后添加
   {api_assertion_results && api_assertion_results.length > 0 && (
       <ApiAssertionResults
           results={api_assertion_results}
           passRate={api_pass_rate || 'N/A'}
       />
   )}
   ```
3. 保存文件
  </action>
  <verify>
    <automated>grep -n "ApiAssertionResults" frontend/src/pages/Report.tsx 2>/dev/null || echo "File may not exist yet - will be created during execution"</automated>
  </verify>
  <done>接口断言组件集成到报告页面，显示在 UI 断言结果下方</done>
</task>

</tasks>

<verification>
- run_agent_background 接受 api_assertions 参数
- UI 测试后执行接口断言
- 断言结果存储到数据库
- 报告服务返回 api_assertion_results
- 前端显示接口断言结果区域
</verification>

<success_criteria>
- `grep -n "api_assertions" backend/api/routes/runs.py` 返回至少 3 处匹配
- `grep -n "ApiAssertionService" backend/api/routes/runs.py` 返回匹配
- `grep -n "SSEApiAssertionEvent" backend/api/routes/runs.py` 返回匹配
- `grep -n "api_assertion_results" backend/core/report_service.py` 返回匹配
- `grep -n "ApiAssertionResults" frontend/src/components/Report/ApiAssertionResults.tsx` 返回匹配
- `test -f frontend/src/components/Report/ApiAssertionResults.tsx` 返回 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-接口断言集成/06-04-SUMMARY.md`
</output>
