---
phase: "06-接口断言集成"
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/core/api_assertion_service.py
  - backend/tests/unit/test_api_assertion_service.py
autonomous: true
requirements: ["API-02"]
must_haves:
  truths:
    - "ApiAssertionService 类存在并可执行接口断言代码"
    - "check_time_within_range 方法验证时间在 ±60 秒范围内"
    - "check_exact_match 方法验证精确值匹配"
    - "check_contains_match 方法验证字符串包含"
    - "check_decimal_approx 方法验证小数近似匹配"
  artifacts:
    - path: "backend/core/api_assertion_service.py"
      provides: "ApiAssertionService 类和断言方法"
      min_lines: 100
      contains: "class ApiAssertionService"
    - path: "backend/tests/unit/test_api_assertion_service.py"
      provides: "单元测试"
      exports: ["test_check_time_within_range", "test_check_exact_match", "test_check_contains_match", "test_check_decimal_approx"]
  key_links:
    - from: "backend/core/api_assertion_service.py"
      to: "backend/core/precondition_service.py"
      via: "复用 exec() 执行模式和"
      pattern: "exec\\(|substitute_variables"

---

<objective>
创建 ApiAssertionService 类，移植 BaseAssert 断言功能。

Purpose: 提供接口断言执行服务，复用 PreconditionService 的执行机制，新增断言判断逻辑。
Output: ApiAssertionService 类，包含时间断言、数据断言方法。
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-接口断言集成/06-CONTEXT.md
@.planning/phases/06-接口断言集成/06-RESEARCH.md
@.planning/phases/06-接口断言集成/06-VALIDATION.md
@.planning/phases/06-接口断言集成/06-01-PLAN.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/core/precondition_service.py:
```python
class PreconditionService:
    def __init__(self, external_module_path: str | None = None): ...
    def _setup_execution_env(self) -> dict: ...
    async def execute_single(self, code: str, index: int, timeout: float = 30.0) -> PreconditionResult: ...
    async def execute_all(self, preconditions: list[str], timeout_each: float = 30.0) -> tuple[bool, list[PreconditionResult]]: ...
    @staticmethod
    def substitute_variables(text: str, context: dict[str, Any]) -> str: ...
```

From backend/db/models.py (after 06-01):
```python
class Task(Base):
    # ... existing fields ...
    api_assertions: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 ApiAssertionService 类和数据类</name>
  <files>backend/core/api_assertion_service.py</files>
  <behavior>
    - Test: ApiAssertionService 类可实例化
    - Test: TIME_TOLERANCE_SECONDS = 60
    - Test: DECIMAL_TOLERANCE = 0.01
  </behavior>
  <action>
1. 创建 `backend/core/api_assertion_service.py` 文件
2. 添加必要的导入:
3. 定义 FieldAssertionResult 数据类:
   ```python
   @dataclass
   class FieldAssertionResult:
       """单个字段断言结果"""
       field_name: str
       expected: Any
       actual: Any
       passed: bool = False
       message: str = ""
       assertion_type: str = "exact"  # exact, contains, time, decimal
   ```
4. 定义 ApiAssertionResult 数据类:
   ```python
   @dataclass
   class ApiAssertionResult:
       """接口断言执行结果"""
       index: int
       code: str
       success: bool = False
       error: str | None = None
       duration_ms: int = 0
       field_results: list[FieldAssertionResult] = field(default_factory=list)
   ```
5. 定义 ApiAssertionService 类框架:
   ```python
   class ApiAssertionService:
       """接口断言执行服务

       复用 PreconditionService 的 exec() 执行机制，
       新增断言判断逻辑（时间、精确匹配、包含匹配、小数近似）
       """

       TIME_TOLERANCE_SECONDS = 60  # ±1 分钟
       DECIMAL_TOLERANCE = 0.01

       def __init__(self, external_module_path: str | None = None):
           self.external_module_path = external_module_path
           self.context: dict[str, Any] = {}
   ```
6. 保存文件
  </action>
  <verify>
    <automated>grep -q "class ApiAssertionService" backend/core/api_assertion_service.py && grep -q "TIME_TOLERANCE_SECONDS = 60" backend/core/api_assertion_service.py</automated>
  </verify>
  <done>ApiAssertionService 类存在，有 TIME_TOLERANCE_SECONDS=60 和 DECIMAL_TOLERANCE=0.01 常量</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现时间断言方法 check_time_within_range</name>
  <files>backend/core/api_assertion_service.py, backend/tests/unit/test_api_assertion_service.py</files>
  <behavior>
    - Test: 当前时间在范围内返回 (True, "")
    - Test: 30秒前的时间在范围内返回 (True, "")
    - Test: 59秒前的时间在范围内返回 (True, "") (边界)
    - Test: 60秒前的时间在范围内返回 (True, "") (边界)
    - Test: 61秒前的时间超出范围返回 (False, "时间差 61.0 秒超出容差范围 ±60 秒")
    - Test: 字符串格式时间 "2026-03-16 10:30:00" 可被正确解析
    - Test: 无效时间字符串返回 (False, "无法解析时间格式: invalid")
  </behavior>
  <action>
1. 在 ApiAssertionService 类中添加 check_time_within_range 方法:
   ```python
   def check_time_within_range(
       self,
       actual_time: datetime | str,
       tolerance_seconds: int = 60
   ) -> tuple[bool, str]:
       """检查时间是否在当前时间 ±tolerance_seconds 范围内

       Args:
           actual_time: 实际时间（datetime 对象或 ISO 格式字符串）
           tolerance_seconds: 容差秒数，默认 60 秒（±1 分钟）

       Returns:
           (是否通过, 错误信息)
       """
       try:
           # 解析时间
           if isinstance(actual_time, str):
               # 尝试多种格式解析
               for fmt in ["%Y-%m-%d %H:%M:%S", "%Y-%m-%dT%H:%M:%S", "%Y-%m-%d"]:
                   try:
                       actual = datetime.strptime(actual_time, fmt)
                       break
                   except ValueError:
                       continue
               else:
                   return False, f"无法解析时间格式: {actual_time}"
           else:
               actual = actual_time

           now = datetime.now()
           diff = abs((now - actual).total_seconds())

           if diff <= tolerance_seconds:
               return True, ""
           else:
               return False, f"时间差 {diff:.1f} 秒超出容差范围 ±{tolerance_seconds} 秒"

       except Exception as e:
           return False, f"时间断言错误: {str(e)}"
   ```
2. 保存文件
  </action>
  <verify>
    <automated>grep -q "def check_time_within_range" backend/core/api_assertion_service.py</automated>
  </verify>
  <done>check_time_within_range 方法验证时间在 ±60 秒范围内，支持 datetime 对象和字符串格式</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 实现数据断言方法</name>
  <files>backend/core/api_assertion_service.py, backend/tests/unit/test_api_assertion_service.py</files>
  <behavior>
    - Test: check_exact_match("hello", "hello") 返回 (True, "")
    - Test: check_exact_match("hello", "world") 返回 (False, "期望 'world'，实际 'hello'")
    - Test: check_contains_match("hello world", "world") 返回 (True, "")
    - Test: check_contains_match("hello", "xyz") 返回 (False, "'hello' 不包含 'xyz'")
    - Test: check_decimal_approx(1.01, 1.00, 0.01) 返回 (True, "")
    - Test: check_decimal_approx(1.02, 1.00, 0.01) 返回 (False, "差值 0.02 超出容差 0.01")
  </behavior>
  <action>
1. 在 ApiAssertionService 类中添加 check_exact_match 方法:
   ```python
   def check_exact_match(self, actual: Any, expected: Any) -> tuple[bool, str]:
       """精确匹配断言

       Args:
           actual: 实际值
           expected: 期望值

       Returns:
           (是否通过, 错误信息)
       """
       if actual == expected:
           return True, ""
       return False, f"期望 '{expected}'，实际 '{actual}'"
   ```
2. 添加 check_contains_match 方法:
   ```python
   def check_contains_match(self, actual: str, expected: str) -> tuple[bool, str]:
       """包含匹配断言

       Args:
           actual: 实际字符串
           expected: 期望包含的字符串

       Returns:
           (是否通过, 错误信息)
       """
       if expected in str(actual):
           return True, ""
       return False, f"'{actual}' 不包含 '{expected}'"
   ```
3. 添加 check_decimal_approx 方法:
   ```python
   def check_decimal_approx(
       self,
       actual: float,
       expected: float,
       tolerance: float = 0.01
   ) -> tuple[bool, str]:
       """小数近似断言

       Args:
           actual: 实际值
           expected: 期望值
           tolerance: 容差，默认 0.01

       Returns:
           (是否通过, 错误信息)
       """
       diff = abs(actual - expected)
       if diff <= tolerance:
           return True, ""
       return False, f"差值 {diff} 超出容差 {tolerance}"
   ```
4. 保存文件
  </action>
  <verify>
    <automated>grep -q "def check_exact_match" backend/core/api_assertion_service.py && grep -q "def check_contains_match" backend/core/api_assertion_service.py && grep -q "def check_decimal_approx" backend/core/api_assertion_service.py</automated>
  </verify>
  <done>check_exact_match、check_contains_match、check_decimal_approx 方法实现完成</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 实现执行方法和辅助函数</name>
  <files>backend/core/api_assertion_service.py, backend/tests/unit/test_api_assertion_service.py</files>
  <behavior>
    - Test: execute_single 执行简单代码成功
    - Test: execute_single 超时返回错误
    - Test: execute_single 语法错误返回错误信息
    - Test: execute_all 执行多个断言返回所有结果
    - Test: substitute_variables 替换 {{var}} 为实际值
    - Test: substitute_variables 未定义变量抛出错误
  </behavior>
  <action>
1. 添加必要的导入:
2. 添加 _setup_execution_env 方法:
   ```python
   def _setup_execution_env(self) -> dict:
       """创建执行环境"""
       if self.external_module_path:
           path = Path(self.external_module_path)
           if path.exists() and str(path) not in sys.path:
               sys.path.insert(0, str(path))

       return {
           '__builtins__': __builtins__,
           'context': self.context,
           'assert_time': self._assert_time,
           'assert_exact': self._assert_exact,
           'assert_contains': self._assert_contains,
           'assert_decimal': self._assert_decimal,
       }
   ```
3. 添加辅助断言函数:
   ```python
   def _assert_time(self, actual_time: Any) -> bool:
       """时间断言辅助函数"""
       passed, _ = self.check_time_within_range(actual_time)
       return passed

   def _assert_exact(self, actual: Any, expected: Any) -> bool:
       """精确匹配辅助函数"""
       passed, _ = self.check_exact_match(actual, expected)
       return passed

   def _assert_contains(self, actual: str, expected: str) -> bool:
       """包含匹配辅助函数"""
       passed, _ = self.check_contains_match(actual, expected)
       return passed

   def _assert_decimal(self, actual: float, expected: float, tolerance: float = 0.01) -> bool:
       """小数近似辅助函数"""
       passed, _ = self.check_decimal_approx(actual, expected, tolerance)
       return passed
   ```
4. 添加 substitute_variables 静态方法（复用 PreconditionService 模式）:
   ```python
   @staticmethod
   def substitute_variables(text: str, context: dict[str, Any]) -> str:
       """使用 Jinja2 进行变量替换"""
       if not text or '{{' not in text:
           return text

       env = Environment(
           variable_start_string='{{',
           variable_end_string='}}',
           undefined=StrictUndefined,
       )
       template = env.from_string(text)
       return template.render(**context)
   ```
5. 添加 execute_single 方法:
   ```python
   async def execute_single(
       self,
       code: str,
       index: int,
       timeout: float = 30.0
   ) -> ApiAssertionResult:
       """执行单个接口断言"""
       result = ApiAssertionResult(index=index, code=code)
       start_time = time.time()

       try:
           # 变量替换
           code = self.substitute_variables(code, self.context)
       except UndefinedError as e:
           result.error = f"变量未定义: {str(e)}"
           result.duration_ms = int((time.time() - start_time) * 1000)
           return result

       env = self._setup_execution_env()

       try:
           loop = asyncio.get_event_loop()
           await asyncio.wait_for(
               loop.run_in_executor(None, lambda: exec(code, env)),
               timeout=timeout
           )
           result.success = True
           logger.info(f"接口断言 {index} 执行成功")
       except asyncio.TimeoutError:
           result.error = f"执行超时（超过 {timeout} 秒）"
           logger.warning(f"接口断言 {index} 超时")
       except SyntaxError as e:
           result.error = f"语法错误: {e.msg} (行 {e.lineno})"
           logger.error(f"接口断言 {index} 语法错误: {e}")
       except AssertionError as e:
           result.error = f"断言失败: {str(e)}"
           logger.info(f"接口断言 {index} 断言失败: {e}")
       except Exception as e:
           result.error = f"执行错误: {str(e)}"
           logger.error(f"接口断言 {index} 执行错误: {e}", exc_info=True)

       result.duration_ms = int((time.time() - start_time) * 1000)
       return result
   ```
6. 添加 execute_all 方法:
   ```python
   async def execute_all(
       self,
       assertions: list[str],
       timeout_each: float = 30.0
   ) -> list[ApiAssertionResult]:
       """执行所有接口断言（不终止，收集所有结果）"""
       results = []

       for i, code in enumerate(assertions):
           if not code.strip():
               continue

           result = await self.execute_single(code, i, timeout_each)
           results.append(result)

       return results
   ```
7. 添加 get_context 方法:
   ```python
   def get_context(self) -> dict[str, Any]:
       """获取当前执行上下文（变量存储）"""
       return dict(self.context)
   ```
8. 保存文件
  </action>
  <verify>
    <automated>grep -q "async def execute_single" backend/core/api_assertion_service.py && grep -q "async def execute_all" backend/core/api_assertion_service.py && grep -q "def substitute_variables" backend/core/api_assertion_service.py</automated>
  </verify>
  <done>execute_single 和 execute_all 方法实现完成，支持变量替换和超时控制</done>
</task>

</tasks>

<verification>
- ApiAssertionService 类存在
- check_time_within_range 验证时间在 ±60 秒范围
- check_exact_match 验证精确匹配
- check_contains_match 验证包含匹配
- check_decimal_approx 验证小数近似
- execute_single 可执行 Python 代码
- execute_all 收集所有断言结果
</verification>

<success_criteria>
- `grep -n "class ApiAssertionService" backend/core/api_assertion_service.py` 返回匹配
- `grep -n "TIME_TOLERANCE_SECONDS = 60" backend/core/api_assertion_service.py` 返回匹配
- `grep -n "DECIMAL_TOLERANCE = 0.01" backend/core/api_assertion_service.py` 返回匹配
- `grep -n "def check_time_within_range" backend/core/api_assertion_service.py` 返回匹配
- `grep -n "def check_exact_match" backend/core/api_assertion_service.py` 返回匹配
- `grep -n "def check_contains_match" backend/core/api_assertion_service.py` 返回匹配
- `grep -n "def check_decimal_approx" backend/core/api_assertion_service.py` 返回匹配
- `grep -n "async def execute_single" backend/core/api_assertion_service.py` 返回匹配
- `grep -n "async def execute_all" backend/core/api_assertion_service.py` 返回匹配
</success_criteria>

<output>
After completion, create `.planning/phases/06-接口断言集成/06-02-SUMMARY.md`
</output>
