---
phase: 08-前端实时监控完善
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - frontend/src/api/reports.ts
autonomous: true
requirements:
  - API-01
user_setup: []
must_haves:
  truths:
    - "Frontend ReportDetailResponse includes ui_assertion_results and api_assertion_results"
    - "Frontend ReportDetailResponse includes pass_rate and api_pass_rate"
    - "getReport function transforms new backend response fields"
  artifacts:
    - path: "frontend/src/api/reports.ts"
      provides: "API client with complete report data transformation"
      exports: ["getReport", "ReportDetailResponse"]
  key_links:
    - from: "frontend/src/api/reports.ts"
      to: "backend/api/routes/reports.py"
      via: "GET /api/reports/{id}"
      pattern: "ui_assertion_results.*transformAssertionResult"
---

<objective>
Update frontend reports API client to handle new response fields (ui_assertion_results, api_assertion_results, pass_rate, api_pass_rate) from the backend.

Purpose: Complete the end-to-end data flow for report details. After Plan 01 updates the backend to return separated assertion results and pass rates, the frontend API client needs to transform these new fields.

Output: Updated frontend/src/api/reports.ts
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key interfaces the executor needs -->

From frontend/src/types/index.ts (ReportDetailResponse at lines 160-168):
```typescript
export interface ReportDetailResponse extends Report {
  steps: Step[]
  assertion_results?: AssertionResult[]
  ui_assertion_results?: AssertionResult[]
  api_assertion_results?: AssertionResult[]
  pass_rate?: string
  api_pass_rate?: string
}
```

From frontend/src/api/reports.ts (current ReportDetailApiResponse at lines 41-44):
```typescript
interface ReportDetailApiResponse extends ReportApiResponse {
  steps: StepApiResponse[]
  assertion_results: AssertionResultApiResponse[]
}
```

From frontend/src/api/reports.ts (current getReport at lines 114-122):
```typescript
export async function getReport(reportId: string): Promise<ReportDetailResponse> {
  const response = await apiClient<ReportDetailApiResponse>(`/reports/${reportId}`)

  return {
    ...transformReport(response),
    steps: response.steps.map(transformStep),
    assertion_results: (response.assertion_results || []).map(transformAssertionResult),
  }
}
```

From frontend/src/api/reports.ts (current local ReportDetailResponse at lines 92-94):
```typescript
export interface ReportDetailResponse extends Report {
  steps: Step[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ReportDetailApiResponse interface with new fields</name>
  <files>frontend/src/api/reports.ts</files>
  <read_first>
    - frontend/src/api/reports.ts (current implementation at lines 1-123)
    - frontend/src/types/index.ts (ReportDetailResponse at lines 160-168)
    - backend/db/schemas.py (updated ReportDetailResponse from Plan 01)
  </read_first>
  <action>
    Update frontend/src/api/reports.ts to handle new response fields:

    1. Update the `ReportDetailApiResponse` interface (lines 41-44) to include new optional fields:

    ```typescript
    interface ReportDetailApiResponse extends ReportApiResponse {
      steps: StepApiResponse[]
      assertion_results: AssertionResultApiResponse[]
      ui_assertion_results?: AssertionResultApiResponse[]
      api_assertion_results?: AssertionResultApiResponse[]
      pass_rate?: string
      api_pass_rate?: string
    }
    ```

    2. Update the local `ReportDetailResponse` export interface (lines 92-94) to match the types/index.ts definition. Replace with:

    ```typescript
    export interface ReportDetailResponse extends Report {
      steps: Step[]
      assertion_results?: AssertionResult[]
      ui_assertion_results?: AssertionResult[]
      api_assertion_results?: AssertionResult[]
      pass_rate?: string
      api_pass_rate?: string
    }
    ```

    3. Update the `getReport` function (lines 114-122) to transform the new fields:

    ```typescript
    export async function getReport(reportId: string): Promise<ReportDetailResponse> {
      const response = await apiClient<ReportDetailApiResponse>(`/reports/${reportId}`)

      return {
        ...transformReport(response),
        steps: response.steps.map(transformStep),
        assertion_results: (response.assertion_results || []).map(transformAssertionResult),
        ui_assertion_results: (response.ui_assertion_results || []).map(transformAssertionResult),
        api_assertion_results: (response.api_assertion_results || []).map(transformAssertionResult),
        pass_rate: response.pass_rate,
        api_pass_rate: response.api_pass_rate,
      }
    }
    ```
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/api/reports.ts `ReportDetailApiResponse` has `ui_assertion_results?: AssertionResultApiResponse[]`
    - frontend/src/api/reports.ts `ReportDetailApiResponse` has `api_assertion_results?: AssertionResultApiResponse[]`
    - frontend/src/api/reports.ts `ReportDetailApiResponse` has `pass_rate?: string`
    - frontend/src/api/reports.ts `ReportDetailApiResponse` has `api_pass_rate?: string`
    - frontend/src/api/reports.ts local `ReportDetailResponse` has all five new fields
    - frontend/src/api/reports.ts `getReport` function returns `ui_assertion_results`
    - frontend/src/api/reports.ts `getReport` function returns `api_assertion_results`
    - frontend/src/api/reports.ts `getReport` function returns `pass_rate`
    - frontend/src/api/reports.ts `getReport` function returns `api_pass_rate`
    - Frontend build succeeds: `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Frontend reports API client transforms and returns ui_assertion_results, api_assertion_results, pass_rate, api_pass_rate from backend</done>
</task>

</tasks>

<verification>
- Verify interface: `grep -n "ui_assertion_results\|api_assertion_results\|pass_rate\|api_pass_rate" frontend/src/api/reports.ts`
- Build check: `cd frontend && npm run build`
</verification>

<success_criteria>
- ReportDetailApiResponse includes new optional fields
- Local ReportDetailResponse matches types/index.ts definition
- getReport function transforms all new fields
- Frontend build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-前端实时监控完善/08-03-SUMMARY.md`
</output>
