---
phase: "05"
plan: "03"
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - backend/core/precondition_service.py
  - backend/config.py
  - backend/tests/unit/test_precondition_service.py
autonomous: true
requirements: [PRE-03]
must_haves:
  truths:
    - "用户可以通过环境变量配置外部 API 模块路径"
    - "PreconditionService 可以加载外部模块中的类和方法"
    - "外部模块中的 API 封装方法可以在前置条件代码中调用"
  artifacts:
    - path: "backend/config.py"
      provides: "ERP_API_MODULE_PATH 配置"
      contains: "erp_api_module_path"
    - path: "backend/core/precondition_service.py"
      provides: "外部模块加载功能"
      contains: "external_module_path"
  key_links:
    - from: "backend/core/precondition_service.py"
      to: "sys.path"
      via: "模块路径添加"
      pattern: "sys.path"
    - from: ".env"
      to: "backend/config.py"
      via: "ERP_API_MODULE_PATH"
      pattern: "ERP_API_MODULE_PATH"
---

<objective>
扩展 PreconditionService 支持加载外部 API 模块，复用现有项目的 API 封装方法。

Purpose: 让用户可以在前置条件代码中 import 并调用现有 ERP 测试项目的 API 封装类。
Output: 配置项、模块加载功能、相关测试
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-前置条件系统/05-CONTEXT.md
@.planning/phases/05-前置条件系统/05-RESEARCH.md

<interfaces>
<!-- Key patterns from existing config and services -->

From backend/config.py:
```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    # LLM 配置
    llm_model: str = "qwen-plus"
    dashscope_api_key: str | None = None
    # ... other settings

    class Config:
        env_file = ".env"
        extra = "ignore"

def get_settings() -> Settings:
    return Settings()
```

From RESEARCH.md - Pattern 3: Dynamic Module Loading:
```python
import importlib.util
import sys
from pathlib import Path

def load_external_module(module_path: str, module_name: str = "external_api"):
    """动态加载外部 Python 模块"""
    path = Path(module_path)
    if not path.exists():
        raise FileNotFoundError(f"模块路径不存在: {module_path}")

    spec = importlib.util.spec_from_file_location(module_name, path)
    if spec is None or spec.loader is None:
        raise ImportError(f"无法加载模块: {module_path}")

    module = importlib.util.module_from_spec(spec)
    sys.modules[module_name] = module
    spec.loader.exec_module(module)
    return module
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 ERP_API_MODULE_PATH 配置项</name>
  <files>backend/config.py</files>
  <read_first>
    - backend/config.py
  </read_first>
  <action>
    在 `backend/config.py` 的 Settings 类中添加外部 API 模块路径配置：

    1. 添加字段：
       ```python
       # 外部 API 模块配置
       erp_api_module_path: str | None = None  # 外部 API 模块路径
       ```

    2. 位置：在 LLM 配置之后，class Config 之前

    3. 添加注释说明用途：
       ```python
       # 外部 API 模块路径（用于前置条件中复用现有项目的 API 封装）
       # 例如：/path/to/your/erp-test-project
       # 设置后可在前置条件代码中 import api.xxx
       ```

    用户使用时在 `.env` 中配置：
    ```
    ERP_API_MODULE_PATH=/path/to/erp-test-project
    ```
  </action>
  <verify>
    <automated>grep -n "erp_api_module_path" backend/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/config.py` 包含 `erp_api_module_path: str | None = None`
    - 字段有注释说明用途
  </acceptance_criteria>
  <done>Settings 类包含 erp_api_module_path 配置项</done>
</task>

<task type="auto">
  <name>Task 2: 增强 PreconditionService 支持外部模块路径验证</name>
  <files>backend/core/precondition_service.py</files>
  <read_first>
    - backend/core/precondition_service.py
    - backend/config.py
  </read_first>
  <action>
    增强 PreconditionService 的外部模块加载功能：

    1. 在 `_setup_execution_env` 方法中添加路径验证日志：

    ```python
    def _setup_execution_env(self) -> dict:
        """创建执行环境"""
        # 添加外部模块路径
        if self.external_module_path:
            path = Path(self.external_module_path)
            if not path.exists():
                logger.warning(f"外部模块路径不存在: {path}")
            elif str(path) not in sys.path:
                sys.path.insert(0, str(path))
                logger.info(f"添加外部模块路径: {path}")
            else:
                logger.debug(f"外部模块路径已在 sys.path 中: {path}")

        return {
            '__builtins__': __builtins__,
            'context': self.context,
        }
    ```

    2. 添加验证方法：
    ```python
    def validate_external_module_path(self) -> tuple[bool, str]:
        """验证外部模块路径是否有效

        Returns:
            (是否有效, 错误信息或成功消息)
        """
        if not self.external_module_path:
            return True, "未配置外部模块路径"

        path = Path(self.external_module_path)
        if not path.exists():
            return False, f"模块路径不存在: {self.external_module_path}"
        if not path.is_dir():
            return False, f"模块路径不是目录: {self.external_module_path}"

        return True, f"外部模块路径有效: {self.external_module_path}"
    ```

    3. 确保导入 `Path`：
    ```python
    from pathlib import Path
    ```
  </action>
  <verify>
    <automated>grep -n "validate_external_module_path" backend/core/precondition_service.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/core/precondition_service.py` 包含 `validate_external_module_path` 方法
    - 方法返回 `tuple[bool, str]`
    - 路径不存在时有警告日志
  </acceptance_criteria>
  <done>PreconditionService 支持外部模块路径验证</done>
</task>

<task type="auto">
  <name>Task 3: 添加外部模块加载测试</name>
  <files>backend/tests/unit/test_precondition_service.py</files>
  <read_first>
    - backend/core/precondition_service.py
    - backend/tests/unit/test_precondition_service.py
  </read_first>
  <action>
    在 `backend/tests/unit/test_precondition_service.py` 中添加外部模块加载测试：

    ```python
    import tempfile
    import os

    class TestPreconditionServiceExternalModule:
        """外部模块加载测试"""

        @pytest.mark.asyncio
        async def test_load_external_module(self, tmp_path):
            """测试加载外部模块（PRE-03）"""
            # 创建临时模块
            module_dir = tmp_path / "test_api"
            module_dir.mkdir()
            api_file = module_dir / "test_api.py"
            api_file.write_text('''
    class TestApi:
        def __init__(self):
            self.base_url = "http://test.com"

        def get_data(self):
            return {"status": "ok"}
    ''')
            # 添加 __init__.py
            (module_dir / "__init__.py").write_text("")

            service = PreconditionService(external_module_path=str(module_dir.parent))

            code = '''
    from test_api.test_api import TestApi
    api = TestApi()
    context['api_result'] = api.get_data()
    '''
            result = await service.execute_single(code, 0)

            assert result.success is True, f"Error: {result.error}"
            assert result.variables.get('api_result') == {"status": "ok"}

        @pytest.mark.asyncio
        async def test_invalid_module_path(self):
            """测试无效模块路径"""
            service = PreconditionService(external_module_path="/nonexistent/path")
            valid, msg = service.validate_external_module_path()

            assert valid is False
            assert "不存在" in msg

        @pytest.mark.asyncio
        async def test_no_external_module_path(self):
            """测试未配置外部模块路径"""
            service = PreconditionService()
            valid, msg = service.validate_external_module_path()

            assert valid is True
            assert "未配置" in msg

        @pytest.mark.asyncio
        async def test_module_import_error(self, tmp_path):
            """测试模块导入错误"""
            module_dir = tmp_path / "broken_api"
            module_dir.mkdir()
            broken_file = module_dir / "broken.py"
            broken_file.write_text("import nonexistent_module")  # 故意导入不存在的模块
            (module_dir / "__init__.py").write_text("")

            service = PreconditionService(external_module_path=str(module_dir.parent))

            code = "from broken_api import broken"
            result = await service.execute_single(code, 0)

            assert result.success is False
            assert "执行错误" in result.error or "ModuleNotFoundError" in result.error

        @pytest.mark.asyncio
        async def test_context_with_external_api(self, tmp_path):
            """测试使用外部 API 并存储结果到 context"""
            module_dir = tmp_path / "erp_api"
            module_dir.mkdir()
            api_file = module_dir / "order_api.py"
            api_file.write_text('''
    class OrderApi:
        def create_order(self, name):
            return {"id": "ORD-001", "name": name}
    ''')
            (module_dir / "__init__.py").write_text("")

            service = PreconditionService(external_module_path=str(module_dir.parent))

            code = '''
    from erp_api.order_api import OrderApi
    api = OrderApi()
    order = api.create_order("测试订单")
    context['order_id'] = order['id']
    context['order_name'] = order['name']
    '''
            result = await service.execute_single(code, 0)

            assert result.success is True
            assert result.variables.get('order_id') == "ORD-001"
            assert result.variables.get('order_name') == "测试订单"
    ```
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_precondition_service.py::TestPreconditionServiceExternalModule -v --tb=short 2>&1 | grep -E "PASSED|test_load_external_module" | wc -l | xargs test 1 -le</automated>
  </verify>
  <acceptance_criteria>
    - 测试文件包含 `TestPreconditionServiceExternalModule` 类
    - 包含 `test_load_external_module` 测试
    - 包含 `test_invalid_module_path` 测试
    - 所有新测试通过
  </acceptance_criteria>
  <done>外部模块加载测试全部通过</done>
</task>

</tasks>

<verification>
- Settings 包含 erp_api_module_path 配置项
- PreconditionService 验证外部模块路径
- 外部模块可以被正确加载和调用
- 无效路径有清晰的错误提示
- 单元测试覆盖外部模块加载场景
</verification>

<success_criteria>
- 用户可以通过 ERP_API_MODULE_PATH 环境变量配置外部模块路径
- 前置条件代码可以 import 外部模块中的类和方法
- 模块加载失败有清晰的错误信息
</success_criteria>

<output>
After completion, create `.planning/phases/05-前置条件系统/05-03-SUMMARY.md`
</output>
