---
phase: "05"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/db/models.py
  - backend/db/schemas.py
  - backend/db/repository.py
  - frontend/src/types/index.ts
  - frontend/src/components/TaskModal/TaskForm.tsx
autonomous: true
requirements: [PRE-01]
must_haves:
  truths:
    - "用户可以在任务编辑页看到前置条件输入区域"
    - "前置条件以代码字符串数组形式存储"
    - "任务模型支持 preconditions 字段"
  artifacts:
    - path: "backend/db/models.py"
      provides: "Task.preconditions 字段"
      contains: "preconditions"
    - path: "backend/db/schemas.py"
      provides: "TaskCreate/TaskUpdate/TaskResponse 中的 preconditions"
      contains: "preconditions"
    - path: "frontend/src/types/index.ts"
      provides: "TypeScript Task 和 CreateTaskDto 中的 preconditions"
      contains: "preconditions"
    - path: "frontend/src/components/TaskModal/TaskForm.tsx"
      provides: "前置条件输入 UI"
      contains: "preconditions"
  key_links:
    - from: "frontend/src/components/TaskModal/TaskForm.tsx"
      to: "backend/db/schemas.py"
      via: "CreateTaskDto.preconditions"
      pattern: "preconditions"
---

<objective>
扩展 Task 模型和前端表单，支持前置条件字段的存储和编辑。

Purpose: 为前置条件系统提供数据模型和 UI 入口，让用户能够定义测试前的准备步骤。
Output: Task 模型扩展、Schema 扩展、前端类型定义、前端表单组件
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-前置条件系统/05-CONTEXT.md
@.planning/phases/05-前置条件系统/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From backend/db/models.py:
```python
class Task(Base):
    __tablename__ = "tasks"
    id: Mapped[str] = mapped_column(String(8), primary_key=True, default=generate_id)
    name: Mapped[str] = mapped_column(String(200), nullable=False)
    description: Mapped[str] = mapped_column(Text, nullable=False)
    # ... other fields
```

From backend/db/schemas.py:
```python
class TaskBase(BaseModel):
    name: str = Field(..., min_length=1, max_length=200)
    description: str = Field(..., min_length=1)
    target_url: str = Field(default="", max_length=500)
    max_steps: int = Field(default=10, ge=1, le=100)

class TaskCreate(TaskBase):
    pass

class TaskUpdate(BaseModel):
    name: Optional[str] = Field(None, min_length=1, max_length=200)
    # ... other optional fields

class TaskResponse(TaskBase):
    id: str
    status: str
    created_at: datetime
    updated_at: datetime
```

From frontend/src/types/index.ts:
```typescript
export interface Task {
  id: string
  name: string
  description: string
  target_url: string
  max_steps: number
  status: 'draft' | 'ready'
  created_at: string
  updated_at: string
}

export interface CreateTaskDto {
  name: string
  description: string
  target_url: string
  max_steps: number
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 Task 模型添加 preconditions 字段</name>
  <files>backend/db/models.py</files>
  <read_first>
    - backend/db/models.py
  </read_first>
  <action>
    在 Task 模型中添加 preconditions 字段：
    - 字段类型：`Mapped[Optional[str]] = mapped_column(Text, nullable=True)`
    - 存储格式：JSON 字符串数组，如 `["code1", "code2"]`
    - 位置：在 `updated_at` 字段之后，`# 关系` 注释之前
    - 添加注释：`# 前置条件（JSON 字符串数组）`
  </action>
  <verify>
    <automated>grep -n "preconditions.*mapped_column" backend/db/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/db/models.py` 包含 `preconditions: Mapped[Optional[str]] = mapped_column(Text, nullable=True)`
    - 字段位于 Task 类中
  </acceptance_criteria>
  <done>Task 模型包含 preconditions 字段，类型为 Optional[str]（JSON 字符串）</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 Pydantic Schemas</name>
  <files>backend/db/schemas.py</files>
  <read_first>
    - backend/db/schemas.py
  </read_first>
  <action>
    在 schemas.py 中添加 preconditions 支持：
    1. 在 `TaskBase` 类中添加：`preconditions: Optional[List[str]] = Field(default=None, description="前置条件代码列表")`
    2. 在 `TaskUpdate` 类中添加：`preconditions: Optional[List[str]] = Field(None, description="前置条件代码列表")`
    3. 在 `TaskResponse` 类中添加：`preconditions: Optional[List[str]] = None`
    4. 需要导入：`from typing import Optional, List`（如果尚未导入）

    注意：TaskBase 中的 preconditions 默认为 None，允许空列表。前端提交时会传递字符串数组。
  </action>
  <verify>
    <automated>grep -n "preconditions.*List\[str\]" backend/db/schemas.py | wc -l | xargs test 3 -le</automated>
  </verify>
  <acceptance_criteria>
    - `backend/db/schemas.py` 中 TaskBase 包含 `preconditions: Optional[List[str]]`
    - TaskUpdate 包含 `preconditions: Optional[List[str]]`
    - TaskResponse 包含 `preconditions: Optional[List[str]]`
  </acceptance_criteria>
  <done>TaskCreate、TaskUpdate、TaskResponse 都支持 preconditions 字段</done>
</task>

<task type="auto">
  <name>Task 3: 更新 TaskRepository 支持 preconditions 序列化</name>
  <files>backend/db/repository.py</files>
  <read_first>
    - backend/db/repository.py
    - backend/db/schemas.py
  </read_first>
  <action>
    在 TaskRepository 中处理 preconditions 的 JSON 序列化/反序列化：
    1. 在 `create` 方法中，检查 data.preconditions 是否存在，如果存在则用 `json.dumps()` 转为字符串
    2. 在 `update` 方法中，同样处理 preconditions 字段
    3. 添加辅助方法 `_serialize_preconditions` 和 `_deserialize_preconditions`
    4. 需要导入：`import json`

    实现细节：
    ```python
    def _serialize_preconditions(self, preconditions: list[str] | None) -> str | None:
        if preconditions is None:
            return None
        return json.dumps(preconditions, ensure_ascii=False)

    def _deserialize_preconditions(self, preconditions: str | None) -> list[str] | None:
        if preconditions is None:
            return None
        return json.loads(preconditions)
    ```
  </action>
  <verify>
    <automated>grep -n "_serialize_preconditions\|_deserialize_preconditions" backend/db/repository.py | wc -l | xargs test 2 -le</automated>
  </verify>
  <acceptance_criteria>
    - `backend/db/repository.py` 包含 `_serialize_preconditions` 方法
    - `backend/db/repository.py` 包含 `_deserialize_preconditions` 方法
    - `create` 方法中调用了 `_serialize_preconditions`
  </acceptance_criteria>
  <done>TaskRepository 正确处理 preconditions 的 JSON 序列化和反序列化</done>
</task>

<task type="auto">
  <name>Task 4: 扩展前端 TypeScript 类型定义</name>
  <files>frontend/src/types/index.ts</files>
  <read_first>
    - frontend/src/types/index.ts
  </read_first>
  <action>
    在 frontend/src/types/index.ts 中添加 preconditions 字段：
    1. 在 `Task` 接口中添加：`preconditions?: string[]`
    2. 在 `CreateTaskDto` 接口中添加：`preconditions?: string[]`
    3. 在 `UpdateTaskDto` 接口中添加：`preconditions?: string[]`

    位置：在 `max_steps` 字段之后，`status` 字段之前（对于 Task 接口）
  </action>
  <verify>
    <automated>grep -n "preconditions.*string\[\]" frontend/src/types/index.ts | wc -l | xargs test 3 -le</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/types/index.ts` 中 Task 接口包含 `preconditions?: string[]`
    - CreateTaskDto 接口包含 `preconditions?: string[]`
    - UpdateTaskDto 接口包含 `preconditions?: string[]`
  </acceptance_criteria>
  <done>前端类型定义支持 preconditions 字段</done>
</task>

<task type="auto">
  <name>Task 5: 扩展 TaskForm 组件添加前置条件输入区域</name>
  <files>frontend/src/components/TaskModal/TaskForm.tsx</files>
  <read_first>
    - frontend/src/components/TaskModal/TaskForm.tsx
    - frontend/src/types/index.ts
  </read_first>
  <action>
    在 TaskForm.tsx 中添加前置条件输入区域：

    1. 更新 FormData 接口，添加 `preconditions: string[]`

    2. 更新初始状态：
       ```typescript
       preconditions: initialData?.preconditions || ['']
       ```

    3. 在表单中添加前置条件输入区域（在"最大步数"之后，提交按钮之前）：
       - 标签："前置条件"（可选）
       - 说明文字："输入 Python 代码，在前置条件中通过 context['变量名'] 存储结果"
       - 每个前置条件是一个 textarea，使用 `font-mono text-sm` 样式
       - 提供"添加前置条件"和"删除"按钮
       - rows=4

    4. 添加处理函数：
       ```typescript
       const handleAddPrecondition = () => {
         setFormData(prev => ({ ...prev, preconditions: [...prev.preconditions, ''] }))
       }
       const handleRemovePrecondition = (index: number) => {
         setFormData(prev => ({
           ...prev,
           preconditions: prev.preconditions.filter((_, i) => i !== index)
         }))
       }
       const handlePreconditionChange = (index: number, value: string) => {
         setFormData(prev => ({
           ...prev,
           preconditions: prev.preconditions.map((p, i) => i === index ? value : p)
         }))
       }
       ```

    5. 提交时过滤空字符串：
       ```typescript
       onSubmit({
         ...formData,
         preconditions: formData.preconditions.filter(p => p.trim())
       })
       ```
  </action>
  <verify>
    <automated>grep -n "handlePreconditionChange\|handleAddPrecondition" frontend/src/components/TaskModal/TaskForm.tsx | wc -l | xargs test 2 -le</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/TaskModal/TaskForm.tsx` 包含 `handlePreconditionChange` 函数
    - 包含 `handleAddPrecondition` 函数
    - FormData 接口包含 `preconditions: string[]`
    - 表单中有前置条件输入区域（textarea 元素）
  </acceptance_criteria>
  <done>TaskForm 组件支持前置条件的添加、编辑和删除</done>
</task>

</tasks>

<verification>
- Task 模型包含 preconditions 字段
- Schema 支持前置条件的创建和响应
- Repository 正确序列化/反序列化 JSON
- 前端类型定义同步
- 表单组件可以添加/删除/编辑前置条件
</verification>

<success_criteria>
- 用户可以在任务编辑页看到"前置条件"输入区域
- 用户可以添加多个前置条件代码块
- 用户可以删除前置条件
- 创建/更新任务时前置条件被保存到数据库
</success_criteria>

<output>
After completion, create `.planning/phases/05-前置条件系统/05-01-SUMMARY.md`
</output>
