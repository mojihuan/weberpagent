---
phase: 33-人工验证断言执行
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - ASSERT-01
  - ASSERT-02
  - ASSERT-03
  - ASSERT-04
user_setup: []
must_haves:
  truths:
    - "断言被正确调用，返回结构包含 success/passed/fields"
    - "'now' 时间转换正确，显示为实际时间字符串（如 2026-03-22 10:30:00）"
    - "断言结果显示在报告的断言结果卡片中"
    - "字段级结果清晰显示 name/expected/actual/passed"
  artifacts:
    - path: ".planning/phases/33-人工验证断言执行/33-VERIFICATION.md"
      provides: "Verification checklist and results documentation"
    - path: ".planning/phases/33-人工验证断言执行/33-ISSUES.md"
      provides: "Bug tracking for discovered issues"
  key_links:
    - from: "Frontend TaskModal"
      to: "Backend /api/tasks"
      via: "Task creation with assertion config"
      pattern: "assertions array with field_params"
    - from: "Backend execute_all_assertions()"
      to: "execute_assertion_method()"
      via: "Three-layer params extraction"
      pattern: "api_params, field_params, params"
    - from: "execute_assertion_method()"
      to: "_convert_now_values()"
      via: "'now' time conversion"
      pattern: "saleTime='now' -> '2026-03-22 10:30:00'"
---

<objective>

Manual verification of assertion execution flow through the complete UI workflow. Execute the sales outbound test case and verify that `sell_sale_item_list_assert` correctly receives parameters, converts 'now' time, and displays results in the report.

**Purpose:** Validate end-to-end assertion system integration (three-layer params, "now" conversion, report display) through actual UI execution.

**Output:** VERIFICATION.md with pass/fail results for all 4 Success Criteria, ISSUES.md with any bugs discovered.

</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-人工验证断言执行/33-CONTEXT.md
@.planning/phases/33-人工验证断言执行/33-RESEARCH.md
@.planning/phases/33-人工验证断言执行/33-VALIDATION.md
</context>

<interfaces>

<!-- Key types and contracts the executor needs -->

### Backend Assertion Config Structure
```python
# From execute_all_assertions() in external_precondition_bridge.py
assertion_config = {
    'class_name': 'PcAssert',           # Required
    'method_name': 'sell_sale_item_list_assert',  # Required
    'headers': 'main',                   # Default: 'main'
    'data': 'main',                      # Default: 'main'
    'api_params': {},                    # API filter params (i, j, k, etc.)
    'field_params': {                    # Field validation params
        'salesOrder': 'SA',
        'articlesStateStr': '已销售',
        'saleTime': 'now'
    },
    'params': {}                         # Legacy fallback
}
```

### Backend Assertion Result Structure
```python
# From execute_assertion_method() return
{
    'success': True,         # Execution succeeded
    'passed': False,         # Validation result
    'fields': [              # Field-level results (was field_results)
        {'name': 'statusStr', 'expected': '已完成', 'actual': '进行中', 'passed': False}
    ],
    'error': None,           # Error message if any
    'error_type': None,      # TimeoutError, ImportError, etc.
    'duration': 1.23         # Execution time in seconds
}
```

### Frontend Assertion Result Type
```typescript
// From frontend/src/types/index.ts
interface AssertionResult {
  id: string
  run_id: string
  assertion_id: string
  assertion_name?: string
  status: 'pass' | 'fail'
  message: string | null
  actual_value: string | null
}
```

### "now" Conversion Logic
```python
# From _convert_now_values() in external_precondition_bridge.py
# saleTime='now' -> saleTime='2026-03-22 10:30:00'
# Conversion triggered when:
#   1. value == 'now'
#   2. _is_time_field(key) returns True (field name ends with Time/time/Date/date)
```

</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Step 1: Start Services and Verify Environment</name>
  <files>N/A - Manual verification</files>
  <read_first>
    - .planning/phases/33-人工验证断言执行/33-CONTEXT.md (for locked decisions)
    - .planning/phases/33-人工验证断言执行/33-RESEARCH.md (for pitfalls to avoid)
  </read_first>
  <action>
  **Prerequisites Check:**
  1. Start backend service: `uv run uvicorn backend.api.main:app --reload --port 8080`
  2. Start frontend service: `cd frontend && npm run dev`
  3. Verify ERP credentials configured in `.env`:
     - ERP_BASE_URL
     - ERP_USERNAME
     - ERP_PASSWORD

  **Environment Verification:**
  - Backend health check: `curl http://localhost:8080/health`
  - Frontend accessible: `http://localhost:5173`
  </action>
  <verify>
  - Backend running on port 8080
  - Frontend running on port 5173
  - No startup errors in terminal
  </verify>
  <acceptance_criteria>
  - `curl http://localhost:8080/health` returns 200 OK
  - Browser can access `http://localhost:5173` and see the app
  </acceptance_criteria>
  <done>Services running, environment ready for verification</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Step 2: Create Task with Assertion Configuration</name>
  <files>N/A - Manual verification</files>
  <read_first>
    - frontend/src/components/TaskModal/FieldParamsEditor.tsx (UI component for field params)
    - .planning/phases/33-人工验证断言执行/33-CONTEXT.md (D-02: default params)
  </read_first>
  <action>
  **Navigate to Task Creation:**
  1. Open `http://localhost:5173` in browser
  2. Click "New Task" or navigate to task creation page

  **Configure Task (per D-01, D-02):**
  - Task name: "销售出库断言验证测试"
  - Test case: Select sales outbound test case

  **Configure Assertion Parameters:**
  - className: `PcAssert`
  - methodName: `sell_sale_item_list_assert`
  - headers: `main`
  - data: `main`
  - field_params:
    - salesOrder: `SA`
    - articlesStateStr: `已销售`
    - saleTime: `now`

  **Take Screenshot:**
  - Screenshot the TaskModal with assertion configuration visible
  - Save as `screenshots/33-01-task-config.png`
  </action>
  <verify>
  - TaskModal opens and shows assertion configuration section
  - Field params editor allows searching and selecting fields
  - All three field params (salesOrder, articlesStateStr, saleTime) are configurable
  </verify>
  <acceptance_criteria>
  - FieldParamsEditor component renders without errors
  - Field search finds 'salesOrder', 'articlesStateStr', 'saleTime'
  - Input fields accept values: 'SA', '已销售', 'now'
  - Assertion config preview shows all configured values
  </acceptance_criteria>
  <done>Task created with correct assertion configuration, screenshot saved</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Step 3: Execute Test and Monitor Backend Logs</name>
  <files>N/A - Manual verification</files>
  <read_first>
    - backend/core/external_precondition_bridge.py (lines 973-1069: execute_all_assertions)
    - backend/core/external_precondition_bridge.py (lines 847-970: execute_assertion_method)
    - backend/core/external_precondition_bridge.py (lines 1404-1419: _convert_now_values)
  </read_first>
  <action>
  **Precondition Data (per D-03):**
  - If no sales outbound records exist, run the full test flow first to create data
  - Alternatively, verify existing data in ERP system

  **Execute Test:**
  1. Click "Run" or "Execute" button in the task UI
  2. Monitor backend terminal for assertion execution logs

  **Expected Log Patterns:**
  ```
  Executing assertion 1/1: PcAssert.sell_sale_item_list_assert
  Assertion 0 passed  OR  Assertion 0 failed: <error message>
  Assertion execution complete: X/Y passed
  ```

  **Take Screenshot:**
  - Screenshot the execution progress/status
  - Save as `screenshots/33-02-execution-progress.png`
  </action>
  <verify>
  - Test execution starts without errors
  - Backend logs show "Executing assertion" message
  - Assertion method is called with correct parameters
  - No unexpected errors in backend terminal
  </verify>
  <acceptance_criteria>
  - Backend log contains: `Executing assertion 1/1: PcAssert.sell_sale_item_list_assert`
  - Backend log contains assertion result (passed or failed with message)
  - Backend log contains: `Assertion execution complete`
  - No ImportError, NotFoundError, or TimeoutError in logs
  </acceptance_criteria>
  <done>Test execution completed, assertion called, logs captured</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Step 4: Verify Report Display - ASSERT-01, ASSERT-02, ASSERT-03, ASSERT-04</name>
  <files>N/A - Manual verification</files>
  <read_first>
    - frontend/src/pages/ReportDetail.tsx (report display)
    - frontend/src/components/Report/AssertionResults.tsx (assertion result cards)
    - frontend/src/components/Report/ApiAssertionResults.tsx (API assertion display)
  </read_first>
  <action>
  **Navigate to Report:**
  1. After test execution completes, navigate to ReportDetail page
  2. URL format: `/reports/{run_id}`

  **Verify ASSERT-01 (Assertion Executes and Returns Result):**
  - Check that assertion_results section exists
  - Verify result structure contains: success, passed, fields
  - Status indicator shows pass (green) or fail (red)

  **Verify ASSERT-02 (Parameters Correctly Passed):**
  - Check backend logs for parameter values
  - Verify salesOrder='SA', articlesStateStr='已销售', saleTime='now' were received

  **Verify ASSERT-03 ('now' Time Conversion):**
  - Check that saleTime displays as actual datetime (e.g., '2026-03-22 10:30:00')
  - NOT the literal string 'now'
  - If assertion failed, check the 'actual' value in fields array

  **Verify ASSERT-04 (Results Stored and Displayed):**
  - AssertionResults component renders
  - Field-level results show name/expected/actual/passed
  - Pass/fail status is correct

  **Take Screenshots:**
  - Full report page: `screenshots/33-03-report-full.png`
  - Assertion results section: `screenshots/33-04-assertion-results.png`
  - Field-level details (if visible): `screenshots/33-05-field-details.png`
  </action>
  <verify>
  **ASSERT-01 Checklist:**
  - [ ] assertion_results array exists in report data
  - [ ] Result contains success/passed/fields keys
  - [ ] Status shows 'pass' or 'fail' (not error)

  **ASSERT-02 Checklist:**
  - [ ] Backend logs show field_params received
  - [ ] Parameters match configured values

  **ASSERT-03 Checklist:**
  - [ ] saleTime does NOT show as literal 'now'
  - [ ] saleTime shows as 'YYYY-MM-DD HH:mm:ss' format

  **ASSERT-04 Checklist:**
  - [ ] AssertionResults component displays
  - [ ] Fields array shows field-level results
  - [ ] Each field has name/expected/actual/passed
  </verify>
  <acceptance_criteria>
  - ReportDetail page loads without errors
  - `assertion_results` section visible in report
  - Status indicator (green/red) displays correctly
  - If failed: field details show expected vs actual values
  - If assertion used saleTime='now': actual value shows datetime format (not 'now')
  </acceptance_criteria>
  <done>All 4 ASSERT requirements verified, screenshots saved</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Step 5: Document Results and Any Issues</name>
  <files>.planning/phases/33-人工验证断言执行/33-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/33-人工验证断言执行/33-CONTEXT.md (D-04, D-05, D-06, D-07 for issue recording)
  </read_first>
  <action>
  **Create VERIFICATION.md:**
  Document the verification results for all 4 Success Criteria:

  ```markdown
  # Phase 33 Verification Results

  **Date:** YYYY-MM-DD
  **Verifier:** [Name]
  **Test Case:** 销售出库断言验证

  ## Success Criteria Results

  | # | Criteria | Status | Notes |
  |---|----------|--------|-------|
  | 1 | 用户能在前端配置断言参数 | PASS/FAIL | [Details] |
  | 2 | 执行测试后断言被调用 | PASS/FAIL | [Details] |
  | 3 | 断言结果显示在测试报告中 | PASS/FAIL | [Details] |
  | 4 | saleTime='now' 显示为实际时间 | PASS/FAIL | [Details] |

  ## Evidence
  - Screenshots: [List saved screenshots]
  - Backend logs: [Key log excerpts]

  ## Conclusion
  [PASS/FAIL with summary]
  ```

  **If Issues Found (per D-06, D-07):**
  Create ISSUES.md with:
  - Issue ID
  - Description
  - Reproduction steps
  - Error messages/evidence
  - Priority (P0-P3)
  </action>
  <verify>
  - VERIFICATION.md created with all 4 criteria results
  - Each criterion has PASS or FAIL status
  - Screenshots referenced in evidence section
  - If issues found: ISSUES.md created with proper format
  </verify>
  <acceptance_criteria>
  - `.planning/phases/33-人工验证断言执行/33-VERIFICATION.md` exists
  - File contains table with 4 Success Criteria rows
  - Each row has Status column with PASS or FAIL
  - Evidence section lists at least 3 screenshots
  - If any FAIL: `33-ISSUES.md` exists with issue details
  </acceptance_criteria>
  <done>VERIFICATION.md completed, ISSUES.md created if bugs found</done>
</task>

</tasks>

<verification>

## Overall Phase Verification

Complete all 5 verification steps in sequence:

1. **Environment Setup** - Services running, credentials configured
2. **Task Configuration** - Assertion params correctly entered in UI
3. **Test Execution** - Assertion called, backend logs confirm
4. **Report Display** - All 4 ASSERT requirements verified
5. **Documentation** - VERIFICATION.md and ISSUES.md completed

**Final Check:**
- All 4 Success Criteria have explicit PASS/FAIL status
- Screenshots provide visual evidence
- Backend logs confirm parameter passing and "now" conversion
- Any issues properly documented with priority

</verification>

<success_criteria>

## Phase 33 Success Criteria (from ROADMAP)

1. **ASSERT-01:** 用户能在前端配置断言参数（salesOrder、articlesStateStr、saleTime）
   - Evidence: Screenshot of TaskModal with configured field_params

2. **ASSERT-02:** 执行测试后断言被调用，返回成功或失败结果
   - Evidence: Backend logs showing assertion execution, result structure with success/passed/fields

3. **ASSERT-03:** 断言结果显示在测试报告中
   - Evidence: Screenshot of ReportDetail with AssertionResults section

4. **ASSERT-04:** `saleTime='now'` 显示为实际时间而非字符串 "now"
   - Evidence: Report shows actual datetime (e.g., '2026-03-22 10:30:00'), not literal 'now'

**Phase Pass Condition:** All 4 criteria PASS in VERIFICATION.md

</success_criteria>

<output>

After completion, create `.planning/phases/33-人工验证断言执行/33-01-SUMMARY.md` with:
- Verification summary
- All 4 criteria results
- Screenshots list
- Issues found (if any)
- Next steps (proceed to Phase 34 for bug fixes, or Phase 35 for documentation)

</output>
