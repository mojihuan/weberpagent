---
phase: 61-e2e
plan: 02
type: execute
wave: 2
depends_on:
  - 61-01
files_modified:
  - docs/test-steps/v0.8.0-综合验证结果.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - FMT-01
  - FMT-02
  - FMT-03
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - RPT-01
  - RPT-02
  - RPT-03
  - FORM-01
  - FORM-02

must_haves:
  truths:
    - "SC-1: Execution monitor shows all step types in StepTimeline with formatted reasoning"
    - "SC-2: Report detail page shows precondition/assertion/normal steps in unified timeline"
    - "SC-3: Task form shows business assertions directly without tab switcher"
    - "SC-4: Historical reports load and display correctly without errors"
  artifacts:
    - path: "docs/test-steps/v0.8.0-综合验证结果.md"
      provides: "Comprehensive E2E verification results for all 4 Success Criteria"
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated FMT-01/02/03 status from pending to complete"
      contains: "FMT-01"
  key_links:
    - from: "docs/test-steps/v0.8.0-综合验证结果.md"
      to: "ROADMAP Phase 61 Success Criteria"
      via: "4 verification results mapped to SC-1 through SC-4"
      pattern: "SC-[1-4].*通过|SC-[1-4].*失败"
---

<objective>
Execute manual E2E verification following the test steps document from Plan 61-01, then generate the comprehensive verification results document.

Purpose: Verify all v0.8.0 changes work end-to-end through the platform UI, per D-04, D-06, D-07, D-08. User manually creates and executes a test task, observes all 4 Success Criteria, and Claude records results.
Output: Verification results document + updated REQUIREMENTS.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-e2e/61-CONTEXT.md
@.planning/phases/61-e2e/61-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Execute E2E verification via platform UI</name>
  <what-built>Plan 61-01 created test steps document and confirmed environment readiness. Now the user must manually execute the E2E verification through the platform UI.</what-built>
  <read_first>
    - docs/test-steps/v0.8.0-综合验证测试步骤.md (the test steps to follow)
    - .planning/phases/61-e2e/61-01-SUMMARY.md (pre-check results)
  </read_first>
  <action>
**Prerequisites (Claude automates):**
1. Start backend server: `cd /Users/huhu/project/weberpagent && uv run uvicorn backend.api.main:app --reload --port 8080`
2. Start frontend dev server: `cd /Users/huhu/project/weberpagent/frontend && npm run dev`
3. Confirm both servers are running by checking http://localhost:8080/api/health (or the frontend URL)

**User verification steps:**

Follow the test steps in `docs/test-steps/v0.8.0-综合验证测试步骤.md` in order:

1. **SC-3 (Task Form) first** — while creating the task:
   - Open task creation form at http://localhost:5173 (or wherever frontend serves)
   - Verify no tab switcher in assertion area
   - Verify business assertion selector is directly visible
   - Create the comprehensive test task per D-01/D-02 (purchase scenario with precondition + assertion)
   - Take note: pass/fail for each check point

2. **SC-1 (Execution Monitor)** — during task execution:
   - Execute the created task
   - Watch the execution monitor (StepTimeline + ReasoningLog)
   - Verify precondition steps appear with status/duration
   - Verify assertion steps appear with status/name
   - Verify all step types interleave in execution order
   - Verify reasoning text shows Eval/Verdict/Memory/Goal on separate lines with colored badges
   - Take note: pass/fail for each check point

3. **SC-2 (Report Detail)** — after execution completes:
   - Navigate to the completed task's report detail page
   - Verify precondition steps show in timeline with results
   - Verify assertion steps show in timeline with pass/fail
   - Verify all steps interleave correctly
   - Verify reasoning text is formatted with labels highlighted
   - Take note: pass/fail for each check point

4. **SC-4 (Backward Compatibility)** — check historical reports:
   - Navigate to a task created before Phase 60
   - Open its report detail page
   - Verify page loads without errors
   - Verify old report content displays (may use fallback rendering)
   - Take note: pass/fail for each check point
  </action>
  <how-to-verify>
    **Step-by-step verification:**

    A. **SC-3 Task Form (verify during task creation):**
       1. Open http://localhost:5173, click "新建任务"
       2. Check: assertion area shows directly (no tab switcher with "接口断言"/"业务断言")
       3. Check: no textarea for api_assertions code input
       4. Check: assertion selector button is clickable
       5. Fill in task name, description, preconditions, assertions
       6. Submit task

    B. **SC-1 Execution Monitor (verify during task run):**
       1. Click "执行" on the created task
       2. Watch StepTimeline: precondition steps should appear (amber color)
       3. Watch StepTimeline: normal UI steps should appear
       4. Watch StepTimeline: assertion steps should appear (purple color)
       5. All types should interleave by execution order, not grouped separately
       6. Watch ReasoningLog: reasoning text should show colored labels (Eval=purple, Verdict=green, Memory=orange, Goal=blue)

    C. **SC-2 Report Detail (verify after completion):**
       1. Click the completed task to view report
       2. Check timeline: precondition steps visible with status/duration/variables
       3. Check timeline: assertion steps visible with status/name
       4. Check timeline: all step types interleaved
       5. Check step items: reasoning text formatted with highlighted labels

    D. **SC-4 Backward Compatibility (verify with old task):**
       1. Find any task from before Phase 60 in the task list
       2. Open its report
       3. Page should load without JavaScript errors
       4. Report content should display (old format fallback is acceptable)
  </how-to-verify>
  <resume-signal>
Type "approved" if all 4 scenarios pass. Type "partial" followed by details of which scenarios failed and why. Type specific issues encountered for any failures.

Expected format:
- SC-1: PASS/FAIL [details]
- SC-2: PASS/FAIL [details]
- SC-3: PASS/FAIL [details]
- SC-4: PASS/FAIL [details]
  </resume-signal>
  <acceptance_criteria>
    - User has manually verified all 4 Success Criteria through platform UI
    - User provides pass/fail status for each SC
    - Server processes started successfully (backend on :8080, frontend dev server)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Generate verification results document and update REQUIREMENTS.md</name>
  <files>docs/test-steps/v0.8.0-综合验证结果.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/test-steps/v0.8.0-综合验证测试步骤.md (test steps reference)
    - docs/test-steps/采购-综合验证结果.md (format reference from Phase 56)
    - .planning/REQUIREMENTS.md (to update FMT-01/02/03 status)
    - .planning/ROADMAP.md (Phase 61 Success Criteria)
  </read_first>
  <action>
Based on the user's verification results from Task 1, create the comprehensive verification results document and update REQUIREMENTS.md.

**1. Create `docs/test-steps/v0.8.0-综合验证结果.md`:**

Use the same format as `采购-综合验证结果.md` from Phase 56:

```markdown
# v0.8.0 E2E 综合验证结果

## 验证日期: {today's date}
## 验证环境: 本地开发机（localhost:8080）
## 测试范围: Phase 57-60 全部变更（AI 推理格式 + 执行步骤展示 + 报告步骤展示 + 任务表单优化）

---

## 一、执行监控验证 (SC-1)

### Phase 57 AI 推理格式 (FMT-01/02/03): {PASS/FAIL}
- **说明:** {based on user feedback}
- **验证点:** ReasoningLog 中 Eval/Verdict/Memory/Goal 各自独占一行，标签有颜色高亮

### Phase 58 执行步骤展示 (EXEC-01/02/03): {PASS/FAIL}
- **说明:** {based on user feedback}
- **验证点:** StepTimeline 显示前置条件步骤、断言步骤，与普通步骤按执行顺序交错排列

---

## 二、报告详情验证 (SC-2)

### Phase 59 报告步骤展示 (RPT-01/02/03): {PASS/FAIL}
- **说明:** {based on user feedback}
- **验证点:** 报告详情页显示前置条件/断言/普通步骤交错排列，推理文本格式化

### Phase 57 推理格式在报告中 (FMT-02): {PASS/FAIL}
- **说明:** {based on user feedback}
- **验证点:** 报告详情页 StepItem 推理文本格式化展示

---

## 三、任务表单验证 (SC-3)

### Phase 60 任务表单优化 (FORM-01/02): {PASS/FAIL}
- **说明:** {based on user feedback}
- **验证点:** 无 tab 切换，断言区域直接展示，无 api_assertions textarea

---

## 四、历史报告兼容验证 (SC-4)

### 向后兼容性: {PASS/FAIL}
- **说明:** {based on user feedback}
- **验证点:** 历史报告正常加载，无报错，内容正常展示

---

## 五、总结

### 通过率

| 验证类别 | 通过 | 失败 | 总计 | 通过率 |
|----------|------|------|------|--------|
| SC-1 执行监控 | N | N | 2 | N% |
| SC-2 报告详情 | N | N | 2 | N% |
| SC-3 任务表单 | N | N | 1 | N% |
| SC-4 向后兼容 | N | N | 1 | N% |
| **总计** | **N** | **N** | **N** | **N%** |

### 失败原因分析
{Only if there are failures — describe root cause analysis per D-08}

### 结论
{Summary statement}
```

**2. Update `.planning/REQUIREMENTS.md`:**

Change FMT-01, FMT-02, FMT-03 status from `- [ ]` to `- [x]` if SC-1 and SC-2 pass (reasoning format confirmed in both monitor and report). These are currently marked as pending even though Phase 57 is complete — E2E confirmation updates them.

Only update if user confirmed those specific scenarios pass. If any fail, leave as `- [ ]` and add a note.

**3. Update traceability table** in REQUIREMENTS.md if status changed:
- FMT-01 Status: `Pending` -> `Complete` (if SC-1 passes)
- FMT-02 Status: `Pending` -> `Complete` (if SC-2 passes)
- FMT-03 Status: `Pending` -> `Complete` (if SC-1 passes)
  </action>
  <verify>
    <automated>test -f /Users/huhu/project/weberpagent/docs/test-steps/v0.8.0-综合验证结果.md && grep -c "SC-[1-4]" /Users/huhu/project/weberpagent/docs/test-steps/v0.8.0-综合验证结果.md && grep "FMT-0[123]" /Users/huhu/project/weberpagent/.planning/REQUIREMENTS.md | head -3</automated>
  </verify>
  <done>Verification results document created with per-SC results. REQUIREMENTS.md FMT-01/02/03 status updated to reflect E2E confirmation results.</done>
  <acceptance_criteria>
    - File exists: `docs/test-steps/v0.8.0-综合验证结果.md`
    - Document contains "SC-1" through "SC-4" result sections
    - Document contains pass rate summary table
    - `.planning/REQUIREMENTS.md` FMT-01/02/03 status reflects E2E results (changed to [x] if passed)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Verification results document exists with results for all 4 Success Criteria
2. REQUIREMENTS.md FMT-01/02/03 status updated per E2E results
3. No code was modified (pure verification phase per D-08)
</verification>

<success_criteria>
- User completed manual E2E verification of all 4 Success Criteria
- Verification results document records each SC result with evidence
- REQUIREMENTS.md reflects confirmed requirement statuses
- Phase 61 E2E verification complete — v0.8.0 milestone ready for release
</success_criteria>

<output>
After completion, create `.planning/phases/61-e2e/61-02-SUMMARY.md`
</output>
