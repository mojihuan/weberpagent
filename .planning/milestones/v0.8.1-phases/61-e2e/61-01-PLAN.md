---
phase: 61-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/test-steps/v0.8.0-综合验证测试步骤.md
autonomous: true
requirements:
  - FMT-01
  - FMT-02
  - FMT-03
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - RPT-01
  - RPT-02
  - RPT-03
  - FORM-01
  - FORM-02

must_haves:
  truths:
    - "All backend unit tests pass (uv run pytest backend/tests/ -v exits 0 or only pre-existing failures)"
    - "Frontend builds successfully (cd frontend && npm run build exits 0)"
    - "v0.8.0 comprehensive test steps document exists with verification scenarios for all 4 Success Criteria"
  artifacts:
    - path: "docs/test-steps/v0.8.0-综合验证测试步骤.md"
      provides: "Test steps covering SC-1 (execution monitor), SC-2 (report detail), SC-3 (task form), SC-4 (backward compat)"
      min_lines: 50
  key_links:
    - from: "docs/test-steps/v0.8.0-综合验证测试步骤.md"
      to: "ROADMAP Phase 61 Success Criteria"
      via: "4 verification scenarios mapped to 4 Success Criteria"
      pattern: "SC-[1-4]"
---

<objective>
Run automated pre-checks (backend tests + frontend build) and create comprehensive v0.8.0 test steps document for E2E verification.

Purpose: Ensure code-level regression is caught before manual E2E, and provide structured test steps for the manual verification in Plan 61-02. Per D-05, D-01, D-02, D-07.
Output: Test steps document + automated check results
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-e2e/61-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated pre-checks</name>
  <files>(no files modified, verification only)</files>
  <read_first>
    - backend/tests/ (directory listing to confirm test files exist)
  </read_first>
  <action>
Run two automated pre-check commands sequentially:

1. **Backend tests:** `uv run pytest backend/tests/ -v --tb=short 2>&1 | tail -30`
   - Record total passed/failed/error counts
   - Compare failure list against Phase 51 baseline (54 pre-existing failures + 22 pre-existing errors)
   - If new failures appear (not in baseline), list them as potential regressions

2. **Frontend build:** `cd frontend && npm run build 2>&1 | tail -20`
   - Must exit with code 0
   - Record build output summary (warnings are acceptable, errors are not)

3. **Summarize results:** Print a summary showing:
   - Backend: N passed, N failed (baseline: N), N errors (baseline: N), regression: YES/NO
   - Frontend: build SUCCESS/FAIL
   - Overall: READY/NOT READY for E2E
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/ -v --tb=short 2>&1 | tail -5 && cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Backend tests complete with no new regressions beyond baseline. Frontend build succeeds with exit code 0.</done>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits with code 0
    - Backend pytest output shows "passed" count in summary line
    - No new test failures compared to Phase 51 baseline
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create v0.8.0 comprehensive test steps document</name>
  <files>docs/test-steps/v0.8.0-综合验证测试步骤.md</files>
  <read_first>
    - docs/test-steps/采购-综合验证结果.md (reference format from Phase 56)
    - .planning/ROADMAP.md (Phase 61 Success Criteria lines 122-127)
    - .planning/phases/61-e2e/61-CONTEXT.md (D-01, D-02 decisions)
  </read_first>
  <action>
Create `docs/test-steps/v0.8.0-综合验证测试步骤.md` with the following structure. This document defines the manual E2E test scenarios for Plan 61-02 to execute.

**Document structure:**

```markdown
# v0.8.0 E2E 综合验证测试步骤

## 验证日期: (to be filled during execution)
## 验证环境: 本地开发机（localhost:8080）
## 测试范围: Phase 57-60 全部变更（AI 推理格式 + 执行步骤展示 + 报告步骤展示 + 任务表单优化）

---

## 综合测试用例设计 (per D-01)

### 用例名称: 采购入库全流程验证
### 场景描述: 创建一个采购入库相关测试任务，包含前置条件（查询供应商信息）和业务断言（验证入库数量），确保触发 AI 推理生成 Eval/Verdict/Memory/Goal

### 前置条件配置:
- 添加 1 个前置条件：查询供应商列表（使用数据获取方法，如 `context.get_data('SupplierList', 'query', {})`）
- 设置变量名如 `supplier_name` 用于后续引用

### 业务断言配置:
- 添加 1 个业务断言：验证库存数量（使用断言方法，headers='main'）
- 配置字段参数验证某个数量字段

### 任务描述模板:
"进入采购管理模块，点击新建采购入库单，选择供应商 {{supplier_name}}，填写入库数量为 1，提交并验证入库成功"

---

## SC-1: 执行监控步骤验证 (per D-06-1)

**验证目标:** 创建含前置条件+断言的测试任务，执行后监控中正确显示所有步骤（含推理格式化）

### 操作步骤:
1. 打开平台首页 http://localhost:8080
2. 点击"新建任务"按钮
3. 填写任务名称: "v0.8.0-采购入库全流程验证"
4. 填写任务描述（使用上述模板）
5. 配置前置条件（添加查询供应商信息代码）
6. 配置业务断言（添加库存验证断言）
7. 点击"创建"提交任务
8. 点击"执行"开始执行
9. **观察执行监控页面**

### 验证条件:
- [ ] StepTimeline 中显示前置条件步骤（per EXEC-01）：显示状态（成功/失败）、耗时、代码摘要
- [ ] StepTimeline 中显示断言步骤（per EXEC-02）：显示状态（通过/失败）、耗时、断言名称
- [ ] 前置条件步骤、断言步骤与普通 UI 步骤按执行顺序交错排列（per EXEC-03）
- [ ] ReasoningLog 中推理文本按行格式化展示（per FMT-01）：Eval/Verdict/Memory/Goal 各自独占一行
- [ ] 推理标签有视觉高亮（颜色标记）

---

## SC-2: 报告详情步骤验证 (per D-06-2)

**验证目标:** 执行完成后，报告详情页正确展示前置条件步骤、断言步骤和普通步骤，推理文本格式化显示

### 操作步骤:
1. 等待任务执行完成（状态变为"已完成"或"失败"）
2. 点击任务进入报告详情页
3. 观察步骤列表

### 验证条件:
- [ ] 报告详情页显示前置条件步骤（per RPT-01）：执行状态、耗时、变量输出信息
- [ ] 报告详情页显示断言步骤（per RPT-02）：执行状态、断言名称、失败信息（如有）
- [ ] 前置条件和断言步骤与普通步骤交错展示（per RPT-03）
- [ ] 推理文本格式化展示（per FMT-02/FMT-03）：标签高亮、分行显示

---

## SC-3: 任务表单验证 (per D-06-3)

**验证目标:** 创建新任务时表单中断言区域直接显示业务断言配置，无 tab 切换

### 操作步骤:
1. 点击"新建任务"打开任务表单
2. 观察断言配置区域

### 验证条件:
- [ ] 表单中不显示"接口断言"/"业务断言"的 tab 切换控件（per FORM-01）
- [ ] 断言配置区域直接展示业务断言（AssertionSelector），无需切换 tab
- [ ] 表单中不显示 api_assertions 的自由代码输入 textarea（per FORM-02）
- [ ] 断言选择按钮可直接点击打开断言选择器

---

## SC-4: 历史报告向后兼容验证 (per D-06-4)

**验证目标:** 已有任务的报告数据不受影响，历史报告正常展示

### 操作步骤:
1. 在任务列表中找到一个历史任务（Phase 60 之前创建的任务）
2. 点击进入该任务的报告详情页
3. 观察报告展示

### 验证条件:
- [ ] 历史报告页面正常加载，无报错
- [ ] 历史报告步骤列表正常展示（即使没有 timeline_items 数据，也能正常回退展示）
- [ ] 历史报告的推理文本正常展示（可能不格式化，但不应报错）

---

## 验证顺序 (per CONTEXT.md specifics)

1. 先运行自动化预检查 (Plan 61-01 Task 1)
2. 创建综合测试任务 (SC-3 表单验证同时完成)
3. 执行任务，观察监控 (SC-1)
4. 完成后查看报告 (SC-2)
5. 打开历史报告 (SC-4)
```
  </action>
  <verify>
    <automated>test -f /Users/huhu/project/weberpagent/docs/test-steps/v0.8.0-综合验证测试步骤.md && grep -c "SC-[1-4]" /Users/huhu/project/weberpagent/docs/test-steps/v0.8.0-综合验证测试步骤.md</automated>
  </verify>
  <done>Test steps document exists with all 4 Success Criteria scenarios (SC-1 through SC-4), covering execution monitor, report detail, task form, and backward compatibility verification.</done>
  <acceptance_criteria>
    - File exists: `docs/test-steps/v0.8.0-综合验证测试步骤.md`
    - Document contains "SC-1" section (execution monitor verification)
    - Document contains "SC-2" section (report detail verification)
    - Document contains "SC-3" section (task form verification)
    - Document contains "SC-4" section (backward compatibility verification)
    - Document contains comprehensive test case design per D-01 (precondition + assertion + AI reasoning trigger)
    - Document follows Phase 56 verification result format (per D-07)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Backend tests: `uv run pytest backend/tests/ -v` — no new regressions vs baseline
2. Frontend build: `cd frontend && npm run build` — exits 0
3. Test steps document: exists and covers all 4 Success Criteria from ROADMAP
</verification>

<success_criteria>
- Backend tests pass (no new failures beyond Phase 51 baseline of 54 failures + 22 errors)
- Frontend build succeeds (exit code 0)
- Test steps document created with 4 verification scenarios mapped to ROADMAP Success Criteria 1-4
- Automated pre-check summary printed showing READY status
</success_criteria>

<output>
After completion, create `.planning/phases/61-e2e/61-01-SUMMARY.md`
</output>
