---
phase: 24-frontend-assertion-ui
plan: 03
type: execute
wave: 3
depends_on: [24-01, 24-02]
files_modified:
  - frontend/src/components/TaskModal/TaskForm.tsx
autonomous: true
requirements: [UI-06]
user_setup: []

must_haves:
  truths:
    - "QA sees Tab switching between '接口断言' and '业务断言' in the assertion section"
    - "接口断言 tab shows existing Python code textarea"
    - "业务断言 tab shows structured assertion configuration with cards"
    - "Each assertion card shows method name, parameters summary, and edit/delete buttons"
    - "Clicking '添加断言' button opens AssertionSelector modal"
    - "Assertion configurations are stored in formData.assertions and submitted with task"
  artifacts:
    - path: "frontend/src/components/TaskModal/TaskForm.tsx"
      provides: "Task form with integrated assertion configuration"
      contains: "assertionTab state"
      contains: "AssertionSelector import"
  key_links:
    - from: "TaskForm.tsx"
      to: "AssertionSelector"
      via: "import and render"
      pattern: "import.*AssertionSelector"
    - from: "TaskForm.tsx"
      to: "formData.assertions"
      via: "state management"
      pattern: "assertions.*AssertionConfig"
---

<objective>
Integrate AssertionSelector into TaskForm with Tab switching UI.

Purpose: Enable QA to configure business assertions in the task form using a tabbed interface that separates Python code assertions from structured business assertions.
Output: Updated TaskForm.tsx with tab switching, assertion cards, and AssertionSelector integration.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/planning/phases/24-frontend-assertion-ui/24-01-SUMMARY.md
@/planning/phases/24-frontend-assertion-ui/24-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-frontend-assertion-ui/24-CONTEXT.md
@.planning/phases/24-frontend-assertion-ui/24-RESEARCH.md

<interfaces>
<!-- Key patterns from existing TaskForm.tsx -->

From TaskForm.tsx (current structure):
```typescript
interface FormData {
  name: string
  description: string
  target_url: string
  max_steps: number
  preconditions: string[]
  api_assertions: string[]
}

// Modal state pattern
const [selectorOpen, setSelectorOpen] = useState(false)
const [selectorIndex, setSelectorIndex] = useState<number | null>(null)

// Handler pattern
const handleSelectorConfirm = async (selectedCodes: string[]) => {
  // Process selection
  setSelectorOpen(false)
  setSelectorIndex(null)
}

// Render existing selector
<OperationCodeSelector
  open={selectorOpen}
  onConfirm={handleSelectorConfirm}
  onCancel={handleSelectorCancel}
/>
```

From CONTEXT.md (Tab switching pattern):
```typescript
const [assertionTab, setAssertionTab] = useState<'api' | 'business'>('api')

// Tab UI
<div className="flex gap-2 mb-3">
  <button
    type="button"
    onClick={() => setAssertionTab('api')}
    className={`px-3 py-1.5 rounded-lg text-sm font-medium ${
      assertionTab === 'api' ? 'bg-blue-100 text-blue-700' : 'bg-gray-100 text-gray-600'
    }`}
  >
    接口断言
  </button>
  <button
    type="button"
    onClick={() => setAssertionTab('business')}
    className={`px-3 py-1.5 rounded-lg text-sm font-medium ${
      assertionTab === 'business' ? 'bg-orange-100 text-orange-700' : 'bg-gray-100 text-gray-600'
    }`}
  >
    业务断言
  </button>
</div>
```

From frontend/src/types/index.ts:
```typescript
interface AssertionConfig {
  className: string
  methodName: string
  headers: string
  data: string
  params: Record<string, number | string>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update FormData interface and add assertion state</name>
  <files>frontend/src/components/TaskModal/TaskForm.tsx</files>
  <read_first>
    - frontend/src/components/TaskModal/TaskForm.tsx (current structure)
    - frontend/src/types/index.ts (AssertionConfig type)
  </read_first>
  <action>
    Update frontend/src/components/TaskModal/TaskForm.tsx:

    1. Add import for AssertionConfig type (add to existing imports from types):
    ```typescript
    import type { Task, CreateTaskDto, OperationsResponse, DataMethodConfig, AssertionConfig } from '../../types'
    ```

    2. Add import for AssertionSelector component:
    ```typescript
    import { AssertionSelector } from './AssertionSelector'
    ```

    3. Update FormData interface to include assertions:
    ```typescript
    interface FormData {
      name: string
      description: string
      target_url: string
      max_steps: number
      preconditions: string[]
      api_assertions: string[]
      assertions: AssertionConfig[]  // NEW
    }
    ```

    4. Add assertions to initial form data state (in useState):
    ```typescript
    const [formData, setFormData] = useState<FormData>({
      name: initialData?.name || '',
      description: initialData?.description || '',
      target_url: initialData?.target_url || '',
      max_steps: initialData?.max_steps || 20,
      preconditions: initialData?.preconditions || [''],
      api_assertions: initialData?.api_assertions || [''],
      assertions: initialData?.assertions || [],  // NEW
    })
    ```

    5. Add assertions to useEffect that syncs with initialData:
    ```typescript
    useEffect(() => {
      if (initialData) {
        setFormData({
          name: initialData.name,
          description: initialData.description,
          target_url: initialData.target_url,
          max_steps: initialData.max_steps,
          preconditions: initialData.preconditions || [''],
          api_assertions: initialData.api_assertions || [''],
          assertions: initialData.assertions || [],  // NEW
        })
      }
    }, [initialData])
    ```

    6. Add assertions to handleSubmit (in onSubmit call):
    ```typescript
    onSubmit({
      ...formData,
      preconditions: formData.preconditions.filter(p => p.trim()),
      api_assertions: formData.api_assertions.filter(a => a.trim()),
      assertions: formData.assertions,  // NEW - pass as-is, no filtering needed
    })
    ```

    7. Add state for assertion selector modal:
    ```typescript
    // Assertion selector state
    const [assertionSelectorOpen, setAssertionSelectorOpen] = useState(false)
    ```

    8. Add state for assertion tab:
    ```typescript
    const [assertionTab, setAssertionTab] = useState<'api' | 'business'>('api')
    ```

    9. Add handler functions for assertion management:
    ```typescript
    const handleAssertionSelectorConfirm = (configs: AssertionConfig[]) => {
      if (configs.length === 0) return
      setFormData(prev => ({
        ...prev,
        assertions: [...prev.assertions, ...configs]
      }))
      setAssertionSelectorOpen(false)
    }

    const handleAssertionSelectorCancel = () => {
      setAssertionSelectorOpen(false)
    }

    const handleRemoveAssertion = (index: number) => {
      setFormData(prev => ({
        ...prev,
        assertions: prev.assertions.filter((_, i) => i !== index)
      }))
    }
    ```

    Do NOT modify the api_assertions section yet - that comes in the next task.
  </action>
  <verify>
    <automated>grep -q "assertions: AssertionConfig" frontend/src/components/TaskModal/TaskForm.tsx && grep -q "AssertionSelector" frontend/src/components/TaskModal/TaskForm.tsx && grep -q "assertionSelectorOpen" frontend/src/components/TaskModal/TaskForm.tsx && echo "State added"</automated>
  </verify>
  <acceptance_criteria>
    - TaskForm.tsx contains "import.*AssertionConfig" from types
    - TaskForm.tsx contains "import.*AssertionSelector" from ./AssertionSelector
    - FormData interface contains "assertions: AssertionConfig[]"
    - useState initial value contains "assertions: initialData?.assertions || []"
    - useEffect contains "assertions: initialData.assertions || []"
    - handleSubmit passes assertions to onSubmit
    - File contains "assertionSelectorOpen" state
    - File contains "assertionTab" state
    - File contains "handleAssertionSelectorConfirm" function
    - File contains "handleRemoveAssertion" function
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    FormData interface updated with assertions field.
    AssertionSelector imported.
    State management for assertion selector and tab switching added.
    Handler functions for add/remove assertions added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Tab switching UI and assertion cards</name>
  <files>frontend/src/components/TaskModal/TaskForm.tsx</files>
  <read_first>
    - frontend/src/components/TaskModal/TaskForm.tsx (current api_assertions section)
    - .planning/phases/24-frontend-assertion-ui/24-CONTEXT.md (Tab UI design)
  </read_first>
  <action>
    Update the "接口断言" section in TaskForm.tsx to use Tab switching.

    Find the existing section starting with:
    ```tsx
    <div>
      <label className="block text-sm font-medium text-gray-700 mb-1">
        接口断言 <span className="text-gray-400 text-xs">(可选)</span>
      </label>
    ```

    Replace the entire section (from that div to the closing div before the submit buttons) with:

    ```tsx
    <div>
      <label className="block text-sm font-medium text-gray-700 mb-1">
        断言 <span className="text-gray-400 text-xs">(可选)</span>
      </label>

      {/* Tab switcher */}
      <div className="flex gap-2 mb-3">
        <button
          type="button"
          onClick={() => setAssertionTab('api')}
          className={`px-3 py-1.5 rounded-lg text-sm font-medium transition-colors ${
            assertionTab === 'api'
              ? 'bg-blue-100 text-blue-700'
              : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
          }`}
        >
          接口断言
        </button>
        <button
          type="button"
          onClick={() => setAssertionTab('business')}
          className={`px-3 py-1.5 rounded-lg text-sm font-medium transition-colors ${
            assertionTab === 'business'
              ? 'bg-orange-100 text-orange-700'
              : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
          }`}
        >
          业务断言
        </button>
      </div>

      {/* Tab content */}
      {assertionTab === 'api' ? (
        /* Existing api_assertions section */
        <div>
          <p className="text-xs text-gray-500 mb-2">
            输入 Python 代码进行 API 断言，支持时间断言、数据匹配等
          </p>
          <div className="space-y-2">
            {formData.api_assertions.map((assertion, index) => (
              <div key={index} className="flex gap-2">
                <textarea
                  value={assertion}
                  onChange={e => handleApiAssertionChange(index, e.target.value)}
                  placeholder="例如：result = api.get_order({{order_id}}); assert result['status'] == 'success'"
                  rows={4}
                  className="flex-1 px-3 py-2 border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 resize-none font-mono text-sm"
                />
                {formData.api_assertions.length > 1 && (
                  <button
                    type="button"
                    onClick={() => handleRemoveApiAssertion(index)}
                    className="px-3 py-2 text-red-500 hover:bg-red-50 rounded-lg"
                  >
                    删除
                  </button>
                )}
              </div>
            ))}
            <button
              type="button"
              onClick={handleAddApiAssertion}
              className="text-sm text-blue-500 hover:text-blue-600"
            >
              + 添加接口断言
            </button>
          </div>
        </div>
      ) : (
        /* Business assertions section */
        <div>
          <p className="text-xs text-gray-500 mb-2">
            选择业务断言方法，配置参数后系统自动执行验证
          </p>

          {/* Add assertion button */}
          <button
            type="button"
            onClick={() => setAssertionSelectorOpen(true)}
            className="inline-flex items-center gap-1 px-3 py-1.5 text-sm rounded-lg border border-orange-200 text-orange-600 hover:bg-orange-50 mb-3"
          >
            <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 4v16m8-8H4" />
            </svg>
            添加断言
          </button>

          {/* Assertion cards */}
          {formData.assertions.length > 0 && (
            <div className="space-y-2">
              {formData.assertions.map((config, index) => (
                <div
                  key={index}
                  className="border border-orange-200 bg-orange-50 rounded-lg p-3"
                >
                  <div className="flex items-center justify-between">
                    <div className="flex-1 min-w-0">
                      <span className="font-mono text-sm text-blue-600">
                        {config.methodName}
                      </span>
                      <span className="text-gray-400 mx-2">|</span>
                      <span className="text-sm text-gray-600 truncate">
                        headers={config.headers}, data={config.data}
                        {Object.keys(config.params).length > 0 && (
                          <span>
                            , {Object.entries(config.params)
                              .map(([k, v]) => `${k}=${v}`)
                              .join(', ')}
                          </span>
                        )}
                      </span>
                    </div>
                    <div className="flex gap-2 flex-shrink-0">
                      <button
                        type="button"
                        onClick={() => handleRemoveAssertion(index)}
                        className="text-sm text-red-500 hover:text-red-600"
                      >
                        删除
                      </button>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          )}

          {/* Empty state */}
          {formData.assertions.length === 0 && (
            <div className="text-center py-4 text-gray-500 text-sm">
              暂无业务断言配置，点击"添加断言"开始配置
            </div>
          )}
        </div>
      )}
    </div>
    ```
  </action>
  <verify>
    <automated>grep -q "assertionTab === 'api'" frontend/src/components/TaskModal/TaskForm.tsx && grep -q "assertionTab === 'business'" frontend/src/components/TaskModal/TaskForm.tsx && grep -q "setAssertionSelectorOpen(true)" frontend/src/components/TaskModal/TaskForm.tsx && grep -q "formData.assertions.map" frontend/src/components/TaskModal/TaskForm.tsx && echo "Tab UI added"</automated>
  </verify>
  <acceptance_criteria>
    - TaskForm.tsx contains "assertionTab === 'api'" conditional
    - TaskForm.tsx contains "assertionTab === 'business'" conditional
    - TaskForm.tsx contains tab buttons with "接口断言" and "业务断言"
    - TaskForm.tsx contains "setAssertionSelectorOpen(true)"
    - TaskForm.tsx contains "formData.assertions.map"
    - TaskForm.tsx contains "handleRemoveAssertion(index)"
    - TaskForm.tsx contains "border-orange-200" for assertion cards
    - TaskForm.tsx contains "添加断言" button
    - Existing api_assertions textarea logic preserved in 'api' tab
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    Tab switching UI implemented with "接口断言" and "业务断言" tabs.
    "接口断言" tab preserves existing Python code textarea functionality.
    "业务断言" tab shows add button and assertion cards.
    Assertion cards display method name and parameter summary.
    Delete button removes assertions from formData.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add AssertionSelector modal to render</name>
  <files>frontend/src/components/TaskModal/TaskForm.tsx</files>
  <read_first>
    - frontend/src/components/TaskModal/TaskForm.tsx (existing modal renders at bottom)
  </read_first>
  <action>
    Add the AssertionSelector modal render at the end of the TaskForm component, after the existing DataMethodSelector modal.

    Find the existing modals at the end of the form (before the closing `</form>`):
    ```tsx
    {/* Operation Code Selector Modal */}
    <OperationCodeSelector
      open={selectorOpen}
      onConfirm={handleSelectorConfirm}
      onCancel={handleSelectorCancel}
    />

    {/* Data Method Selector Modal */}
    <DataMethodSelector
      open={dataSelectorOpen}
      onConfirm={handleDataSelectorConfirm}
      onCancel={handleDataSelectorCancel}
    />
    ```

    Add after the DataMethodSelector:
    ```tsx
    {/* Assertion Selector Modal */}
    <AssertionSelector
      open={assertionSelectorOpen}
      onConfirm={handleAssertionSelectorConfirm}
      onCancel={handleAssertionSelectorCancel}
    />
    ```
  </action>
  <verify>
    <automated>grep -q "AssertionSelector" frontend/src/components/TaskModal/TaskForm.tsx && grep -q "assertionSelectorOpen" frontend/src/components/TaskModal/TaskForm.tsx && grep -q "handleAssertionSelectorConfirm" frontend/src/components/TaskModal/TaskForm.tsx && echo "Modal added"</automated>
  </verify>
  <acceptance_criteria>
    - TaskForm.tsx contains AssertionSelector component render
    - AssertionSelector receives open={assertionSelectorOpen}
    - AssertionSelector receives onConfirm={handleAssertionSelectorConfirm}
    - AssertionSelector receives onCancel={handleAssertionSelectorCancel}
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    AssertionSelector modal rendered in TaskForm.
    Modal opens when "添加断言" button is clicked.
    Modal closes on confirm or cancel.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run TypeScript check: `cd frontend && npx tsc --noEmit`
2. Verify TaskForm compiles without errors
3. Build frontend: `cd frontend && npm run build`
4. Verify build succeeds
</verification>

<success_criteria>
- Tab switching UI works between 接口断言 and 业务断言
- 接口断言 tab preserves existing functionality
- 业务断言 tab shows add button and assertion cards
- AssertionSelector modal opens and closes correctly
- Selected assertions appear as cards with method name and parameters
- Delete button removes assertions
- TypeScript compiles without errors
- Frontend builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/24-frontend-assertion-ui/24-03-SUMMARY.md`
</output>
