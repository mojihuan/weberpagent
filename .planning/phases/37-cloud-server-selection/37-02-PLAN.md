---
phase: 37-cloud-server-selection
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified: []
autonomous: false
requirements: [CLOUD-02]
user_setup:
  - service: 阿里云
    why: "购买云服务器需要用户支付费用"
    env_vars: []
    dashboard_config:
      - task: "购买云服务器"
        location: "https://www.aliyun.com/activity/new"

must_haves:
  truths:
    - "用户已购买云服务器"
    - "用户可通过 SSH 登录服务器"
    - "服务器系统为 Ubuntu 22.04"
  artifacts:
    - path: ".planning/phases/37-cloud-server-selection/37-服务器信息.md"
      provides: "服务器 IP 和登录信息记录"
      contains: "公网IP"
  key_links:
    - from: "用户电脑"
      to: "云服务器"
      via: "SSH 登录"
      pattern: "ssh root@<公网IP>"
---

<objective>
引导用户购买云服务器并验证 SSH 登录，完成服务器初始化。

Purpose: 确保用户成功购买并能够访问云服务器，为 Phase 38 部署做准备
Output: 服务器信息记录文件
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-cloud-server-selection/37-CONTEXT.md
@.planning/phases/37-cloud-server-selection/37-调研报告.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: 用户购买云服务器</name>
  <files>.planning/phases/37-cloud-server-selection/37-服务器信息.md</files>
  <read_first>
    - .planning/phases/37-cloud-server-selection/37-调研报告.md (购买步骤指引)
  </read_first>
  <action>
请按照调研报告中的购买步骤，在阿里云购买云服务器。

**购买要求:**
- 厂商: 阿里云
- 产品: 轻量应用服务器
- 配置: 2核 CPU / 4GB 内存 / 4Mbps 带宽
- 存储: 60GB SSD
- 系统: Ubuntu 22.04 LTS
- 地域: 华东1(杭州) 或 华南1(深圳)
- 付费: 按月付费

**购买链接:**
https://www.aliyun.com/activity/new

**购买完成后，请记录以下信息:**
1. 服务器公网 IP 地址
2. root 用户密码 (或密钥文件路径)

**然后将以下信息回复给我:**
- 公网 IP: (例如: 123.45.67.89)
- 登录方式: (密码 或 密钥)
</action>
  <verify>
    <automated>MISSING — 需要用户完成购买操作</automated>
  </verify>
  <done>
    - 用户已完成云服务器购买
    - 用户提供了服务器公网 IP 地址
    - 用户确认了登录方式 (密码或密钥)
  </done>
  <resume-signal>提供服务器公网IP和登录方式，例如: "IP: 123.45.67.89, 登录方式: 密码"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 验证 SSH 登录</name>
  <files>.planning/phases/37-cloud-server-selection/37-服务器信息.md</files>
  <read_first>
    - .planning/phases/37-cloud-server-selection/37-服务器信息.md (用户购买后记录的信息)
  </read_first>
  <action>
请验证 SSH 登录是否正常，并确认操作系统版本。

**验证步骤:**

1. 在终端执行 SSH 登录:
   ```bash
   ssh root@<你的公网IP>
   ```

2. 输入密码 (购买时设置的 root 密码)

3. 登录成功后，执行以下命令确认系统信息:
   ```bash
   # 确认操作系统版本
   cat /etc/os-release | grep VERSION_ID
   # 预期输出: VERSION_ID="22.04"

   # 确认内核版本
   uname -r
   # 预期输出类似: 5.15.0-xx-generic

   # 确认内存
   free -h
   # 预期输出: Mem: 3.8Gi (接近 4GB)

   # 确认 CPU
   nproc
   # 预期输出: 2
   ```

4. 执行 `exit` 退出 SSH

**验证结果请回复:**
- SSH 登录是否成功: (是/否)
- 操作系统版本是否为 22.04: (是/否)
- CPU 核心数: (数字)
- 内存大小: (例如: 3.8Gi)
</action>
  <verify>
    <automated>MISSING — 需要用户手动 SSH 登录验证</automated>
  </verify>
  <done>
    - 用户成功通过 SSH 登录服务器
    - 操作系统确认为 Ubuntu 22.04
    - CPU 为 2 核
    - 内存约 4GB
  </done>
  <resume-signal>回复验证结果，例如: "SSH成功, Ubuntu 22.04, CPU 2核, 内存 3.8Gi"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 创建服务器信息记录文件</name>
  <files>.planning/phases/37-cloud-server-selection/37-服务器信息.md</files>
  <read_first>
    - .planning/phases/37-cloud-server-selection/37-调研报告.md (参考配置信息)
  </read_first>
  <behavior>
    - 文件必须包含服务器公网 IP 占位符
    - 文件必须包含 SSH 登录命令模板
    - 文件必须包含安全组端口配置清单
  </behavior>
  <action>
创建服务器信息记录文件 `.planning/phases/37-cloud-server-selection/37-服务器信息.md`，内容如下:

```markdown
# 云服务器信息记录

**创建时间:** 2026-03-23
**Phase:** 37 - 云服务器选型

## 服务器基本信息

| 项目 | 值 |
|------|-----|
| 云厂商 | 阿里云 |
| 产品 | 轻量应用服务器 |
| 公网 IP | (用户购买后填写) |
| 地域 | 华东1(杭州) 或 华南1(深圳) |
| 规格 | 2核 CPU / 4GB 内存 / 4Mbps 带宽 |
| 存储 | 60GB SSD |
| 操作系统 | Ubuntu 22.04 LTS |
| 付费方式 | 按月付费 |

## 登录信息

**SSH 登录命令:**
```bash
ssh root@<公网IP>
```

**登录方式:** (密码 或 密钥)

## 安全组端口配置

| 端口 | 协议 | 用途 | 状态 |
|------|------|------|------|
| 22 | TCP | SSH 管理登录 | 待配置 |
| 80 | TCP | HTTP (Nginx) | 待配置 |
| 443 | TCP | HTTPS (Nginx SSL) | 待配置 |
| 8080 | TCP | FastAPI 后端 (可选) | 待配置 |

## 配置步骤 (阿里云控制台)

1. 登录阿里云控制台: https://ecs.console.aliyun.com/
2. 进入「轻量应用服务器」
3. 点击服务器实例
4. 选择「安全」→「防火墙」
5. 添加规则开放上述端口

## Phase 38 部署前置条件

在开始 Phase 38 部署前，确保:
- [ ] 服务器可通过 SSH 登录
- [ ] 操作系统为 Ubuntu 22.04
- [ ] 安全组已开放必要端口
- [ ] 已记录公网 IP 地址

## 备注

(其他需要记录的信息)
```
</action>
  <verify>
    <automated>test -f .planning/phases/37-cloud-server-selection/37-服务器信息.md && grep -q "公网 IP" .planning/phases/37-cloud-server-selection/37-服务器信息.md && echo "PASS"</automated>
  </verify>
  <done>
    - 服务器信息文件已创建
    - 文件包含公网 IP 占位符
    - 文件包含 SSH 登录命令
    - 文件包含安全组端口配置清单
  </done>
</task>

</tasks>

<verification>
1. 服务器信息记录文件存在
2. 文件包含公网 IP、SSH 登录、安全组配置信息
3. 用户已完成 SSH 登录验证
</verification>

<success_criteria>
- 用户已购买云服务器
- 用户可通过 SSH 登录服务器
- 服务器系统确认为 Ubuntu 22.04
- 服务器信息已记录到文件
</success_criteria>

<output>
After completion, create `.planning/phases/37-cloud-server-selection/37-02-SUMMARY.md`
</output>
