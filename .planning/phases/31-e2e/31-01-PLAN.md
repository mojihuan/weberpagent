---
phase: 31-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [e2e/tests/assertion-flow.spec.ts]
autonomous: true
requirements: [E2E-01, E2E-02]
user_setup: []

must_haves:
  truths:
    - "User can configure field_params in assertion selector"
    - "User can execute assertion with field_params and see results in report"
    - "Time field configured with 'now' shows correct conversion"
    - "Three-layer params assertion shows success status in green card"
  artifacts:
    - path: "e2e/tests/assertion-flow.spec.ts"
      provides: "E2E test coverage for v0.4.1 features"
      min_lines: 500
      contains: "test('field_params configuration"
      contains: "test('now time conversion"
      contains: "test('three-layer params success"
  key_links:
    - from: "e2e/tests/assertion-flow.spec.ts"
      to: "FieldParamsEditor component"
      via: "Playwright UI interaction"
      pattern: "Search fields|Expected value|now"
    - from: "e2e/tests/assertion-flow.spec.ts"
      to: "Report page assertion results"
      via: "Result card verification"
      pattern: "bg-green-50.border-green-200|接口断言结果"
---

<objective>
Add 3 new E2E test cases to assertion-flow.spec.ts covering v0.4.1 features: field_params configuration, "now" time conversion, and three-layer params success scenario.

Purpose: Verify the complete assertion flow works with the new three-layer parameter structure (data/api_params/field_params) and "now" time conversion.
Output: Extended E2E test file with 8 total tests (5 existing + 3 new).
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-e2e/31-CONTEXT.md
@.planning/phases/31-e2e/31-RESEARCH.md
@.planning/phases/31-e2e/31-VALIDATION.md

<interfaces>

From e2e/tests/assertion-flow.spec.ts (existing patterns):
```typescript
// Environment detection pattern
const erpBaseUrl = process.env.ERP_BASE_URL
test.skip(!erpBaseUrl, 'ERP_BASE_URL environment variable not set')

// Timeout for AI-driven execution
test.setTimeout(300000) // 5 minutes

// Navigate and create task
await page.goto('/tasks')
await page.click('button:has-text("新建任务"), button:has-text("New Task")')
await page.fill('[name="name"]', 'Test Task Name')
await page.fill('[name="target_url"]', erpBaseUrl!)

// Switch to assertions tab
await page.click('button:has-text("业务断言")')
await page.click('button:has-text("添加断言")')
await expect(page.locator('text=Select Assertion Methods')).toBeVisible({ timeout: 10000 })
await page.waitForTimeout(2000) // Wait for methods to load

// Select method and confirm
await page.locator('input[type="checkbox"]').first().click()
await page.click('button:has-text("Confirm")')

// Execute and wait for completion
await page.click('tr:has-text("Test Task") button:has-text("执行")')
await page.waitForSelector('text=已完成, text=失败', { timeout: 180000 })

// Navigate to report and verify
await page.click('button:has-text("查看报告")')
await expect(page.locator('.bg-green-50.border-green-200, .bg-red-50.border-red-200')).toBeVisible()
```

From frontend/src/components/TaskModal/FieldParamsEditor.tsx:
```typescript
// Component structure for E2E interaction
// - Search input: input[placeholder*="Search"]
// - Group accordion: button with group.name text
// - Field checkbox: input[type="checkbox"] inside label
// - Value input: input[placeholder="Expected value"]
// - "now" button: button:has-text("now") (only for time fields)
```

From frontend/src/components/Report/ApiAssertionResults.tsx:
```typescript
// Result card styling
// - Pass: bg-green-50 border-green-200, CheckCircle icon text-green-500
// - Fail: bg-red-50 border-red-200, XCircle icon text-red-500
// - Section header: text=接口断言结果
// - Pass rate: text=通过率
```

From frontend/src/components/TaskModal/AssertionSelector.tsx:
```typescript
// Three-layer params structure
// - Headers dropdown: select with headers_options
// - Data dropdown: select with data_options
// - Filter params (i/j/k): input[type="number"] or select
// - Field params section: text=Assertion Fields (field_params)
// - FieldParamsEditor integration inside bordered container
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add field_params configuration test</name>
  <files>e2e/tests/assertion-flow.spec.ts</files>
  <read_first>
    - e2e/tests/assertion-flow.spec.ts (existing test patterns)
    - frontend/src/components/TaskModal/FieldParamsEditor.tsx (UI structure)
    - frontend/src/components/TaskModal/AssertionSelector.tsx (three-layer params integration)
  </read_first>
  <behavior>
    - Test configures field_params through FieldParamsEditor UI
    - Test verifies field_params is saved and transmitted correctly
    - Test executes task and checks report shows assertion results
  </behavior>
  <action>
    Add a new test case `test('field_params configuration - verify field parameter transmission', ...)` to assertion-flow.spec.ts after the existing 5 tests.

    Follow the established pattern from existing tests:
    1. Skip if !erpBaseUrl
    2. Set test.setTimeout(300000)
    3. Navigate to /tasks, click "New Task" button
    4. Fill task name: 'E2E Field Params Test', target_url: erpBaseUrl
    5. Switch to "Business Assertions" tab, click "Add Assertion"
    6. Wait 2000ms for methods to load, select first method checkbox
    7. Wait for "Selected" text to appear

    8. NEW: Configure field_params:
       - Wait for "Assertion Fields (field_params)" section to be visible
       - Find search input with placeholder containing "Search" inside the field params area
       - Search for a field by typing in search input (e.g., type "status" or any field name)
       - Wait 1000ms for filtering
       - Click first checkbox in field list to select a field
       - Find input with placeholder="Expected value" that appears after checkbox selection
       - Fill with test value 'test_expected_value'

    9. Click "Confirm" button
    10. Verify assertion card appears (.border-orange-200)
    11. Create task, verify it appears in list
    12. Execute task, wait for completion (180000ms timeout)
    13. Navigate to report, verify URL contains /reports/
    14. Verify assertion results section appears (text=接口断言结果 or text=断言结果)
    15. Verify result card is visible (either green or red card)

    Key selectors for FieldParamsEditor:
    - Search input: `input[placeholder*="Search"]` inside field params section
    - Field checkbox: `.border-gray-100 input[type="checkbox"]` or nested checkbox
    - Value input: `input[placeholder="Expected value"]`
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent/e2e && npx playwright test assertion-flow.spec.ts -g "field_params configuration"</automated>
  </verify>
  <acceptance_criteria>
    - assertion-flow.spec.ts contains `test('field_params configuration`
    - Test includes `test.skip(!erpBaseUrl, 'ERP_BASE_URL environment variable not set')`
    - Test includes `test.setTimeout(300000)`
    - Test includes FieldParamsEditor interaction (search input, checkbox, value input)
    - Test verifies report shows assertion results section
  </acceptance_criteria>
  <done>
    New test case added to assertion-flow.spec.ts that:
    - Configures field_params through FieldParamsEditor UI
    - Executes task with field_params configuration
    - Verifies report displays assertion results
    - Follows established test pattern (skip without ERP, 5min timeout)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "now" time conversion test</name>
  <files>e2e/tests/assertion-flow.spec.ts</files>
  <read_first>
    - e2e/tests/assertion-flow.spec.ts (existing test patterns and Task 1 test)
    - frontend/src/components/TaskModal/FieldParamsEditor.tsx (now button implementation)
    - backend/core/external_precondition_bridge.py (_convert_now_values function)
  </read_first>
  <behavior>
    - Test searches for a time field (is_time_field=true)
    - Test clicks "now" button to fill value with "now" string
    - Test verifies "now" appears in the input field
    - Test executes and checks report shows assertion results
  </behavior>
  <action>
    Add a new test case `test('now time conversion - verify "now" converts to current datetime', ...)` after Task 1's test.

    Follow the established pattern:
    1. Skip if !erpBaseUrl
    2. Set test.setTimeout(300000)
    3. Navigate, create task named 'E2E Now Time Test'
    4. Switch to assertions tab, add assertion, wait for methods
    5. Select first method

    6. NEW: Configure "now" time conversion:
       - Wait for field params section
       - Search for "Time" in search input (time fields like createTime, updateTime have is_time_field=true)
       - Wait 1000ms for filtering
       - Click checkbox to select a time field
       - Look for "now" button: `button:has-text("now")`
       - If "now" button exists (count > 0):
         - Click the "now" button
         - Verify the value input contains "now": `await expect(valueInput).toHaveValue('now')`
       - If no "now" button found, fill value manually with 'now' string as fallback

    7. Confirm, verify card, create task
    8. Execute, wait for completion
    9. Navigate to report, verify assertion results section and result card visible

    The "now" button only appears for fields where is_time_field=true. If the search doesn't find time fields, the test should handle gracefully by using a fallback approach.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent/e2e && npx playwright test assertion-flow.spec.ts -g "now time conversion"</automated>
  </verify>
  <acceptance_criteria>
    - assertion-flow.spec.ts contains `test('now time conversion`
    - Test searches for time fields (search query contains "Time" or "time")
    - Test attempts to click "now" button if available
    - Test verifies value input contains "now" after clicking
    - Test verifies report shows assertion results
  </acceptance_criteria>
  <done>
    New test case added that:
    - Searches for time fields in FieldParamsEditor
    - Clicks "now" button for time field (if available)
    - Verifies "now" string appears in input field
    - Executes task and verifies report displays results
  </done>
</task>

<task type="auto">
  <name>Task 3: Add three-layer params success test</name>
  <files>e2e/tests/assertion-flow.spec.ts</files>
  <read_first>
    - e2e/tests/assertion-flow.spec.ts (existing tests including Tasks 1 and 2)
    - frontend/src/components/TaskModal/AssertionSelector.tsx (three-layer params UI)
    - frontend/src/components/Report/ApiAssertionResults.tsx (success card styling)
  </read_first>
  <behavior>
    - Test configures all three parameter layers (data, api_params, field_params)
    - Test executes assertion and waits for completion
    - Test verifies green success card (bg-green-50 border-green-200) OR red fail card
    - Test verifies field-level details are displayed
  </behavior>
  <action>
    Add a new test case `test('three-layer params success - all fields pass with green display', ...)` after Task 2's test.

    Follow the established pattern:
    1. Skip if !erpBaseUrl
    2. Set test.setTimeout(300000)
    3. Navigate, create task named 'E2E Three Layer Success'
    4. Switch to assertions tab, add assertion, wait for methods
    5. Select first method

    6. Configure all three layers:
       a) Data layer (should already have default 'main'):
          - Verify data dropdown exists, can optionally change value
       b) api_params layer:
          - Look for i parameter input: `label:has-text("i")` parent's input
          - If i input exists, fill with '1'
          - Look for j parameter input, can leave empty
       c) field_params layer:
          - Wait for field params section
          - Search for fields
          - Select 1-2 fields by clicking checkboxes
          - Fill expected values for selected fields

    7. Confirm, verify card shows method name
    8. Create task, execute, wait for completion
    9. Navigate to report
    10. Verify assertion results:
        - Check for section header (text=接口断言结果)
        - Check for result cards (either green or red)
        - Count success cards: `.bg-green-50.border-green-200`
        - Count fail cards: `.bg-red-50.border-red-200`
        - Verify total cards > 0 (assertion executed)
        - Log the pass/fail counts for debugging

    Note: Since we're using real ERP data, we can't guarantee assertion passes. The test verifies the three-layer params are transmitted and results are displayed correctly, regardless of pass/fail status.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent/e2e && npx playwright test assertion-flow.spec.ts -g "three-layer params success"</automated>
  </verify>
  <acceptance_criteria>
    - assertion-flow.spec.ts contains `test('three-layer params success`
    - Test configures api_params (i parameter)
    - Test configures field_params (at least one field)
    - Test verifies report shows assertion results section
    - Test verifies result cards are visible (green or red)
    - Test logs pass/fail counts
  </acceptance_criteria>
  <done>
    New test case added that:
    - Configures all three parameter layers (data, api_params, field_params)
    - Executes assertion with complete configuration
    - Verifies report displays assertion results with proper styling
    - Verifies either green (pass) or red (fail) card is visible
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run full assertion-flow.spec.ts suite: `cd /Users/huhu/project/weberpagent/e2e && npx playwright test assertion-flow.spec.ts`
2. Verify all 8 tests (5 existing + 3 new) are detected
3. Verify new tests follow established patterns (skip without ERP, 5min timeout)
4. Verify new tests interact with FieldParamsEditor component correctly
5. Verify new tests check report page assertion results
</verification>

<success_criteria>
1. assertion-flow.spec.ts contains 3 new test cases (8 total)
2. All new tests include proper skip conditions for missing ERP env vars
3. All new tests have 5-minute timeout for AI execution
4. New tests cover: field_params configuration, "now" time conversion, three-layer params
5. All new tests verify report page assertion results display
6. Running `cd e2e && npx playwright test assertion-flow.spec.ts --list` shows 8 tests
</success_criteria>

<output>
After completion, create `.planning/phases/31-e2e/31-01-SUMMARY.md`
</output>
