---
phase: 20-e2e-testing-manual-verification
plan: 03
type: execute
wave: 2
depends_on: [20-01, 20-02]
files_modified: [e2e/tests/variable-substitution.spec.ts]
autonomous: true
requirements: [E2E-03]
user_setup: []

must_haves:
  truths:
    - "Variables from data methods are stored in context"
    - "Task description with {{variable}} is correctly replaced with actual values"
    - "API assertions with {{variable}} are correctly replaced with actual values"
  artifacts:
    - path: "e2e/tests/variable-substitution.spec.ts"
      provides: "E2E tests for variable substitution"
      min_lines: 150
  key_links:
    - from: "runs.py"
      to: "Precondition_service.substitute_variables"
      via: "line 129"
      pattern: "substitute_variables"
    - from: "api_assertion_service.py"
      to: "context"
      via: "api_assertion_service.execute_single"
      pattern: "api_assertion_service.execute_single"
    - from: "Report page"
      to: "variables section"
      via: "DOM rendering"
      pattern: "variables-section|text=变量"
---

<objective>
Test variable substitution in precondition execution and report display.

Purpose: Verify {{variable}} patterns are correctly replaced with actual data from data methods.
Output: E2E test file verifying variable substitution in task descriptions and report pages.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/state.md
@.planning/phases/20-e2e-testing-manual-verification/20-CONTEXT.md

@backend/core/precondition_service.py
@backend/api/routes/runs.py
@frontend/src/pages/ReportDetail.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly - no codebase exploration needed. -->

From backend/core/precondition_service.py:
```python
class ContextWrapper:
    def get_data(self, class_name: str, method_name: str, **params) -> Any:
        """Execute a data method and return the result."""

    @staticmethod
    def substitute_variables(text: str, context: dict[str, Any]) -> str:
        """Use Jinja2 for variable replacement."""

    # Dict-like interface methods
    def __getitem__(self, key: str) -> Any:
    def __setitem__(self, key: str, value: Any) -> None:
    def get(self, key: str, default: Any = None) -> return self._data.get(key, default)

    def keys(self):
        return self._data.keys()

    def to_dict(self) -> dict[str, Any]:
        """Return a plain dict copy for variable substitution."""
```

    def get_context(self) -> dict[str, Any]:
        """Get current execution context (variable storage)"""

        return self.context
```

From backend/api/routes/runs.py (lines 124-131):
```python
# 获取 context 用于变量替换
        context = precondition_service.get_context()
        logger.info(f"[{run_id}] 御ant后的变量替换: {task_description[:100]}...")
        # 替换 task_description 中的变量
        task_description = PreconditionService.substitute_variables(task_description, context)
        logger.info(f"[{run_id}] 变量替换后的任务描述: {task_description[:100]}...")
```

From frontend/src/pages/ReportDetail.tsx:
```tsx
import { useParams, Navigate } from 'react-router-dom'
import { Layers, CheckCircle, XCircle, Clock } from 'lucide-react'
import { ReportHeader, SummaryCard, StepItem, AssertionResults, ApiAssertionResults } from '../components/Report'
import { getReport, type ReportDetailResponse } from '../api/reports'

```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test for variable substitution</name>
  <files>e2e/tests/variable-substitution.spec.ts</files>
  <action>
    Create E2E test file ate2e/tests/variable-substitution.spec.ts) with the following tests:

    1. **Variable Storage in Precondition**
       - Create task with data method that sets variable `imei`
       - Create task description with template text: "Search for product with {{imei}}"
       - Verify precondition code sets variable correctly

    2. **Task Description Variable Replacement**
       - Execute task
       - Wait for execution to complete
       - Navigate to report page
       - Verify task description shows actual IMie value (not "{{imei}}")

    3. **API Assertion Variable Replacement**
       - Create API assertion code that variable `{{imei}}`
       - Execute task
       - Wait for execution to complete
       - Navigate to report page
       - Verify API assertion code shows actual imei value (not "{{imei}}")

    Use Playwright patterns from existing e2e/tests/smoke.spec.ts
    - test.setTimeout(180000) for long-running operations
    - Use page.waitForSelector() for waiting
    - Use expect().toContain() for text assertions
  </action>
  <verify>
    <automated>cd e2e && npx playwright test e2e/tests/variable-substitution.spec.ts</automated>
  </verify>
  <done>
    E2E test file exists at e2e/tests/variable-substitution.spec.ts
    Tests cover:
    - Precondition variable storage
    - Task description variable replacement
    - API assertion variable replacement
    - Report page displays actual values
  </done>
</task>

</tasks>

<verification>
All tests in variable-substitution.spec.ts pass
</verification>

<success_criteria>
- E2E test file created at e2e/tests/variable-substitution.spec.ts
- Test covers variable storage in precondition, task description substitution, and API assertion substitution
- Report page shows actual variable values
</success_criteria>

<output>
After completion, create .planning/phases/20-e2e-testing-manual-verification/20-03-SUMmary.md
</output>
