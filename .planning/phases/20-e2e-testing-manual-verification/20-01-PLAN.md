---
phase: 20-e2e-testing-manual-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [e2e/tests/data-method-selector.spec.ts]
autonomous: true
requirements: [E2E-01]
user_setup: []

must_haves:
  truths:
    - "User can open DataMethodSelector modal via button"
    - "User can search and select data methods"
    - "User can configure parameters for selected methods"
    - "User can navigate through all 4 steps of the wizard"
  artifacts:
    - path: "e2e/tests/data-method-selector.spec.ts"
      provides: "E2E tests for DataMethodSelector"
      min_lines: 150
  key_links:
    - from: "TaskForm.tsx"
      to: "DataMethodSelector"
      via: "button click"
      pattern: "获取数据.*button"
    - from: "DataMethodSelector.tsx"
      to: "externalDataMethodsApi.list"
      via: "useEffect fetch"
      pattern: "externalDataMethodsApi.list"
---

<objective>
Test DataMethodSelector 4-step wizard functionality.

Purpose: Verify users can select and configure data methods for use in preconditions.
Output: E2E test file proving selection, parameter config, preview, and code generation work
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/20-e2e-testing-manual-verification/20-CONTEXT.md

@frontend/src/components/TaskModal/DataMethodSelector.tsx
@frontend/src/components/TaskModal/TaskForm.tsx
@frontend/src/api/externalDataMethods.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly - no codebase exploration needed. -->

From frontend/src/types/index.ts:
```typescript
export interface DataMethodConfig {
  className: string
  methodName: string
  parameters: Record<string, any>
  extractions: FieldExtraction[]
}

export interface FieldExtraction {
  path: string           // e.g., "[0].imei"
  variableName: string   // e.g., "imei"
}
```

From frontend/src/api/externalDataMethods.ts:
```typescript
export const externalDataMethodsApi = {
  list: () => Promise<DataMethodsResponse>,
  execute: (className: string, methodName: string, params: Record<string, any>) => Promise<ExecuteDataMethodResponse>,
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test file</name>
  <files>e2e/tests/data-method-selector.spec.ts</files>
  <action>
    Create E2E test file ate2e/tests/data-method-selector.spec.ts) with the following tests:

    1. **DataMethodSelector Modal Opens**
       - Navigate to /tasks
       - Click "New Task" button
       - Click "Get Data" button
       - Wait for modal to appear
       - Verify modal title "Select Data Method" is visible

    2. **Step 1 - Method Selection**
       - Verify search input is visible
       - Verify class groups are displayed
       - Verify method items exist
       - Select first method by clicking checkbox
       - Verify selected method appears in selected area
       - Click Next button

    3. **Step 2 - Parameter Configuration**
       - Verify parameter inputs exist
       - Fill required parameter with value "2"
       - Click Next button

    4. **Step 3 - Data Preview**
       - Verify preview button is visible
       - Click preview button
       - Wait for data to load
       - Verify JSON viewer displays data
       - Click on a field to select for extraction
       - Verify extraction appears in list

    5. **Step 4 - Variable Naming**
       - Verify variable name inputs exist
       - Edit variable name to "imei"
       - Verify code preview shows generated code with "context.get_data"
       - Click Confirm button

    6. **Code Generation**
       - Verify modal closes
       - Verify generated code appears in precondition textarea

    Use Playwright patterns from existing e2e/tests/smoke.spec.ts:
    - test.setTimeout(180000) for long-running operations
    - await expect().toBeVisible() for visibility checks
    - Use page.locator() for element selection
    - Handle modal with backdrop click for cancellation
  </action>
  <verify>
    <automated>cd e2e && npx playwright test e2e/tests/data-method-selector.spec.ts</automated>
  </verify>
  <done>
    E2E test file exists at e2e/tests/data-method-selector.spec.ts
    Test suite covers:
    - Modal open and close
    - All 4 wizard steps
    - Method selection with search
    - Parameter configuration
    - Data preview with field extraction
    - Variable naming with code generation
  </done>
</task>

</tasks>

<verification>
All tests in data-method-selector.spec.ts pass
</verification>

<success_criteria>
- E2E test file created at e2e/tests/data-method-selector.spec.ts
- Test covers modal open/close, search, method selection, parameter config, preview, variable naming
- Generated code correctly inserted into precondition textarea
</success_criteria>

<output>
After completion, create .planning/phases/20-e2e-testing-manual-verification/20-01-SUMMARY.md
</output>
