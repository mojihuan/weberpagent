---
phase: 20-e2e-testing-manual-verification
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03]
files_modified: [e2e/tests/full-flow.spec.ts]
autonomous: true
requirements: [E2E-04]
user_setup: []

must_haves:
  truths:
    - "User can create task with data method configuration"
    - "User can execute task and monitor progress"
    - "User can view report with variable values displayed"
    - "Full flow from task creation to report viewing works end-to-end"
  artifacts:
    - path: "e2e/tests/full-flow.spec.ts"
      provides: "E2E tests for complete user flow"
      min_lines: 200
  key_links:
    - from: "runs.py"
      to: "/api/runs"
      via: "POST request"
      pattern: "POST.*api/runs"
    - from: "/api/runs"
      to: "SSE subscribe"
      via: "eventSource on"
      pattern: "eventSource.onmessage"
    - from: "ReportDetail.tsx"
      to: "/api/reports/{id}"
      via: "getReport fetch"
      pattern: "getReport.*id"
---

<objective>
Test complete end-to-end flow with data method integration.

Purpose: Verify the entire user journey works correctly from task creation to report viewing.
Output: E2E test file proving full flow works with data methods and variable substitution.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-e2e-testing-manual-verification/20-CONTEXT.md

@e2e/playwright.config.ts
@e2e/tests/smoke.spec.ts
@e2e/tests/task-flow.spec.ts
@frontend/src/components/TaskModal/DataMethodSelector.tsx
@frontend/src/components/TaskModal/TaskForm.tsx
@frontend/src/pages/ReportDetail.tsx
@backend/api/routes/runs.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly - no codebase exploration needed. -->

From backend/api/routes/runs.py (lines 124-131):
```python
# 获取 context 用于变量替换
context = precondition_service.get_context()
logger.info(f"[{run_id}] 前置条件执行完成，变量: {list(context.keys())}")

# 替换 task_description 中的变量
task_description = PreconditionService.substitute_variables(task_description, context)
logger.info(f"[{run_id}] 变量替换后的任务描述: {task_description[:100]}...")
```

From e2e/tests/smoke.spec.ts:
```typescript
test.describe('Smoke Test - Complete User Flow', () => {
  test('create -> execute -> monitor -> report', async ({ page }) => {
    test.setTimeout(180000) // 3 minutes

    await page.goto('/')
    await expect(page.locator('text=任务管理')).toBeVisible({ timeout: 10000 })
    // ... create, execute, monitor, report flow
  })
})
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test for complete flow</name>
  <files>e2e/tests/full-flow.spec.ts</files>
  <read_first>
    e2e/playwright.config.ts
    e2e/tests/smoke.spec.ts
    e2e/tests/task-flow.spec.ts
    frontend/src/components/TaskModal/DataMethodSelector.tsx
    frontend/src/components/TaskModal/TaskForm.tsx
    frontend/src/pages/ReportDetail.tsx
    backend/api/routes/runs.py
  </read_first>
  <action>
    Create E2E test file at `e2e/tests/full-flow.spec.ts` with the following test:

    Test name: "complete flow with data method and variable substitution"

    Steps:
    1. Navigate to /tasks
    2. Click "New Task" button
    3. Fill task form:
       - Name: "E2E Full Flow Test"
       - Description: "Test data method {{imei}}" (with variable placeholder)
       - Target URL: valid URL
    4. Click "Get Data" button to open DataMethodSelector
    5. In Step 1: Select first available data method
    6. In Step 2: Fill required parameter (e.g., i=2)
    7. In Step 3: Click "Preview Data" button, wait for data, click on first field to select extraction
    8. In Step 4: Set variable name to "imei", verify code preview shows "context.get_data"
    9. Click "Confirm" button, verify modal closes
    10. Verify precondition textarea contains generated code with "imei = context.get_data"
    11. Click "Create Task" button
    12. Verify task appears in task list
    13. Click "Execute" button for the created task
    14. Wait for execution to complete (monitor status change)
    15. Click "View Report" button
    16. Verify report page displays variable values (not {{imei}})

    Use Playwright patterns from existing smoke.spec.ts:
    - test.setTimeout(180000) for long-running operations
    - await expect().toBeVisible() for visibility checks
    - Use page.locator() for element selection
    - Use page.waitForSelector() for waiting
  </action>
  <verify>
    <automated>cd e2e && npx playwright test e2e/tests/full-flow.spec.ts</automated>
  </verify>
  <done>
    E2E test file exists at e2e/tests/full-flow.spec.ts
    Test covers complete flow:
    - Task creation with data method configuration
    - Data method selection and parameter configuration
    - Field extraction and variable naming
    - Code generation and insertion into precondition
    - Task execution and monitoring
    - Report viewing with variable verification
  </done>
</task>

</tasks>

<verification>
All tests in full-flow.spec.ts pass
</verification>

<success_criteria>
- E2E test file created at e2e/tests/full-flow.spec.ts
- Test passes with all assertions
- Report page displays actual variable values (not {{variable}} placeholders)
</success_criteria>

<output>
After completion, create .planning/phases/20-e2e-testing-manual-verification/20-04-SUMMARY.md
</output>
