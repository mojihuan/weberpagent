---
phase: 20-e2e-testing-manual-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [e2e/tests/data-method-execution.spec.ts]
autonomous: true
requirements: [E2E-02]
user_setup: []

must_haves:
  truths:
    - "User can execute data method and get response"
    - "Response data is displayed in preview"
    - "User can select fields for extraction"
    - "Extracted data is stored as variables"
  artifacts:
    - path: "e2e/tests/data-method-execution.spec.ts"
      provides: "E2E tests for data method execution"
      min_lines: 100
  key_links:
    - from: "DataMethodSelector.tsx"
      to: "/api/external-data-methods/execute"
      via: "externalDataMethodsApi.execute"
      pattern: "externalDataMethodsApi.execute"
---

<objective>
Test data method execution and response handling.

Purpose: Verify that data methods can be executed and responses are properly handled.
Output: E2E test file proving execution and response handling.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/state.md
@.planning/phases/20-e2e-testing-manual-verification/20-CONTEXT.md

@frontend/src/components/TaskModal/DataMethodSelector.tsx
@frontend/src/api/externalDataMethods.ts
@backend/core/external_precondition_bridge.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly - no codebase exploration needed. -->

From frontend/src/api/externalDataMethods.ts:
```typescript
export interface ExecuteDataMethodResponse {
  success: boolean
  data?: Array<Record<string, any>>
  error?: string
  error_type?: string
}
```

From backend/core/external_precondition_bridge.py (line 267-364):
```python
async def execute_data_method(
    class_name: str,
    method_name: str,
    params: dict,
    timeout: float = 30.0
) -> dict:
    """Execute a data method with timeout protection."""
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test for data method execution</name>
  <files>e2e/tests/data-method-execution.spec.ts</files>
  <action>
    Create E2E test file ate2e/tests/data-method-execution.spec.ts) with the following tests:

    1. **Successful Execution Test**
       - Navigate to /tasks
       - Click "New Task" button
       - Click "Get Data" button
       - Wait for modal
       - Select a data method
       - Configure parameters (e.g., i=2)
       - Go to Step 3 (Preview)
       - Click "Preview Data" button
       - Wait for response to load
       - Verify JSON viewer displays data (not error)
       - Verify data is an array with expected structure

    2. **Error Handling Test**
       - Configure invalid parameters
       - Execute preview
       - Verify error message is displayed
       - Verify error_type is shown

    3. **Timeout Handling Test**
       - Configure parameters that will timeout
       - Execute preview
       - Verify timeout error is displayed

    Use Playwright patterns from existing e2e/tests/smoke.spec.ts:
    - test.setTimeout(180000) for long-running operations
    - Use try/catch for error handling
    - Use page.waitForTimeout() for timeout tests
  </action>
  <verify>
    <automated>cd e2e && npx playwright test e2e/tests/data-method-execution.spec.ts</automated>
  </verify>
  <done>
    E2E test file exists at e2e/tests/data-method-execution.spec.ts
    Tests cover:
    - Successful execution
    - Error handling
    - Timeout handling
  </done>
</task>

</tasks>

<verification>
All tests in data-method-execution.spec.ts pass
</verification>

<success_criteria>
- E2E test file created at e2e/tests/data-method-execution.spec.ts
- Test covers successful execution, error handling, and timeout scenarios
</success_criteria>

<output>
After completion, create .planning/phases/20-e2e-testing-manual-verification/20-02-SUMMARY.md
</output>
