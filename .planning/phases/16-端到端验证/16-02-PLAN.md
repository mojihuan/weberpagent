---
phase: 16-端到端验证
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/test_e2e_precondition_integration.py
autonomous: true
requirements:
  - VAL-02
user_setup: []
must_haves:
  truths:
    - "API returns 503 when WEBSERP_PATH not configured"
    - "API returns 503 when WEBSERP_PATH points to non-existent directory"
    - "Bridge returns error when module import fails"
    - "PreconditionService captures execution exceptions in result"
  artifacts:
    - path: "backend/tests/integration/test_e2e_precondition_integration.py"
      provides: "Error scenario tests for VAL-02"
      contains: "class TestErrorScenarios"
  key_links:
    - from: "TestErrorScenarios"
      to: "external_operations.py"
      via: "HTTPException 503"
      pattern: "status_code == 503"
    - from: "TestErrorScenarios"
      to: "precondition_service.py"
      via: "result.success == False"
      pattern: "result.success is False"
---

<objective>
Create integration tests for VAL-02: Error handling scenarios including path not configured, path doesn't exist, module import failure, and execution exception.

Purpose: Verify all error scenarios defined in CONTEXT.md are properly handled
Output: TestErrorScenarios class with 4 error scenario tests
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-端到端验证/16-CONTEXT.md
@.planning/phases/16-端到端验证/16-RESEARCH.md

<interfaces>
<!-- Key interfaces from existing codebase for error handling -->

From backend/core/external_precondition_bridge.py:
```python
def is_available() -> bool  # Returns False when module not loaded
def get_unavailable_reason() -> str | None  # Returns error message
def load_pre_front_class() -> tuple[type | None, str | None]  # Returns (None, error) on failure
```

From backend/api/routes/external_operations.py:
```python
# Returns HTTPException(status_code=503, detail={...}) when is_available() is False
@router.get("", response_model=OperationsResponse)
async def list_operations()
```

From backend/core/precondition_service.py:
```python
@dataclass
class PreconditionResult:
    success: bool = False
    error: str | None = None  # Contains error message on failure
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TestErrorScenarios class with 4 error tests</name>
  <files>backend/tests/integration/test_e2e_precondition_integration.py</files>
  <read_first>
    - backend/tests/integration/test_e2e_precondition_integration.py (file created in 16-01)
    - backend/tests/api/test_external_operations.py (existing 503 test patterns)
    - backend/api/routes/external_operations.py (HTTPException handling)
    - backend/core/precondition_service.py (exception handling in execute_single)
  </read_first>
  <behavior>
    - Test 1: test_path_not_configured_returns_error - is_available() returns False when path not set
    - Test 2: test_path_not_exists_returns_error - configure_external_path returns (False, error) for non-existent path
    - Test 3: test_module_import_failure_returns_error - Missing __init__.py causes import error
    - Test 4: test_execution_exception_captured_in_result - PreconditionService captures exceptions in result.error
  </behavior>
  <action>
Add `TestErrorScenarios` class to `backend/tests/integration/test_e2e_precondition_integration.py` with 4 test methods:

1. **test_path_not_configured_returns_error:**
   - Reset bridge cache to clear any cached state
   - Call `is_available()` without configuring path
   - Assert `is_available() is False`
   - Assert `get_unavailable_reason()` returns a non-None error message

2. **test_path_not_exists_returns_error:**
   - Call `configure_external_path('/nonexistent/path/12345')`
   - Assert return is `(False, ...)` with error message containing "does not exist"

3. **test_module_import_failure_returns_error:**
   - Create mock directory WITHOUT `common/__init__.py` (missing required file)
   - Configure path to this incomplete mock
   - Call `load_pre_front_class()`
   - Assert returns `(None, error_message)`
   - Assert error message mentions import failure

4. **test_execution_exception_captured_in_result:**
   - Create mock PreFront that raises exception in operations():
   ```python
   class PreFront:
       def operations(self, codes):
           raise ValueError("Simulated execution error")
   ```
   - Generate code that imports and calls this PreFront
   - Execute via PreconditionService
   - Assert `result.success is False`
   - Assert `result.error` contains error message (e.g., "Simulated execution error" or "ValueError")

All tests must use `@pytest.mark.asyncio` for async tests (test_execution_exception_captured_in_result).
Use `tmp_path` fixture to create isolated test directories.
Reset bridge cache in each test to ensure isolation.
  </action>
  <verify>
    <automated>uv run pytest backend/tests/integration/test_e2e_precondition_integration.py::TestErrorScenarios -v</automated>
  </verify>
  <done>
    - `class TestErrorScenarios` exists in test_e2e_precondition_integration.py
    - Contains 4 test methods matching the 4 error scenarios from CONTEXT.md
    - All tests pass: `uv run pytest backend/tests/integration/test_e2e_precondition_integration.py::TestErrorScenarios -v` exits 0
    - Each error scenario has specific assertions for expected error behavior
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Run `uv run pytest backend/tests/integration/test_e2e_precondition_integration.py::TestErrorScenarios -v`
2. All 4 tests must pass
3. Verify each test covers one of the 4 error scenarios from CONTEXT.md
</verification>

<success_criteria>
- TestErrorScenarios class exists with 4 passing tests
- Tests cover all 4 VAL-02 error scenarios:
  1. Path not configured
  2. Path doesn't exist
  3. Module import failure
  4. Execution exception
- Error messages are captured and verifiable
</success_criteria>

<output>
After completion, create `.planning/phases/16-端到端验证/16-02-SUMMARY.md`
</output>
