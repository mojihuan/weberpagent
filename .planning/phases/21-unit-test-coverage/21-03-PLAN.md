---
phase: 21-unit-test-coverage
plan: 03
type: tdd
wave: 2
depends_on:
  - 21-01
  - 21-02
files_modified:
  - backend/tests/api/test_external_data_methods.py
autonomous: true
requirements:
  - UNIT-02
must_haves:
  truths:
    - "API endpoint handles concurrent execution requests"
    - "API endpoint validates request body schema"
    - "API endpoint returns correct error format for all error types"
    - "API endpoint handles edge cases (empty params, large responses)"
  artifacts:
    - path: backend/tests/api/test_external_data_methods.py
      provides: Extended API endpoint tests for boundary cases
      contains: "test_concurrent"
      contains: "test_invalid_request"
  key_links:
    - from: test_external_data_methods.py
      to: backend/api/routes/external_data_methods.py
      via: "TestClient HTTP requests"
      pattern: "client.post|client.get"
---

<objective>
Add boundary case tests for data fetching API endpoints.

Purpose: Ensure API endpoints handle edge cases like invalid requests, concurrent access, and large responses.
Output: Extended test coverage for external data methods API.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-unit-test-coverage/21-CONTEXT.md

<interfaces>
<!-- API routes to test -->
**GET /api/external-data-methods** - Returns list of available data methods
**POST /api/external-data-methods/execute** - Executes a data method

Request body for execute:
```json
{
  "class_name": "BaseParams",
  "method_name": "inventory_list_data",
  "params": {"i": 2, "j": 13}
}
```

Response structure:
```json
{
  "success": true,
  "data": [...],
  "error": null,
  "error_type": null
}
```
</interfaces>

<!-- Existing test file -->
@backend/tests/api/test_external_data_methods.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add request validation tests</name>
  <files>backend/tests/api/test_external_data_methods.py</files>
  <read_first>
    - backend/api/routes/external_data_methods.py (API route definitions)
    - backend/tests/api/test_external_data_methods.py (existing test patterns)
  </read_first>
  <behavior>
    - Test 1: Missing class_name in request body returns 422
    - Test 2: Missing method_name in request body returns 422
    - Test 3: Empty params dict is accepted (valid)
    - Test 4: params as non-dict returns 422
    - Test 5: Extra fields in request body are ignored or rejected
  </behavior>
  <action>
Add request validation tests to `backend/tests/api/test_external_data_methods.py`:

```python
class TestExecuteDataMethodValidation:
    """Tests for request validation on execute endpoint."""

    @pytest.fixture
    def client(self):
        from fastapi.testclient import TestClient
        from backend.api.main import app
        return TestClient(app)

    def test_missing_class_name_returns_422(self, client):
        """Test missing class_name returns validation error."""
        response = client.post(
            "/api/external-data-methods/execute",
            json={"method_name": "test", "params": {}}
        )
        assert response.status_code == 422

    def test_missing_method_name_returns_422(self, client):
        """Test missing method_name returns validation error."""
        response = client.post(
            "/api/external-data-methods/execute",
            json={"class_name": "TestClass", "params": {}}
        )
        assert response.status_code == 422

    def test_empty_params_accepted(self, client):
        """Test empty params dict is valid."""
        with patch(
            'backend.api.routes.external_data_methods.is_available',
            return_value=True
        ), patch(
            'backend.api.routes.external_data_methods.execute_data_method',
            return_value={"success": True, "data": []}
        ):
            response = client.post(
                "/api/external-data-methods/execute",
                json={"class_name": "TestClass", "method_name": "test", "params": {}}
            )
            assert response.status_code == 200

    def test_params_non_dict_returns_422(self, client):
        """Test params as non-dict returns validation error."""
        response = client.post(
            "/api/external-data-methods/execute",
            json={"class_name": "TestClass", "method_name": "test", "params": "invalid"}
        )
        assert response.status_code == 422

    def test_extra_fields_ignored(self, client):
        """Test extra fields in request body are handled gracefully."""
        with patch(
            'backend.api.routes.external_data_methods.is_available',
            return_value=True
        ), patch(
            'backend.api.routes.external_data_methods.execute_data_method',
            return_value={"success": True, "data": []}
        ):
            response = client.post(
                "/api/external-data-methods/execute",
                json={
                    "class_name": "TestClass",
                    "method_name": "test",
                    "params": {},
                    "extra_field": "should_be_ignored"
                }
            )
            # FastAPI by default ignores extra fields
            assert response.status_code == 200
```
  </action>
  <verify>
    <automated>uv run pytest backend/tests/api/test_external_data_methods.py::TestExecuteDataMethodValidation -v</automated>
  </verify>
  <done>
    - backend/tests/api/test_external_data_methods.py contains TestExecuteDataMethodValidation class
    - All 5 validation tests pass
    - 422 errors for invalid requests, 200 for valid requests
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add edge case response tests</name>
  <files>backend/tests/api/test_external_data_methods.py</files>
  <read_first>
    - backend/api/routes/external_data_methods.py
    - backend/tests/api/test_external_data_methods.py (existing tests)
  </read_first>
  <behavior>
    - Test 1: Large data response (100+ items) handled correctly
    - Test 2: Response with unicode characters handled correctly
    - Test 3: Response with null values handled correctly
    - Test 4: Response with nested structures handled correctly
    - Test 5: Response with empty string values handled correctly
  </behavior>
  <action>
Add edge case tests to `TestExecuteDataMethod` class or create new test class:

```python
class TestExecuteDataMethodEdgeCases:
    """Tests for edge case responses."""

    @pytest.fixture
    def client(self):
        from fastapi.testclient import TestClient
        from backend.api.main import app
        return TestClient(app)

    @pytest.fixture(autouse=True)
    def reset_cache(self):
        from backend.core import external_precondition_bridge
        external_precondition_bridge.reset_cache()
        yield
        external_precondition_bridge.reset_cache()

    def test_large_data_response(self, client):
        """Test handling of large data responses (100+ items)."""
        large_data = [{"id": i, "name": f"Item {i}"} for i in range(150)]

        with patch(
            'backend.api.routes.external_data_methods.is_available',
            return_value=True
        ), patch(
            'backend.api.routes.external_data_methods.execute_data_method',
            return_value={"success": True, "data": large_data}
        ):
            response = client.post(
                "/api/external-data-methods/execute",
                json={"class_name": "TestClass", "method_name": "large_method", "params": {}}
            )

            assert response.status_code == 200
            data = response.json()
            assert data["success"] is True
            assert len(data["data"]) == 150

    def test_response_with_unicode(self, client):
        """Test handling of unicode characters in response."""
        unicode_data = [
            {"name": "Product A", "desc": "Description with Chinese: "},
            {"name": "Product B", "desc": "Emoji test: test"}
        ]

        with patch(
            'backend.api.routes.external_data_methods.is_available',
            return_value=True
        ), patch(
            'backend.api.routes.external_data_methods.execute_data_method',
            return_value={"success": True, "data": unicode_data}
        ):
            response = client.post(
                "/api/external-data-methods/execute",
                json={"class_name": "TestClass", "method_name": "unicode_method", "params": {}}
            )

            assert response.status_code == 200
            data = response.json()
            assert data["data"][0]["desc"] == "Description with Chinese: "

    def test_response_with_null_values(self, client):
        """Test handling of null values in response."""
        data_with_nulls = [
            {"id": 1, "name": "Item", "optional": None},
            {"id": 2, "name": None, "optional": "value"}
        ]

        with patch(
            'backend.api.routes.external_data_methods.is_available',
            return_value=True
        ), patch(
            'backend.api.routes.external_data_methods.execute_data_method',
            return_value={"success": True, "data": data_with_nulls}
        ):
            response = client.post(
                "/api/external-data-methods/execute",
                json={"class_name": "TestClass", "method_name": "null_method", "params": {}}
            )

            assert response.status_code == 200
            data = response.json()
            assert data["data"][0]["optional"] is None
            assert data["data"][1]["name"] is None

    def test_response_with_nested_structures(self, client):
        """Test handling of deeply nested structures."""
        nested_data = {
            "level1": {
                "level2": {
                    "level3": {
                        "items": [{"id": 1}, {"id": 2}]
                    }
                }
            }
        }

        with patch(
            'backend.api.routes.external_data_methods.is_available',
            return_value=True
        ), patch(
            'backend.api.routes.external_data_methods.execute_data_method',
            return_value={"success": True, "data": nested_data}
        ):
            response = client.post(
                "/api/external-data-methods/execute",
                json={"class_name": "TestClass", "method_name": "nested_method", "params": {}}
            )

            assert response.status_code == 200
            data = response.json()
            assert data["data"]["level1"]["level2"]["level3"]["items"][0]["id"] == 1

    def test_response_with_empty_strings(self, client):
        """Test handling of empty string values."""
        data_with_empty = [
            {"id": 1, "name": "", "desc": "has value"},
            {"id": 2, "name": "has value", "desc": ""}
        ]

        with patch(
            'backend.api.routes.external_data_methods.is_available',
            return_value=True
        ), patch(
            'backend.api.routes.external_data_methods.execute_data_method',
            return_value={"success": True, "data": data_with_empty}
        ):
            response = client.post(
                "/api/external-data-methods/execute",
                json={"class_name": "TestClass", "method_name": "empty_method", "params": {}}
            )

            assert response.status_code == 200
            data = response.json()
            assert data["data"][0]["name"] == ""
            assert data["data"][1]["desc"] == ""
```
  </action>
  <verify>
    <automated>uv run pytest backend/tests/api/test_external_data_methods.py::TestExecuteDataMethodEdgeCases -v</automated>
  </verify>
  <done>
    - backend/tests/api/test_external_data_methods.py contains TestExecuteDataMethodEdgeCases class
    - All 5 edge case tests pass
    - Large data, unicode, nulls, nested, empty strings all handled
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify API test coverage</name>
  <files>backend/tests/api/test_external_data_methods.py</files>
  <read_first>
    - backend/tests/api/test_external_data_methods.py (all tests)
  </read_first>
  <action>
Run all API tests and verify coverage:

```bash
uv run pytest --cov=backend.api.routes.external_data_methods --cov-report=term-missing backend/tests/api/test_external_data_methods.py -v
```

Verify:
1. All tests pass
2. Coverage on external_data_methods route is >= 80%
3. Key endpoints (list, execute) are covered
</action>
  <verify>
    <automated>uv run pytest backend/tests/api/test_external_data_methods.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All tests in test_external_data_methods.py pass
    - Test count >= 25 (existing + new validation + edge cases)
    - No failing tests
  </done>
</task>

</tasks>

<verification>
- All API tests pass: `uv run pytest backend/tests/api/test_external_data_methods.py -v`
- Validation tests exist: `grep -c "TestExecuteDataMethodValidation" backend/tests/api/test_external_data_methods.py`
- Edge case tests exist: `grep -c "TestExecuteDataMethodEdgeCases" backend/tests/api/test_external_data_methods.py`
</verification>

<success_criteria>
1. TestExecuteDataMethodValidation class exists with >= 5 tests
2. TestExecuteDataMethodEdgeCases class exists with >= 5 tests
3. All tests pass
4. Request validation tests cover missing fields, invalid types
5. Edge case tests cover large data, unicode, nulls, nested, empty strings
</success_criteria>

<output>
After completion, create `.planning/phases/21-unit-test-coverage/21-03-SUMMARY.md`
</output>
