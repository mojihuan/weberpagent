---
phase: 09-登录用例调通
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [LOGN-01, LOGN-02]
user_setup: []

must_haves:
  truths:
    - "User can create a login test task from the frontend"
    - "Login test case executes with all steps showing success status"
    - "AI determines login success based on URL change"
  artifacts:
    - path: "frontend/src/components/TaskModal/TaskForm.tsx"
      provides: "Task creation form"
      contains: "name, description, target_url, max_steps"
    - path: "backend/api/routes/runs.py"
      provides: "Run execution endpoint"
      exports: ["POST /runs", "run_agent_background"]
    - path: "backend/core/agent_service.py"
      provides: "Browser-Use Agent execution"
      exports: ["AgentService", "run_with_cleanup"]
  key_links:
    - from: "frontend TaskForm"
      to: "POST /api/tasks"
      via: "tasksApi.create()"
      pattern: "tasksApi.create"
    - from: "frontend RunList"
      to: "POST /api/runs"
      via: "start run button"
      pattern: "POST.*runs"
    - from: "runs.py run_agent_background"
      to: "AgentService.run_with_cleanup"
      via: "agent execution"
      pattern: "agent_service.run_with_cleanup"
---

<objective>
Create and execute a login test case from the frontend to verify the end-to-end execution flow works correctly.

Purpose: Validate that the v0.1-v0.2 infrastructure (task creation, AI execution, SSE monitoring) works together for a simple login scenario.
Output: A completed login test run with all steps showing success status.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-登录用例调通/09-CONTEXT.md

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From frontend/src/api/tasks.ts:
```typescript
export const tasksApi = {
  async create(data: CreateTaskDto): Promise<Task>
  async get(id: string): Promise<Task | null>
}
```

From frontend/src/types/index.ts (CreateTaskDto):
```typescript
interface CreateTaskDto {
  name: string
  description: string
  target_url: string
  max_steps: number
  preconditions?: string[]
  api_assertions?: string[]
}
```

From backend/api/routes/runs.py:
```python
@router.post("", response_model=RunResponse)
async def create_run(task_id: str, ...) -> RunResponse
```

From backend/core/agent_service.py:
```python
class AgentService:
    async def run_with_cleanup(
        self,
        task: str,
        run_id: str,
        on_step: Callable,
        max_steps: int = 10,
        llm_config: dict | None = None,
    ) -> Any
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Start backend and frontend services</name>
  <what-built>Verify development environment is running</what-built>
  <read_first>
    - /Users/huhu/project/weberpagent/CLAUDE.md
    - /Users/huhu/project/weberpagent/.env (check environment variables are configured)
  </read_first>
  <action>
    Verify the development environment is ready:

    1. Check that `.env` file exists with required configuration:
       - DASHSCOPE_API_KEY must be set (not the placeholder sk-xxx)
       - ERP_BASE_URL must be set to https://erptest.epbox.cn/

    2. Start the backend server:
       ```bash
       cd /Users/huhu/project/weberpagent && uv run uvicorn backend.api.main:app --reload --port 8080
       ```

    3. In a separate terminal, start the frontend:
       ```bash
       cd /Users/huhu/project/weberpagent/frontend && npm run dev
       ```

    4. Verify both services are running:
       - Backend: http://localhost:8080/api/health should return 200
       - Frontend: http://localhost:5173 should load the app
  </action>
  <acceptance_criteria>
    - Backend server running on port 8080 (curl http://localhost:8080/api/health returns 200)
    - Frontend server running on port 5173 (curl http://localhost:5173 returns HTML)
    - DASHSCOPE_API_KEY in .env is not "sk-xxx" placeholder
    - ERP_BASE_URL in .env is "https://erptest.epbox.cn/"
  </acceptance_criteria>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/health | grep -q "200" && echo "Backend OK" || echo "Backend NOT running"</automated>
  </verify>
  <done>Both backend (port 8080) and frontend (port 5173) servers are running and accessible</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Create login test task from frontend</name>
  <what-built>Login test task created through frontend UI</what-built>
  <read_first>
    - /Users/huhu/project/weberpagent/frontend/src/components/TaskModal/TaskForm.tsx
    - /Users/huhu/project/weberpagent/frontend/src/api/tasks.ts
  </read_first>
  <action>
    Navigate to the frontend and create a new task with these exact values:

    1. Open browser to http://localhost:5173

    2. Click "Create Task" button to open the task form

    3. Fill in the form with these values:
       - **Task Name (任务名称)**: `登录测试用例`
       - **Task Description (任务描述)**:
         ```
         打开 https://erptest.epbox.cn/
         找到用户名输入框，输入 Y59800075
         找到密码输入框，输入 Aa123456
         点击登录按钮
         ```
       - **Target URL (目标 URL)**: `https://erptest.epbox.cn/`
       - **Max Steps (最大步数)**: `20` (default is fine)
       - **Preconditions (前置条件)**: Leave empty (login test has no preconditions)
       - **API Assertions (接口断言)**: Leave empty (login test has no API assertions)

    4. Click "Create Task" button to save

    5. Verify the task appears in the task list with name "登录测试用例"
  </action>
  <acceptance_criteria>
    - Task "登录测试用例" appears in the task list
    - Task has target_url = "https://erptest.epbox.cn/"
    - Task has description containing "Y59800075" and "Aa123456"
    - Task has max_steps = 20
    - Task has empty preconditions and api_assertions
  </acceptance_criteria>
  <verify>
    <automated>curl -s http://localhost:8080/api/tasks | grep -q "登录测试用例" && echo "Task created" || echo "Task NOT found"</automated>
  </verify>
  <done>Login test task "登录测试用例" created successfully and visible in task list</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute login test and verify results</name>
  <what-built>Login test execution with all steps succeeding</what-built>
  <read_first>
    - /Users/huhu/project/weberpagent/backend/api/routes/runs.py (lines 55-313)
    - /Users/huhu/project/weberpagent/backend/core/agent_service.py
    - /Users/huhu/project/weberpagent/frontend/src/pages/RunMonitor.tsx
  </read_first>
  <action>
    Execute the login test and monitor the execution:

    1. Find the task "登录测试用例" in the task list

    2. Click the "Run" (执行) button to start execution

    3. You will be redirected to the Run Monitor page (http://localhost:5173/runs/{run_id})

    4. Monitor the execution:
       - Watch the SSE events update the UI in real-time
       - Steps should appear one by one as the AI executes them
       - Screenshots should be visible in the left panel
       - Step timeline should show success status (green)

    5. Wait for execution to complete (status changes from "running" to "success" or "failed")

    6. Record the run_id from the URL for verification
  </action>
  <acceptance_criteria>
    - Run execution starts and redirects to Run Monitor page
    - SSE events are received and UI updates in real-time
    - At least 2 steps are executed (open page + login actions)
    - All executed steps show "success" status
    - Final run status is "success"
    - Screenshots are visible for each step
  </acceptance_criteria>
  <verify>
    <automated>curl -s http://localhost:8080/api/runs | grep -q '"status":"success"' && echo "Run succeeded" || echo "Run may have failed or not completed"</automated>
  </verify>
  <done>
    Login test execution completed with:
    - All steps showing success status
    - Screenshots visible in the UI
    - Final status = "success" (indicating AI determined login was successful)
  </done>
  <troubleshooting>
    If execution fails:
    1. Check backend logs for error messages
    2. Verify DASHSCOPE_API_KEY is valid and has quota
    3. Verify ERP system is accessible from the network
    4. If it's a Bug, record it for Phase 11 but continue with next plan
  </troubleshooting>
</task>

</tasks>

<verification>
After all tasks complete:
1. Task "登录测试用例" exists in the database
2. At least one Run record exists with status "success"
3. Run has multiple steps with screenshots
4. All steps have status "success"
</verification>

<success_criteria>
1. Login test task created successfully from frontend
2. Login test executed and completed with all steps showing success
3. Run status is "success" (AI determined login was successful based on URL change)
4. Screenshots are visible in the Run Monitor
</success_criteria>

<output>
After completion, create `.planning/phases/09-登录用例调通/09-01-SUMMARY.md` with:
- Task ID of the created login test
- Run ID of the execution
- Execution result (success/failure)
- Any issues encountered during execution
</output>
