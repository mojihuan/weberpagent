---
phase: 07-动态数据支持
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - backend/core/precondition_service.py
  - backend/tests/unit/test_precondition_service.py
autonomous: true
requirements:
  - DYN-01
  - DYN-03
  - DYN-04
user_setup: []

must_haves:
  truths:
    - "前置条件中可以调用 sf_waybill() 并存储到 context"
    - "前置条件中可以调用 time_now() 并存储到 context"
    - "变量替换时可以引用前置条件生成的动态数据"
    - "context 在多次执行间持久化"
  artifacts:
    - path: "backend/core/precondition_service.py"
      provides: "集成了动态数据函数的前置条件服务"
      contains: "from backend.core.random_generators import"
      contains: "from backend.core.time_utils import"
      min_lines: 80
    - path: "backend/tests/unit/test_precondition_service.py"
      provides: "集成测试"
      contains: "test_dynamic_data_integration"
  key_links:
    - from: "PreconditionService._setup_execution_env()"
      to: "random_generators.py, time_utils.py"
      via: "函数注入到执行环境"
      pattern: "env['sf_waybill'] = sf_waybill"
    - from: "exec(code, env)"
      to: "context['var']"
      via: "用户代码中调用辅助函数并存储结果"
      pattern: "context\\['.*'\\] = sf_waybill\\(\\)"
---

<objective>
将随机数生成器和时间计算函数集成到 PreconditionService，让用户可以在前置条件代码中直接调用这些函数。

Purpose: 实现动态数据支持的核心集成点
Output: 增强的 PreconditionService + 集成测试
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-动态数据支持/07-CONTEXT.md
@.planning/phases/07-动态数据支持/07-RESEARCH.md
@.planning/phases/05-前置条件系统/05-CONTEXT.md

<interfaces>
<!-- 现有 _setup_execution_env 实现 -->
From backend/core/precondition_service.py:
```python
def _setup_execution_env(self) -> dict:
    """创建执行环境"""
    # 添加外部模块路径
    if self.external_module_path:
        path = Path(self.external_module_path)
        if not path.exists():
            logger.warning(f"外部模块路径不存在: {path}")
        elif str(path) not in sys.path:
            sys.path.insert(0, str(path))
            logger.info(f"添加外部模块路径: {path}")

    # 受限的全局环境
    return {
        '__builtins__': __builtins__,
        'context': self.context,
    }
```

<!-- 需要注入的函数 -->
From backend/core/random_generators.py (07-01):
- sf_waybill, random_phone, random_imei, random_serial, random_numbers

From backend/core/time_utils.py (07-02):
- time_now
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 集成动态数据函数到 PreconditionService</name>
  <files>backend/core/precondition_service.py</files>
  <read_first>
    - /Users/huhu/project/weberpagent/backend/core/precondition_service.py (当前实现)
    - /Users/huhu/project/weberpagent/backend/core/random_generators.py (需要导入的函数)
    - /Users/huhu/project/weberpagent/backend/core/time_utils.py (需要导入的函数)
  </read_first>
  <behavior>
    - 前置条件代码可以调用 sf_waybill() 并获得 SF 物流单号
    - 前置条件代码可以调用 time_now(30) 并获得未来时间
    - 执行环境包含所有动态数据辅助函数
  </behavior>
  <action>
修改 `backend/core/precondition_service.py` 文件：

1. **添加导入语句**（文件顶部，约第 6 行之后）:
   ```python
   from backend.core.random_generators import (
       sf_waybill,
       random_phone,
       random_imei,
       random_serial,
       random_numbers,
   )
   from backend.core.time_utils import time_now
   ```

2. **修改 _setup_execution_env 方法**，将动态数据函数注入到执行环境：
   ```python
   def _setup_execution_env(self) -> dict:
       """创建执行环境

       包含动态数据辅助函数，供前置条件代码调用。
       """
       # 添加外部模块路径
       if self.external_module_path:
           path = Path(self.external_module_path)
           if not path.exists():
               logger.warning(f"外部模块路径不存在: {path}")
           elif str(path) not in sys.path:
               sys.path.insert(0, str(path))
               logger.info(f"添加外部模块路径: {path}")
           else:
               logger.debug(f"外部模块路径已在 sys.path 中: {path}")

       # 受限的全局环境 + 动态数据辅助函数
       return {
           '__builtins__': __builtins__,
           'context': self.context,
           # 随机数生成函数
           'sf_waybill': sf_waybill,
           'random_phone': random_phone,
           'random_imei': random_imei,
           'random_serial': random_serial,
           'random_numbers': random_numbers,
           # 时间计算函数
           'time_now': time_now,
       }
   ```

保持现有代码结构不变，只添加导入和修改返回字典。
</action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_precondition_service.py -v -k "dynamic"</automated>
  </verify>
  <done>
    - precondition_service.py 包含 from backend.core.random_generators import
    - precondition_service.py 包含 from backend.core.time_utils import
    - _setup_execution_env 返回的字典包含 sf_waybill, random_phone, time_now 等
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 添加动态数据集成测试</name>
  <files>backend/tests/unit/test_precondition_service.py</files>
  <read_first>
    - /Users/huhu/project/weberpagent/backend/tests/unit/test_precondition_service.py (现有测试)
    - /Users/huhu/project/weberpagent/backend/core/precondition_service.py (集成后的服务)
  </read_first>
  <behavior>
    - test_dynamic_data_sf_waybill: 调用 sf_waybill() 存储到 context，验证格式
    - test_dynamic_data_random_phone: 调用 random_phone() 存储到 context，验证格式
    - test_dynamic_data_time_now: 调用 time_now() 存储到 context，验证格式
    - test_dynamic_data_time_offset: 调用 time_now(30) 存储到 context，验证是未来时间
    - test_dynamic_data_substitution: 生成动态数据后进行变量替换
  </behavior>
  <action>
在 `backend/tests/unit/test_precondition_service.py` 文件末尾添加新的测试类：

```python
class TestPreconditionServiceDynamicData:
    """动态数据集成测试 (Phase 7)"""

    @pytest.fixture
    def service(self):
        return PreconditionService()

    @pytest.mark.asyncio
    async def test_dynamic_data_sf_waybill(self, service):
        """测试 SF 物流单号生成 (DYN-01)"""
        code = "context['order_no'] = sf_waybill()"
        result = await service.execute_single(code, 0)

        assert result.success is True
        order_no = result.variables.get('order_no')
        assert order_no is not None
        assert order_no.startswith("SF")
        assert len(order_no) == 14

    @pytest.mark.asyncio
    async def test_dynamic_data_random_phone(self, service):
        """测试手机号生成 (DYN-01)"""
        code = "context['phone'] = random_phone()"
        result = await service.execute_single(code, 0)

        assert result.success is True
        phone = result.variables.get('phone')
        assert phone is not None
        assert phone.startswith("13")
        assert len(phone) == 11
        assert phone.isdigit()

    @pytest.mark.asyncio
    async def test_dynamic_data_random_imei(self, service):
        """测试 IMEI 生成 (DYN-01)"""
        code = "context['imei'] = random_imei()"
        result = await service.execute_single(code, 0)

        assert result.success is True
        imei = result.variables.get('imei')
        assert imei is not None
        assert imei.startswith("I")
        assert len(imei) == 15

    @pytest.mark.asyncio
    async def test_dynamic_data_time_now(self, service):
        """测试当前时间获取 (DYN-04)"""
        code = "context['current_time'] = time_now()"
        result = await service.execute_single(code, 0)

        assert result.success is True
        current_time = result.variables.get('current_time')
        assert current_time is not None
        # 验证格式可以解析
        from datetime import datetime
        parsed = datetime.strptime(current_time, "%Y-%m-%d %H:%M:%S")
        assert isinstance(parsed, datetime)

    @pytest.mark.asyncio
    async def test_dynamic_data_time_offset(self, service):
        """测试时间偏移计算 (DYN-04)"""
        code = "context['future_time'] = time_now(30)"
        result = await service.execute_single(code, 0)

        assert result.success is True
        future_time = result.variables.get('future_time')
        assert future_time is not None

        from datetime import datetime, timedelta
        parsed = datetime.strptime(future_time, "%Y-%m-%d %H:%M:%S")
        expected = datetime.now() + timedelta(minutes=30)
        # 允许 2 秒误差
        assert abs((parsed - expected).total_seconds()) < 2

    @pytest.mark.asyncio
    async def test_dynamic_data_multiple_functions(self, service):
        """测试多个动态数据函数组合使用"""
        code = '''
context['order_no'] = sf_waybill()
context['phone'] = random_phone()
context['imei'] = random_imei()
context['time'] = time_now()
'''
        result = await service.execute_single(code, 0)

        assert result.success is True
        assert result.variables.get('order_no', '').startswith('SF')
        assert result.variables.get('phone', '').startswith('13')
        assert result.variables.get('imei', '').startswith('I')
        assert result.variables.get('time') is not None

    @pytest.mark.asyncio
    async def test_dynamic_data_substitution(self, service):
        """测试动态数据 + 变量替换 (DYN-03)"""
        # 先执行前置条件生成动态数据
        code = "context['order_no'] = sf_waybill()"
        result = await service.execute_single(code, 0)
        assert result.success is True

        # 获取 context 并进行变量替换
        context = service.get_context()
        text = "查询订单 {{order_no}}"
        substituted = PreconditionService.substitute_variables(text, context)

        assert substituted.startswith("查询订单 SF")
        assert len(substituted) == len("查询订单 ") + 14
```

确保新测试类添加在文件末尾，不修改现有测试。
</action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_precondition_service.py -v -k "TestPreconditionServiceDynamicData"</automated>
  </verify>
  <done>
    - test_precondition_service.py 包含 TestPreconditionServiceDynamicData 类
    - 测试覆盖 sf_waybill, random_phone, random_imei, time_now 函数
    - 所有新测试通过
  </done>
</task>

</tasks>

<verification>
运行完整的前置条件服务测试套件，验证动态数据集成：
```bash
uv run pytest backend/tests/unit/test_precondition_service.py -v
```
</verification>

<success_criteria>
1. 前置条件中可以调用 sf_waybill() 生成 SF 物流单号 (DYN-01)
2. 前置条件中可以调用 time_now() 获取当前时间 (DYN-04)
3. 动态数据可以存储到 context 并通过变量替换引用 (DYN-03)
4. 所有单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-动态数据支持/07-03-SUMMARY.md`
</output>
