---
phase: 07-动态数据支持
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/time_utils.py
  - backend/tests/unit/test_time_utils.py
autonomous: true
requirements:
  - DYN-04
user_setup: []

must_haves:
  truths:
    - "time_now() 返回当前时间字符串，格式为 '%Y-%m-%d %H:%M:%S'"
    - "time_now(+30) 返回 30 分钟后的时间"
    - "time_now(-5) 返回 5 分钟前的时间"
    - "time_now(offset_minutes=0) 默认返回当前时间"
  artifacts:
    - path: "backend/core/time_utils.py"
      provides: "时间计算函数"
      exports: ["time_now"]
      min_lines: 30
    - path: "backend/tests/unit/test_time_utils.py"
      provides: "时间计算工具单元测试"
      contains: "def test_time_now"
  key_links:
    - from: "backend/core/time_utils.py"
      to: "PreconditionService._setup_execution_env()"
      via: "函数导入并注入到执行环境"
      pattern: "from backend.core.time_utils import time_now"
---

<objective>
创建时间计算工具模块，支持获取当前时间和时间偏移计算。

Purpose: 让用户在前置条件中生成时间相关测试数据
Output: 时间计算函数模块 + 单元测试
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-动态数据支持/07-CONTEXT.md
@.planning/phases/07-动态数据支持/07-RESEARCH.md

<interfaces>
<!-- 时间计算参考实现 -->
From RESEARCH.md (get_time_stamp_by_minute):
```python
def get_time_stamp_by_minute(self, minute: int) -> str:
    formatted_datetime = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    return (datetime.strptime(formatted_datetime, "%Y-%m-%d %H:%M:%S") +
            timedelta(minutes=int(minute))).strftime("%Y-%m-%d %H:%M:%S")
```

<!-- 用户决策的函数签名 -->
From CONTEXT.md:
- time_now(offset_minutes=0) - 获取当前时间，支持 ±N 分钟偏移
- 输出格式: datetime 字符串格式（如 `2026-03-17 10:30:00`）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建时间计算工具模块</name>
  <files>backend/core/time_utils.py</files>
  <read_first>
    - /Users/huhu/project/weberpagent/backend/core/precondition_service.py (了解现有代码结构和风格)
    - /Users/huhu/project/weberpagent/.planning/phases/07-动态数据支持/07-RESEARCH.md (参考实现)
  </read_first>
  <behavior>
    - time_now() 返回格式为 "%Y-%m-%d %H:%M:%S" 的当前时间字符串
    - time_now(30) 返回 30 分钟后的时间字符串
    - time_now(-15) 返回 15 分钟前的时间字符串
    - time_now(offset_minutes=0) 等同于 time_now()
  </behavior>
  <action>
创建 `backend/core/time_utils.py` 文件，实现以下函数：

1. **time_now(offset_minutes: int = 0) -> str** - 获取当前时间
   - 参数: offset_minutes - 分钟偏移量（正数为未来，负数为过去，默认 0）
   - 返回: 时间字符串，格式 "%Y-%m-%d %H:%M:%S"
   - 实现:
     ```python
     """时间计算工具模块"""

     from datetime import datetime, timedelta


     def time_now(offset_minutes: int = 0) -> str:
         """获取当前时间，支持分钟偏移

         Args:
             offset_minutes: 分钟偏移量，正数为未来时间，负数为过去时间

         Returns:
             时间字符串，格式为 "%Y-%m-%d %H:%M:%S"

         Examples:
             time_now()        # "2026-03-17 10:30:00"
             time_now(30)      # "2026-03-17 11:00:00" (30分钟后)
             time_now(-5)      # "2026-03-17 10:25:00" (5分钟前)
         """
         current = datetime.now()
         if offset_minutes != 0:
             current = current + timedelta(minutes=offset_minutes)
         return current.strftime("%Y-%m-%d %H:%M:%S")
     ```

文件简洁，只包含 time_now 函数。使用标准库 datetime 模块，不添加外部依赖。
</action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_time_utils.py -v</automated>
  </verify>
  <done>
    - backend/core/time_utils.py 文件存在
    - 包含 time_now 函数
    - 函数有类型提示和文档字符串
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建时间计算工具单元测试</name>
  <files>backend/tests/unit/test_time_utils.py</files>
  <read_first>
    - /Users/huhu/project/weberpagent/backend/tests/unit/test_precondition_service.py (参考测试模式)
    - /Users/huhu/project/weberpagent/backend/core/time_utils.py (被测试的模块)
  </read_first>
  <behavior>
    - test_time_now_format: 验证返回值格式为 "%Y-%m-%d %H:%M:%S"
    - test_time_now_current: 验证 time_now() 返回当前时间（允许 1 秒误差）
    - test_time_now_future: 验证 time_now(30) 返回约 30 分钟后的时间
    - test_time_now_past: 验证 time_now(-15) 返回约 15 分钟前的时间
    - test_time_now_zero_offset: 验证 time_now(0) 等同于 time_now()
  </behavior>
  <action>
创建 `backend/tests/unit/test_time_utils.py` 文件，实现以下测试：

```python
"""时间计算工具单元测试"""

import pytest
from datetime import datetime, timedelta
from backend.core.time_utils import time_now


class TestTimeNow:
    """time_now 函数测试"""

    def test_time_now_format(self):
        """测试返回格式为 '%Y-%m-%d %H:%M:%S'"""
        result = time_now()
        # 验证格式：尝试解析
        parsed = datetime.strptime(result, "%Y-%m-%d %H:%M:%S")
        assert isinstance(parsed, datetime)

    def test_time_now_current(self):
        """测试无偏移时返回当前时间"""
        before = datetime.now()
        result = time_now()
        after = datetime.now()

        parsed = datetime.strptime(result, "%Y-%m-%d %H:%M:%S")

        # 结果应在调用前后之间（允许 1 秒误差）
        assert before - timedelta(seconds=1) <= parsed <= after + timedelta(seconds=1)

    def test_time_now_future(self):
        """测试正偏移返回未来时间"""
        offset = 30
        result = time_now(offset)
        parsed = datetime.strptime(result, "%Y-%m-%d %H:%M:%S")

        expected = datetime.now() + timedelta(minutes=offset)
        # 允许 1 秒误差
        assert abs((parsed - expected).total_seconds()) < 1

    def test_time_now_past(self):
        """测试负偏移返回过去时间"""
        offset = -15
        result = time_now(offset)
        parsed = datetime.strptime(result, "%Y-%m-%d %H:%M:%S")

        expected = datetime.now() + timedelta(minutes=offset)
        # 允许 1 秒误差
        assert abs((parsed - expected).total_seconds()) < 1

    def test_time_now_zero_offset(self):
        """测试偏移为 0 等同于无偏移"""
        result_default = time_now()
        result_zero = time_now(0)

        # 两个调用应在同一秒内
        parsed_default = datetime.strptime(result_default, "%Y-%m-%d %H:%M:%S")
        parsed_zero = datetime.strptime(result_zero, "%Y-%m-%d %H:%M:%S")

        assert abs((parsed_default - parsed_zero).total_seconds()) < 1

    def test_time_now_large_offset(self):
        """测试大偏移值"""
        # 测试 1440 分钟 = 24 小时 = 1 天
        result = time_now(1440)
        parsed = datetime.strptime(result, "%Y-%m-%d %H:%M:%S")

        expected = datetime.now() + timedelta(days=1)
        # 允许 1 秒误差
        assert abs((parsed - expected).total_seconds()) < 1
```

遵循现有测试模式：使用 pytest + 类组织测试，每个测试有清晰的文档字符串。
</action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_time_utils.py -v</automated>
  </verify>
  <done>
    - backend/tests/unit/test_time_utils.py 文件存在
    - 测试覆盖 time_now 函数的各种场景
    - 所有测试通过
  </done>
</task>

</tasks>

<verification>
运行时间计算工具测试套件，验证函数正确工作：
```bash
uv run pytest backend/tests/unit/test_time_utils.py -v
```
</verification>

<success_criteria>
1. time_now() 返回当前时间字符串 (DYN-04)
2. time_now(+N) 返回 N 分钟后的时间 (DYN-04)
3. time_now(-N) 返回 N 分钟前的时间 (DYN-04)
4. 输出格式为 "%Y-%m-%d %H:%M:%S" (DYN-04)
5. 所有单元测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-动态数据支持/07-02-SUMMARY.md`
</output>
