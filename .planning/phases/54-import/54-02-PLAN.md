---
phase: 54-import
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - docs/test-steps/采购-文件导入测试步骤.md
  - docs/test-steps/采购-文件导入验证结果.md
autonomous: false
requirements: [IMP-01, IMP-02]

must_haves:
  truths:
    - "Agent can upload an Excel file via upload_file action on the purchase order import page (IMP-01)"
    - "Agent can upload an image file via upload_file action on the product management page (IMP-02)"
    - "Verification results document records pass/fail for each scenario"
  artifacts:
    - path: "docs/test-steps/采购-文件导入测试步骤.md"
      provides: "Test step document for Excel import and image upload scenarios"
      contains: "IMP-01"
    - path: "docs/test-steps/采购-文件导入验证结果.md"
      provides: "Human verification results for file upload scenarios"
      contains: "IMP-02"
  key_links:
    - from: "Test step document"
      to: "ERP purchase order import page"
      via: "upload_file action with .xlsx file from data/test-files/"
    - from: "Test step document"
      to: "ERP product management page"
      via: "upload_file action with .jpg/.png file from data/test-files/"
---

<objective>
Verify file upload capability in real ERP environment: Excel import on purchase order page (IMP-01) and image upload on product management page (IMP-02).

Purpose: Confirm that the infrastructure from Plan 01 (available_file_paths + Section 8 prompt) enables the Agent to successfully upload files in production ERP scenarios.

Output: Test steps document and human verification results document.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-import/54-CONTEXT.md
@.planning/phases/54-import/54-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From backend/core/agent_service.py (after Plan 01):
```python
def scan_test_files() -> list[str]:
    """Returns absolute paths for all files in data/test-files/."""
    test_dir = Path("data/test-files")
    if not test_dir.exists():
        return []
    return [str(f.resolve()) for f in sorted(test_dir.iterdir()) if f.is_file()]
```

From backend/agent/prompts.py (after Plan 01):
```python
## 8. 文件上传
遇到导入/上传按钮触发文件选择时 → 不要 click type="file" 的 input 元素（会被拦截），用 upload_file(index, '文件路径') 上传。
文件路径从 <available_file_paths> 标签中选择匹配类型的文件。Excel 导入选 .xlsx，图片上传选 .jpg/.png。
上传后等待文件名显示确认成功，不要用 evaluate 模拟文件选择。
```

Key browser-use behavior:
- click on file input elements is BLOCKED by browser-use (returns validation_error)
- Agent must use upload_file(index, path) action
- File paths must be in available_file_paths whitelist
- Agent sees <available_file_paths> XML tag in context
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create file upload test steps document</name>
  <files>docs/test-steps/采购-文件导入测试步骤.md</files>
  <read_first>
    .planning/phases/54-import/54-CONTEXT.md
    docs/test-steps/采购-表格交互测试步骤.md
  </read_first>
  <action>
Create a test steps document following the format from Phase 53's table interaction test steps (采购-表格交互测试步骤.md). The document should contain two scenarios:

**Scenario 1: Excel Import (IMP-01, per D-07)**
- Page: Purchase order import page (采购单导入)
- Pre-condition: Logged into ERP, test .xlsx file exists in data/test-files/
- Steps:
  1. Navigate to purchase order import page
  2. Find the import/upload button and click it
  3. Agent should identify the file input element
  4. Agent should use upload_file(index, 'absolute_path_to_xlsx') to upload
  5. Wait for file name to appear confirming upload success
  6. Click confirm/submit to complete import
- Expected: File uploaded successfully, import completed

**Scenario 2: Image Upload (IMP-02, per D-08)**
- Page: Product management - product detail image upload (商品管理 - 商品图片上传)
- Pre-condition: Logged into ERP, test .jpg file exists in data/test-files/
- Steps:
  1. Navigate to product management page
  2. Open a product detail or create new product
  3. Find the image upload area/button
  4. Agent should identify the file input element
  5. Agent should use upload_file(index, 'absolute_path_to_jpg') to upload
  6. Wait for image thumbnail to appear confirming upload success
- Expected: Image uploaded and preview visible

Include notes about:
- Agent should NOT click file input elements directly (browser-use blocks it)
- Agent should select matching file type (.xlsx for Excel, .jpg/.png for images) from available_file_paths
- If upload_file fails, check that file exists in data/test-files/

Commit with message "docs(54-02): create file upload test steps document".
  </action>
  <verify>
    <automated>test -f docs/test-steps/采购-文件导入测试步骤.md && grep -c "IMP-01\|IMP-02" docs/test-steps/采购-文件导入测试步骤.md</automated>
  </verify>
  <done>
    - docs/test-steps/采购-文件导入测试步骤.md exists
    - Contains IMP-01 Excel import scenario with steps
    - Contains IMP-02 image upload scenario with steps
    - References upload_file action and available_file_paths
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify Excel import and image upload in ERP</name>
  <files>N/A</files>
  <action>
Human verifies file upload capability in the ERP environment.

**Pre-requisite:** Ensure test files exist in data/test-files/ on the server. If not, create them:
- Create data/test-files/ directory: `mkdir -p data/test-files`
- Create a simple Excel file: use openpyxl to create import.xlsx with headers and one data row
- Create a test image: use Pillow to create product.jpg (100x100 pixel colored rectangle)

**Scenario 1: Excel Import (IMP-01)**
1. Start the backend server: `uv run uvicorn backend.api.main:app --reload --port 8080`
2. Open the frontend and create a test case: "在采购单导入页面，点击导入按钮，上传 data/test-files/ 目录中的 Excel 文件完成导入"
3. Run the test case
4. Observe the Agent execution: it should navigate to the import page, find the import button, identify the file input, and use upload_file action
5. Verify: file uploaded successfully, import dialog shows file name

**Scenario 2: Image Upload (IMP-02)**
1. Create a test case: "在商品管理页面，打开一个商品详情，上传 data/test-files/ 目录中的图片文件"
2. Run the test case
3. Observe: Agent navigates to product management, opens product detail, finds image upload, uses upload_file
4. Verify: image uploaded, thumbnail/preview visible

**Check Agent logs for:**
- `Available upload files: N files` confirms scan_test_files worked
- Agent uses `upload_file` action (not `click` on file input)
- If Agent gets "validation_error" for clicking file input, Section 8 negation instruction may need strengthening
  </action>
  <verify>Human confirms both IMP-01 and IMP-02 scenarios pass by typing "approved"</verify>
  <done>Human has verified both Excel import (IMP-01) and image upload (IMP-02) work correctly in ERP. Type "approved" or describe issues.</done>
</task>

<task type="auto">
  <name>Task 3: Record file upload verification results</name>
  <files>docs/test-steps/采购-文件导入验证结果.md</files>
  <read_first>
    docs/test-steps/采购-表格交互验证结果.md
  </read_first>
  <action>
Create a verification results document following the format from Phase 53's table interaction verification results (采购-表格交互验证结果.md).

Structure:
- Title: 采购-文件导入验证结果
- Date and environment info
- Per-scenario results:

| Scenario | Requirement | Status | Notes |
|----------|-------------|--------|-------|
| Excel Import (采购单导入) | IMP-01 | PASS/FAIL | [Agent strategy used, whether upload_file was used correctly] |
| Image Upload (商品图片上传) | IMP-02 | PASS/FAIL | [Agent strategy used, whether upload_file was used correctly] |

- Overall summary: N/2 scenarios passed
- Key observations about Agent behavior
- Any issues encountered and how they were resolved

Fill in results based on the human verification from the checkpoint task above.

Commit with message "test(54-02): record file upload verification results".
  </action>
  <verify>
    <automated>test -f docs/test-steps/采购-文件导入验证结果.md && grep -c "IMP-01\|IMP-02" docs/test-steps/采购-文件导入验证结果.md</automated>
  </verify>
  <done>
    - docs/test-steps/采购-文件导入验证结果.md exists
    - Contains per-scenario results for IMP-01 and IMP-02
    - Records actual pass/fail status from human verification
  </done>
</task>

</tasks>

<verification>
```bash
# Test steps document exists with both scenarios
grep "IMP-01" docs/test-steps/采购-文件导入测试步骤.md
grep "IMP-02" docs/test-steps/采购-文件导入测试步骤.md

# Verification results document exists with results
grep "IMP-01" docs/test-steps/采购-文件导入验证结果.md
grep "IMP-02" docs/test-steps/采购-文件导入验证结果.md

# Plan 01 infrastructure still works
uv run pytest backend/tests/unit/test_enhanced_prompt.py backend/tests/unit/test_agent_service.py -x -v
```
</verification>

<success_criteria>
1. Excel import (IMP-01) verified: Agent uses upload_file to upload .xlsx on purchase order import page
2. Image upload (IMP-02) verified: Agent uses upload_file to upload .jpg/.png on product management page
3. Test steps and verification results documents created
4. Human confirms both scenarios pass (or gap closure plan created for failures)
</success_criteria>

<output>
After completion, create `.planning/phases/54-import/54-02-SUMMARY.md`
</output>
