---
phase: 26-e2e-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [e2e/tests/assertion-flow.spec.ts]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "E2E test file exists with assertion configuration and execution flow tests"
    - "Tests verify QA can create task with assertion and run end-to-end"
    - "Tests verify assertion results display in report page"
    - "Tests verify multiple assertions execute (non-fail-fast)"
  artifacts:
    - path: "e2e/tests/assertion-flow.spec.ts"
      provides: "E2E tests for assertion workflow"
      min_lines: 100
  key_links:
    - from: "e2e/tests/assertion-flow.spec.ts"
      to: "/tasks"
      via: "page.goto"
      pattern: "page.goto.*tasks"
    - from: "e2e/tests/assertion-flow.spec.ts"
      to: "AssertionSelector"
      via: "click button:has-text('添加断言')"
      pattern: "添加断言"
---

<objective>
Create E2E test file for assertion configuration and execution flow.

Purpose: Verify the complete assertion workflow from task creation to report display through automated E2E tests.
Output: e2e/tests/assertion-flow.spec.ts with comprehensive assertion flow tests.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-e2e-testing/26-CONTEXT.md
@.planning/phases/26-e2e-testing/26-RESEARCH.md

<interfaces>
<!-- Key interfaces from codebase that executor needs -->

From e2e/tests/smoke.spec.ts:
```typescript
test.setTimeout(180000) // 3 minutes for AI execution
await page.waitForSelector('text=已完成, text=失败', { timeout: 120000 })
```

From e2e/tests/full-flow.spec.ts:
```typescript
await page.click('button:has-text("新建任务")')
await page.fill('[name="name"]', 'E2E Test Task')
await page.click('button:has-text("业务断言")') // Tab switch
await page.click('button:has-text("添加断言")')
await expect(page.locator('text=Select Assertion Methods')).toBeVisible()
```

From frontend/src/components/Report/ApiAssertionResults.tsx:
```typescript
// Pass status UI elements
<div className="bg-green-50 border-green-200">
  <CheckCircle className="w-5 h-5 text-green-500" />
</div>

// Fail status UI elements
<div className="bg-red-50 border-red-200">
  <XCircle className="w-5 h-5 text-red-500" />
</div>
```

From frontend/src/pages/ReportDetail.tsx:
```typescript
// Assertion results section
<ApiAssertionResults
  results={data.api_assertion_results}
  passRate={data.api_pass_rate || 'N/A'}
/>
```

From frontend/src/types/index.ts:
```typescript
interface AssertionConfig {
  className: string      // e.g., "PcAssert"
  methodName: string     // e.g., "attachment_inventory_list_assert"
  headers: string        // e.g., "main"
  data: string           // e.g., "main"
  params: Record<string, number | string>  // i, j, k etc.
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test for assertion configuration and execution flow</name>
  <files>e2e/tests/assertion-flow.spec.ts</files>
  <read_first>
    - e2e/tests/smoke.spec.ts
    - e2e/tests/full-flow.spec.ts
    - e2e/tests/data-method-execution.spec.ts
    - frontend/src/components/TaskModal/TaskForm.tsx
    - frontend/src/components/TaskModal/AssertionSelector.tsx
    - frontend/src/components/Report/ApiAssertionResults.tsx
  </read_first>
  <action>
    Create `e2e/tests/assertion-flow.spec.ts` with the following test structure:

    1. **Test: "single assertion success - task creation with assertion config"**
       - Navigate to /tasks
       - Click "New Task" button
       - Fill task name: "E2E Assertion Success Test"
       - Fill target_url with ERP_BASE_URL env var (skip test if not set)
       - Switch to "Business Assertions" tab (click button with text "业务断言")
       - Click "Add Assertion" button (button:has-text("添加断言"))
       - Verify AssertionSelector modal opens (expect text "Select Assertion Methods" visible)
       - Wait 2 seconds for methods to load
       - Select first available assertion method (click first checkbox)
       - Verify "Selected" text appears
       - Click "Confirm" button
       - Verify assertion card appears (border-orange-200 class)
       - Create the task
       - Verify task appears in list
       - Execute the task
       - Wait for completion (text "已完成" or "失败" with 180000ms timeout)
       - Navigate to report
       - Verify report page displays assertion results section

    2. **Test: "single assertion failure - displays fail status in report"**
       - Create task with assertion configured with intentionally failing params
       - Use filter params (i, j, k) that return no data to trigger failure
       - Execute task
       - Navigate to report
       - Verify assertion result card shows fail status (bg-red-50 border-red-200)
       - Verify XCircle icon visible (text-red-500)

    3. **Test: "multiple assertions execute independently - non fail-fast"**
       - Create task with 2+ assertions
       - Execute task
       - Verify BOTH assertion results appear in report (count >= 2)
       - This verifies non-fail-fast behavior

    Use these patterns from existing tests:
    - `test.setTimeout(300000)` for 5-minute timeout
    - `test.skip()` if ERP_BASE_URL not configured
    - Text-based selectors: `page.locator('text=断言结果')`
    - Wait patterns: `page.waitForSelector('text=已完成', { timeout: 180000 })`
  </action>
  <verify>
    <automated>npx playwright test e2e/tests/assertion-flow.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - e2e/tests/assertion-flow.spec.ts exists
    - File contains test.describe('Assertion Flow Tests')
    - File contains test 'single assertion success'
    - File contains test 'single assertion failure'
    - File contains test 'multiple assertions'
    - Each test has test.setTimeout(180000) or higher
    - Each test checks for ERP_BASE_URL env var
    - Each test navigates to /tasks
    - Each test clicks "业务断言" tab
    - Each test opens AssertionSelector modal
    - Report verification uses bg-green-50/border-green-200 for pass status
    - Report verification uses bg-red-50/border-red-200 for fail status
  </acceptance_criteria>
  <done>
    E2E test file exists with 3 tests covering assertion configuration, execution, and report display verification. All tests use proper timeouts, ERP environment checks, and text-based selectors.
  </done>
</task>

</tasks>

<verification>
Run the E2E test suite:
```bash
npx playwright test e2e/tests/assertion-flow.spec.ts --reporter=list
```

Tests should pass when ERP environment is properly configured.
</verification>

<success_criteria>
- E2E test file created with 3 test cases
- Tests cover assertion configuration, execution, and report display
- Tests use appropriate timeouts (180000ms+)
- Tests skip gracefully when ERP_BASE_URL not configured
</success_criteria>

<output>
After completion, create `.planning/phases/26-e2e-testing/26-01-SUMMARY.md`
</output>
