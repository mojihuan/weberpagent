---
phase: 67-基础层-行标识检测与失败追踪状态
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/stall_detector.py
  - backend/tests/unit/test_stall_detector_phase67.py
autonomous: true
requirements:
  - RECOV-01

must_haves:
  truths:
    - "detect_failure_mode(action_name='click', same dom_hash) 返回 FailureDetectionResult(failure_mode='click_no_effect')"
    - "detect_failure_mode(evaluation 包含 '错误列') 返回 FailureDetectionResult(failure_mode='wrong_column')"
    - "detect_failure_mode(action_name='input', evaluation 包含 '无法输入') 返回 FailureDetectionResult(failure_mode='edit_not_active')"
    - "detect_failure_mode(正常操作) 返回 FailureDetectionResult(failure_mode=None)"
    - "FailureDetectionResult 是 frozen dataclass，设置属性会抛出 FrozenInstanceError"
  artifacts:
    - path: "backend/agent/stall_detector.py"
      provides: "FailureDetectionResult dataclass, detect_failure_mode 方法"
      contains: "class FailureDetectionResult"
    - path: "backend/tests/unit/test_stall_detector_phase67.py"
      provides: "单元测试覆盖 RECOV-01"
      contains: "test_detect_failure_mode"
  key_links:
    - from: "backend/agent/stall_detector.py"
      to: "StallDetector"
      via: "detect_failure_mode 作为独立方法"
      pattern: "def detect_failure_mode\\(self"
---

<objective>
在 stall_detector.py 中新增 FailureDetectionResult frozen dataclass 和 detect_failure_mode() 方法。

Purpose: 为 ERP 表格交互提供三种失败模式检测能力（点击无 DOM 变化、误点错误列、编辑态未激活），供 Phase 69 step_callback 集成使用。
Output: FailureDetectionResult immutable dataclass、detect_failure_mode() 方法、>= 80% 单元测试覆盖
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-基础层-行标识检测与失败追踪状态/67-CONTEXT.md
@.planning/phases/67-基础层-行标识检测与失败追踪状态/67-RESEARCH.md
@backend/agent/stall_detector.py
@backend/tests/unit/test_stall_detector.py

<interfaces>
<!-- Key types and contracts from stall_detector.py that executor needs -->

From backend/agent/stall_detector.py:
```python
# Frozen dataclass pattern (line 20-23)
@dataclass(frozen=True)
class StallResult:
    should_intervene: bool
    message: str

# Internal state record (line 28-34)
@dataclass
class _StepRecord:
    action_name: str
    target_index: int | None
    evaluation: str
    dom_hash: str

# Main detector class (line 38-51)
@dataclass
class StallDetector:
    max_consecutive_failures: int = 2
    max_stagnant_steps: int = 3
    _history: list[_StepRecord] = field(default_factory=list, repr=False)

    def check(self, action_name, target_index, evaluation, dom_hash) -> StallResult:
        # Records step and checks stall conditions
```

From backend/tests/unit/test_stall_detector.py:
```python
# Test pattern: direct instantiation + method calls
detector = StallDetector(max_consecutive_failures=2)
result = detector.check(action_name="click", target_index=6246,
                        evaluation="Failed", dom_hash="abc123")
assert result.should_intervene is True

# Frozen test pattern (line 188-191)
result = StallResult(should_intervene=True, message="test")
with pytest.raises(dataclasses.FrozenInstanceError):
    result.should_intervene = False
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 FailureDetectionResult 和 detect_failure_mode()</name>
  <files>backend/agent/stall_detector.py, backend/tests/unit/test_stall_detector_phase67.py</files>
  <read_first>
    backend/agent/stall_detector.py
    backend/tests/unit/test_stall_detector.py
    .planning/phases/67-基础层-行标识检测与失败追踪状态/67-CONTEXT.md
    .planning/phases/67-基础层-行标识检测与失败追踪状态/67-RESEARCH.md
  </read_first>
  <behavior>
    - click_no_effect: action_name="click", dom_hash_before="abc", dom_hash_after="abc" -> FailureDetectionResult(failure_mode="click_no_effect", details={"target_index": 5, "dom_hash": "abc"})
    - wrong_column (中文): evaluation="点了错误列" -> FailureDetectionResult(failure_mode="wrong_column", details={"keywords_matched": ["错误列"], ...})
    - wrong_column (英文): evaluation="clicked wrong column" -> FailureDetectionResult(failure_mode="wrong_column", details={"keywords_matched": ["wrong column"], ...})
    - wrong_column (非目标列): evaluation="非目标列误点了" -> FailureDetectionResult(failure_mode="wrong_column")
    - edit_not_active (中文): action_name="input", evaluation="元素不可操作" -> FailureDetectionResult(failure_mode="edit_not_active")
    - edit_not_active (英文): action_name="input", evaluation="not editable" -> FailureDetectionResult(failure_mode="edit_not_active")
    - edit_not_active (无法输入): action_name="input", evaluation="无法输入到目标" -> FailureDetectionResult(failure_mode="edit_not_active")
    - no failure: action_name="click", dom_hash_before="abc", dom_hash_after="def", evaluation="成功点击" -> FailureDetectionResult(failure_mode=None, details={})
    - no failure: action_name="input", evaluation="成功输入" -> FailureDetectionResult(failure_mode=None, details={})
    - frozen: FailureDetectionResult(failure_mode="test", details={}) -> setattr raises FrozenInstanceError
    - wrong_column 优先于 click_no_effect: action_name="click", same hash, 但 evaluation 包含 "错误列" -> wrong_column（因为 wrong_column 检查在 click_no_effect 之前）
  </behavior>
  <action>
    **先写测试文件 `backend/tests/unit/test_stall_detector_phase67.py`**

    测试类结构：
    ```python
    import dataclasses
    import pytest
    from backend.agent.stall_detector import StallDetector, FailureDetectionResult
    ```

    TestDetectFailureMode 类：
    - 每个测试创建新的 StallDetector() 实例
    - 直接调用 detect_failure_mode(action_name, target_index, evaluation, dom_hash_before, dom_hash_after)
    - 测试三种失败模式的触发条件和不触发条件
    - 测试 FrozenInstanceError

    测试用例（按 behavior 列表）：
    1. test_click_no_effect: action_name="click", dom_hash_before="abc", dom_hash_after="abc", evaluation="ok" -> failure_mode=="click_no_effect"
    2. test_click_no_effect_different_hash: action_name="click", dom_hash_before="abc", dom_hash_after="def" -> failure_mode is None
    3. test_wrong_column_chinese: evaluation="点了错误列的元素" -> failure_mode=="wrong_column", details 包含 "keywords_matched"
    4. test_wrong_column_english: evaluation="clicked wrong column" -> failure_mode=="wrong_column"
    5. test_wrong_column_non_target: evaluation="非目标列" -> failure_mode=="wrong_column"
    6. test_edit_not_active_chinese: action_name="input", evaluation="元素不可操作" -> failure_mode=="edit_not_active"
    7. test_edit_not_active_english: action_name="input", evaluation="not editable element" -> failure_mode=="edit_not_active"
    8. test_edit_not_active_cannot_type: action_name="input", evaluation="无法输入" -> failure_mode=="edit_not_active"
    9. test_no_failure_normal_click: action_name="click", hashes differ, evaluation="成功" -> failure_mode is None
    10. test_no_failure_normal_input: action_name="input", evaluation="成功输入" -> failure_mode is None
    11. test_frozen_dataclass: 尝试设置属性 -> FrozenInstanceError

    **再写实现到 `backend/agent/stall_detector.py`**

    1. 在 `StallResult` dataclass 之后（约 line 24），添加：

    ```python
    @dataclass(frozen=True)
    class FailureDetectionResult:
        """失败模式检测结果（不可变）。

        Per D-02: 与 StallResult 平级的 frozen dataclass。
        failure_mode=None 表示未检测到失败模式。
        details 包含诊断信息（关键词匹配、hash 比对结果等）。
        """
        failure_mode: str | None
        details: dict
    ```

    2. 在文件顶部 `FAILURE_KEYWORDS` 之后（约 line 17），添加关键词正则：

    ```python
    _WRONG_COLUMN_KEYWORDS = re.compile(
        r"wrong.?column|错误列|误点|非目标列|clicked.*wrong",
        re.IGNORECASE,
    )

    _EDIT_NOT_ACTIVE_KEYWORDS = re.compile(
        r"not.?editable|无法输入|元素不可操作|cannot.?type|not.?interactable",
        re.IGNORECASE,
    )
    ```

    3. 在 StallDetector 类中（`_check_stagnant_dom` 方法之后），添加 `detect_failure_mode` 方法：

    ```python
    def detect_failure_mode(
        self,
        action_name: str,
        target_index: int | None,
        evaluation: str,
        dom_hash_before: str,
        dom_hash_after: str,
    ) -> FailureDetectionResult:
        """检测三种 ERP 表格交互失败模式。

        Per D-03: 与 check() 平级的独立方法，不修改 _history。
        检测顺序: wrong_column -> edit_not_active -> click_no_effect。

        Args:
            action_name: 操作类型（click, input 等）。
            target_index: 目标元素索引。
            evaluation: Agent 评估文本。
            dom_hash_before: 操作前 DOM 哈希。
            dom_hash_after: 操作后 DOM 哈希。

        Returns:
            FailureDetectionResult（frozen），failure_mode=None 表示无失败。
        """
        # 检测 wrong_column（evaluation 关键词匹配）
        wrong_match = _WRONG_COLUMN_KEYWORDS.search(evaluation)
        if wrong_match:
            return FailureDetectionResult(
                failure_mode="wrong_column",
                details={
                    "keywords_matched": [wrong_match.group()],
                    "evaluation_snippet": evaluation[:100],
                    "target_index": target_index,
                },
            )

        # 检测 edit_not_active（input 操作 + evaluation 关键词）
        if action_name == "input":
            edit_match = _EDIT_NOT_ACTIVE_KEYWORDS.search(evaluation)
            if edit_match:
                return FailureDetectionResult(
                    failure_mode="edit_not_active",
                    details={
                        "keywords_matched": [edit_match.group()],
                        "evaluation_snippet": evaluation[:100],
                        "target_index": target_index,
                    },
                )

        # 检测 click_no_effect（click 操作 + DOM 无变化）
        if action_name == "click" and dom_hash_before == dom_hash_after:
            return FailureDetectionResult(
                failure_mode="click_no_effect",
                details={
                    "target_index": target_index,
                    "dom_hash": dom_hash_before,
                },
            )

        # 无失败
        return FailureDetectionResult(failure_mode=None, details={})
    ```

    **实现要点：**
    - 检测顺序：wrong_column -> edit_not_active -> click_no_effect。wrong_column 优先因为 evaluation 中的"误点"信息比 dom_hash 比对更有诊断价值
    - 不修改 _history — detect_failure_mode 是纯检测方法，不做状态记录
    - evaluation_snippet 截取前 100 字符避免过长
    - details 中的 keywords_matched 是列表格式，方便下游扩展
    </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/unit/test_stall_detector_phase67.py -v</automated>
  </verify>
  <done>
    - `FailureDetectionResult` 是 frozen dataclass，包含 `failure_mode: str | None` 和 `details: dict`
    - `detect_failure_mode()` 作为 StallDetector 的独立方法，签名包含 action_name, target_index, evaluation, dom_hash_before, dom_hash_after
    - 三种失败模式检测：click_no_effect（dom_hash 比对）、wrong_column（关键词匹配）、edit_not_active（input + 关键词）
    - 无失败返回 `FailureDetectionResult(failure_mode=None, details={})`
    - frozen dataclass 设置属性抛出 FrozenInstanceError
    - 所有测试通过
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/unit/test_stall_detector_phase67.py -v
```
所有测试通过，覆盖 FailureDetectionResult 和 detect_failure_mode 三种模式。
</verification>

<success_criteria>
- backend/agent/stall_detector.py 包含 `FailureDetectionResult` frozen dataclass
- backend/agent/stall_detector.py StallDetector 类包含 `detect_failure_mode` 方法
- `_WRONG_COLUMN_KEYWORDS` 正则匹配 "wrong column"、"错误列"、"误点"、"非目标列"
- `_EDIT_NOT_ACTIVE_KEYWORDS` 正则匹配 "not editable"、"无法输入"、"元素不可操作"
- `uv run pytest backend/tests/unit/test_stall_detector_phase67.py -v` 全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/67-基础层-行标识检测与失败追踪状态/67-02-SUMMARY.md`
</output>
