---
phase: 67-基础层-行标识检测与失败追踪状态
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/dom_patch.py
  - backend/tests/unit/test_dom_patch_phase67.py
autonomous: true
requirements:
  - ROW-01
  - ANTI-01

must_haves:
  truths:
    - "给定包含 td 子节点文本 I352017041234567 的 tr 节点，_detect_row_identity() 返回 I352017041234567"
    - "给定不包含 IMEI 格式文本的 tr 节点，_detect_row_identity() 返回 None"
    - "_failure_tracker 以 backend_node_id 为键存储 {count, last_error, mode}，update 能新建和累加"
    - "reset_failure_tracker() 清空所有记录，在 apply_dom_patch() 中独立于 _PATCHED 幂等保护被调用"
  artifacts:
    - path: "backend/agent/dom_patch.py"
      provides: "_detect_row_identity, _failure_tracker, update_failure_tracker, reset_failure_tracker"
      contains: "def _detect_row_identity"
    - path: "backend/tests/unit/test_dom_patch_phase67.py"
      provides: "单元测试覆盖 ROW-01 和 ANTI-01"
      contains: "test_detect_row_identity"
  key_links:
    - from: "backend/agent/dom_patch.py"
      to: "apply_dom_patch()"
      via: "reset_failure_tracker() 调用"
      pattern: "reset_failure_tracker\\(\\)"
---

<objective>
在 dom_patch.py 中实现行标识检测函数和失败追踪状态管理。

Purpose: 为 Phase 68 DOM Patch 增强（行标识注释注入、失败动态标注）提供基础检测和状态管理能力。
Output: _detect_row_identity() 函数、_failure_tracker 字典 + update/reset 函数、>= 80% 单元测试覆盖
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-基础层-行标识检测与失败追踪状态/67-CONTEXT.md
@.planning/phases/67-基础层-行标识检测与失败追踪状态/67-RESEARCH.md
@backend/agent/dom_patch.py
@backend/tests/unit/test_dom_patch.py

<interfaces>
<!-- Key types and contracts from dom_patch.py that executor needs -->

From backend/agent/dom_patch.py:
```python
# Module-level state pattern (line 14)
_PATCHED = False

# DOM traversal pattern used by _is_textual_td_cell (line 37-81)
def _is_textual_td_cell(node) -> bool:
    # node = SimplifiedNode
    # node.original_node = AccessibilityNode (EnhancedDOMTreeNode)
    # original_node.tag_name -> "td", "tr", "input"
    # original_node.parent_node -> AccessibilityNode parent
    # original_node.get_all_children_text() -> str (recursive TEXT_NODE collection)
    # original_node.attributes -> dict (class, placeholder, etc.)
    # original_node.children -> list of child AccessibilityNodes

# ERP placeholders (line 21-27)
_ERP_TABLE_CELL_PLACEHOLDERS = frozenset({"销售金额", "物流费用", "备注", "单号", "数量"})

# apply_dom_patch entry point (line 214-247)
def apply_dom_patch() -> None:
    global _PATCHED
    if _PATCHED:
        logger.debug("dom_patch: already applied, skipping")
        return
    # ... patches ...
    _PATCHED = True
```

From backend/tests/unit/test_dom_patch.py:
```python
# Mock node pattern (established in existing tests)
class MockOriginalNode:
    def __init__(self, class_name: str = ""):
        self.attributes = {"class": class_name} if class_name else {}
        self.tag_name = "span"

class MockSimplifiedNode:
    def __init__(self, class_name: str = "", children: list | None = None):
        self.original_node = MockOriginalNode(class_name)
        self.children = children or []
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 _detect_row_identity() 和 _failure_tracker 状态管理</name>
  <files>backend/agent/dom_patch.py, backend/tests/unit/test_dom_patch_phase67.py</files>
  <read_first>
    backend/agent/dom_patch.py
    backend/tests/unit/test_dom_patch.py
    .planning/phases/67-基础层-行标识检测与失败追踪状态/67-CONTEXT.md
    .planning/phases/67-基础层-行标识检测与失败追踪状态/67-RESEARCH.md
  </read_first>
  <behavior>
    - _detect_row_identity: 给定 mock node（td inside tr，td 的 get_all_children_text 返回 "I352017041234567"），返回 "I352017041234567"
    - _detect_row_identity: 给定 mock node（td inside tr，td 的 get_all_children_text 返回 "普通文本"），返回 None
    - _detect_row_identity: 给定非 td 的 node（如 span），返回 None
    - _detect_row_identity: 给定 td 但 parent 非 tr 的 node，返回 None
    - _detect_row_identity: 给定 original_node 为 None 的 node，返回 None
    - update_failure_tracker: 首次调用 update_failure_tracker("id1", "error1", "mode1")，tracker["id1"]["count"] == 1
    - update_failure_tracker: 连续 3 次调用同一 id，tracker["id1"]["count"] == 3，last_error 和 mode 更新为最后一次值
    - update_failure_tracker: 不同 id 各自独立计数
    - reset_failure_tracker: 添加条目后调用 reset，len(tracker) == 0
    - reset 独立于 _PATCHED: apply_dom_patch() 两次调用后 tracker 仍被重置（第二次调用走 if _PATCHED 分支但仍调用 reset）
  </behavior>
  <action>
    **先写测试文件 `backend/tests/unit/test_dom_patch_phase67.py`**

    创建 MockAccessibilityNode 类用于测试 _detect_row_identity：
    ```python
    class MockAccessibilityNode:
        """Mock AccessibilityNode for _detect_row_identity tests."""
        def __init__(self, tag_name: str = "", children_text: str = "",
                     parent=None, children=None):
            self.tag_name = tag_name
            self._children_text = children_text
            self.parent_node = parent
            self.children = children or []

        def get_all_children_text(self) -> str:
            return self._children_text
    ```

    测试 _detect_row_identity（需要 mock tr 节点带 td 子节点）：
    - 创建 mock 结构：tr (AccessibilityNode) -> td (AccessibilityNode，get_all_children_text 返回含 IMEI 文本) -> 然后传入 td 对应的 SimplifiedNode 的 original_node 设置 parent_node 为 tr
    - 注意：_detect_row_identity 接收的 node 是 SimplifiedNode，其 original_node 是 AccessibilityNode。函数需要找到 original_node 的 parent_node（即 tr），然后遍历 tr 的 children（即各个 td），对每个 td 调用 get_all_children_text() 进行正则匹配
    - 正则：`I\d{15}` — 匹配如 "I352017041234567"、"I01784004409597"
    - 返回第一个匹配的字符串或 None

    测试 _failure_tracker：
    - 导入 `_failure_tracker`, `update_failure_tracker`, `reset_failure_tracker`
    - 每个测试前调用 reset_failure_tracker() 确保干净状态
    - 测试新建记录、累加计数、不同 id 独立、reset 清空

    测试 reset 独立于 _PATCHED：
    - patch 所有 patch 函数，调用 apply_dom_patch() 一次（_PATCHED=True）
    - 调用 update_failure_tracker 添加条目
    - 再次调用 apply_dom_patch()（走 if _PATCHED 分支）
    - 断言 _failure_tracker 已被清空

    **再写实现到 `backend/agent/dom_patch.py`**

    1. 在文件顶部（`_ERP_NUMERIC_CELL_PATTERNS` 之后）添加：
    ```python
    import re

    # IMEI/商品编号格式：I + 15位数字
    _ROW_IDENTITY_PATTERN = re.compile(r"I\d{15}")

    # 失败追踪状态：以 backend_node_id 为键
    _failure_tracker: dict[str, dict] = {}
    ```

    2. 添加 `_detect_row_identity(node) -> str | None`：
    ```python
    def _detect_row_identity(node) -> str | None:
        """从 ERP 表格行中检测行标识（IMEI/商品编号）。

        遍历 node 所在 <tr> 的所有子 <td>，对每个 <td> 的文本内容
        应用正则 I\\d{15} 匹配，返回第一个匹配的字符串。

        Args:
            node: SimplifiedNode 实例。

        Returns:
            匹配到的 IMEI 字符串，或 None。
        """
        original = getattr(node, "original_node", None)
        if original is None:
            return None

        # 找到 <tr> 父级
        parent = getattr(original, "parent_node", None)
        if parent is None:
            return None
        parent_tag = getattr(parent, "tag_name", None)
        if not parent_tag or parent_tag.lower() != "tr":
            return None

        # 遍历 <tr> 的子节点（即各个 <td>）
        tr_children = getattr(parent, "children", [])
        for child in tr_children:
            child_tag = getattr(child, "tag_name", "")
            if child_tag.lower() != "td":
                continue
            text = child.get_all_children_text()
            if not text:
                continue
            match = _ROW_IDENTITY_PATTERN.search(text)
            if match:
                return match.group()

        return None
    ```

    3. 添加 `update_failure_tracker` 和 `reset_failure_tracker`：
    ```python
    def update_failure_tracker(backend_node_id: str, error: str, mode: str) -> None:
        """更新失败追踪记录。

        Args:
            backend_node_id: 目标元素的 backend_node_id。
            error: 失败描述信息。
            mode: 失败模式（click_no_effect / wrong_column / edit_not_active）。
        """
        global _failure_tracker
        if backend_node_id in _failure_tracker:
            _failure_tracker[backend_node_id]["count"] += 1
            _failure_tracker[backend_node_id]["last_error"] = error
            _failure_tracker[backend_node_id]["mode"] = mode
        else:
            _failure_tracker[backend_node_id] = {
                "count": 1,
                "last_error": error,
                "mode": mode,
            }

    def reset_failure_tracker() -> None:
        """清空失败追踪状态。每次 run 开始时调用。"""
        global _failure_tracker
        _failure_tracker = {}
    ```

    4. 修改 `apply_dom_patch()` 函数：
    - 在 `if _PATCHED:` 分支中，return 之前调用 `reset_failure_tracker()`
    - 这样每次 run（即每次调用 apply_dom_patch）都会重置 tracker，无论 patch 是否已应用

    ```python
    def apply_dom_patch() -> None:
        global _PATCHED
        if _PATCHED:
            reset_failure_tracker()  # 每次 run 重置追踪状态（独立于 _PATCHED）
            logger.debug("dom_patch: already applied, skipping")
            return
        # ... 原有逻辑不变 ...
    ```
    </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/unit/test_dom_patch_phase67.py -v</automated>
  </verify>
  <done>
    - `_detect_row_identity()` 能从 mock tr/td 结构中提取 "I352017041234567"
    - `_detect_row_identity()` 对无 IMEI 文本、非 td、非 tr 父级、None original_node 均返回 None
    - `update_failure_tracker()` 能新建记录和累加 count
    - `reset_failure_tracker()` 能清空所有记录
    - `reset_failure_tracker()` 在 `apply_dom_patch()` 中独立于 `_PATCHED` 被调用
    - 所有测试通过
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/unit/test_dom_patch_phase67.py -v
```
所有测试通过，覆盖 _detect_row_identity、_failure_tracker、update_failure_tracker、reset_failure_tracker。
</verification>

<success_criteria>
- backend/agent/dom_patch.py 包含 `_detect_row_identity` 函数，正则 `I\d{15}` 匹配
- backend/agent/dom_patch.py 包含 `_failure_tracker` 模块级变量、`update_failure_tracker` 和 `reset_failure_tracker` 函数
- reset_failure_tracker() 在 apply_dom_patch() 的 `if _PATCHED:` 分支中被调用
- `uv run pytest backend/tests/unit/test_dom_patch_phase67.py -v` 全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/67-基础层-行标识检测与失败追踪状态/67-01-SUMMARY.md`
</output>
