---
phase: 53-prompt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/prompts.py
  - backend/tests/unit/test_enhanced_prompt.py
autonomous: true
requirements:
  - TBL-01
  - TBL-02
  - TBL-03
  - TBL-04

must_haves:
  truths:
    - "ENHANCED_SYSTEM_MESSAGE 包含表格交互指导段落（第 7 段）"
    - "表格段落覆盖 checkbox 定位（thead/tbody 区分全选/单选）、超链接文本点击、图标按钮 title/aria-label 定位三种场景"
    - "表格段落包含否定指令，防止常见表格操作错误"
    - "表格段落不超过 10 行"
    - "单元测试验证表格交互关键词存在"
  artifacts:
    - path: "backend/agent/prompts.py"
      provides: "ENHANCED_SYSTEM_MESSAGE 第 7 段表格交互指导"
      contains: "## 7. 表格交互"
    - path: "backend/tests/unit/test_enhanced_prompt.py"
      provides: "test_contains_table_interaction_keywords 等测试方法"
      contains: "test_contains_table_interaction_keywords"
  key_links:
    - from: "backend/tests/unit/test_enhanced_prompt.py"
      to: "backend/agent/prompts.py"
      via: "from backend.agent.prompts import ENHANCED_SYSTEM_MESSAGE"
      pattern: "from backend.agent.prompts import"
---

<objective>
扩展 ENHANCED_SYSTEM_MESSAGE 添加第 7 段表格交互指导，覆盖 checkbox 定位（thead 全选 / tbody 单选）、超链接文本点击、图标按钮 title/aria-label 定位三种场景，并加入否定指令。

Purpose: ERP 系统中表格操作频繁，Agent 需要准确定位表格中的 checkbox、超链接和图标按钮。当前 prompt 无此指导，Agent 经常无法区分全选/单选 checkbox，也无法正确点击表格中的操作按钮。
Output: 更新的 prompts.py（含第 7 段）+ 扩展的 test_enhanced_prompt.py（含表格交互关键词测试）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-prompt/53-CONTEXT.md
@backend/agent/prompts.py
@backend/tests/unit/test_enhanced_prompt.py

<interfaces>
<!-- Current ENHANCED_SYSTEM_MESSAGE structure — 6 sections, will add section 7 -->

```python
# From backend/agent/prompts.py
ENHANCED_SYSTEM_MESSAGE = """
## 1. 表格编辑模式
...
## 2. 失败恢复强制规则
...
## 3. 字段填写后验证
...
## 4. 提交前校验
...
## 5. 元素识别与表单策略
...
## 6. 键盘操作
...
"""
```

<!-- Test pattern — keyword-based assertions (per D-10) -->
```python
# From backend/tests/unit/test_enhanced_prompt.py
def test_contains_keyboard_operation_keywords(self):
    lower = ENHANCED_SYSTEM_MESSAGE.lower()
    assert "send_keys" in lower
    assert "enter" in lower
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加表格交互关键词测试（RED 阶段）</name>
  <files>backend/tests/unit/test_enhanced_prompt.py</files>
  <read_first>
    - backend/tests/unit/test_enhanced_prompt.py（现有测试模式，复制其关键词断言风格）
    - backend/agent/prompts.py（确认 ENHANCED_SYSTEM_MESSAGE 当前没有表格交互内容）
  </read_first>
  <behavior>
    - Test 1: test_contains_table_interaction_keywords — ENHANCED_SYSTEM_MESSAGE 包含 "checkbox"
    - Test 2: test_contains_table_interaction_keywords — ENHANCED_SYSTEM_MESSAGE 包含 "thead" 和 "tbody"
    - Test 3: test_contains_table_interaction_keywords — ENHANCED_SYSTEM_MESSAGE 包含 "aria-label" 和 "title"（用于图标按钮定位）
    - Test 4: test_contains_table_interaction_keywords — ENHANCED_SYSTEM_MESSAGE 包含表格交互段落标题 "表格交互"
    - Test 5: test_table_section_line_count — 表格交互段落不超过 10 行（per D-03）
  </behavior>
  <action>
    在 test_enhanced_prompt.py 的 TestEnhancedPrompt 类末尾添加两个测试方法（per D-10 结构+关键词检查模式）：

    1. test_contains_table_interaction_keywords — 验证 ENHANCED_SYSTEM_MESSAGE 包含以下关键词（小写比较）：
       - "checkbox"（TBL-01/TBL-02 checkbox 定位）
       - "thead"（TBL-02 全选 checkbox 在 thead 中，per D-04）
       - "tbody"（TBL-01 单选 checkbox 在 tbody 行中，per D-04）
       - "aria-label"（TBL-04 图标按钮通过 aria-label 定位，per D-06）
       - "title"（TBL-04 图标按钮通过 title 定位，per D-06）
       同时验证包含中文段落标题 "表格交互"。

    2. test_table_section_line_count — 提取 "## 7. 表格交互" 到下一个 "##" 之间的行（或到字符串末尾），验证该段不超过 10 行（per D-03 增量控制在 10 行以内）。参考现有的 test_keyboard_section_line_count 实现模式。

    遵循现有测试风格：lower = ENHANCED_SYSTEM_MESSAGE.lower() 然后用 assert "keyword" in lower。
    同时需要更新 test_line_count_under_60 中的行数上限，考虑新增段落后的总行数，调整为 70 行。

    先只写测试，不改 prompts.py，确保测试失败（RED 阶段）。
  </action>
  <acceptance_criteria>
    - test_enhanced_prompt.py 包含方法 test_contains_table_interaction_keywords
    - test_enhanced_prompt.py 包含方法 test_table_section_line_count
    - test_contains_table_interaction_keywords 断言 checkbox、thead、tbody、aria-label、title、"表格交互" 6 个关键词
    - test_table_section_line_count 查找 "## 7." 段落并验证行数 <= 10
    - test_line_count_under_60 中行数上限已调整为 70
    - 运行测试时 test_contains_table_interaction_keywords 和 test_table_section_line_count 必须失败（RED）
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_enhanced_prompt.py::TestEnhancedPrompt::test_contains_table_interaction_keywords backend/tests/unit/test_enhanced_prompt.py::TestEnhancedPrompt::test_table_section_line_count -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>两个新测试方法已添加，运行后因 ENHANCED_SYSTEM_MESSAGE 尚无表格交互段落而失败（RED）</done>
</task>

<task type="auto">
  <name>Task 2: 添加表格交互段落到 ENHANCED_SYSTEM_MESSAGE（GREEN 阶段）</name>
  <files>backend/agent/prompts.py</files>
  <read_first>
    - backend/agent/prompts.py（现有 6 段结构，在 "## 6. 键盘操作" 段落末尾之后追加第 7 段）
  </read_first>
  <action>
    在 ENHANCED_SYSTEM_MESSAGE 的第 6 段（"## 6. 键盘操作"）末尾、结束三引号 """ 之前，追加第 7 段表格交互指导。

    精确追加内容（per D-02 场景-动作对格式，D-03 中文精简指令式，D-04 DOM 位置区分 checkbox，D-05 文本定位超链接，D-06 title/aria-label 定位图标，D-07 否定指令）：

    ```
    ## 7. 表格交互
    表头全选 → click thead 中的 checkbox；行选择 → click tbody 行中的 checkbox。
    表格超链接（订单号、编号等）→ click 可见文本匹配的 <a> 元素，无需 find_elements 预查找。
    行末操作按钮（编辑、删除等）→ click title 或 aria-label 属性匹配的元素。
    不要通过索引或坐标定位表格元素，不要假设 checkbox 固定在某一列。
    ```

    注意事项：
    - 在第 6 段最后一行后加空行，再添加 "## 7."
    - 保留 ENHANCED_SYSTEM_MESSAGE 结尾的 """ 在最后一行
    - 不修改 CHINESE_ENHANCEMENT 别名（它只是指向 ENHANCED_SYSTEM_MESSAGE）
    - 不修改 LOGIN_TASK_PROMPT
    - 第 7 段共 5 行内容（1 标题 + 4 规则行），远低于 10 行限制（per D-03）

    添加后运行全部测试验证通过（GREEN 阶段）。
  </action>
  <acceptance_criteria>
    - prompts.py 中 ENHANCED_SYSTEM_MESSAGE 包含 "## 7. 表格交互" 段落
    - 段落包含 "checkbox" 关键词
    - 段落包含 "thead" 和 "tbody" 关键词（per D-04）
    - 段落包含 "aria-label" 和 "title" 关键词（per D-06）
    - 段落包含否定指令 "不要"（per D-07）
    - 段落行数 <= 10 行
    - 全部 test_enhanced_prompt.py 测试通过
    - ENHANCED_SYSTEM_MESSAGE 总行数 <= 70 行
    - CHINESE_ENHANCEMENT 别名未修改
    - LOGIN_TASK_PROMPT 未修改
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_enhanced_prompt.py -x -v</automated>
  </verify>
  <done>
    - prompts.py 中 ENHANCED_SYSTEM_MESSAGE 包含 "## 7. 表格交互" 段落
    - 段落覆盖三种场景：checkbox（thead 全选/tbody 单选）、超链接文本点击、图标按钮 title/aria-label 定位
    - 包含否定指令（不要通过索引/坐标定位、不要假设固定列）
    - 全部 test_enhanced_prompt.py 测试通过
    - 总行数 <= 70 行
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest backend/tests/unit/test_enhanced_prompt.py -x -v` — 所有测试通过
2. `grep -c "## " backend/agent/prompts.py` — 确认包含 7 个段落标题
3. `grep "## 7\." backend/agent/prompts.py` — 确认第 7 段存在
4. `grep -i "checkbox\|thead\|tbody\|aria-label" backend/agent/prompts.py` — 确认表格交互关键词
5. `grep "不要" backend/agent/prompts.py` — 确认否定指令存在
</verification>

<success_criteria>
- ENHANCED_SYSTEM_MESSAGE 包含 "## 7. 表格交互" 段落（3 条场景-动作对 + 1 条否定指令）
- 段落覆盖 checkbox 全选/单选（thead/tbody 区分）、超链接文本点击、图标按钮 title/aria-label 定位
- 包含否定指令
- 段落不超过 10 行
- test_contains_table_interaction_keywords 通过
- test_table_section_line_count 通过
- 全部原有测试仍通过
- 总行数 <= 70 行
</success_criteria>

<output>
After completion, create `.planning/phases/53-prompt/53-01-SUMMARY.md`
</output>
