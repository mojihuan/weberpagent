---
phase: 53-prompt
plan: 02
type: execute
wave: 2
depends_on:
  - 53-01
files_modified:
  - docs/test-steps/采购-表格交互测试步骤.md
  - docs/test-steps/采购-表格交互验证结果.md
autonomous: false
requirements:
  - TBL-01
  - TBL-02
  - TBL-03
  - TBL-04

must_haves:
  truths:
    - "Agent 在采购单列表中能定位并点击 tbody 行中 checkbox 实现单行选择（TBL-01）"
    - "Agent 能定位并点击 thead 中 checkbox 实现全选操作（TBL-02）"
    - "Agent 能识别并点击表格中超链接文本（TBL-03）"
    - "Agent 能定位并点击表格行中图标/操作按钮（TBL-04）"
  artifacts:
    - path: "docs/test-steps/采购-表格交互测试步骤.md"
      provides: "采购单表格交互测试步骤文档"
      contains: "checkbox"
    - path: "docs/test-steps/采购-表格交互验证结果.md"
      provides: "人工验证结果记录"
      contains: "TBL-0"
  key_links:
    - from: "docs/test-steps/采购-表格交互测试步骤.md"
      to: "backend/agent/prompts.py"
      via: "runtime dependency — Agent loads ENHANCED_SYSTEM_MESSAGE at execution time, test steps guide which scenarios to exercise"
      pattern: "checkbox"
---

<objective>
通过采购单列表 ERP 场景验证 Agent 能准确定位并操作表格中的 checkbox（单选/全选）、超链接文本和图标按钮。

Purpose: 确认 Plan 53-01 添加的表格交互 prompt 段落能被 Qwen 3.5 Plus 遵守，Agent 在实际 ERP 采购单列表页面正确操作表格元素。采购单列表一站式验证覆盖 TBL-01~04 全部 4 个需求（per D-08）。
Output: 表格交互测试步骤文档（Markdown）+ 人工验证结果记录
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-prompt/53-CONTEXT.md
@.planning/phases/53-prompt/53-01-SUMMARY.md
@backend/agent/prompts.py
@docs/test-steps/采购-测试步骤.md

<interfaces>
<!-- Existing test step format — executor MUST follow this pattern exactly -->
<!-- From docs/test-steps/采购-测试步骤.md -->

```markdown
# 商品采购模块测试步骤

## TestPurchaseAdd - 采购管理 | 新增采购单

### test_xxx - [描述]

前置条件：xxx

步骤1：{动作}目标{参数}
步骤2：{动作}目标{参数}
...

断言：调用方法：xxx
```

动作类型包括：{点击}、{输入}、{选择}、{send_keys}

<!-- Section 7 prompt content from 53-01 (executor must read prompts.py for exact wording) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建采购单表格交互测试步骤文档</name>
  <files>docs/test-steps/采购-表格交互测试步骤.md</files>
  <read_first>
    - backend/agent/prompts.py（确认表格交互段落已添加，了解具体措辞和关键词）
    - docs/test-steps/采购-测试步骤.md（现有采购模块测试步骤的格式风格，MUST 参考）
    - docs/test-steps/采购-键盘操作测试步骤.md（Phase 52 同类验证文档格式参考）
  </read_first>
  <action>
    在 docs/test-steps/ 目录下创建 采购-表格交互测试步骤.md，遵循该目录下现有 Markdown 文件的格式（如 采购-测试步骤.md 的 `{动作}目标{参数}` 步骤格式），覆盖四个验证场景（per D-08 采购单列表一站式验证）：

    场景 1 — checkbox 单行选择（TBL-01）：
    - 标题格式：### test_table_checkbox_row_select - [表格]-采购单列表单行 checkbox 选择
    - 前置条件：已登录 ERP 系统，采购单列表有数据
    - 步骤：进入采购管理 → 采购单列表 → {点击}某一行 tbody 中的 checkbox
    - 预期：该行被选中（checkbox 勾选状态），其他行未选中
    - 验证条件：Agent 通过 DOM 位置定位 tbody 中的 checkbox 而非索引

    场景 2 — checkbox 全选（TBL-02）：
    - 标题格式：### test_table_checkbox_select_all - [表格]-采购单列表表头全选 checkbox
    - 前置条件：已登录 ERP 系统，采购单列表有多行数据
    - 步骤：进入采购管理 → 采购单列表 → {点击}表头 thead 中的全选 checkbox
    - 预期：所有行被选中
    - 验证条件：Agent 通过 DOM 位置定位 thead 中的 checkbox

    场景 3 — 超链接文本点击（TBL-03）：
    - 标题格式：### test_table_link_click - [表格]-采购单列表超链接点击
    - 前置条件：已登录 ERP 系统，采购单列表有数据
    - 步骤：进入采购管理 → 采购单列表 → {点击}某个采购单号超链接
    - 预期：页面跳转到该采购单详情页（或弹出详情弹窗）
    - 验证条件：Agent 通过可见文本直接 click 定位 <a> 元素

    场景 4 — 图标按钮点击（TBL-04）：
    - 标题格式：### test_table_icon_button_click - [表格]-采购单列表操作按钮点击
    - 前置条件：已登录 ERP 系统，采购单列表有数据
    - 步骤：进入采购管理 → 采购单列表 → {点击}某行的"编辑"或"查看"图标按钮
    - 预期：进入编辑页面或弹出详情弹窗
    - 验证条件：Agent 通过 title 或 aria-label 属性定位图标按钮

    每个场景必须包含：
    - 步骤描述（中文，使用 {动作} 格式）
    - 预期的 Agent 动作（包含 click 操作和定位策略说明）
    - 验证条件（成功/失败的判断标准）
  </action>
  <acceptance_criteria>
    - 文件 docs/test-steps/采购-表格交互测试步骤.md 存在
    - 包含 4 个测试场景标题（test_table_checkbox_row_select、test_table_checkbox_select_all、test_table_link_click、test_table_icon_button_click）
    - 每个场景包含"前置条件"、"步骤"、"预期"部分
    - 文件包含关键词 checkbox、thead、tbody、aria-label、title
    - 格式与 采购-键盘操作测试步骤.md 风格一致
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/test-steps/采购-表格交互测试步骤.md && grep -c "test_table_" docs/test-steps/采购-表格交互测试步骤.md && grep -c "checkbox\|thead\|tbody" docs/test-steps/采购-表格交互测试步骤.md</automated>
  </verify>
  <done>采购-表格交互测试步骤.md 包含 4 个测试场景，覆盖 checkbox 单选/全选、超链接点击、图标按钮点击，格式与现有测试步骤文件一致</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证 ERP 采购单列表表格交互</name>
  <files>docs/test-steps/采购-表格交互验证结果.md</files>
  <action>
    通过前端 UI 运行采购单列表测试步骤，验证 Agent 能否正确定位并操作表格元素。
    前置条件：确保后端服务运行中（uv run uvicorn backend.api.main:app --reload --port 8080）且前端可用。

    验证步骤：
    1. 打开前端页面，创建新测试用例或使用 采购-表格交互测试步骤.md 中的步骤
    2. 场景 1（checkbox 单选）：在采购单列表，观察 Agent 是否通过 DOM 位置定位 tbody 行中的 checkbox 并点击实现单行选择
    3. 场景 2（checkbox 全选）：在采购单列表，观察 Agent 是否定位 thead 中的全选 checkbox 并点击
    4. 场景 3（超链接）：在采购单列表，观察 Agent 是否通过可见文本直接 click 超链接（如采购单号）
    5. 场景 4（图标按钮）：在采购单列表，观察 Agent 是否通过 title 或 aria-label 属性定位并点击操作按钮

    预期结果：
    - Agent 能区分 thead 和 tbody 中的 checkbox（全选 vs 单选）
    - Agent 通过可见文本直接点击超链接，不使用 find_elements 预查找
    - Agent 通过 title/aria-label 属性定位图标按钮，不通过索引或坐标
    - Agent 不尝试通过列索引定位表格元素

    验证完成后，由执行者创建 docs/test-steps/采购-表格交互验证结果.md，记录每个场景的通过/失败状态和简要说明（如 "场景1 (TBL-01): 通过 — Agent 正确定位 tbody 中 checkbox" 或 "场景3 (TBL-03): 失败 — Agent 未通过文本定位超链接"）。
  </action>
  <verify>
    <automated>test -f docs/test-steps/采购-表格交互测试步骤.md && test -f docs/test-steps/采购-表格交互验证结果.md && echo "Both files exist"</automated>
  </verify>
  <done>docs/test-steps/采购-表格交互验证结果.md 已创建，记录 4 个 ERP 表格交互场景的验证结果（通过/失败+说明）</done>
</task>

</tasks>

<verification>
1. `test -f docs/test-steps/采购-表格交互测试步骤.md` — 测试步骤文件存在
2. `grep -c "test_table_" docs/test-steps/采购-表格交互测试步骤.md` — 包含 4 个测试场景
3. `test -f docs/test-steps/采购-表格交互验证结果.md` — 验证结果文件存在
4. 人工验证 4 个 ERP 场景结果记录
</verification>

<success_criteria>
- 表格交互测试步骤文档已创建（4 个场景，Markdown 格式，与现有测试步骤风格一致）
- 验证结果文件已创建，记录每个场景的通过/失败状态
- 人工验证 Agent 在采购单列表中正确操作 checkbox（单选/全选）、超链接、图标按钮
- Agent 通过 DOM 位置区分全选/单选 checkbox
- Agent 通过文本定位超链接、通过 title/aria-label 定位图标按钮
</success_criteria>

<output>
After completion, create `.planning/phases/53-prompt/53-02-SUMMARY.md`
</output>
