---
phase: 45-code-removal
plan: 05
type: execute
wave: 3
depends_on:
  - 45-01
  - 45-02
  - 45-03
  - 45-04
files_modified:
  - backend/tests/unit/test_agent_service.py
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - Unit tests for removed methods are removed from test_agent_service.py
    - No compilation errors in test file
    - Remaining tests pass
  artifacts:
    - path: backend/tests/unit/test_agent_service.py
      provides: Agent service unit tests
      excludes:
        - TestLoopInterventionTracker
        - TestTdPostProcess
        - TestTDPostProcessing
        - TestFallbackInput
        - TestElementDiagnostics
  key_links: []
---

<objective>
Remove unit tests for deleted methods from test_agent_service.py.

Purpose: Clean up test file to remove tests for methods that were deleted in Plans 02-04. This ensures the test suite does not have broken imports or test failures for non-existent code.
Output: Test classes for removed methods deleted, remaining tests pass
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-code-removal/45-RESEARCH.md
@.planning/phases/45-code-removal/45-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove test classes for deleted methods</name>
  <files>
    backend/tests/unit/test_agent_service.py
  </files>
  <read_first>
    backend/tests/unit/test_agent_service.py (current test file)
  </read_first>
  <action>
Remove test classes for the methods that were deleted in Plans 02-04.

**This plan runs AFTER all other plans to ensure the source code is clean first.**

Step 1: Read the test file to identify test classes to remove:
Run: grep -n "class Test" backend/tests/unit/test_agent_service.py

Step 2: Remove the following test classes (if they exist):
- `TestLoopInterventionTracker` - tests for LoopInterventionTracker class (CLEANUP-05)
- `TestTdPostProcess` - tests for _post_process_td_click method (CLEANUP-02)
- `TestTDPostProcessing` - tests for TD post-processing integration (CLEANUP-02)
- `TestFallbackInput` - tests for _fallback_input method (CLEANUP-03)
- `TestElementDiagnostics` - tests for _collect_element_diagnostics method (CLEANUP-04)

For each test class, delete the entire class from `class Test...:` to the next class definition or end of file.

Step 3: Remove any imports that are no longer needed:
- Remove imports related to LoopInterventionTracker
- Remove imports related to scroll_table_tool (if any)
- Remove any mock imports that were only used by deleted tests

Step 4: Verify the test file has no syntax errors:
Run: uv run python -m py_compile backend/tests/unit/test_agent_service.py

Step 5: Run the remaining tests to ensure they pass:
Run: uv run pytest backend/tests/unit/test_agent_service.py -v --tb=short

Note: Do NOT delete test_scroll_table_tool.py or test_scroll_table_e2e.py in this phase.
Those files will be handled in Phase 46 (TEST-01).
</action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_agent_service.py -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Test classes for removed methods deleted from test_agent_service.py
    - No import errors for deleted modules
    - Remaining unit tests pass
    - No compilation errors in test file
</done>
</task>

</tasks>

<verification>
- Run `uv run pytest backend/tests/unit/test_agent_service.py -v --tb=short`
- Run `grep -c "TestLoopInterventionTracker\|TestTdPostProcess\|TestFallbackInput\|TestElementDiagnostics" backend/tests/unit/test_agent_service.py` should return 0
</verification>

<success_criteria>
- Test classes for removed methods deleted
- No compilation errors in test file
- Remaining unit tests pass
- No broken imports in test file
</success_criteria>

<output>
After completion, create `.planning/phases/45-code-removal/45-05-SUMMARY.md`
</output>
