---
phase: 45-code-removal
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/agent_service.py
autonomous: true
requirements:
  - CLEANUP-05
user_setup: []
must_haves:
  truths:
    - LoopInterventionTracker class does not exist in agent_service.py
    - All tracker-related code removed from run_with_streaming method
    - step_callback no longer references tracker
    - stagnation field removed from step_stats
    - loop_intervention_data variable removed
  artifacts:
    - path: backend/core/agent_service.py
      provides: Agent service implementation
      excludes: class LoopInterventionTracker
  key_links: []
---

<objective>
Remove LoopInterventionTracker class and all references from agent_service.py.

Purpose: Clean up code that was added for loop detection, which is no longer needed with browser-use's native detection.
Output: LoopInterventionTracker class removed, tracker instance removed, should_intervene() check removed, tracker.record_page_state() removed, tracker.record_action() removed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-code-removal/45-RESEARCH.md
@.planning/phases/45-code-removal/45-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove LoopInterventionTracker class and references</name>
  <files>
    backend/core/agent_service.py
  </files>
  <read_first>
    backend/core/agent_service.py (full file)
  </read_first>
  <action>
Remove the `LoopInterventionTracker` class and all its references from `backend/core/agent_service.py`.

**Critical: Remove references BEFORE deleting the class (per RESEARCH Pitfall 1).**

Note: This plan is INDEPENDENT of Plan 03. Both remove different classes/methods from the same file and can run in parallel (Wave 1).

Step 1: Remove tracker instantiation in run_with_streaming:
   - Find and remove: `tracker = LoopInterventionTracker(window_size=20, stagnation_threshold=5)`
   - Run: grep -n "tracker = LoopInterventionTracker" backend/core/agent_service.py
   - Delete the entire line

Step 2: Remove loop_intervention_data variable:
   - Find and remove: `loop_intervention_data = {"value": None}`
   - Run: grep -n "loop_intervention_data" backend/core/agent_service.py
   - Delete the entire line

Step 3: Remove tracker.record_action() calls in step_callback:
   - Find and remove: `tracker.record_action(action_name, action_params)`
   - Run: grep -n "tracker.record_action" backend/core/agent_service.py
   - Delete the entire line

Step 4: Remove tracker.record_page_state() calls in step_callback:
   - Find and remove: `tracker.record_page_state(url, dom_hash)`
   - Run: grep -n "tracker.record_page_state" backend/core/agent_service.py
   - Delete the entire line

Step 5: Remove tracker.should_intervene() check block:
   - Find the block with `if tracker.should_intervene():` (around lines 735-750)
   - Delete the entire block including `get_intervention_message()` and `get_diagnostic_info()` calls
   - Run: grep -n "tracker.should_intervene" backend/core/agent_service.py
   - Delete the entire block

Step 6: Remove stagnation from step_stats:
   - Find and remove: `stagnation` field from step_stats
   - Run: grep -n "stagnation" backend/core/agent_service.py
   - Remove any stagnation-related assignments

Step 7: Delete the LoopInterventionTracker class definition:
   - Find the class (around lines 29-158) with signature: `class LoopInterventionTracker:`
   - Delete the entire class including all methods: `__init__`, `record_action`, `record_page_state`, `should_intervene`, `get_intervention_message`, `get_diagnostic_info`

Step 8: Verify no remaining references:
   Run: grep -n "LoopInterventionTracker\|tracker\." backend/core/agent_service.py
   (Should return nothing)
</action>
  <verify>
    <automated>grep -c "LoopInterventionTracker" backend/core/agent_service.py | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    - `LoopInterventionTracker` class removed from agent_service.py
    - `tracker` variable removed
    - `loop_intervention_data` variable removed
    - No references to tracker remain in agent_service.py
    - No compilation errors from missing class/variable references
</done>
</task>

</tasks>

<verification>
- Run `grep -c "LoopInterventionTracker" backend/core/agent_service.py` should return 0
- Run `grep -c "tracker\." backend/core/agent_service.py` should return 0
- Run `grep -c "loop_intervention_data" backend/core/agent_service.py` should return 0
</verification>

<success_criteria>
- `grep -c "LoopInterventionTracker" backend/core/agent_service.py` returns 0 (class removed)
- No references to tracker in agent_service.py or step_callback
- No compilation errors when running `uv run python -c "from backend.core.agent_service import AgentService"`
</success_criteria>

<output>
After completion, create `.planning/phases/45-code-removal/45-04-SUMMARY.md`
</output>
