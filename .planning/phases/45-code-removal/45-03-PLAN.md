---
phase: 45-code-removal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/agent_service.py
autonomous: true
requirements:
  - CLEANUP-03
  - CLEANUP-04
user_setup: []
must_haves:
  truths:
    - _fallback_input method does not exist in agent_service.py
    - _collect_element_diagnostics method does not exist in agent_service.py
    - Fallback input calls in step_callback are removed
    - element_diagnostics variable is removed
    - No compilation errors for missing method calls
  artifacts:
    - path: backend/core/agent_service.py
      provides: Agent service implementation
      excludes: def _fallback_input, def _collect_element_diagnostics
  key_links: []
---

<objective>
Remove JavaScript fallback and element diagnostics logging from agent_service.py.

Purpose: Clean up code that was added for JavaScript-based fallback and element diagnostics, which are no longer needed with native browser-use.
Output: _fallback_input and _collect_element_diagnostics methods removed from agent_service.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-code-removal/45-RESEARCH.md
@.planning/phases/45-code-removal/45-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove _fallback_input and _collect_element_diagnostics methods</name>
  <files>
    backend/core/agent_service.py
  </files>
  <read_first>
    backend/core/agent_service.py (full file)
  </read_first>
  <action>
Remove the `_fallback_input` and `_collect_element_diagnostics` methods and all their references from `backend/core/agent_service.py`.

**Critical: Remove references BEFORE deleting methods (per RESEARCH Pitfall 1).**

Step 1: Remove element_diagnostics variable declaration:
- Find in run_with_streaming (around lines 528-533):
  ```python
  element_diagnostics = {
      "non_interactive_elements": [],
      "fallback_triggered": False,
      "fallback_reason": None
  }
  ```
- Delete this entire block

Step 2: Remove fallback input logic from step_callback:
- Find the block starting with `if action_name == 'input' and self._browser_session:` (around lines 621-671)
- This block calls `_fallback_input` - delete the entire block

Step 3: Remove element_diagnostics linking from step_callback:
- Find the block that links fallback info to element_diagnostics (around lines 673-679)
- Delete this block

Step 4: Remove element_diagnostics from step_stats:
- Find and remove: `step_stats['element_diagnostics'] = diagnostics` or similar
- Remove any `del step_stats['element_diagnostics']` lines

Step 5: Delete the `_fallback_input` method definition:
- Find the method (around lines 257-330) with signature: `async def _fallback_input(self, page, element, value):`
- Delete the entire method

Step 6: Delete the `_collect_element_diagnostics` method definition:
- Find the method (around lines 332-422) with signature: `def _collect_element_diagnostics(self, element_tree, target_index, action_params):`
- Delete the entire method

Step 7: Verify no remaining references:
Run: grep -n "_fallback_input\|_collect_element_diagnostics\|element_diagnostics" backend/core/agent_service.py
(Should return nothing related to these methods)
</action>
  <verify>
    <automated>grep -c "_fallback_input\|_collect_element_diagnostics" backend/core/agent_service.py | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    - `_fallback_input` method removed from agent_service.py
    - `_collect_element_diagnostics` method removed from agent_service.py
    - `element_diagnostics` variable removed
    - No references to these methods/variables remain in agent_service.py
    - No compilation errors from missing method calls
</done>
</task>

</tasks>

<verification>
- Run `grep -c "_fallback_input" backend/core/agent_service.py` should return 0
- Run `grep -c "_collect_element_diagnostics" backend/core/agent_service.py` should return 0
- Run `grep -c "element_diagnostics" backend/core/agent_service.py` should return 0
</verification>

<success_criteria>
- `grep -c "_fallback_input" backend/core/agent_service.py` returns 0 (method removed)
- `grep -c "_collect_element_diagnostics" backend/core/agent_service.py` returns 0 (method removed)
- No compilation errors when running `uv run python -c "from backend.core.agent_service import AgentService"`
</success_criteria>

<output>
After completion, create `.planning/phases/45-code-removal/45-03-SUMMARY.md`
</output>
