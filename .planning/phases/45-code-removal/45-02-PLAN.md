---
phase: 45-code-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/agent_service.py
  - backend/agent/__init__.py
autonomous: true
requirements:
  - CLEANUP-02
user_setup: []
must_haves:
  truths:
    - _post_process_td_click method does not exist in agent_service.py
    - TD post-processing calls in step_callback are removed
    - td_post_process_result variable is removed
    - Import from backend.agent.tools is removed from agent_service.py
    - No compilation errors for missing method calls
  artifacts:
    - path: backend/core/agent_service.py
      provides: Agent service implementation
      excludes:
        - def _post_process_td_click
        - "from backend.agent.tools import register_scroll_table_tool"
    - path: backend/agent/__init__.py
      provides: Agent module exports
      excludes: register_scroll_table_tool, ScrollTableTool exports
  key_links:
    - from: backend/core/agent_service.py
      to: backend/agent/tools
      via: "import statement"
      must_remove: "from backend.agent.tools import register_scroll_table_tool"
---

<objective>
Remove TD post-processing logic and all references from agent_service.py.

Purpose: Clean up code that was added for TD focus transfer workaround, which is no longer needed.
Output: _post_process_td_click method removed, TD-related calls in step_callback removed, td_post_process_result variable removed, import from backend.agent.tools removed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-code-removal/45-RESEARCH.md
@.planning/phases/45-code-removal/45-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove _post_process_td_click method and references</name>
  <files>
    backend/core/agent_service.py
    backend/agent/__init__.py
  </files>
  <read_first>
    backend/core/agent_service.py (full file)
    backend/agent/__init__.py (for exports patterns)
  </read_first>
  <action>
Remove the `_post_process_td_click` method and all its references from `backend/core/agent_service.py`.

**Critical: Remove references BEFORE deleting the method (per RESEARCH Pitfall 1).**

**CRITICAL FOR CLEANUP-01 (Blocker Fix):** This task MUST also remove the import statement that references the tools directory that will be deleted in Plan 45-01.

Step 0: Remove the import from backend.agent.tools (CLEANUP-01 blocker fix):
   - Find and remove: `from backend.agent.tools import register_scroll_table_tool` (line 15)
   - Run: grep -n "from backend.agent.tools import" backend/core/agent_service.py
   - Delete the entire line
   - This import MUST be removed BEFORE Plan 45-01 runs to avoid ImportError

Step 1: Find and remove all references in step_callback function:
   - Search for: `_post_process_td_click`, `td_post_process_result`
   - Remove the `nonlocal td_post_process_result` declaration
   - Remove the TD post-processing block (around lines 609-619) that calls `await self._post_process_td_click(page)`
   - Remove the `td_post_process_result = td_result` assignment
   - Remove the `if td_post_process_result:` block in step_stats

   Run: grep -n "_post_process_td_click\|td_post_process_result" backend/core/agent_service.py

Step 2: Remove the variable declaration in run_with_streaming:
   - Find and remove: `td_post_process_result = None  # TD post-processing result temporary storage`
   - Run: grep -n "td_post_process_result" backend/core/agent_service.py

Step 3: Delete the `_post_process_td_click` method definition:
   - Find the method (around lines 184-255) with signature: `async def _post_process_td_click(self, page):`
   - Delete the entire method

Step 4: Verify no remaining references:
   Run: grep -n "_post_process_td_click" backend/core/agent_service.py
   (Should return nothing)

Step 5: Verify import removed (for CLEANUP-01):
   Run: grep -c "from backend.agent.tools import" backend/core/agent_service.py
   (Should return 0)
</action>
  <verify>
    <automated>grep -c "_post_process_td_click\|from backend.agent.tools import" backend/core/agent_service.py | xargs -I {} test {} -eq 0</automated>
  </verify>
  <done>
    - `_post_process_td_click` method removed from agent_service.py
    - `td_post_process_result` variable removed
    - `from backend.agent.tools import register_scroll_table_tool` import removed
    - No references to `_post_process_td_click` remain in agent_service.py
    - No compilation errors from missing method calls
</done>
</task>

</tasks>

<verification>
- Run `grep -c "_post_process_td_click" backend/core/agent_service.py` should return 0
- Run `grep -c "td_post_process_result" backend/core/agent_service.py` should return 0
- Run `grep -c "from backend.agent.tools import" backend/core/agent_service.py` should return 0
</verification>

<success_criteria>
- `grep -c "_post_process_td_click" backend/core/agent_service.py` returns 0 (method removed)
- `grep -c "td_post_process_result" backend/core/agent_service.py` returns 0 (variable removed)
- `grep -c "from backend.agent.tools import" backend/core/agent_service.py` returns 0 (import removed)
- No compilation errors when running `uv run python -c "from backend.core.agent_service import AgentService"`
</success_criteria>

<output>
After completion, create `.planning/phases/45-code-removal/45-02-SUMMARY.md`
</output>
