---
phase: 45-code-removal
plan: 01
type: execute
wave: 2
depends_on:
  - 45-02
  - 45-03
  - 45-04
files_modified:
  - backend/agent/tools/__init__.py
  - backend/agent/tools/scroll_table_tool.py
autonomous: true
requirements:
  - CLEANUP-01
user_setup: []
must_haves:
  truths:
    - backend/agent/tools/ directory does not exist
    - No imports from backend.agent.tools remain in codebase
  artifacts:
    - path: backend/agent/tools/__init__.py
      exists: false
    - path: backend/agent/tools/scroll_table_tool.py
      exists: false
  key_links: []
---

<objective>
Delete the backend/agent/tools/ directory AFTER all imports have been removed.

Purpose: Remove custom browser-use extension tool files. Must run AFTER Plans 02-04 have removed all imports from agent_service.py and __init__.py to prevent broken imports.
Output: backend/agent/tools/ directory completely removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-code-removal/45-RESEARCH.md
@.planning/phases/45-code-removal/45-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete backend/agent/tools/ directory</name>
  <files>
    backend/agent/tools/__init__.py
    backend/agent/tools/scroll_table_tool.py
  </files>
  <read_first>
    backend/agent/tools/__init__.py
    backend/agent/tools/scroll_table_tool.py
  </read_first>
  <action>
Delete the entire `backend/agent/tools/` directory.

This task runs AFTER Plans 02-04 have removed all imports. Execute the following steps:

1. Verify no remaining imports exist:
   Run: grep -r "from backend.agent.tools" backend/ || echo "No imports found (expected)"
   Run: grep -r "import backend.agent.tools" backend/ || echo "No imports found (expected)"

2. Delete the directory:
   Run: rm -rf backend/agent/tools/

3. Verify deletion:
   Run: test ! -d backend/agent/tools/ && echo "Directory deleted successfully"
</action>
  <verify>
    <automated>test ! -d backend/agent/tools/</automated>
  </verify>
  <done>
    - `backend/agent/tools/` directory does NOT exist
    - `ls backend/agent/tools/` returns "No such file or directory"
    - No broken imports remain in the codebase
</done>
</task>

</tasks>

<verification>
- Run `test ! -d backend/agent/tools/`
- Run `grep -r "from backend.agent.tools" backend/` should return nothing
</verification>

<success_criteria>
- backend/agent/tools/ directory no longer exists
- All tool files (__init__.py, scroll_table_tool.py) deleted
- No broken imports in the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/45-code-removal/45-01-SUMMARY.md`
</output>
