---
phase: 13-配置基础
plan: 03
type: execute
wave: 1
depends_on: [13-01, 13-02]
files_modified:
  - README.md
autonomous: true
requirements:
  - CONFIG-03
user_setup: []

must_haves:
  truths:
    - "User can find webseleniumerp configuration instructions in README.md"
    - "Documentation includes config/settings.py template"
    - "Documentation includes DATA_PATHS configuration example"
    - "Documentation explains how to verify configuration"
  artifacts:
    - path: "README.md"
      provides: "webseleniumerp configuration documentation"
      contains: "webseleniumerp Configuration"
  key_links:
    - from: "README.md"
      to: ".env"
      via: "WEBSERP_PATH configuration"
      pattern: "WEBSERP_PATH"
    - from: "README.md"
      to: "webseleniumerp/config/settings.py"
      via: "template documentation"
      pattern: "DATA_PATHS"
---

<objective>
Add webseleniumerp configuration documentation to README.md, including environment variable setup and config/settings.py template.

Purpose: Provide clear, copy-paste ready documentation so users can configure webseleniumerp integration without guessing.
Output: README.md with new "webseleniumerp Configuration" section.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-配置基础/13-CONTEXT.md
@.planning/phases/13-配置基础/13-RESEARCH.md

<interfaces>
<!-- Documentation patterns from existing README.md -->

From README.md structure:
- Uses Markdown headings with ##
- Includes code blocks with syntax highlighting
- Has numbered steps for procedures
- Uses tables for configuration reference
- Includes warning callouts with > syntax

Existing configuration section pattern (lines 348-424):
```markdown
## Configuration

### LLM Configuration
...

### Target System Configuration
...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add webseleniumerp Configuration section to README.md</name>
  <files>README.md</files>
  <read_first>
    - /Users/huhu/project/weberpagent/README.md (file being modified)
    - /Users/huhu/project/weberpagent/.planning/phases/13-配置基础/13-CONTEXT.md (documentation requirements)
    - /Users/huhu/project/weberpagent/.planning/phases/13-配置基础/13-RESEARCH.md (config/settings.py template)
  </read_first>
  <action>
    Add a new "webseleniumerp Configuration" section to README.md.

    Insert the new section AFTER the existing "Target System Configuration" section (after line ~398, before "Browser Configuration").

    The new section content:

    ```markdown

    ### webseleniumerp Configuration

    To use external precondition operations from the webseleniumerp project, you need to configure the project path and create a settings file.

    #### 1. Configure Environment Variable

    Add the path to your webseleniumerp project in `.env`:

    \`\`\`env
    WEBSERP_PATH=/path/to/your/webseleniumerp
    \`\`\`

    The path should point to the root directory of webseleniumerp containing `base_prerequisites.py`.

    #### 2. Create config/settings.py

    The webseleniumerp project requires a `config/settings.py` file. This file is in `.gitignore` and must be created manually.

    Create `webseleniumerp/config/settings.py` with the following content:

    \`\`\`python
    # webseleniumerp/config/settings.py

    # Data paths for test data files
    DATA_PATHS = {
        'test_data': '/path/to/your/test/data',
        'excel_files': '/path/to/your/excel/files',
        # Add other paths as needed by your precondition operations
    }
    \`\`\`

    > **Note**: The actual paths depend on your test environment. Update them to match your local setup.

    #### 3. Verify Configuration

    Start the server to verify your configuration:

    \`\`\`bash
    uv run uvicorn backend.api.main:app --reload --port 8080
    \`\`\`

    If configuration is invalid, you will see an error message with specific instructions for fixing the issue:

    \`\`\`
    [CONFIG ERROR] WEBSERP_PATH directory not found: /invalid/path
      Solution: Verify the path in your .env file
    \`\`\`

    #### Available Operations

    Once configured, you can use operations from webseleniumerp in your precondition code:

    | Operation Code | Description |
    |----------------|-------------|
    | FA1 | Finance - Create account |
    | HC1 | Inventory - Create stock |
    | ... | See webseleniumerp documentation for full list |

    To use these operations in your precondition:

    \`\`\`python
    # In task precondition
    self.pre.operations(data=['FA1', 'HC1'])
    \`\`\`
    ```

    IMPORTANT:
    - Use proper Markdown escaping for code blocks (backticks)
    - Maintain consistent indentation with the rest of README.md
    - Insert between "Target System Configuration" and "Browser Configuration" sections
  </action>
  <verify>
    <automated>grep -A 5 "webseleniumerp Configuration" README.md</automated>
  </verify>
  <done>
    - README.md contains "### webseleniumerp Configuration" heading
    - Section includes 3 numbered subsections (Configure, Create settings, Verify)
    - Section includes config/settings.py template with DATA_PATHS
    - Section includes example error output
    - Section includes operations table
    - `grep -c "webseleniumerp" README.md` returns >= 5
  </done>
</task>

</tasks>

<verification>
Verify documentation completeness:
```bash
grep -A 30 "webseleniumerp Configuration" README.md
grep -c "DATA_PATHS" README.md
grep -c "WEBSERP_PATH" README.md
```
</verification>

<success_criteria>
- README.md has new "webseleniumerp Configuration" section
- Section includes environment variable setup instructions
- Section includes config/settings.py template with DATA_PATHS
- Section explains how to verify configuration
- Section lists available operations
- Documentation is copy-paste ready
</success_criteria>

<output>
After completion, create `.planning/phases/13-配置基础/13-03-SUMMARY.md`
</output>
