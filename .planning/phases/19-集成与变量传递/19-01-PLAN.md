---
phase: 19-集成与变量传递
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/TaskModal/TaskForm.tsx
  - frontend/src/components/TaskModal/DataMethodSelector.tsx
autonomous: true
requirements:
  - INT-01
must_haves:
  truths:
    - "Frontend code generation includes className parameter in get_data() call"
    - "Generated code format matches: context.get_data('ClassName', 'method_name', i=2)"
    - "Both TaskForm and DataMethodSelector generate identical code format"
  artifacts:
    - path: "frontend/src/components/TaskModal/TaskForm.tsx"
      provides: "Code generation with className"
      pattern: "context.get_data\\('\\$\{config.className\\}'"
    - path: "frontend/src/components/TaskModal/DataMethodSelector.tsx"
      provides: "Code preview with className"
      pattern: "context.get_data\\('\\$\{config.className\\}'"
  key_links:
    - from: "TaskForm.tsx"
      to: "PreconditionService"
      via: "Generated code string"
      pattern: "handleDataSelectorConfirm"
    - from: "DataMethodSelector.tsx"
      to: "PreconditionService"
      via: "generateCode function"
      pattern: "generateCode\\(\\)"
---

<objective>
Update frontend code generation to include `className` parameter in `context.get_data()` calls.

Purpose: The backend `ContextWrapper.get_data()` method requires `className` as the first argument to identify which class in `base_params.py` contains the method.
Output: Both `TaskForm.tsx` and `DataMethodSelector.tsx` generate code in format: `context.get_data('ClassName', 'method_name', i=2)`
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-集成与变量传递/19-CONTEXT.md

<interfaces>
<!-- Current code generation format (needs modification) -->

From TaskForm.tsx (line 219):
```javascript
const methodCall = `context.get_data('${config.methodName}', ${params})`
```

From DataMethodSelector.tsx (line 214):
```javascript
const methodCall = `context.get_data('${config.methodName}', ${params})`
```

<!-- Target format (with className) -->
```javascript
const methodCall = `context.get_data('${config.className}', '${config.methodName}', ${params})`
```

<!-- DataMethodConfig interface (already has className) -->
From frontend/src/types/index.ts:
```typescript
export interface DataMethodConfig {
  className: string
  methodName: string
  parameters: Record<string, any>
  extractions: FieldExtraction[]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update code generation in TaskForm.tsx</name>
  <files>frontend/src/components/TaskModal/TaskForm.tsx</files>
  <read_first>
    - frontend/src/components/TaskModal/TaskForm.tsx
  </read_first>
  <action>
    In the `handleDataSelectorConfirm` function (around line 210-238), modify the code generation logic:

    **Current code (line 219):**
    ```javascript
    const methodCall = `context.get_data('${config.methodName}', ${params})`
    ```

    **Change to:**
    ```javascript
    const methodCall = `context.get_data('${config.className}', '${config.methodName}', ${params})`
    ```

    This adds `className` as the first argument and properly quotes `methodName` as the second argument.
  </action>
  <verify>
    <automated>grep -n "context.get_data('\${config.className}'" frontend/src/components/TaskModal/TaskForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TaskForm.tsx contains `context.get_data('${config.className}', '${config.methodName}'`
    - The generated code format matches: `context.get_data('ClassName', 'method_name', i=2)`
    - No other code generation patterns remain using the old format
  </acceptance_criteria>
  <done>
    TaskForm.tsx generates code with className parameter included.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update code generation in DataMethodSelector.tsx</name>
  <files>frontend/src/components/TaskModal/DataMethodSelector.tsx</files>
  <read_first>
    - frontend/src/components/TaskModal/DataMethodSelector.tsx
  </read_first>
  <action>
    In the `generateCode` function (around line 206-224), modify the code generation logic:

    **Current code (line 214):**
    ```javascript
    const methodCall = `context.get_data('${config.methodName}', ${params})`
    ```

    **Change to:**
    ```javascript
    const methodCall = `context.get_data('${config.className}', '${config.methodName}', ${params})`
    ```

    This ensures the code preview shown in Step 4 matches the actual generated code format.
  </action>
  <verify>
    <automated>grep -n "context.get_data('\${config.className}'" frontend/src/components/TaskModal/DataMethodSelector.tsx</automated>
  </verify>
  <acceptance_criteria>
    - DataMethodSelector.tsx contains `context.get_data('${config.className}', '${config.methodName}'`
    - The code preview in Step 4 shows the correct format
    - Both TaskForm and DataMethodSelector generate identical code format
  </acceptance_criteria>
  <done>
    DataMethodSelector.tsx generates code preview with className parameter included.
  </done>
</task>

</tasks>

<verification>
- Both files contain the new code generation pattern
- grep finds `context.get_data('${config.className}'` in both files
- Frontend builds without errors: `cd frontend && npm run build`
</verification>

<success_criteria>
- TaskForm.tsx generates code: `context.get_data('ClassName', 'method_name', i=2)`
- DataMethodSelector.tsx shows identical code preview
- Both files have identical code generation logic
- Frontend build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/19-集成与变量传递/19-01-SUMMARY.md`
</output>
