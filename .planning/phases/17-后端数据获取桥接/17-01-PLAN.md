---
phase: 17-后端数据获取桥接
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/external_precondition_bridge.py
  - backend/tests/unit/test_external_bridge.py
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "Bridge module can scan base_params.py and extract all public methods"
    - "Each method includes name, description, and parameter signature"
    - "Parameters include name, type, required flag, and default value"
  artifacts:
    - path: "backend/core/external_precondition_bridge.py"
      provides: "Data method discovery and signature extraction"
      exports: ["load_base_params_class", "get_data_methods_grouped", "reset_cache"]
      contains: "def load_base_params_class"
    - path: "backend/tests/unit/test_external_bridge.py"
      provides: "Unit tests for data method discovery"
      contains: "class TestDataMethodsDiscovery"
  key_links:
    - from: "backend/core/external_precondition_bridge.py"
      to: "common.base_params"
      via: "import after sys.path configuration"
      pattern: "from common.base_params import"
---

<objective>
Extend external_precondition_bridge.py to support data method discovery from base_params.py.

Purpose: Enable scanning of webseleniumerp's base_params.py to extract all public methods with their signatures, parameters, and descriptions. This provides the foundation for the data method API.

Output: Extended bridge module with data method discovery functions and unit tests.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-后端数据获取桥接/17-CONTEXT.md
@.planning/phases/17-后端数据获取桥接/17-RESEARCH.md

<interfaces>
<!-- Key patterns from existing bridge module that executor needs -->

From backend/core/external_precondition_bridge.py:
```python
# Singleton pattern for module-level caching
_base_params_class = None
_base_params_import_error = None
_data_methods_cache: list[dict] | None = None

# Path configuration pattern
def configure_external_path(weberp_path: str | None) -> tuple[bool, str]

# Lazy loading pattern
def load_pre_front_class() -> tuple[type | None, str | None]

# Cache reset for testing
def reset_cache()
```

From CONTEXT.md locked decisions:
- Parse method: Use `inspect.signature()` + `typing.get_type_hints()`
- Scan scope: All public methods (not starting with `_`)
- Parameter info: Include name, type, required, default
- Grouping: By class name (e.g., BaseParams, InventoryParams)
- Caching: Method signatures cached at startup, execution results not cached
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Add base_params module loading support</name>
<files>backend/core/external_precondition_bridge.py, backend/tests/unit/test_external_bridge.py</files>
<requirements>[DATA-01]</requirements>
<read_first>
- backend/core/external_precondition_bridge.py
- .planning/phases/17-后端数据获取桥接/17-CONTEXT.md
</read_first>
<behavior>
- Test 1: load_base_params_class() returns (None, error_message) when WEBSERP_PATH not configured
- Test 2: load_base_params_class() returns (class, None) when module available
- Test 3: load_base_params_class() caches result and returns same instance on subsequent calls
</behavior>
<action>
1. Add new module-level singleton variables after existing ones (around line 22):
   ```python
   _base_params_class = None
   _base_params_import_error = None
   _data_methods_cache: list[dict] | None = None
   ```

2. Add function `load_base_params_class()` after `load_pre_front_class()` (after line 92):
   - Follow the same lazy loading pattern as `load_pre_front_class()`
   - Import from `common.base_params` (the target module in webseleniumerp)
   - Return tuple `(class_or_none, error_or_none)`
   - Cache both success and failure results
   - Handle ImportError with helpful message about config/settings.py

3. Update `reset_cache()` function (around line 233) to reset new cache variables:
   ```python
   global _base_params_class, _base_params_import_error, _data_methods_cache
   _base_params_class = None
   _base_params_import_error = None
   _data_methods_cache = None
   ```

4. Create unit tests in `backend/tests/unit/test_external_bridge.py`:
   - Add test class `TestDataMethodsDiscovery`
   - Test `load_base_params_class()` returns None with error when module unavailable
   - Test cache behavior using `reset_cache()` fixture
</action>
<acceptance_criteria>
- backend/core/external_precondition_bridge.py contains `_base_params_class = None`
- backend/core/external_precondition_bridge.py contains `def load_base_params_class()`
- backend/core/external_precondition_bridge.py contains `from common.base_params import`
- backend/tests/unit/test_external_bridge.py contains `class TestDataMethodsDiscovery`
</acceptance_criteria>
<verify>
<automated>uv run pytest backend/tests/unit/test_external_bridge.py::TestDataMethodsDiscovery::test_load_base_params_class_unavailable -x -v</automated>
</verify>
<done>load_base_params_class() function exists, returns (class, None) on success or (None, error) on failure, with proper caching</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Implement method signature extraction</name>
<files>backend/core/external_precondition_bridge.py, backend/tests/unit/test_external_bridge.py</files>
<requirements>[DATA-01]</requirements>
<read_first>
- backend/core/external_precondition_bridge.py
- .planning/phases/17-后端数据获取桥接/17-RESEARCH.md (Pattern 1: Method Introspection)
</read_first>
<behavior>
- Test 1: extract_method_info() returns None for private methods (starting with _)
- Test 2: extract_method_info() returns dict with name, description, parameters for public methods
- Test 3: Parameters include correct type inference from type hints
- Test 4: Parameters include correct required flag (no default = required)
- Test 5: Description extracted from docstring first line, falls back to method name
</behavior>
<action>
1. Add import at top of file (around line 12):
   ```python
   from typing import Any, get_type_hints
   ```

2. Add helper function `extract_method_info()` after `load_base_params_class()`:
   - Parameters: `cls: type`, `method_name: str`
   - Returns: `dict | None`
   - Skip private methods (starting with `_`)
   - Use `inspect.signature()` to get parameters
   - Use `typing.get_type_hints()` to get type annotations
   - For each parameter (skip `self`):
     - `name`: parameter name
     - `type`: type hint name (e.g., "int", "str", "Any")
     - `required`: True if no default value
     - `default`: repr(default_value) if has default, else None
   - Extract description from `method.__doc__` first line, fallback to method_name
   - Return structure:
     ```python
     {
         "name": method_name,
         "description": "First line of docstring or method_name",
         "parameters": [
             {"name": "i", "type": "int", "required": False, "default": "0"},
             ...
         ]
     }
     ```

3. Create unit tests for `extract_method_info()`:
   - Create mock class with various method signatures in test file
   - Test private method returns None
   - Test public method returns correct structure
   - Test parameter type inference
   - Test required flag logic
   - Test docstring extraction
</action>
<acceptance_criteria>
- backend/core/external_precondition_bridge.py contains `def extract_method_info(`
- backend/core/external_precondition_bridge.py contains `inspect.signature(`
- backend/core/external_precondition_bridge.py contains `get_type_hints(`
- backend/core/external_precondition_bridge.py contains `"parameters"`
- backend/tests/unit/test_external_bridge.py contains `test_extract_method_info`
</acceptance_criteria>
<verify>
<automated>uv run pytest backend/tests/unit/test_external_bridge.py::TestDataMethodsDiscovery::test_extract_method_info -x -v</automated>
</verify>
<done>extract_method_info() correctly parses method signatures, extracts parameters with types, required flags, and defaults</done>
</task>

<task type="auto" tdd="true">
<name>Task 3: Implement class method discovery and grouping</name>
<files>backend/core/external_precondition_bridge.py, backend/tests/unit/test_external_bridge.py</files>
<requirements>[DATA-01]</requirements>
<read_first>
- backend/core/external_precondition_bridge.py
- .planning/phases/17-后端数据获取桥接/17-CONTEXT.md
</read_first>
<behavior>
- Test 1: discover_class_methods() returns list of method info dicts
- Test 2: get_data_methods_grouped() returns list of class groups with methods
- Test 3: Cache is populated after first call to get_data_methods_grouped()
- Test 4: Subsequent calls return cached result without re-scanning
</behavior>
<action>
1. Add function `discover_class_methods()` after `extract_method_info()`:
   - Parameters: `cls: type`
   - Returns: `list[dict]`
   - Use `inspect.getmembers(cls, predicate=inspect.isfunction)` to get methods
   - Filter to public methods only (not starting with `_`)
   - Call `extract_method_info()` for each method
   - Filter out None results
   - Return list of method info dicts

2. Add function `get_data_methods_grouped()` after `discover_class_methods()`:
   - Parameters: none
   - Returns: `list[dict]`
   - Check cache first (`_data_methods_cache`)
   - Load base_params module using `load_base_params_class()`
   - If error, return empty list
   - Scan module for all classes using `inspect.getmembers()`
   - For each class, call `discover_class_methods()`
   - Only include classes that have public methods
   - Build structure:
     ```python
     [
         {
             "name": "BaseParams",
             "methods": [
                 {"name": "inventory_list_data", "description": "...", "parameters": [...]},
                 ...
             ]
         },
         ...
     ]
     ```
   - Cache result in `_data_methods_cache`
   - Return grouped list

3. Create unit tests:
   - Test `discover_class_methods()` with mock class
   - Test `get_data_methods_grouped()` returns empty list when module unavailable
   - Test cache behavior
   - Test grouping by class name
</action>
<acceptance_criteria>
- backend/core/external_precondition_bridge.py contains `def discover_class_methods(`
- backend/core/external_precondition_bridge.py contains `def get_data_methods_grouped(`
- backend/core/external_precondition_bridge.py contains `inspect.getmembers`
- backend/core/external_precondition_bridge.py contains `_data_methods_cache`
- backend/tests/unit/test_external_bridge.py contains `test_get_data_methods_grouped`
</acceptance_criteria>
<verify>
<automated>uv run pytest backend/tests/unit/test_external_bridge.py::TestDataMethodsDiscovery -x -v</automated>
</verify>
<done>get_data_methods_grouped() returns list of classes with their public methods and parameter signatures, properly cached</done>
</task>

</tasks>

<verification>
**Unit Tests:**
```bash
uv run pytest backend/tests/unit/test_external_bridge.py::TestDataMethodsDiscovery -v
```

**Expected Results:**
- All tests pass
- load_base_params_class() handles unavailable module gracefully
- extract_method_info() correctly parses method signatures
- get_data_methods_grouped() returns grouped methods with caching
</verification>

<success_criteria>
1. Bridge module can load base_params.py classes lazily
2. Method signatures extracted with full parameter info (name, type, required, default)
3. Methods grouped by class name
4. Results cached at module level
5. All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/17-后端数据获取桥接/17-01-SUMMARY.md`
</output>
