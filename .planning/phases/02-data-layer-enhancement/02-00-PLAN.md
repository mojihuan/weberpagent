---
phase: 02-data-layer-enhancement
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/unit/test_models.py
  - backend/tests/unit/test_repository.py
  - backend/tests/conftest.py
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-05]
must_haves:
  truths:
    - "TDD workflow is enabled for assertion models and repository methods"
    - "Test stubs are discoverable by pytest without import errors"
    - "Test fixtures support assertion test scenarios"
  artifacts:
    - path: "backend/tests/unit/test_models.py"
      provides: "Test stubs for Assertion and AssertionResult models"
      contains: "test_assertion_model"
    - path: "backend/tests/unit/test_repository.py"
      provides: "Test stub for RunRepository.get_steps()"
      contains: "test_run_repository_get_steps"
    - path: "backend/tests/conftest.py"
      provides: "Test fixtures for assertion data"
  key_links: []
---

<objective>
Create test scaffolding for Phase 2 requirements. Test stubs use @pytest.mark.skip with reason pointing to implementing plan, following the pattern established in Phase 1 (01-00).

Purpose: Enable TDD workflow for subsequent plans
Output: Test stub files with skip markers
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer-enhancement/02-CONTEXT.md
@.planning/phases/02-data-layer-enhancement/02-RESEARCH.md
@.planning/phases/02-data-layer-enhancement/02-VALIDATION.md

<interfaces>
<!-- Key patterns from existing codebase -->

From backend/db/models.py:
```python
def generate_id() -> str:
    return uuid.uuid4().hex[:8]

class Task(Base):
    __tablename__ = "tasks"
    id: Mapped[str] = mapped_column(String(8), primary_key=True, default=generate_id)
    # ... relationships ...
    runs: Mapped[List["Run"]] = relationship("Run", back_populates="task")
```

From backend/db/repository.py:
```python
class StepRepository:
    async def list_by_run(self, run_id: str) -> List[Step]:
        stmt = select(Step).where(Step.run_id == run_id).order_by(Step.step_index)
        result = await self.session.execute(stmt)
        return list(result.scalars())
```

From backend/tests/unit/test_database_async.py (Phase 1 pattern):
```python
@pytest.mark.asyncio
async def test_concurrent_sessions():
    # Test implementation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create model test stubs</name>
  <files>backend/tests/unit/test_models.py</files>
  <action>
Create test stubs for DATA-01, DATA-02, DATA-03. Each test uses @pytest.mark.skip with reason pointing to plan 02-01.

Add the following test stubs:
1. `test_assertion_model` - verifies Assertion model has id, task_id, name, type, expected fields
2. `test_assertion_result_model` - verifies AssertionResult model has id, run_id, assertion_id, status, message, actual_value fields
3. `test_assertion_relationships` - verifies Task.assertions relationship and cascade delete

Follow the pattern from Phase 1 test stubs (01-00): skip with reason explaining which plan will implement.
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_models.py --collect-only -q 2>&1 | grep -E "test_assertion|test_relationship"</automated>
  </verify>
  <done>Test stubs exist with skip markers, collectible by pytest</done>
</task>

<task type="auto">
  <name>Task 2: Create repository test stubs</name>
  <files>backend/tests/unit/test_repository.py</files>
  <action>
Create test stub for DATA-05. Use @pytest.mark.skip with reason pointing to plan 02-02.

Add test stub:
1. `test_run_repository_get_steps` - verifies RunRepository.get_steps() returns list of Step ordered by step_index

Follow the skip pattern from Phase 1 test stubs.
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_repository.py --collect-only -q 2>&1 | grep "test_run_repository_get_steps"</automated>
  </verify>
  <done>Test stub exists with skip marker, collectible by pytest</done>
</task>

<task type="auto">
  <name>Task 3: Add test fixtures for assertions</name>
  <files>backend/tests/conftest.py</files>
  <action>
Add fixtures to support assertion-related tests. These fixtures will be used by plans 02-01 and 02-02.

Add fixtures:
1. `sample_assertion_data` - dict with name, type, expected for creating Assertion
2. `sample_assertion_result_data` - dict with status, message, actual_value for creating AssertionResult

Use existing conftest.py patterns (async session fixtures already exist from Phase 1).

Note: Do NOT create fixtures that require database models that don't exist yet. Only create data dicts.
  </action>
  <verify>
    <automated>uv run python -c "from backend.tests.conftest import *; print('conftest loads successfully')"</automated>
  </verify>
  <done>Fixtures exist and conftest.py loads without errors</done>
</task>

</tasks>

<verification>
- All test files created and collectible by pytest
- Skip markers point to correct implementing plans
- Fixtures provide test data without requiring non-existent models
</verification>

<success_criteria>
- `backend/tests/unit/test_models.py` exists with 3 skipped test stubs
- `backend/tests/unit/test_repository.py` exists with 1 skipped test stub
- `backend/tests/conftest.py` has assertion data fixtures
- All files load without import errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer-enhancement/02-00-SUMMARY.md`
</output>
