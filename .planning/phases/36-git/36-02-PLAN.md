---
phase: 36-git
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - webseleniumerp/
  - .gitignore
  - .env.example
autonomous: false
requirements: [GIT-02]
user_setup: []

must_haves:
  truths:
    - "webseleniumerp 目录存在于项目根目录"
    - "webseleniumerp 目录中没有嵌套的 .git 目录"
    - "webseleniumerp/config/settings.py 被正确忽略（不会被提交）"
    - "服务启动后可以正常导入 webseleniumerp 模块"
  artifacts:
    - path: "webseleniumerp/"
      provides: "External ERP automation module"
      contains: "common/base_prerequisites.py"
    - path: ".gitignore"
      provides: "Sensitive file exclusion"
      contains: "webseleniumerp/config/settings.py"
    - path: ".env.example"
      provides: "Configuration template"
      contains: "WEBSERP_PATH="
  key_links:
    - from: "backend/core/external_precondition_bridge.py"
      to: "webseleniumerp/common/"
      via: "sys.path.insert + import"
      pattern: "WEBSERP_PATH"
    - from: ".gitignore"
      to: "webseleniumerp/config/settings.py"
      via: "git check-ignore"
      pattern: "webseleniumerp/config/settings.py"
---

<objective>
将外部的 webseleniumerp 项目复制到 weberpagent 项目中，作为子目录管理，便于统一部署到云端。

Purpose: 用户只需管理一个 Git 仓库，云端部署时不需要额外配置外部依赖路径
Output: webseleniumerp 目录存在于项目中，敏感配置被正确忽略，模块可正常引用
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-git/36-RESEARCH.md
@.planning/phases/36-git/36-VALIDATION.md

<interfaces>
<!-- External Module Configuration -->
Source path: /Users/huhu/project/webseleniumerp
Target path: ./webseleniumerp (relative to project root)
Current WEBSERP_PATH in .env.example: /Users/huhu/project/webseleniumerp
New WEBSERP_PATH: ./webseleniumerp

<!-- Sensitive Files to Ignore -->
webseleniumerp/config/settings.py - Contains ERP credentials

<!-- Key Files in webseleniumerp -->
webseleniumerp/common/base_prerequisites.py - Core module
webseleniumerp/common/base_params.py - Parameter definitions
webseleniumerp/config/settings.py - Sensitive configuration (MUST be ignored)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 复制 webseleniumerp 到项目目录</name>
  <read_first>
    - .gitignore (to check existing ignore rules)
    - /Users/huhu/project/webseleniumerp/.gitignore (source gitignore for reference)
  </read_first>
  <action>
    使用 rsync 命令复制 webseleniumerp 目录到项目中，同时排除 .git 目录以避免嵌套 Git 仓库问题。

    执行命令:
    ```bash
    # 使用 rsync 复制，排除 .git 目录
    rsync -av --exclude='.git' /Users/huhu/project/webseleniumerp/ ./webseleniumerp/
    ```

    关键参数说明:
    - `-a`: 归档模式，保留文件属性
    - `-v`: 显示详细信息
    - `--exclude='.git'`: 排除 .git 目录，避免嵌套 Git 仓库

    复制完成后验证:
    ```bash
    # 确认目录存在
    ls -la ./webseleniumerp/

    # 确认没有嵌套 .git
    test ! -d ./webseleniumerp/.git && echo "OK: no nested .git"
    ```
  </action>
  <acceptance_criteria>
    - `./webseleniumerp/common/base_prerequisites.py` file exists
    - `./webseleniumerp/config/settings.py` file exists
    - `test ! -d ./webseleniumerp/.git` exits with code 0 (no nested .git)
    - `ls ./webseleniumerp/ | wc -l` shows at least 10 items (directory structure preserved)
  </acceptance_criteria>
  <verify>
    <automated>ls ./webseleniumerp/common/base_prerequisites.py && test ! -d ./webseleniumerp/.git && echo "Copy successful"</automated>
  </verify>
  <done>
    - webseleniumerp 目录存在于项目根目录
    - webseleniumerp 目录中没有 .git 子目录
    - 关键文件 common/base_prerequisites.py 存在
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 .gitignore 忽略敏感文件</name>
  <read_first>
    - .gitignore (current ignore rules)
    - ./webseleniumerp/config/settings.py (to verify sensitive file exists)
  </read_first>
  <action>
    在项目根目录的 .gitignore 文件末尾添加 webseleniumerp 敏感文件的忽略规则。

    要添加的内容（追加到 .gitignore 末尾）:
    ```gitignore

    # webseleniumerp sensitive files
    # Contains ERP credentials and must NOT be committed
    webseleniumerp/config/settings.py
    ```

    添加位置: 文件末尾（在 `_documents/` 之后）

    添加后验证:
    ```bash
    # 确认 settings.py 被正确忽略
    git check-ignore ./webseleniumerp/config/settings.py && echo "OK: settings.py is ignored"
    ```
  </action>
  <acceptance_criteria>
    - `.gitignore` contains line `webseleniumerp/config/settings.py`
    - `git check-ignore ./webseleniumerp/config/settings.py` exits with code 0
    - `grep -c "webseleniumerp/config/settings.py" .gitignore` returns 1
  </acceptance_criteria>
  <verify>
    <automated>git check-ignore ./webseleniumerp/config/settings.py</automated>
  </verify>
  <done>
    - .gitignore 包含 webseleniumerp/config/settings.py 忽略规则
    - git check-ignore 确认 settings.py 被忽略
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 .env.example 配置模板</name>
  <read_first>
    - .env.example (current template)
    - .env (current user config, for reference only)
  </read_first>
  <action>
    更新 .env.example 文件中的 WEBSERP_PATH 配置说明，使用相对路径替代绝对路径。

    找到以下内容:
    ```
    WEBSERP_PATH=/Users/huhu/project/webseleniumerp
    ```

    替换为:
    ```
    # Path to webseleniumerp project (now included in project)
    # Use relative path for cloud deployment compatibility
    WEBSERP_PATH=./webseleniumerp
    ```

    同时更新注释说明，告知用户迁移后应使用相对路径。

    完整的 External Precondition Module Configuration 部分应变为:
    ```env
    # ============================================
    # External Precondition Module Configuration
    # ============================================
    # Path to webseleniumerp project (now included in project)
    # Use relative path for cloud deployment compatibility
    # The project must contain base_prerequisites.py and config/settings.py
    WEBSERP_PATH=./webseleniumerp
    ```
  </action>
  <acceptance_criteria>
    - `.env.example` contains `WEBSERP_PATH=./webseleniumerp`
    - `.env.example` does NOT contain `/Users/huhu/project/webseleniumerp` (absolute path removed)
    - Comment mentions "relative path" or "相对路径"
  </acceptance_criteria>
  <verify>
    <automated>grep "WEBSERP_PATH=./webseleniumerp" .env.example</automated>
  </verify>
  <done>
    - .env.example 中 WEBSERP_PATH 更新为相对路径 ./webseleniumerp
    - 配置注释说明使用相对路径的原因
  </done>
</task>

<task type="auto">
  <name>Task 4: 更新用户 .env 配置</name>
  <read_first>
    - .env (current user configuration)
  </read_first>
  <action>
    更新用户本地 .env 文件中的 WEBSERP_PATH 配置为相对路径。

    找到或添加:
    ```
    WEBSERP_PATH=./webseleniumerp
    ```

    如果 .env 中已有 WEBSERP_PATH 配置:
    - 将绝对路径 `/Users/huhu/project/webseleniumerp` 替换为相对路径 `./webseleniumerp`

    如果 .env 中没有 WEBSERP_PATH 配置:
    - 在 External Precondition Module Configuration 部分添加 `WEBSERP_PATH=./webseleniumerp`

    注意: 根据当前 .env 文件内容，WEBSERP_PATH 被注释掉了，需要取消注释并设置值。
  </action>
  <acceptance_criteria>
    - `.env` contains line `WEBSERP_PATH=./webseleniumerp` (uncommented)
    - `.env` does NOT contain commented `# WEBSERP_PATH=/Users/huhu/project/webseleniumerp`
  </acceptance_criteria>
  <verify>
    <automated>grep "^WEBSERP_PATH=./webseleniumerp" .env</automated>
  </verify>
  <done>
    - .env 中 WEBSERP_PATH 设置为 ./webseleniumerp
    - 配置可被后端服务正确读取
  </done>
</task>

<task type="auto">
  <name>Task 5: 验证模块可正常导入</name>
  <read_first>
    - backend/core/external_precondition_bridge.py (how the module is imported)
    - backend/config/settings.py (how WEBSERP_PATH is read)
    - ./webseleniumerp/common/base_prerequisites.py (target module)
  </read_first>
  <action>
    运行集成测试验证 webseleniumerp 模块可正常导入和使用。

    执行测试:
    ```bash
    # 运行外部桥接模块的集成测试
    uv run pytest backend/tests/unit/test_external_bridge.py -v
    ```

    如果测试通过，说明:
    1. WEBSERP_PATH 配置正确
    2. webseleniumerp 模块可正常导入
    3. 模块中的函数可正常调用

    如果测试失败，检查:
    1. .env 中 WEBSERP_PATH 是否设置正确
    2. webseleniumerp 目录结构是否完整
    3. 敏感文件 settings.py 是否存在（即使被忽略也需要存在）
  </action>
  <acceptance_criteria>
    - `uv run pytest backend/tests/unit/test_external_bridge.py -v` exits with code 0
    - All tests in test_external_bridge.py pass
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_external_bridge.py -v --tb=short</automated>
  </verify>
  <done>
    - 集成测试全部通过
    - webseleniumerp 模块可正常导入
    - 外部前置条件桥接功能正常工作
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: 验证集成完成</name>
  <read_first>
    - .gitignore (verify ignore rules)
    - ./webseleniumerp/ (verify directory structure)
  </read_first>
  <action>
    Claude 已完成 webseleniumerp 集成，请用户验证各项配置正确。
  </action>
  <what-built>
    - webseleniumerp 目录已复制到项目根目录
    - .gitignore 已添加敏感文件忽略规则
    - .env.example 和 .env 已更新为相对路径
    - 集成测试通过，模块可正常导入
  </what-built>
  <how-to-verify>
    1. 执行 `ls webseleniumerp/` 确认目录存在
    2. 执行 `test ! -d webseleniumerp/.git && echo "OK"` 确认无嵌套 .git
    3. 执行 `git check-ignore webseleniumerp/config/settings.py` 确认敏感文件被忽略
    4. 执行 `uv run uvicorn backend.api.main:app --port 8080` 启动服务，确认无错误
  </how-to-verify>
  <acceptance_criteria>
    - `ls webseleniumerp/` shows directory contents
    - `test ! -d webseleniumerp/.git` exits with code 0
    - `git check-ignore webseleniumerp/config/settings.py` exits with code 0
    - Server starts without module import errors
  </acceptance_criteria>
  <verify>
    <automated>ls webseleniumerp/common/base_prerequisites.py && test ! -d webseleniumerp/.git && git check-ignore webseleniumerp/config/settings.py</automated>
    <manual>Server startup test</manual>
  </verify>
  <done>
    - 用户确认 webseleniumerp 目录存在且结构正确
    - 用户确认无嵌套 .git 目录
    - 用户确认敏感文件被正确忽略
    - 用户确认服务可正常启动
  </done>
  <resume-signal>输入 "approved" 继续，或描述问题</resume-signal>
</task>

</tasks>

<verification>
## Phase Checks

1. **目录存在验证**
   ```bash
   ls webseleniumerp/common/base_prerequisites.py
   # 应显示文件存在
   ```

2. **无嵌套 Git 验证**
   ```bash
   test ! -d webseleniumerp/.git && echo "OK"
   # 应输出 OK
   ```

3. **敏感文件忽略验证**
   ```bash
   git check-ignore webseleniumerp/config/settings.py
   # 应无输出且退出码为 0
   ```

4. **模块导入验证**
   ```bash
   uv run pytest backend/tests/unit/test_external_bridge.py -v
   # 所有测试应通过
   ```
</verification>

<success_criteria>
- [ ] webseleniumerp 目录存在于项目根目录
- [ ] webseleniumerp 目录中没有嵌套的 .git 目录
- [ ] webseleniumerp/config/settings.py 被 .gitignore 正确忽略
- [ ] .env.example 中 WEBSERP_PATH 更新为 ./webseleniumerp
- [ ] .env 中 WEBSERP_PATH 更新为 ./webseleniumerp
- [ ] 集成测试通过，模块可正常导入
</success_criteria>

<output>
After completion, create `.planning/phases/36-git/36-02-SUMMARY.md`
</output>
