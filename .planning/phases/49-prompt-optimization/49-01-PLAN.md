---
phase: 49-prompt-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/prompts.py
  - backend/tests/unit/test_enhanced_prompt.py
autonomous: true
requirements:
  - PRM-01
  - PRM-02
  - PRM-03
  - PRM-04
  - PRM-05

must_haves:
  truths:
    - "ENHANCED_SYSTEM_MESSAGE contains click-to-edit instruction for Ant Design table cells"
    - "ENHANCED_SYSTEM_MESSAGE contains failure recovery rule: 2 consecutive failures force strategy change"
    - "ENHANCED_SYSTEM_MESSAGE contains post-fill verification guidance"
    - "ENHANCED_SYSTEM_MESSAGE contains pre-submit validation rule"
    - "ENHANCED_SYSTEM_MESSAGE is exported from prompts.py and ready for extend_system_message injection"
  artifacts:
    - path: "backend/agent/prompts.py"
      provides: "ENHANCED_SYSTEM_MESSAGE constant merging CHINESE_ENHANCEMENT valuable content with 4 new ERP-specific sections"
      contains: "ENHANCED_SYSTEM_MESSAGE"
    - path: "backend/tests/unit/test_enhanced_prompt.py"
      provides: "Unit tests verifying ENHANCED_SYSTEM_MESSAGE structure and key content"
      exports: ["TestEnhancedPrompt"]
  key_links:
    - from: "backend/agent/prompts.py"
      to: "backend/core/agent_service.py"
      via: "import ENHANCED_SYSTEM_MESSAGE then pass as extend_system_message parameter"
      pattern: "from backend\\.agent\\.prompts import.*ENHANCED_SYSTEM_MESSAGE"
---

<objective>
Create ENHANCED_SYSTEM_MESSAGE replacing CHINESE_ENHANCEMENT with ERP-specific guidance.

Purpose: Address the 5 core Agent reliability problems from outputs/7fcea593 analysis. The prompt guides Agent behavior for click-to-edit tables, failure recovery, field verification, and pre-submit validation (PRM-01~04). Merges valuable content from existing CHINESE_ENHANCEMENT (PRM-05).

Output: `backend/agent/prompts.py` with new ENHANCED_SYSTEM_MESSAGE constant, `backend/tests/unit/test_enhanced_prompt.py` with structural keyword tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-prompt-optimization/49-CONTEXT.md
@.planning/phases/49-prompt-optimization/49-RESEARCH.md
@backend/agent/prompts.py
@backend/agent/monitored_agent.py
@docs/plans/2026-03-27-agent-reliability-design.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/agent/prompts.py (current file being modified):
```python
CHINESE_ENHANCEMENT = """..."""  # Will be replaced by ENHANCED_SYSTEM_MESSAGE
LOGIN_TASK_PROMPT = """..."""     # Keep unchanged
```

From browser_use/agent/service.py (Agent constructor reference):
```python
class Agent:
    def __init__(self, ..., extend_system_message: str | None = None, ...):
        # extend_system_message appends to base system prompt via SystemPrompt class
```

From backend/agent/monitored_agent.py (Phase 48 intervention message format):
```python
# Intervention messages use Chinese format: 【type_label】description + suggested action
# Example: 【停滞警告】连续2次操作同一元素失败。建议：使用 evaluate 执行 JS 查找元素
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write ENHANCED_SYSTEM_MESSAGE structure tests</name>
  <files>backend/tests/unit/test_enhanced_prompt.py</files>
  <read_first>
    backend/agent/prompts.py
    backend/tests/unit/test_stall_detector.py
  </read_first>
  <behavior>
    - Test 1: ENHANCED_SYSTEM_MESSAGE is a non-empty string exported from backend.agent.prompts
    - Test 2: ENHANCED_SYSTEM_MESSAGE contains click-to-edit keywords (click, td, input, edit)
    - Test 3: ENHANCED_SYSTEM_MESSAGE contains failure recovery keywords (fail, evaluate, find_elements, skip)
    - Test 4: ENHANCED_SYSTEM_MESSAGE contains post-fill verification keywords (verify, confirm, value)
    - Test 5: ENHANCED_SYSTEM_MESSAGE contains pre-submit validation keywords (submit, confirm, validate, check)
    - Test 6: ENHANCED_SYSTEM_MESSAGE is under 60 lines (per D-01 conciseness rule)
    - Test 7: ENHANCED_SYSTEM_MESSAGE contains Chinese form field mapping content from CHINESE_ENHANCEMENT (login, search, cancel keywords)
    - Test 8: ENHANCED_SYSTEM_MESSAGE does NOT contain the browser-use action output format JSON template (was section 4 of old CHINESE_ENHANCEMENT, redundant with browser-use built-in)
  </behavior>
  <action>Create test file `backend/tests/unit/test_enhanced_prompt.py` with class `TestEnhancedPrompt` containing 8 test methods as listed in behavior. Import ENHANCED_SYSTEM_MESSAGE from backend.agent.prompts. Each test uses keyword-based assertions (checking for presence of specific Chinese/English terms) not exact string matching (per D-09). Use `len(ENHANCED_SYSTEM_MESSAGE.strip().splitlines())` for line count. For Test 8, verify absence of `"action":` and `"selector":` JSON template strings. Run tests to confirm RED state (ENHANCED_SYSTEM_MESSAGE does not exist yet).</action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/unit/test_enhanced_prompt.py -v --tb=short 2>&1 | head -40</automated>
  </verify>
  <done>8 test methods exist in TestEnhancedPrompt class, all fail because ENHANCED_SYSTEM_MESSAGE does not yet exist in prompts.py</done>
</task>

<task type="auto">
  <name>Task 2: Create ENHANCED_SYSTEM_MESSAGE in prompts.py</name>
  <files>backend/agent/prompts.py</files>
  <read_first>
    backend/agent/prompts.py
    backend/agent/monitored_agent.py
  </read_first>
  <action>
Replace the existing `CHINESE_ENHANCEMENT` constant with `ENHANCED_SYSTEM_MESSAGE` in `backend/agent/prompts.py`.

**IMPORTANT:** Keep the `CHINESE_ENHANCEMENT` variable name as an alias pointing to `ENHANCED_SYSTEM_MESSAGE` for backward compatibility with `backend/agent/browser_agent.py:87` and `backend/agent/proxy_agent.py:111` which still import `CHINESE_ENHANCEMENT`. Add after ENHANCED_SYSTEM_MESSAGE:
```python
# Backward compatibility alias for existing imports
CHINESE_ENHANCEMENT = ENHANCED_SYSTEM_MESSAGE
```

**ENHANCED_SYSTEM_MESSAGE content (Chinese, per D-03):**

Write the prompt in concise imperative style (D-01: one sentence per rule, under 60 lines total). Structure as 4 sections per D-05:

**Section 1 - 表格编辑模式 (PRM-01):**
- Ant Design table `<td>` cells appear empty in DOM snapshot - they are click-to-edit
- Rule: click the `<td>` first, wait for `<input>` to appear, then input value
- Identifying characteristic: consecutive empty `<td>` elements in a table row

**Section 2 - 失败恢复强制规则 (PRM-02):**
- Same element failing 2 consecutive times: MUST NOT retry same operation
- Forced alternatives: use `evaluate` to execute JS, use `find_elements` for precise locate, scroll to find element, or skip step
- Coordinate with Phase 48 StallDetector (also uses 2-failure threshold)

**Section 3 - 字段填写后验证 (PRM-03):**
- After filling a value field, immediately verify the value was correctly entered
- Use screenshot or evaluate JS to check `input.value` matches intended value
- If mismatch, clear and re-fill rather than proceeding

**Section 4 - 提交前校验 (PRM-04):**
- Before clicking submit/confirm/save: check all required fields are filled
- Verify key field values match task description requirements
- If any field is incorrect or unfilled, fix it before submitting

**Merged from CHINESE_ENHANCEMENT (per D-02, D-04):**
- Section 5 - 元素识别策略: Keep selector priority (text > role > CSS) and Chinese form field mapping (username/password/login/search/cancel)
- Do NOT include the JSON output format template (section 4 of old CHINESE_ENHANCEMENT) - browser-use has its own built-in format
- Add ERP-specific tips: popup handling, list selector strategy (per D-04 "Claude may supplement ERP-related guidance")

Keep `LOGIN_TASK_PROMPT` unchanged at the bottom of the file.
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/unit/test_enhanced_prompt.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ENHANCED_SYSTEM_MESSAGE exported from backend/agent/prompts.py
    - CHINESE_ENHANCEMENT = ENHANCED_SYSTEM_MESSAGE alias for backward compat
    - LOGIN_TASK_PROMPT unchanged
    - All 8 TestEnhancedPrompt tests pass (GREEN)
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/huhu/project/weberpagent
uv run pytest backend/tests/unit/test_enhanced_prompt.py -v
```
All 8 tests pass. `grep -c "ENHANCED_SYSTEM_MESSAGE" backend/agent/prompts.py` returns >= 1. `grep -c "CHINESE_ENHANCEMENT = ENHANCED_SYSTEM_MESSAGE" backend/agent/prompts.py` returns 1.
</verification>

<success_criteria>
- ENHANCED_SYSTEM_MESSAGE is a Python string constant in backend/agent/prompts.py
- Contains all 4 required sections: click-to-edit, failure recovery, field verification, pre-submit validation
- Merges valuable CHINESE_ENHANCEMENT content (form field mapping, selector strategy)
- Under 60 lines, concise imperative Chinese style
- CHINESE_ENHANCEMENT backward compat alias exists
- All 8 unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/49-prompt-optimization/49-01-SUMMARY.md`
</output>
