---
phase: 69-prompt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/prompts.py
  - backend/tests/unit/test_enhanced_prompt.py
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - RECOV-03

must_haves:
  truths:
    - "Section 9 包含行标识定位规则，指导 Agent 看到 <!-- 行: X --> 注释后锁定目标行"
    - "Section 9 包含反重复规则，指导 Agent 看到失败标注后切换策略"
    - "Section 9 包含策略优先级规则，指导 Agent 按标注策略操作"
    - "Section 9 包含三种失败模式的检测-标注-切换恢复流程"
    - "追加后 ENHANCED_SYSTEM_MESSAGE 总行数不超过 80 行"
  artifacts:
    - path: "backend/agent/prompts.py"
      provides: "Section 9 新增四组操作规则（行标识/反重复/策略优先级/失败恢复）"
      contains: "行标识定位"
    - path: "backend/tests/unit/test_enhanced_prompt.py"
      provides: "Phase 69 Section 9 内容断言测试"
      contains: "TestSection9Phase69"
  key_links:
    - from: "prompts.py Section 9.1 行标识定位"
      to: "dom_patch.py <!-- 行: {id} --> annotation format"
      via: "Agent reads DOM dump, sees row comment, follows rule to lock target row"
      pattern: "行:.*行标识"
    - from: "prompts.py Section 9.2 反重复"
      to: "dom_patch.py [已尝试 N 次 模式: ...] annotation format"
      via: "Agent sees failure annotation, switches strategy"
      pattern: "已尝试.*模式"
    - from: "prompts.py Section 9.3 策略优先级"
      to: "dom_patch.py [策略: 1-原生 input] annotation format"
      via: "Agent sees strategy annotation, uses indicated strategy"
      pattern: "策略.*优先"
---

<objective>
在 prompts.py 的 Section 9 末尾追加四组 ERP 表格操作规则（行标识定位、反重复、策略优先级、失败恢复），让 Agent 理解 DOM Patch 注入的标注含义并据此操作。

Purpose: 将 Phase 68 注入到 DOM dump 中的机器标注（行注释、失败标注、策略标注）翻译为 Agent 可理解的操作指令
Output: prompts.py Section 9 新增 ~16 行规则；test_enhanced_prompt.py 新增内容断言测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-prompt/69-CONTEXT.md
@.planning/phases/69-prompt/69-RESEARCH.md
@.planning/phases/68-dom-patch/68-02-SUMMARY.md

<interfaces>
<!-- Annotation formats from Phase 68 that the prompt rules must reference -->

From Phase 68 serialize_tree annotations (what Agent sees in DOM dump):
```html
<!-- Row identity comment above <tr>: -->
<!-- 行: I01784004409597 -->

<!-- Failure + strategy annotation (only on failed ERP inputs): -->
<!-- 行内 input [行: I01784004409597] [策略: 2-需先 click] [已尝试 2 次 模式: click_no_effect] -->

<!-- Strategy-only annotation (after first failure, before downgrade): -->
<!-- 行内 input [行: I01784004409597] [策略: 1-原生 input] -->
```

From backend/agent/dom_patch.py _STRATEGY_NAMES:
```python
# Phase 68 D-03 strategy naming
{1: "1-原生 input", 2: "2-需先 click", 3: "3-evaluate JS"}
```

From backend/agent/stall_detector.py failure modes:
```python
# Three failure modes detected by detect_failure_mode():
# "click_no_effect" -- click 后 DOM 无变化
# "wrong_column" -- 误点错误列
# "edit_not_active" -- input 操作失败（编辑态未激活）
```

Current Section 9 structure in prompts.py (lines 52-83):
```
## 9. ERP 表格单元格填写
...existing content...
- 填写后验证表格中显示的值已变为目标值（如 0.00 变为 150）
"""  <-- closing triple-quote at line 83
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for Section 9 Phase 69 prompt rules</name>
  <files>backend/tests/unit/test_enhanced_prompt.py</files>
  <read_first>
    - backend/tests/unit/test_enhanced_prompt.py (current test file, existing patterns)
    - backend/agent/prompts.py lines 52-83 (current Section 9 content)
    - .planning/phases/69-prompt/69-CONTEXT.md (D-05 through D-07 decisions)
  </read_first>
  <behavior>
    - Test 1 (PROMPT-01): ENHANCED_SYSTEM_MESSAGE contains row identity rules -- assert '行标识' or '行定位' keyword present, and '<!-- 行:' annotation reference present
    - Test 2 (PROMPT-02): ENHANCED_SYSTEM_MESSAGE contains anti-repeat rules -- assert '已尝试' or '反重复' or '不要重复' keyword present
    - Test 3 (PROMPT-03): ENHANCED_SYSTEM_MESSAGE contains strategy priority rules -- assert '策略' keyword present with '1-原生 input' or '2-需先 click' or '3-evaluate JS' reference
    - Test 4 (RECOV-03): ENHANCED_SYSTEM_MESSAGE contains failure recovery rules for all three modes -- assert 'click_no_effect' or 'click.*无.*效' and 'wrong_column' or '错误列' and 'edit_not_active' or '编辑.*未激活' present
    - Test 5: ENHANCED_SYSTEM_MESSAGE total line count remains under 80 after append
    - Test 6: Existing Section 9 content is NOT modified -- assert '单元格定位' and '禁止行为' and '点击编辑工作流' still present
  </behavior>
  <action>
Add a new test class `TestSection9Phase69` to the END of `backend/tests/unit/test_enhanced_prompt.py`.

Follow the existing test patterns in the file:
- Import ENHANCED_SYSTEM_MESSAGE from backend.agent.prompts (already imported at top of file)
- Use `.lower()` for case-insensitive keyword matching where appropriate
- Use `ENHANCED_SYSTEM_MESSAGE` directly for Chinese keyword matching

Test methods to add:

```python
class TestSection9Phase69:
    """Tests for Phase 69 Section 9 prompt rule additions (PROMPT-01/02/03, RECOV-03)."""

    def test_contains_row_identity_rules(self):
        """PROMPT-01: Section 9 must contain row identity usage rules."""
        lower = ENHANCED_SYSTEM_MESSAGE.lower()
        assert "行标识" in ENHANCED_SYSTEM_MESSAGE or "行定位" in ENHANCED_SYSTEM_MESSAGE
        assert "行:" in ENHANCED_SYSTEM_MESSAGE  # References annotation format <!-- 行: X -->

    def test_contains_anti_repeat_rules(self):
        """PROMPT-02: Section 9 must contain anti-repeat rules."""
        assert "已尝试" in ENHANCED_SYSTEM_MESSAGE or "反重复" in ENHANCED_SYSTEM_MESSAGE or "不要重复" in ENHANCED_SYSTEM_MESSAGE

    def test_contains_strategy_priority_rules(self):
        """PROMPT-03: Section 9 must contain strategy priority rules."""
        assert "策略" in ENHANCED_SYSTEM_MESSAGE
        # Must reference at least one strategy name
        assert "1-原生 input" in ENHANCED_SYSTEM_MESSAGE or "2-需先 click" in ENHANCED_SYSTEM_MESSAGE or "3-evaluate JS" in ENHANCED_SYSTEM_MESSAGE

    def test_contains_failure_recovery_rules(self):
        """RECOV-03: Section 9 must contain failure recovery rules for all three modes."""
        # click_no_effect mode
        assert "click_no_effect" in ENHANCED_SYSTEM_MESSAGE or ("click" in ENHANCED_SYSTEM_MESSAGE and ("无变化" in ENHANCED_SYSTEM_MESSAGE or "无响应" in ENHANCED_SYSTEM_MESSAGE))
        # wrong_column mode
        assert "wrong_column" in ENHANCED_SYSTEM_MESSAGE or "错误列" in ENHANCED_SYSTEM_MESSAGE or "误点" in ENHANCED_SYSTEM_MESSAGE
        # edit_not_active mode
        assert "edit_not_active" in ENHANCED_SYSTEM_MESSAGE or "未激活" in ENHANCED_SYSTEM_MESSAGE

    def test_total_line_count_under_80(self):
        """D-05/RESEARCH Pitfall 4: Total ENHANCED_SYSTEM_MESSAGE must stay under 80 lines."""
        lines = ENHANCED_SYSTEM_MESSAGE.strip().splitlines()
        assert len(lines) <= 80

    def test_existing_section9_content_unchanged(self):
        """D-07: Existing Section 9 content must not be modified."""
        assert "单元格定位" in ENHANCED_SYSTEM_MESSAGE
        assert "禁止行为" in ENHANCED_SYSTEM_MESSAGE
        assert "点击编辑工作流" in ENHANCED_SYSTEM_MESSAGE
```

Commit with: `test(69-02): add failing tests for Section 9 prompt rules`
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/unit/test_enhanced_prompt.py::TestSection9Phase69 -x -q 2>&1 | tail -5; echo "EXIT=$?"</automated>
  </verify>
  <done>
    Test class TestSection9Phase69 exists with 6 test methods.
    All tests FAIL (because Section 9 does not yet have the new rules).
    `grep -c "def test_" backend/tests/unit/test_enhanced_prompt.py` shows at least 6 more tests than before.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append four rule groups to Section 9 in prompts.py</name>
  <files>backend/agent/prompts.py</files>
  <read_first>
    - backend/agent/prompts.py (full file, especially lines 52-83 Section 9)
    - backend/tests/unit/test_enhanced_prompt.py (tests just written)
    - .planning/phases/69-prompt/69-CONTEXT.md (D-05 concise style, D-06 append order)
    - .planning/phases/69-prompt/69-RESEARCH.md Example 4 (current Section 9 format) and Example 5 (annotation formats)
  </read_first>
  <acceptance_criteria>
    - prompts.py Section 9 contains '**行标识定位：**' or equivalent heading
    - prompts.py Section 9 contains '**反重复操作：**' or equivalent heading
    - prompts.py Section 9 contains '**策略优先级：**' or equivalent heading
    - prompts.py Section 9 contains '**失败恢复：**' or equivalent heading
    - Four sections appear in order: 行标识 -> 反重复 -> 策略优先级 -> 失败恢复 (per D-06)
    - Each section has 2-4 content lines (per D-05)
    - '行:' appears in the new rules (referencing Phase 68 annotation format)
    - '已尝试' appears in the new rules (referencing Phase 68 failure annotation)
    - '1-原生 input' or '2-需先 click' or '3-evaluate JS' appears in strategy rules
    - 'click_no_effect' or '无变化' or '无响应' appears in failure recovery rules
    - 'wrong_column' or '错误列' or '误点' appears in failure recovery rules
    - 'edit_not_active' or '未激活' appears in failure recovery rules
    - ENHANCED_SYSTEM_MESSAGE total lines <= 80
    - `uv run pytest backend/tests/unit/test_enhanced_prompt.py::TestSection9Phase69 -x -q` exits 0
    - `uv run pytest backend/tests/unit/ -x -q` exits 0 (no regressions)
  </acceptance_criteria>
  <action>
In `backend/agent/prompts.py`, modify the `ENHANCED_SYSTEM_MESSAGE` string. The new rules must be appended BEFORE the closing `"""` of the triple-quoted string, AFTER the last existing line (line 82: `- 填写后验证表格中显示的值已变为目标值（如 0.00 变为 150）`).

Insert an empty line, then append the following four sections. Per D-05: concise "看到 X -> 做 Y" format, 2-4 lines each. Per D-06: order is 行标识 -> 反重复 -> 策略优先级 -> 失败恢复. Per D-07: only append, do not modify existing content.

Exact text to append after line 82 (before the closing `"""`):

```

**行标识定位：**
- 看到 `<!-- 行: I数字 -->` 注释 → 该注释下方的 `<tr>` 是目标行，在行内查找目标 input
- 多行有相同 placeholder 时（如两行都有"销售金额"），用行标识注释区分：找到目标行标识所在行的 input
- 不要操作行标识注释下方非目标行的 input

**反重复操作：**
- 看到 `<!-- 行内 input [已尝试 N 次 ...] -->` 标注 → 该元素已失败 N 次，不要重复相同操作
- 失败标注中的 `模式:` 表示失败原因（click_no_effect / wrong_column / edit_not_active），据此切换策略

**策略优先级：**
- 看到 `[策略: 1-原生 input]` → 直接用 input action 填值
- 看到 `[策略: 2-需先 click]` → 先 click 该元素激活编辑态，再 input 填值
- 看到 `[策略: 3-evaluate JS]` → 用 evaluate 执行 JS 直接设值（`document.querySelector('...').value = '...'`）

**失败恢复：**
- click 无响应（click_no_effect）：换用 evaluate JS 点击 → `document.querySelector('...').click()`
- 点错列（wrong_column）：重新定位目标列的 td，确认 IMEI/商品编号所在行后再点击
- 编辑态未激活（edit_not_active）：先 click td 单元格激活编辑态，等待 input 出现后再填值
```

Count the lines after insertion: Section 9 currently ends at line 83. Adding ~17 lines makes it ~100 total for ENHANCED_SYSTEM_MESSAGE. But the existing prompt is 83 lines, Sections 1-8 are lines 10-51 (42 lines). Total after append: ~83 + 17 = ~100 lines.

IMPORTANT: The existing test `test_line_count_under_80` checks total ENHANCED_SYSTEM_MESSAGE lines <= 80. After appending ~17 lines to the current 83 lines, the total would be ~100 lines which EXCEEDS 80. Check the actual line count:
- Current ENHANCED_SYSTEM_MESSAGE: lines 10-83 in the Python file = 74 lines of content inside the triple-quote
- Adding 17 lines = 91 lines total
- The test checks <= 80

SOLUTION: Compress each section to the MINIMUM 2-3 lines while keeping all required keywords. Target: 12-14 new lines total to stay under 80. Use tighter formatting:

```

**行标识定位：**
- 看到 `<!-- 行: I数字 -->` → 锁定该行，在行内找目标 input；多行相同 placeholder 时用行标识区分

**反重复操作：**
- 看到 `[已尝试 N 次 模式: ...]` → 该元素已失败，切换策略，不要重复相同操作

**策略优先级：**
- 看到 `[策略: 1-原生 input]` → 直接 input；`[策略: 2-需先 click]` → 先 click 再 input；`[策略: 3-evaluate JS]` → 用 JS 设值

**失败恢复：**
- click 无响应（click_no_effect）→ 换 evaluate JS 点击；点错列（wrong_column）→ 按 IMEI 重定位目标列；编辑未激活（edit_not_active）→ 先 click td 激活再填值
```

This compressed version is ~10 new lines (1 blank + 4 headings + 4 content lines + 1 blank), keeping total under 80.

Actually, first read the ACTUAL line count of the current prompt before deciding. Read prompts.py and count lines inside the triple-quote. The current test `test_line_count_under_80` passes, meaning current content is <= 80 lines. Count exactly how many lines are available.

After reading prompts.py: ENHANCED_SYSTEM_MESSAGE starts at line 10 (`## 1.`) and ends at line 83 (closing `"""`). That's 74 lines of content. Budget: 80 - 74 = 6 lines available. This is VERY tight.

Wait -- the test splits ENHANCED_SYSTEM_MESSAGE by newlines and checks strip().splitlines(). The string itself starts with a newline after the opening `"""`. Let me re-read: the triple-quote starts on line 9, content starts on line 10. The string's strip().splitlines() would give lines 10-82 content = 73 lines.

Budget: 80 - 73 = 7 lines. Must be extremely concise. Maximum 7 new lines (including blank separators).

Revised append text (7 lines max):

```

**行标识定位：** 看到 `<!-- 行: I数字 -->` → 锁定该行内 input，多行相同 placeholder 用行标识区分
**反重复操作：** 看到 `[已尝试 N 次 模式: ...]` → 切换策略，不重复相同操作
**策略优先级：** `[策略: 1-原生 input]`→直接 input；`[策略: 2-需先 click]`→先 click 再 input；`[策略: 3-evaluate JS]`→用 JS
**失败恢复：** click_no_effect→换 evaluate JS 点击；wrong_column→按 IMEI 重定位；edit_not_active→先 click td 再填值
```

This is 5 new lines (1 blank + 4 rule lines). Total: 73 + 5 = 78 lines. Under 80.

Commit with: `feat(69-02): append row identity, anti-repeat, strategy, and failure recovery rules to Section 9`
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && uv run pytest backend/tests/unit/test_enhanced_prompt.py::TestSection9Phase69 -x -q && uv run pytest backend/tests/unit/ -x -q</automated>
  </verify>
  <done>
    All TestSection9Phase69 tests pass (6 tests).
    All existing tests pass (no regressions).
    ENHANCED_SYSTEM_MESSAGE contains all four rule groups with required keywords.
    Total line count <= 80.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest backend/tests/unit/test_enhanced_prompt.py::TestSection9Phase69 -x -q` -- all Phase 69 prompt tests pass
2. `uv run pytest backend/tests/unit/ -x -q` -- no regressions
3. `grep "行标识定位" backend/agent/prompts.py` -- row identity section present
4. `grep "反重复操作" backend/agent/prompts.py` -- anti-repeat section present
5. `grep "策略优先级" backend/agent/prompts.py` -- strategy priority section present
6. `grep "失败恢复" backend/agent/prompts.py` -- failure recovery section present
</verification>

<success_criteria>
- Section 9 contains row identity rules referencing <!-- 行: --> format (PROMPT-01)
- Section 9 contains anti-repeat rules referencing [已尝试 N 次] format (PROMPT-02)
- Section 9 contains strategy priority rules referencing 策略 1/2/3 (PROMPT-03)
- Section 9 contains failure recovery rules for click_no_effect/wrong_column/edit_not_active (RECOV-03)
- All four sections follow D-05 concise style (2-4 lines each)
- Sections ordered per D-06 (行标识 -> 反重复 -> 策略优先级 -> 失败恢复)
- No existing Section 9 content modified (D-07)
- Total line count under 80
</success_criteria>

<output>
After completion, create `.planning/phases/69-prompt/69-02-SUMMARY.md`
</output>
