---
phase: 10-销售出库用例调通
plan: 04
type: execute
wave: 2
depends_on: [10-01, 10-02, 10-03]
files_modified: []
autonomous: false
requirements: [SALE-05]
user_setup:
  - service: erp_api_module
    why: "External ERP API module for precondition data and assertion verification"
    env_vars:
      - name: ERP_API_MODULE_PATH
        source: "Path to erp-test-project directory containing __init__.py with API functions"
    dashboard_config: []

must_haves:
  truths:
    - "Complete sales outbound test case executes end-to-end"
    - "All UI steps execute successfully"
    - "Preconditions execute and store variables"
    - "API assertions execute and verify results"
    - "Final report shows complete execution results"
  artifacts:
    - path: "backend/api/routes/runs.py"
      provides: "Complete execution flow"
      contains: "run_agent_background"
    - path: "backend/core/agent_service.py"
      provides: "AI-driven browser automation"
      exports: ["AgentService"]
    - path: "frontend/src/pages/ReportDetail.tsx"
      provides: "Complete report display"
      contains: "Report"
  key_links:
    - from: "preconditions"
      to: "UI steps"
      via: "context variable substitution"
      pattern: "substitute_variables"
    - from: "UI steps"
      to: "API assertions"
      via: "sequential execution after UI complete"
      pattern: "if api_assertions:"
    - from: "all results"
      to: "report"
      via: "database storage and ReportService"
      pattern: "report_service.generate_report"
---

<objective>
Execute the complete sales outbound test case end-to-end and verify all components work together.

Purpose: Validate SALE-05 (end-to-end execution success) by running the full test with preconditions, UI steps, and API assertions.
Output: Successful execution with complete report showing all results.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-销售出库用例调通/10-CONTEXT.md
@.planning/phases/10-销售出库用例调通/10-RESEARCH.md
@.planning/phases/09-登录用例调通/09-VERIFICATION.md

<interfaces>
<!-- Key interfaces for executor reference -->

From backend/api/routes/runs.py (complete execution flow):
```python
async def run_agent_background(
    run_id: str,
    task_id: str,
    task_name: str,
    task_description: str,
    max_steps: int,
    preconditions: list[str] | None = None,
    api_assertions: list[str] | None = None,
):
    # 1. Execute preconditions -> context
    # 2. Substitute variables in task_description
    # 3. Execute UI steps via AgentService
    # 4. Execute API assertions (with shared context)
    # 5. Generate report
    # 6. Send finished SSE event
```

Execution Order (from runs.py):
1. Preconditions (lines 84-125) -> Fail-fast on error
2. Variable substitution (line 128)
3. UI execution via AgentService (lines 181-189)
4. API assertions (lines 211-279) -> Collect all results, non-terminating
5. Status determination and report generation (lines 282-297)

From .planning/phases/09-登录用例调通/09-VERIFICATION.md:
```
Phase 9 verified:
- Task creation from frontend works
- AI execution completes successfully
- SSE monitoring shows real-time updates
- Report page displays results and screenshots correctly

Bugs fixed in Phase 9:
- Task API Response Validation Error (JSON deserialization)
- Screenshot URL Construction Error (double /api prefix)
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Execute complete sales outbound test case</name>
  <read_first>
    - backend/api/routes/runs.py (complete execution flow)
    - .planning/phases/09-登录用例调通/09-VERIFICATION.md (Phase 9 patterns)
  </read_first>
  <files>(no code changes - manual execution)</files>
  <action>
    Execute the complete sales outbound test case that was configured in Plans 10-01 through 10-03.

    **Prerequisites Check:**
    1. ERP_API_MODULE_PATH is configured in .env
    2. External API module provides required functions (get_order, etc.)
    3. ERP_BASE_URL, ERP_USERNAME, ERP_PASSWORD are configured
    4. Backend server is running on port 8080
    5. Frontend server is running on port 5173

    **Execution Steps:**
    1. Open frontend at http://localhost:5173
    2. Navigate to task list
    3. Find the sales outbound test task
    4. Click "执行" to start execution
    5. Monitor execution on Run Monitor page

    **Expected Execution Flow:**
    1. SSE event: `precondition` with status "running"
    2. SSE event: `precondition` with status "success" and variables
    3. SSE event: `started` with run_id
    4. SSE events: `step` for each UI action (1-6 steps)
    5. SSE event: `api_assertion` with status "running"
    6. SSE event: `api_assertion` with status "success" or "failed"
    7. SSE event: `finished` with final status

    **Monitor for issues:**
    - Backend terminal logs for errors
    - Browser console for SSE connection issues
    - AI behavior in the automated browser window
  </action>
  <verify>
    <automated>MISSING - Manual E2E verification required</automated>
  </verify>
  <acceptance_criteria>
    - All preconditions execute successfully (status "success")
    - All UI steps execute (1-6 steps, all "success")
    - API assertions execute (status "success" or documented failure reason)
    - Final status is "success" (if all pass) or "failed" (with reason)
    - Report is generated automatically
  </acceptance_criteria>
  <done>
    <what_built>Complete sales outbound test case executed end-to-end</what_built>
    <how_to_verify>
      1. Watch Run Monitor for complete execution flow
      2. Verify all SSE events appear in sequence
      3. Note final status (success/failed)
      4. Navigate to report page
    </how_to_verify>
    <resume_signal>Type "approved" with Run ID and final status, or describe any issues</resume_signal>
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete report with all results</name>
  <read_first>
    - frontend/src/pages/ReportDetail.tsx (report page)
    - frontend/src/components/Report/StepItem.tsx (step display)
    - frontend/src/components/Report/ApiAssertionResults.tsx (API assertion display)
  </read_first>
  <files>(no code changes - manual verification)</files>
  <action>
    Navigate to the report page and verify all results are displayed correctly.

    **Steps:**
    1. Navigate to http://localhost:5173/reports/{run_id}
    2. Verify the following sections are present and correct:

    **Report Header:**
    - Task name: "销售出库测试用例"
    - Status: success (green) or failed (red) with reason
    - Total steps count
    - Duration

    **Precondition Results:**
    - Each precondition shows execution status
    - Variables stored in context are visible (order_no, phone)
    - Duration for each precondition

    **UI Step Results:**
    - Each step shows action, reasoning, and screenshot
    - Step status (success/failed)
    - Screenshots load correctly (no broken images)

    **API Assertion Results:**
    - Each assertion shows code and status
    - Pass/fail status clearly indicated
    - Error message if assertion failed
    - Duration for each assertion

    **Screenshot Verification:**
    - Each step screenshot loads correctly
    - No broken image icons
    - Screenshots show actual UI state at each step

    **Known Issues from Phase 9 (should be fixed):**
    - Screenshot URL should not have double /api prefix
    - JSON fields should deserialize correctly
  </action>
  <verify>
    <automated>MISSING - Manual E2E verification required</automated>
  </verify>
  <acceptance_criteria>
    - Report page loads without errors
    - Task name and status displayed correctly
    - All steps listed with screenshots
    - Screenshots load correctly (no broken images)
    - API assertion results section present
    - Each assertion shows pass/fail status
    - Browser console shows no errors
  </acceptance_criteria>
  <done>
    <what_built>Verified complete report displays all execution results</what_built>
    <how_to_verify>
      1. Open report page
      2. Check each section (header, steps, assertions)
      3. Verify screenshots load
      4. Check browser console for errors
    </how_to_verify>
    <resume_signal>Type "approved" with report URL, or describe any issues</resume_signal>
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Document any bugs discovered during execution</name>
  <read_first>
    - .planning/phases/09-登录用例调通/09-VERIFICATION.md (bug documentation pattern)
  </read_first>
  <files>(no code changes - documentation)</files>
  <action>
    Document any bugs or issues discovered during the complete execution.

    **Bug Documentation Format:**
    For each issue found, document:
    1. Issue description
    2. Steps to reproduce
    3. Expected behavior
    4. Actual behavior
    5. Root cause (if known)
    6. Suggested fix (if known)

    **Common areas to check:**
    - Precondition execution failures
    - Variable substitution errors
    - AI execution errors (element not found, timeout, etc.)
    - API assertion failures (network, data mismatch)
    - Report display issues (missing data, broken images)
    - SSE connection issues

    **Bug Priority:**
    - P0: Blocks execution completely
    - P1: Execution completes but results incorrect
    - P2: Minor issues, workarounds available

    Note: Bug fixes are deferred to Phase 11 as per user decision.
  </action>
  <verify>
    <automated>MISSING - Manual documentation</automated>
  </verify>
  <acceptance_criteria>
    - All discovered bugs are documented
    - Each bug has description, steps to reproduce, and priority
    - Bugs are categorized (execution engine vs frontend)
    - Bug list is ready for Phase 11 planning
  </acceptance_criteria>
  <done>
    <what_built>Bug list documented for Phase 11</what_built>
    <how_to_verify>
      List all bugs discovered during Plans 10-01 through 10-04 execution
    </how_to_verify>
    <resume_signal>Type "approved" with bug list, or "no bugs found"</resume_signal>
  </done>
</task>

</tasks>

<verification>
- Complete test case executes from start to finish
- All phases work together: preconditions -> UI -> assertions -> report
- Report shows complete and accurate results
- Any bugs are documented for Phase 11
</verification>

<success_criteria>
1. Sales outbound test case executes end-to-end without blocking errors (SALE-05)
2. All components integrate correctly (preconditions, UI steps, API assertions)
3. Report displays complete execution results
4. Bugs discovered are documented for Phase 11
</success_criteria>

<output>
After completion, create `.planning/phases/10-销售出库用例调通/10-04-SUMMARY.md`
</output>
