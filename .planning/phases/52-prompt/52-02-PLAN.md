---
phase: 52-prompt
plan: 02
type: execute
wave: 2
depends_on:
  - 52-01
files_modified:
  - docs/test-steps/采购-键盘操作测试步骤.md
autonomous: false
requirements:
  - KB-01
  - KB-02
  - KB-03

must_haves:
  truths:
    - "Agent 在采购单场景中能使用 send_keys('Enter') 在物品编号搜索框触发搜索"
    - "Agent 能使用 send_keys('Escape') 关闭日期选择器弹窗"
    - "Agent 能使用 send_keys('Control+a') + input 覆盖输入框内容"
  artifacts:
    - path: "docs/test-steps/采购-键盘操作测试步骤.md"
      provides: "采购单键盘操作测试步骤文档"
      contains: "send_keys"
  key_links:
    - from: "docs/test-steps/采购-键盘操作测试步骤.md"
      to: "backend/agent/prompts.py"
      via: "runtime dependency — Agent loads ENHANCED_SYSTEM_MESSAGE at execution time, test steps guide which scenarios to exercise"
      pattern: "send_keys"
---

<objective>
通过采购单 ERP 场景验证 Agent 能正确使用键盘操作（Enter 搜索触发、Escape 关闭弹窗、Control+a 全选覆盖）。

Purpose: 确认 Plan 52-01 添加的键盘操作 prompt 段落能被 Qwen 3.5 Plus 遵守，Agent 在实际 ERP 操作中正确使用 send_keys 动作。
Output: 键盘操作测试步骤文档（Markdown）+ 人工验证结果记录
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-prompt/52-CONTEXT.md
@.planning/phases/52-prompt/52-RESEARCH.md
@.planning/phases/52-prompt/52-01-SUMMARY.md
@docs/test-steps/采购-测试步骤.md

<interfaces>
<!-- Existing test step format — executor MUST follow this pattern exactly -->
<!-- From docs/test-steps/采购-测试步骤.md -->

```markdown
# 商品采购模块测试步骤

## TestPurchaseAdd - 采购管理 | 新增采购单

### test_xxx - [描述]

前置条件：xxx

步骤1：{动作}目标{参数}
步骤2：{动作}目标{参数}
...

断言：调用方法：xxx
```

动作类型包括：{点击}、{输入}、{选择}、{send_keys}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建采购单键盘操作测试步骤文档</name>
  <files>docs/test-steps/采购-键盘操作测试步骤.md</files>
  <read_first>
    - backend/agent/prompts.py（确认键盘操作段落已添加，了解具体措辞）
    - docs/test-steps/采购-测试步骤.md（现有采购模块测试步骤的格式风格，MUST 参考）
  </read_first>
  <action>
    在 docs/test-steps/ 目录下创建 采购-键盘操作测试步骤.md，遵循该目录下现有 Markdown 文件的格式（如 采购-测试步骤.md 的 `{动作}目标{参数}` 步骤格式），覆盖三个验证场景（per D-09 采购单场景）：

    场景 1 — Enter 搜索触发（KB-02）：
    - 标题格式：### test_keyboard_enter_search - [键盘]-采购单物品编号回车搜索
    - 前置条件：已登录 ERP 系统
    - 步骤：进入采购管理 → 采购单列表 → 新增采购单 → 在"IMEI/物品编号"输入框输入物品编号 → {send_keys}物品编号输入框{Enter}
    - 预期：搜索结果列表出现匹配项

    场景 2 — Escape 关闭弹窗（KB-03）：
    - 标题格式：### test_keyboard_escape_close_popup - [键盘]-ESC关闭日期选择器
    - 前置条件：采购单表单已打开
    - 步骤：点击日期字段触发日期选择器弹窗 → {send_keys}日期选择器{Escape}
    - 预期：日期选择器已关闭，回到表单状态

    场景 3 — Control+a 全选覆盖（KB-01，per D-05/D-06 重新定义为 Control+a + input 覆盖模式）：
    - 标题格式：### test_keyboard_select_all_overwrite - [键盘]-全选覆盖输入框内容
    - 前置条件：采购单表单已打开，有含内容的输入框
    - 步骤：点击备注输入框 → {send_keys}备注输入框{Control+a} → {输入}备注输入框{新备注内容}
    - 预期：输入框显示新值而非旧值

    每个场景必须包含：
    - 步骤描述（中文，使用 {动作} 格式）
    - 预期的 Agent 动作（包含 send_keys）
    - 验证条件（成功/失败的判断标准）
    - 断言行
  </action>
  <verify>
    <automated>test -f docs/test-steps/采购-键盘操作测试步骤.md && grep -c "send_keys" docs/test-steps/采购-键盘操作测试步骤.md</automated>
  </verify>
  <done>采购-键盘操作测试步骤.md 包含 3 个测试场景，每个场景至少包含 1 次 send_keys 调用，格式与现有测试步骤文件一致</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证 ERP 场景键盘操作</name>
  <files>docs/test-steps/采购-键盘操作验证结果.md</files>
  <action>
    通过前端 UI 运行采购单测试步骤，验证 Agent 能否正确使用 send_keys 动作。
    前置条件：确保后端服务运行中（uv run uvicorn backend.api.main:app --reload --port 8080）且前端可用。

    验证步骤：
    1. 打开前端页面，创建新测试用例或使用 采购-键盘操作测试步骤.md 中的步骤
    2. 场景 1（Enter 搜索）：在采购单物品编号搜索框输入后，观察 Agent 是否使用 send_keys('Enter') 触发搜索
    3. 场景 2（Escape 关闭弹窗）：点击日期字段后，观察 Agent 是否使用 send_keys('Escape') 关闭日期选择器
    4. 场景 3（Control+a 全选覆盖）：在已有内容的输入框，观察 Agent 是否使用 send_keys('Control+a') 后 input 新值

    预期结果：
    - Agent 在搜索框场景主动使用 send_keys('Enter')
    - Agent 在日期选择器场景主动使用 send_keys('Escape')
    - Agent 在需清空输入框场景主动使用 send_keys('Control+a')
    - Agent 不尝试使用 send_keys('Control+v') 粘贴

    验证完成后，由执行者创建 docs/test-steps/采购-键盘操作验证结果.md，记录每个场景的通过/失败状态和简要说明（如 "场景1: 通过 — Agent 正确使用 send_keys('Enter')" 或 "场景2: 失败 — Agent 未使用 Escape 关闭弹窗，而是点击了空白区域"）。
  </action>
  <verify>
    <automated>test -f docs/test-steps/采购-键盘操作测试步骤.md && test -f docs/test-steps/采购-键盘操作验证结果.md && echo "Both files exist"</automated>
  </verify>
  <done>docs/test-steps/采购-键盘操作验证结果.md 已创建，记录 3 个 ERP 键盘操作场景的验证结果（通过/失败+说明）</done>
</task>

</tasks>

<verification>
1. `test -f docs/test-steps/采购-键盘操作测试步骤.md` — 测试步骤文件存在
2. `grep -c "send_keys" docs/test-steps/采购-键盘操作测试步骤.md` — 包含 send_keys 关键词
3. `test -f docs/test-steps/采购-键盘操作验证结果.md` — 验证结果文件存在
4. 人工验证 3 个 ERP 场景均通过
</verification>

<success_criteria>
- 键盘操作测试步骤文档已创建（Markdown 格式，与现有测试步骤风格一致）
- 验证结果文件已创建，记录每个场景的通过/失败状态
- 人工验证 Agent 在采购单场景中正确使用 Enter、Escape、Control+a 三种键盘操作
- Agent 未尝试使用 Ctrl+V 粘贴
</success_criteria>

<output>
After completion, create `.planning/phases/52-prompt/52-02-SUMMARY.md`
</output>
