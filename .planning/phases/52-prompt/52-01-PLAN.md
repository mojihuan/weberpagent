---
phase: 52-prompt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/prompts.py
  - backend/tests/unit/test_enhanced_prompt.py
autonomous: true
requirements:
  - KB-01
  - KB-02
  - KB-03

must_haves:
  truths:
    - "ENHANCED_SYSTEM_MESSAGE 包含键盘操作指导段落（第 6 段）"
    - "键盘段落覆盖 Enter 搜索触发、Escape 关闭弹窗、Control+a 全选覆盖三种场景"
    - "不包含 Ctrl+V 粘贴指导"
    - "键盘段落不超过 10 行"
    - "单元测试验证键盘操作关键词存在"
  artifacts:
    - path: "backend/agent/prompts.py"
      provides: "ENHANCED_SYSTEM_MESSAGE 第 6 段键盘操作指导"
      contains: "## 6. 键盘操作"
    - path: "backend/tests/unit/test_enhanced_prompt.py"
      provides: "test_contains_keyboard_operation_keywords 测试方法"
      contains: "test_contains_keyboard_operation_keywords"
  key_links:
    - from: "backend/tests/unit/test_enhanced_prompt.py"
      to: "backend/agent/prompts.py"
      via: "from backend.agent.prompts import ENHANCED_SYSTEM_MESSAGE"
      pattern: "from backend.agent.prompts import"
---

<objective>
扩展 ENHANCED_SYSTEM_MESSAGE 添加第 6 段键盘操作指导，覆盖 Enter 搜索触发、Escape 关闭弹窗、Control+a 全选覆盖三种场景。

Purpose: browser-use 的 send_keys 动作注册时 description 为空字符串，LLM 无法自行发现该动作的用途。通过 prompt 指导让 Qwen 3.5 Plus 知道何时及如何使用 send_keys。
Output: 更新的 prompts.py（含第 6 段）+ 扩展的 test_enhanced_prompt.py（含键盘操作关键词测试）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-prompt/52-CONTEXT.md
@.planning/phases/52-prompt/52-RESEARCH.md
@backend/agent/prompts.py
@backend/tests/unit/test_enhanced_prompt.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加键盘操作关键词测试（RED 阶段）</name>
  <files>backend/tests/unit/test_enhanced_prompt.py</files>
  <read_first>
    - backend/tests/unit/test_enhanced_prompt.py（现有测试模式，复制其关键词断言风格）
    - backend/agent/prompts.py（确认 ENHANCED_SYSTEM_MESSAGE 当前没有键盘操作内容）
  </read_first>
  <behavior>
    - Test 1: test_contains_keyboard_operation_keywords — ENHANCED_SYSTEM_MESSAGE 包含 "send_keys"
    - Test 2: test_contains_keyboard_operation_keywords — ENHANCED_SYSTEM_MESSAGE 包含 "Enter"（不区分大小写）
    - Test 3: test_contains_keyboard_operation_keywords — ENHANCED_SYSTEM_MESSAGE 包含 "Escape"（不区分大小写）
    - Test 4: test_contains_keyboard_operation_keywords — ENHANCED_SYSTEM_MESSAGE 包含 "Control"（不区分大小写）
    - Test 5: test_contains_keyboard_operation_keywords — ENHANCED_SYSTEM_MESSAGE 包含键盘操作段落标题 "键盘操作"
    - Test 6: test_no_ctrl_v_in_keyboard_section — ENHANCED_SYSTEM_MESSAGE 不包含 "Control+v" 或 "Ctrl+v"（per D-06）
    - Test 7: test_keyboard_section_line_count — 键盘操作段落不超过 10 行（per D-04）
  </behavior>
  <action>
    在 test_enhanced_prompt.py 的 TestEnhancedPrompt 类中添加两个测试方法：

    1. test_contains_keyboard_operation_keywords — 验证 ENHANCED_SYSTEM_MESSAGE 包含以下关键词（小写比较）：
       - "send_keys"
       - "enter"
       - "escape"
       - "control"
       同时验证包含中文段落标题"键盘操作"。

    2. test_no_ctrl_v_guidance — 验证 ENHANCED_SYSTEM_MESSAGE 不包含 "Control+v" 或 "Ctrl+v" 或 "control+v"（per D-06 不指导粘贴操作）。

    3. test_keyboard_section_line_count — 提取 "## 6. 键盘操作" 到下一个 "##" 之间的行（或到字符串末尾），验证该段不超过 10 行（per D-04）。

    遵循现有测试风格：lower = ENHANCED_SYSTEM_MESSAGE.lower() 然后用 assert "keyword" in lower。

    先只写测试，不改 prompts.py，确保测试失败（RED 阶段）。
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_enhanced_prompt.py::TestEnhancedPrompt::test_contains_keyboard_operation_keywords backend/tests/unit/test_enhanced_prompt.py::TestEnhancedPrompt::test_no_ctrl_v_guidance backend/tests/unit/test_enhanced_prompt.py::TestEnhancedPrompt::test_keyboard_section_line_count -x -v 2>&1 | head -30</automated>
  </verify>
  <done>三个新测试方法已添加，运行后因 ENHANCED_SYSTEM_MESSAGE 尚无键盘段落而失败（RED）</done>
</task>

<task type="auto">
  <name>Task 2: 添加键盘操作段落到 ENHANCED_SYSTEM_MESSAGE（GREEN 阶段）</name>
  <files>backend/agent/prompts.py</files>
  <read_first>
    - backend/agent/prompts.py（现有 5 段结构，在第 33 行 "## 5. 元素识别与表单策略" 段落末尾之后追加）
    - .planning/phases/52-prompt/52-RESEARCH.md（"Prompt Addition Pattern" 推荐内容）
  </read_first>
  <action>
    在 ENHANCED_SYSTEM_MESSAGE 的第 5 段（"## 5. 元素识别与表单策略"）末尾、结束三引号之前，追加第 6 段键盘操作指导。

    精确追加内容（per D-01 场景-动作对格式，D-03 中文，D-04 不超过 10 行，D-05/D-06 不含 Ctrl+V，D-07 仅搜索触发，D-08 仅关闭弹窗）：

    ```
    ## 6. 键盘操作
    搜索框输入后 → send_keys('Enter') 触发搜索，不用于表单提交。
    日期选择器或弹窗遮挡 → send_keys('Escape') 关闭。
    需清空输入框 → send_keys('Control+a') 全选后用 input 输入新值覆盖。
    ```

    注意事项：
    - 在第 5 段最后一行（"弹窗处理..."）后面加空行，再添加 "## 6."
    - 保留 ENHANCED_SYSTEM_MESSAGE 结尾的 """ 在最后一行
    - 不修改 CHINESE_ENHANCEMENT 别名（它只是指向 ENHANCED_SYSTEM_MESSAGE）
    - 不修改 LOGIN_TASK_PROMPT

    添加后运行全部测试验证通过（GREEN 阶段）。
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_enhanced_prompt.py -x -v</automated>
  </verify>
  <done>
    - prompts.py 中 ENHANCED_SYSTEM_MESSAGE 包含 "## 6. 键盘操作" 段落
    - 段落覆盖三种场景：Enter 搜索触发、Escape 关闭弹窗、Control+a 全选覆盖
    - 不包含 Control+v / Ctrl+V 粘贴指导
    - 全部 test_enhanced_prompt.py 测试通过
    - 总行数 <= 60 行
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest backend/tests/unit/test_enhanced_prompt.py -x -v` — 所有测试通过
2. `grep -c "## " backend/agent/prompts.py` — 确认包含 6 个段落标题
3. `grep -i "control+v\|ctrl+v" backend/agent/prompts.py` — 无输出（不含粘贴指导）
</verification>

<success_criteria>
- ENHANCED_SYSTEM_MESSAGE 包含 "## 6. 键盘操作" 段落（3 条场景-动作对）
- 不含 Ctrl+V 粘贴指导
- 键盘段落不超过 10 行
- test_contains_keyboard_operation_keywords 通过
- test_no_ctrl_v_guidance 通过
- test_keyboard_section_line_count 通过
- 全部原有测试仍通过
- 总行数 <= 60 行
</success_criteria>

<output>
After completion, create `.planning/phases/52-prompt/52-01-SUMMARY.md`
</output>
