---
phase: 52-prompt
plan: 03
type: execute
wave: 3
depends_on:
  - 52-01
  - 52-02
files_modified:
  - backend/agent/prompts.py
  - docs/test-steps/采购-键盘操作验证结果-补充.md
autonomous: false
requirements:
  - KB-01
  - KB-03

must_haves:
  truths:
    - "ENHANCED_SYSTEM_MESSAGE 中 Escape 规则强调不要点击关闭按钮，必须使用 send_keys"
    - "ENHANCED_SYSTEM_MESSAGE 中 Control+a 规则明确说明输入框有内容时使用"
    - "补充测试步骤文档包含两个聚焦场景，分别隔离 Escape 和 Control+a"
  artifacts:
    - path: "backend/agent/prompts.py"
      provides: "强化后的键盘操作 Escape 和 Control+a 指导措辞"
      contains: "不要点击关闭按钮"
    - path: "docs/test-steps/采购-键盘操作验证结果-补充.md"
      provides: "补充验证结果记录文档"
      contains: "send_keys"
  key_links:
    - from: "docs/test-steps/采购-键盘操作验证结果-补充.md"
      to: "backend/agent/prompts.py"
      via: "runtime dependency — Agent loads ENHANCED_SYSTEM_MESSAGE at execution time"
      pattern: "send_keys"
---

<objective>
针对 VERIFICATION.md 中两个 PARTIAL 状态的 gap（Escape 关闭弹窗、Control+a 全选覆盖），强化 prompt 措辞并创建聚焦的补充测试步骤，确保 Agent 在下次测试中能正确使用 send_keys。

Purpose: 原始测试（52-02）中 Agent 遇到弹窗时选择了点击 Close 按钮而非 send_keys('Escape')，且未遇到需要覆盖已有内容的场景。根本原因是 prompt 措辞不够强烈且测试步骤没有隔离这两个场景。通过强化 prompt 措辞（增加"不要点击关闭按钮"等否定指令）和创建聚焦测试步骤（明确设置前置条件），提高 Agent 遵守率。
Output: 强化后的 prompts.py + 补充测试步骤文档 + 人工验证结果文档
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-prompt/52-CONTEXT.md
@.planning/phases/52-prompt/52-VERIFICATION.md
@.planning/phases/52-prompt/52-01-SUMMARY.md
@.planning/phases/52-prompt/52-02-SUMMARY.md

<interfaces>
<!-- Current keyboard section in prompts.py (line 35-38) that will be modified -->
<!-- From backend/agent/prompts.py -->

```python
## 6. 键盘操作
搜索框输入后 → send_keys('Enter') 触发搜索，不用于表单提交。
日期选择器或弹窗遮挡 → send_keys('Escape') 关闭。
需清空输入框 → send_keys('Control+a') 全选后用 input 输入新值覆盖。
```

<!-- Existing test file pattern -->
<!-- From backend/tests/unit/test_enhanced_prompt.py -->

```python
def test_keyboard_section_line_count(self):
    """D-04: Keyboard operation section must not exceed 10 lines."""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 强化 ENHANCED_SYSTEM_MESSAGE 键盘操作措辞</name>
  <files>backend/agent/prompts.py</files>
  <read_first>
    - backend/agent/prompts.py（当前第 35-38 行的键盘操作段落，需修改）
    - backend/tests/unit/test_enhanced_prompt.py（确认 test_keyboard_section_line_count 测试的 10 行限制）
  </read_first>
  <action>
    修改 prompts.py 中 ENHANCED_SYSTEM_MESSAGE 的 "## 6. 键盘操作" 段落（第 35-38 行）。

    根因分析：
    - Escape gap: Agent 遇到弹窗时点击了 Close 按钮(aria-label=Close)而非使用 send_keys('Escape')。原措辞"日期选择器或弹窗遮挡 → send_keys('Escape') 关闭"缺少否定指令，Agent 视点击按钮为更"自然"的操作。
    - Control+a gap: 原措辞"需清空输入框"条件不明确，Agent 没有意识到什么时候"需要清空"。

    将第 35-38 行替换为以下内容（per D-04 不超过 10 行，共 6 行含标题行）：

    ```
    ## 6. 键盘操作
    搜索框输入后 → send_keys('Enter') 触发搜索，不用于表单提交。
    遇到日期选择器、下拉弹窗遮挡页面时 → 必须用 send_keys('Escape') 关闭，不要点击关闭按钮或弹窗外区域。
    输入框有旧内容需要改为新值时 → 先 send_keys('Control+a') 全选旧内容，再 input 新值覆盖，不要逐字删除。
    ```

    关键变化：
    1. Escape 行：增加"必须用"强调语气 + 增加"不要点击关闭按钮或弹窗外区域"否定指令，直接阻断 Agent 的点击退路
    2. Control+a 行：从"需清空输入框"改为"输入框有旧内容需要改为新值时"，明确触发条件 + 增加"不要逐字删除"否定指令

    注意：
    - 保持场景-动作对格式（per D-01）
    - 中文撰写（per D-03）
    - 修改后总行数不得超过 10 行（per D-04，test_keyboard_section_line_count 断言 <= 10）
    - 不修改 ENHANCED_SYSTEM_MESSAGE 的其他段落
    - 不修改 CHINESE_ENHANCEMENT 别名和 LOGIN_TASK_PROMPT
    - 修改后运行 `uv run pytest backend/tests/unit/test_enhanced_prompt.py -x -v` 确保全部通过
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_enhanced_prompt.py -x -v</automated>
  </verify>
  <done>
    - prompts.py 键盘操作段落包含"不要点击关闭按钮"否定指令
    - prompts.py 键盘操作段落包含"不要逐字删除"否定指令
    - 键盘段落不超过 10 行
    - 全部 test_enhanced_prompt.py 测试通过
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证 Escape 和 Control+a 聚焦场景</name>
  <files>docs/test-steps/采购-键盘操作验证结果-补充.md</files>
  <action>
    使用强化后的 prompt 运行两个聚焦 ERP 场景验证。每个场景必须独立运行、独立观察 Agent 行为。

    前置条件：确保后端服务运行中（uv run uvicorn backend.api.main:app --reload --port 8080）

    === 场景 A: Escape 关闭日期选择器（KB-03）===

    目的：专门测试 Agent 遇到日期选择器时是否使用 send_keys('Escape')。

    测试步骤（在前端输入这些步骤作为测试用例）：
    1. 点击"商品采购"
    2. 点击"采购管理"
    3. 点击"新增采购单"
    4. 点击"采购日期"字段（触发日期选择器弹窗）
    5. 观察 Agent 如何关闭日期选择器

    判定标准：
    - 通过：Agent 使用 send_keys('Escape') 关闭日期选择器
    - 失败：Agent 点击日期选择器的关闭按钮、点击确定/取消按钮、或点击弹窗外区域

    === 场景 B: Control+a 全选覆盖（KB-01）===

    目的：专门测试 Agent 需要修改已有内容时是否使用 send_keys('Control+a') + input 覆盖。

    测试步骤（在前端输入这些步骤作为测试用例）：
    1. 点击"商品采购"
    2. 点击"采购管理"
    3. 点击"新增采购单"
    4. 在"备注"输入框输入"旧备注内容"
    5. 将备注内容改为"新备注内容"（此步骤迫使 Agent 遇到已有内容的输入框）
    6. 观察 Agent 如何修改备注内容

    判定标准：
    - 通过：Agent 先使用 send_keys('Control+a') 全选旧内容，再 input "新备注内容"覆盖
    - 失败：Agent 逐字符删除旧内容、直接 input 导致内容追加、或其他非 Control+a 方式

    === 验证完成后 ===

    创建 docs/test-steps/采购-键盘操作验证结果-补充.md，格式如下：

    ```markdown
    # 采购单键盘操作补充验证结果

    ## 验证日期: [日期]

    ## 场景 A: Escape 关闭日期选择器 (KB-03)
    **状态: 通过/失败**
    - Agent 行为: [描述 Agent 实际做了什么]
    - 日志证据: [相关 step 编号和 send_keys/click 动作]

    ## 场景 B: Control+a 全选覆盖 (KB-01)
    **状态: 通过/失败**
    - Agent 行为: [描述 Agent 实际做了什么]
    - 日志证据: [相关 step 编号和 send_keys/click 动作]

    ## 总结
    - [通过/失败统计]
    - [与上次测试对比]
    ```
  </action>
  <verify>
    <automated>test -f docs/test-steps/采购-键盘操作验证结果-补充.md && grep -c "send_keys" docs/test-steps/采购-键盘操作验证结果-补充.md</automated>
  </verify>
  <done>docs/test-steps/采购-键盘操作验证结果-补充.md 已创建，记录 Escape 和 Control+a 两个聚焦场景的验证结果（通过/失败+Agent 行为描述+日志证据）</done>
</task>

</tasks>

<verification>
1. `uv run pytest backend/tests/unit/test_enhanced_prompt.py -x -v` — 全部测试通过（含强化后措辞的关键词检查和 10 行限制）
2. `grep "不要点击关闭按钮" backend/agent/prompts.py` — 确认否定指令已添加
3. `grep "不要逐字删除" backend/agent/prompts.py` — 确认否定指令已添加
4. `test -f docs/test-steps/采购-键盘操作验证结果-补充.md` — 补充验证结果文件存在
5. `grep -c "send_keys" docs/test-steps/采购-键盘操作验证结果-补充.md` — 验证结果中包含 send_keys 记录
</verification>

<success_criteria>
- ENHANCED_SYSTEM_MESSAGE 键盘操作段落包含"不要点击关闭按钮"否定指令（阻断 Escape 场景的点击退路）
- ENHANCED_SYSTEM_MESSAGE 键盘操作段落包含"不要逐字删除"否定指令（阻断 Control+a 场景的逐字删除退路）
- 键盘段落不超过 10 行（per D-04）
- 全部 test_enhanced_prompt.py 测试通过
- 补充验证结果文档已创建，记录两个聚焦场景的验证状态
- 人工验证 Agent 在日期选择器场景使用 send_keys('Escape')
- 人工验证 Agent 在覆盖场景使用 send_keys('Control+a') + input
</success_criteria>

<output>
After completion, create `.planning/phases/52-prompt/52-03-SUMMARY.md`
</output>
