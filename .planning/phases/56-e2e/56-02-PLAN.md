---
phase: 56-e2e
plan: 02
type: execute
wave: 2
depends_on:
  - 56-01
files_modified:
  - docs/test-steps/采购-综合验证结果.md
autonomous: false
requirements:
  - KB-01
  - KB-02
  - KB-03
  - TBL-01
  - TBL-02
  - TBL-03
  - TBL-04
  - IMP-01
  - IMP-02
  - AST-01
  - AST-02

must_haves:
  truths:
    - "11 个 E2E 测试用例全部在平台 UI 创建并执行完成"
    - "每个用例有独立的通过/失败判定和日志证据"
    - "综合验证报告汇总了全部 11 个场景的执行结果"
    - "失败场景有原因分类分析（prompt/环境/数据）"
  artifacts:
    - path: "docs/test-steps/采购-综合验证结果.md"
      provides: "全部 11 个 E2E 测试用例的综合验证报告"
      contains: "KB-01"
  key_links:
    - from: "docs/test-steps/采购-综合验证结果.md"
      to: "docs/test-steps/采购-键盘操作验证结果.md"
      via: "键盘操作结果对比参考"
      pattern: "KB-0[123]"
    - from: "docs/test-steps/采购-综合验证结果.md"
      to: "docs/test-steps/采购-表格交互验证结果.md"
      via: "表格交互结果对比参考"
      pattern: "TBL-0[1234]"
    - from: "docs/test-steps/采购-综合验证结果.md"
      to: "docs/test-steps/采购-文件导入验证结果.md"
      via: "文件导入结果对比参考"
      pattern: "IMP-0[12]"
---

<objective>
执行全部 11 个 E2E 测试用例并生成综合验证报告。

Purpose: 验证 Phase 52-54 新增的所有操作能力（键盘/表格/文件上传）+ 断言功能（AST-01/02）在完整 ERP 场景中协同工作（per D-01 全部 11 个用例）。
Output: docs/test-steps/采购-综合验证结果.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-e2e/56-CONTEXT.md
@.planning/phases/56-e2e/56-01-SUMMARY.md

@docs/test-steps/采购-键盘操作测试步骤.md
@docs/test-steps/采购-键盘操作验证结果.md
@docs/test-steps/采购-键盘操作验证结果-补充.md
@docs/test-steps/采购-表格交互测试步骤.md
@docs/test-steps/采购-表格交互验证结果.md
@docs/test-steps/采购-文件导入测试步骤.md
@docs/test-steps/采购-文件导入验证结果.md
@docs/test-steps/采购-断言验证测试步骤.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 执行全部 11 个 E2E 测试用例</name>
  <files>docs/test-steps/采购-综合验证结果.md</files>
  <read_first>
    - docs/test-steps/采购-键盘操作测试步骤.md (3 个键盘测试用例步骤)
    - docs/test-steps/采购-表格交互测试步骤.md (4 个表格交互测试用例步骤)
    - docs/test-steps/采购-文件导入测试步骤.md (2 个文件导入测试用例步骤)
    - docs/test-steps/采购-断言验证测试步骤.md (2 个断言验证测试用例步骤，Plan 56-01 创建)
    - docs/test-steps/采购-键盘操作验证结果.md (Phase 52 基线结果)
    - docs/test-steps/采购-键盘操作验证结果-补充.md (Phase 52 补充基线)
    - docs/test-steps/采购-表格交互验证结果.md (Phase 53 基线结果)
    - docs/test-steps/采购-文件导入验证结果.md (Phase 54 基线结果)
    - .planning/phases/56-e2e/56-CONTEXT.md (D-02/D-04/D-06 决策)
  </read_first>
  <what-built>
    测试步骤文档和环境就绪状态（Plan 56-01 完成）。现在需要用户通过平台 UI 手动执行 11 个 E2E 测试用例。
  </what-built>
  <action>
    Per D-02（通过平台 UI 手动执行）和 D-04（逐场景判定），引导用户完成全部 11 个测试用例的执行。

    **执行前准备:**
    1. 确认后端服务运行: `uv run uvicorn backend.api.main:app --reload --port 8080`
    2. 确认前端服务运行: `cd frontend && npm run dev`
    3. 打开浏览器访问平台 UI

    **执行顺序（per CONTEXT.md specifics）:**

    **第一批 — 已独立验证的 7 个场景（确认回归）:**
    1. KB-02: Enter 搜索触发（参考 采购-键盘操作测试步骤.md test_keyboard_enter_search）
    2. TBL-01: checkbox 单行选择（参考 采购-表格交互测试步骤.md test_table_checkbox_row_select）
    3. TBL-02: checkbox 全选（参考 采购-表格交互测试步骤.md test_table_checkbox_select_all）
    4. TBL-03: 超链接点击（参考 采购-表格交互测试步骤.md test_table_link_click）
    5. TBL-04: 图标按钮点击（参考 采购-表格交互测试步骤.md test_table_icon_button_click）
    6. IMP-01: Excel 文件导入（参考 采购-文件导入测试步骤.md test_excel_import）
    7. IMP-02: 图片上传（参考 采购-文件导入测试步骤.md test_image_upload）

    **第二批 — 未独立验证的 2 个键盘场景:**
    8. KB-03: Escape 关闭日期选择器（参考 采购-键盘操作测试步骤.md test_keyboard_escape_close_popup）
    9. KB-01: Control+a 全选覆盖（参考 采购-键盘操作测试步骤.md test_keyboard_select_all_overwrite）

    **第三批 — 断言验证场景:**
    10. AST-01: headers 参数传递验证（参考 采购-断言验证测试步骤.md test_assertion_headers_param）
    11. AST-02: i/j 参数组合验证（参考 采购-断言验证测试步骤.md test_assertion_ij_params）

    **每个用例执行方式（per D-02）:**
    1. 在平台 UI 新建测试用例
    2. 将测试步骤文档中的步骤描述粘贴为用例步骤
    3. 配置断言（如有）
    4. 执行测试用例
    5. 观察 Agent 行为和执行日志
    6. 判定通过/失败（per D-04 逐场景判定）
    7. 记录结果（通过/失败 + 日志证据）

    **每个用例执行后记录:**
    - 用例编号（如 KB-02）
    - 执行结果: 通过/失败/部分通过
    - Agent 关键动作（日志中观察到的 send_keys/click/upload_file 等）
    - 日志证据（关键 step 的动作描述）
    - 失败原因分类（如失败）: prompt 问题/环境问题/数据问题
  </action>
  <how-to-verify>
    **验证方式:** 用户通过平台 UI 完成全部 11 个测试用例执行后，将结果汇报给 Claude。

    具体验证步骤:
    1. 确认后端服务运行在 localhost:8080
    2. 逐个创建并执行 11 个测试用例（按上述执行顺序）
    3. 每个用例执行后记录: 用例编号、通过/失败、Agent 关键动作、日志证据
    4. 全部完成后，将结果以以下格式反馈:

    ```
    KB-02: 通过/失败 — [Agent 关键动作描述]
    TBL-01: 通过/失败 — [Agent 关键动作描述]
    ...
    AST-02: 通过/失败 — [Agent 关键动作描述]
    ```

    5. 对于失败场景，额外说明: 失败原因分类（prompt/环境/数据）、日志中的错误信息
  </how-to-verify>
  <resume-signal>
    汇报全部 11 个用例的执行结果（格式: 用例编号 + 通过/失败 + 关键动作描述），或输入 "partial" 汇报部分结果并说明剩余用例状态。
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: 生成综合验证报告</name>
  <files>docs/test-steps/采购-综合验证结果.md</files>
  <read_first>
    - docs/test-steps/采购-键盘操作验证结果.md (Phase 52 基线格式参考)
    - docs/test-steps/采购-键盘操作验证结果-补充.md (Phase 52 补充基线)
    - docs/test-steps/采购-表格交互验证结果.md (Phase 53 基线格式参考)
    - docs/test-steps/采购-文件导入验证结果.md (Phase 54 基线格式参考)
    - .planning/phases/56-e2e/56-CONTEXT.md (D-05/D-06 决策)
  </read_first>
  <action>
    Per D-05（新建综合验证报告 docs/test-steps/采购-综合验证结果.md）和 D-06（记录失败原因并分析），基于用户反馈的 11 个测试用例执行结果，创建综合验证报告。

    报告结构:

    ```markdown
    # 采购模块 E2E 综合验证结果

    ## 验证日期: [日期]
    ## 验证环境: 本地开发机（localhost:8080）
    ## 测试范围: Phase 52-54 操作能力 + AST-01/02 断言验证

    ---

    ## 一、键盘操作测试 (Phase 52)

    ### 场景 1 (KB-02) — Enter 搜索触发: [通过/失败]
    - **测试用例:** test_keyboard_enter_search
    - **结果:** [通过/失败]
    - **说明:** [Agent 行为描述]
    - **与 Phase 52 基线对比:** [一致/不一致 + 说明]

    ### 场景 2 (KB-03) — Escape 关闭弹窗: [通过/失败]
    - **测试用例:** test_keyboard_escape_close_popup
    - **结果:** [通过/失败/部分通过]
    - **说明:** [Agent 行为描述]
    - **与 Phase 52 补充基线对比:** [一致/不一致 + 说明]

    ### 场景 3 (KB-01) — Control+a 全选覆盖: [通过/失败]
    - **测试用例:** test_keyboard_select_all_overwrite
    - **结果:** [通过/失败/部分通过]
    - **说明:** [Agent 行为描述]
    - **与 Phase 52 补充基线对比:** [一致/不一致 + 说明]

    ---

    ## 二、表格交互测试 (Phase 53)

    ### 场景 4 (TBL-01) — checkbox 单行选择: [通过/失败]
    ### 场景 5 (TBL-02) — checkbox 全选: [通过/失败]
    ### 场景 6 (TBL-03) — 超链接点击: [通过/失败]
    ### 场景 7 (TBL-04) — 图标按钮点击: [通过/失败]

    （每个场景格式同上，包含与 Phase 53 基线对比）

    ---

    ## 三、文件导入测试 (Phase 54)

    ### 场景 8 (IMP-01) — Excel 文件导入: [通过/失败]
    ### 场景 9 (IMP-02) — 图片文件上传: [通过/失败]

    （每个场景格式同上，包含与 Phase 54 基线对比）

    ---

    ## 四、断言验证测试

    ### 场景 10 (AST-01) — headers 参数传递: [通过/失败]
    - **测试用例:** test_assertion_headers_param
    - **结果:** [通过/失败]
    - **说明:** [断言执行结果描述]
    - **验证点:** headers='main' 参数正确传递，无 HeaderResolutionError

    ### 场景 11 (AST-02) — i/j 参数组合: [通过/失败]
    - **测试用例:** test_assertion_ij_params
    - **结果:** [通过/失败]
    - **说明:** [断言执行结果描述]
    - **验证点:** i=2, j=13 参数正确传递，返回有效库存数据

    ---

    ## 五、总结

    ### 通过率
    | 类型 | 通过 | 部分通过 | 失败 | 总计 | 通过率 |
    |------|------|----------|------|------|--------|
    | 键盘操作 (KB) | X | X | X | 3 | X% |
    | 表格交互 (TBL) | X | X | X | 4 | X% |
    | 文件导入 (IMP) | X | X | X | 2 | X% |
    | 断言验证 (AST) | X | X | X | 2 | X% |
    | **总计** | **X** | **X** | **X** | **11** | **X%** |

    ### 失败原因分析（如有失败场景）
    [对每个失败场景进行原因分类]
    - 分类: prompt 问题 / 环境问题 / 数据问题 / 浏览器运行时限制
    - 日志证据引用
    - 建议后续处理方向（per D-06 不做自动修复）

    ### 回归对比
    [与 Phase 52-54 独立验证结果对比，说明是否有退化]

    ### 结论
    [总结 v0.7.0 新增操作能力的 E2E 验证状态]
    ```

    注意: 所有 [X] 占位符需要根据用户反馈的实际执行结果填充。
  </action>
  <verify>
    <automated>test -f docs/test-steps/采购-综合验证结果.md && grep -c 'KB-01\|KB-02\|KB-03\|TBL-01\|TBL-02\|TBL-03\|TBL-04\|IMP-01\|IMP-02\|AST-01\|AST-02' docs/test-steps/采购-综合验证结果.md</automated>
  </verify>
  <done>
    - docs/test-steps/采购-综合验证结果.md 存在
    - 报告包含全部 11 个场景（KB-01/02/03, TBL-01~04, IMP-01/02, AST-01/02）的执行结果
    - 每个场景有通过/失败判定和说明
    - 包含按类型的通过率汇总表
    - 失败场景有原因分类分析
    - 包含与 Phase 52-54 基线的回归对比
  </done>
</task>

</tasks>

<verification>
1. `docs/test-steps/采购-综合验证结果.md` 存在
2. 报告包含 11 个场景（KB-01/02/03, TBL-01~04, IMP-01/02, AST-01/02）
3. 每个场景有独立的通过/失败判定
4. 包含通过率汇总表
5. 失败场景有原因分析
</verification>

<success_criteria>
- 全部 11 个 E2E 测试用例在平台 UI 手动执行完成
- 综合验证报告 docs/test-steps/采购-综合验证结果.md 创建完成
- 报告包含每个场景的执行结果、通过率汇总、失败分析
- v0.7.0 新增操作能力的 E2E 验证状态明确
</success_criteria>

<output>
After completion, create `.planning/phases/56-e2e/56-02-SUMMARY.md`
</output>
