---
phase: 22-bug-fix-sprint
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/_archived/ (18 files moved here)
  - backend/tests/_archived/README.md (new)
autonomous: true
requirements: [BUG-01]
must_haves:
  truths:
    - "Legacy test files no longer cause import errors"
    - "Archived tests are documented with reason for archival"
    - "pytest collection does not fail on missing modules"
  artifacts:
    - path: "backend/tests/_archived/"
      provides: "Archived legacy test files"
      contains: "18 .py files + README.md"
    - path: "backend/tests/_archived/README.md"
      provides: "Documentation explaining archival reason"
  key_links:
    - from: "backend/tests/_archived/"
      to: "Original test files"
      via: "git mv (preserves history)"
---

<objective>
Archive 18 legacy test files that import deleted modules to eliminate import errors during test collection.

Purpose: Clean up test suite by removing tests that reference non-existent modules.
Output: All legacy tests moved to _archived/ directory with documentation.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bug-fix-sprint/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create archive directory and move legacy test files</name>
  <files>backend/tests/_archived/, 18 legacy test files</files>
  <read_first>
    - backend/tests/test_agent.py (verify it imports deleted module)
    - backend/tests/test_agent_optimized.py (verify it imports deleted module)
  </read_first>
  <action>
    Create archive directory and move 18 legacy test files:

    1. Create directory: `mkdir -p backend/tests/_archived`

    2. Move the following files using `git mv` to preserve history:
       - test_agent.py
       - test_agent_optimized.py
       - test_code_generator.py
       - test_code_optimizer.py
       - test_code_reviewer.py
       - test_dashboard_api.py
       - test_decision.py
       - test_delivery_form.py
       - test_executor.py
       - test_form_filler_integration.py
       - test_login_e2e.py
       - test_memory.py
       - test_memory_integration.py
       - test_orchestrator.py
       - test_perception.py
       - test_phase5_unit.py
       - test_purchase_e2e.py
       - test_sandbox.py

    3. Command pattern:
       ```bash
       cd backend/tests
       git mv test_agent.py _archived/
       git mv test_agent_optimized.py _archived/
       # ... repeat for all 18 files
       ```

    4. These files import `backend.agent_simple` and other deleted modules, causing import errors.
  </action>
  <verify>
    <automated>ls -la backend/tests/_archived/*.py | wc -l | xargs -I {} test {} -eq 18 && echo "18 files archived"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/_archived/` directory exists
    - `ls backend/tests/_archived/*.py | wc -l` returns 18
    - Original files no longer exist in `backend/tests/` root
  </acceptance_criteria>
  <done>18 legacy test files moved to _archived/ directory</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md documenting archival reason</name>
  <files>backend/tests/_archived/README.md</files>
  <read_first>
    - backend/tests/_archived/ (directory just created)
  </read_first>
  <action>
    Create README.md in _archived/ directory with documentation:

    ```markdown
    # Archived Test Files

    **Archived:** 2026-03-19
    **Reason:** These test files import modules that have been deleted or refactored out of the codebase.

    ## Files

    | File | Reason for Archival |
    |------|---------------------|
    | test_agent.py | Imports `backend.agent_simple` (deleted) |
    | test_agent_optimized.py | Imports `backend.agent_simple` (deleted) |
    | test_code_generator.py | Imports deleted code generator module |
    | test_code_optimizer.py | Imports deleted optimizer module |
    | test_code_reviewer.py | Imports deleted reviewer module |
    | test_dashboard_api.py | References old API structure |
    | test_decision.py | Imports `backend.agent_simple.decision` (deleted) |
    | test_delivery_form.py | Uses deprecated form filler pattern |
    | test_executor.py | Imports deleted executor module |
    | test_form_filler_integration.py | Uses deprecated integration pattern |
    | test_login_e2e.py | Superseded by e2e/tests/ tests |
    | test_memory.py | Imports deleted memory module |
    | test_memory_integration.py | Imports deleted memory module |
    | test_orchestrator.py | Imports deleted orchestrator module |
    | test_perception.py | Imports `backend.agent_simple.perception` (deleted) |
    | test_phase5_unit.py | Phase-specific test, no longer relevant |
    | test_purchase_e2e.py | Superseded by e2e/tests/ tests |
    | test_sandbox.py | Sandbox experiment file |

    ## Restoration

    To restore any of these tests:
    1. Update imports to use current module structure
    2. Verify test logic is still relevant
    3. Move file back to `backend/tests/` or appropriate subdirectory
    ```
  </action>
  <verify>
    <automated>test -f backend/tests/_archived/README.md && grep -q "Archived" backend/tests/_archived/README.md && echo "README exists"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/_archived/README.md` file exists
    - File contains "Archived: 2026-03-19"
    - File lists all 18 archived files with reasons
  </acceptance_criteria>
  <done>README.md created documenting all archived tests</done>
</task>

<task type="auto">
  <name>Task 3: Verify pytest collection works without import errors</name>
  <files>pytest configuration (implicit)</files>
  <read_first>
    - pyproject.toml (pytest configuration)
    - backend/tests/conftest.py
  </read_first>
  <action>
    Verify that pytest can collect tests without import errors:

    1. Run pytest collection only:
       ```bash
       uv run pytest --collect-only backend/tests/ 2>&1 | head -50
       ```

    2. Verify no "ModuleNotFoundError" or "ImportError" messages

    3. If pytest still tries to collect from _archived/, add to pytest config:
       - Check pyproject.toml for `testpaths` or `collect_ignore`
       - Add `_archived` to ignore list if needed:
         ```toml
         [tool.pytest.ini_options]
         collect_ignore = ["_archived"]
         ```

    4. Re-verify collection works cleanly
  </action>
  <verify>
    <automated>uv run pytest --collect-only backend/tests/ 2>&1 | grep -c "ModuleNotFoundError" | xargs -I {} test {} -eq 0 && echo "No import errors"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest --collect-only backend/tests/` completes without ModuleNotFoundError
    - No ImportError messages in collection output
    - _archived/ directory is excluded from collection
  </acceptance_criteria>
  <done>Pytest collection works without import errors from archived files</done>
</task>

</tasks>

<verification>
Run full test collection to verify clean state:
```bash
uv run pytest --collect-only backend/tests/ 2>&1 | grep -E "(error|Error|ERROR|import)" | head -20
```
Should show no import errors.
</verification>

<success_criteria>
- All 18 legacy test files moved to _archived/
- README.md documents archival reason
- Pytest collection works without import errors
- Git history preserved via `git mv`
</success_criteria>

<output>
After completion, create `.planning/phases/22-bug-fix-sprint/22-02-SUMMARY.md`
</output>
