---
phase: 22-bug-fix-sprint
plan: 06
type: execute
wave: 4
depends_on: [22-01, 22-02, 22-03, 22-04, 22-05]
files_modified: []
autonomous: false
requirements: [BUG-03]
must_haves:
  truths:
    - "All unit tests pass with no failures"
    - "All API tests pass with no failures"
    - "All E2E tests pass with no new failures"
    - "No regressions introduced by bug fixes"
  artifacts:
    - path: "backend/tests/unit/"
      provides: "Unit test suite"
      contains: "test files for isolation, mock signatures"
    - path: "backend/tests/api/"
      provides: "API test suite"
      contains: "API endpoint tests"
    - path: "e2e/tests/"
      provides: "E2E test suite"
      contains: "playwright test files"
  key_links:
    - from: "bug fixes"
      to: "test suite"
      via: "pytest and playwright execution"
---

<objective>
Run comprehensive regression testing to verify all bug fixes work correctly and no new issues were introduced.

Purpose: Ensure the bug fix sprint delivers a stable, tested codebase.
Output: All tests pass, confirming bug fixes are complete and working
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bug-fix-sprint/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all unit tests and verify no failures</name>
  <files>Test output (read-only)</files>
  <read_first>
    - pyproject.toml (pytest configuration)
  </read_first>
  <action>
    Run all unit tests and verify they pass:

    1. Run unit tests with verbose output:
       ```bash
       uv run pytest backend/tests/unit/ -v --tb=short 2>&1 | tee /tmp/unit-test-output.txt
       ```

    2. Check for failures:
       ```bash
       grep -E "FAILED|ERROR" /tmp/unit-test-output.txt
       ```

    3. If failures exist:
       - Identify which tests failed
       - Determine if failure is due to incomplete bug fix or new issue
       - Report findings for remediation

    4. Expected: All tests pass (exit code 0)

    5. Save test summary for verification report
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/ -v --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest backend/tests/unit/ -v` exits with code 0
    - No FAILED or ERROR status in test output
    - All collected tests show PASSED
  </acceptance_criteria>
  <done>All unit tests pass with no failures</done>
</task>

<task type="auto">
  <name>Task 2: Run all API tests and verify no failures</name>
  <files>Test output (read-only)</files>
  <read_first>
    - backend/tests/api/ (API test directory)
  </read_first>
  <action>
    Run all API tests and verify they pass

    1. Run API tests with verbose output:
       ```bash
       uv run pytest backend/tests/api/ -v --tb=short 2>&1 | tee /tmp/api-test-output.txt
       ```

    2. Check for failures:
       ```bash
       grep -E "FAILED|ERROR" /tmp/api-test-output.txt
       ```

    3. If failures exist:
       - Identify which tests failed
       - Determine if failure is related to bug fixes or pre-existing
       - Report findings

    4. Expected: All tests pass (exit code 0)
  </action>
  <verify>
    <automated>uv run pytest backend/tests/api/ -v --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest backend/tests/api/ -v` exits with code 0
    - No FAILED or ERROR status in test output
  </acceptance_criteria>
  <done>All API tests pass with no failures</done>
</task>

<task type="auto">
  <name>Task 3: Run frontend build and type check</name>
  <files>Frontend build output (read-only)</files>
  <read_first>
    - frontend/package.json (build scripts)
  </read_first>
  <action>
    Run frontend build to verify no TypeScript errors

    1. Run TypeScript check:
       ```bash
       cd frontend && npm run build 2>&1 | tee /tmp/frontend-build.txt
       ```

    2. Check for errors:
       ```bash
       grep -E "error|Error" /tmp/frontend-build.txt
       ```

    3. Expected: Build completes successfully (exit code 0)

    4. If errors exist:
       - Identify TypeScript errors
       - Determine which component caused the error
       - Report for remediation
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits with code 0
    - No TypeScript compilation errors
    - Build output shows successful compilation
  </acceptance_criteria>
  <done>Frontend builds successfully with no errors</done>
</task>

<task type="auto">
  <name>Task 4: Run E2E tests (if applicable)</name>
  <files>E2E test output (read-only)</files>
  <read_first>
    - e2e/tests/ (E2E test directory)
    - playwright.config.ts (E2E configuration)
  </read_first>
  <action>
    Run E2E tests to verify no regressions in critical user flows

    1. Run E2E tests:
       ```bash
       cd frontend && npx playwright test --reporter=list 2>&1 | tee /tmp/e2e-test-output.txt
       ```
       Or:
       ```bash
       uv run pytest e2e/tests/ -v 2>&1 | tee /tmp/e2e-test-output.txt
       ```

    2. Check for failures:
       ```bash
       grep -E "FAILED|failed|Error" /tmp/e2e-test-output.txt
       ```

    3. Note: E2E tests may require running backend/frontend servers

    4. If E2E tests are not set up or cannot run:
       - Document this limitation
       - Recommend manual testing of critical flows
  </action>
  <verify>
    <automated>ls e2e/tests/*.py 2>/dev/null | wc -l | xargs -I {} echo "{} E2E test files found"</automated>
  </verify>
  <acceptance_criteria>
    - E2E tests executed (or documented why not)
    - No new E2E test failures introduced
    - Critical user flows verified
  </acceptance_criteria>
  <done>E2E tests pass or manual verification documented</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All bug fixes have been implemented and automated tests have been run:
    - 16 failing tests fixed (test isolation, mock signatures)
    - 18 legacy test files archived
    - 4 DataMethodSelector UI bugs fixed (grouping, selection, type hints, validation, quotes, escape key)
    - 1 report page bug fixed (precondition section)
    - All unit tests pass
    - All API tests pass
    - Frontend builds successfully
  </what-built>
  <how-to-verify>
    1. Review test output summary to confirm all tests pass
    2. Start frontend and backend servers:
       - Backend: `uv run uvicorn backend.api.main:app --reload --port 8080`
       - Frontend: `cd frontend && npm run dev`
    3. Test DataMethodSelector fixes manually:
       - Open a task, click "Get Data" button
       - Verify methods are grouped by class (collapsible)
       - Select multiple methods, verify count is correct
       - Go to Step 2, verify type hints are shown
       - Try entering letters in numeric field - should not work
       - Press Escape key - modal should close
    4. Test report page:
       - Run a task with preconditions
       - View the report
       - Verify precondition section shows status, duration, variables
    5. Confirm all critical flows work as expected
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all fixes work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Final verification summary:
```bash
echo "=== Unit Tests ==="
uv run pytest backend/tests/unit/ -q 2>&1 | tail -3

echo "=== API Tests ==="
uv run pytest backend/tests/api/ -q 2>&1 | tail -3

echo "=== Frontend Build ==="
cd frontend && npm run build 2>&1 | tail -3
```
</verification>

<success_criteria>
- All unit tests pass (0 failures)
- All API tests pass (0 failures)
- Frontend builds without errors
- E2E tests pass (or manual verification complete)
- Human verification confirms all fixes work correctly
- No regressions introduced
</success_criteria>

<output>
After completion, create `.planning/phases/22-bug-fix-sprint/22-06-SUMMARY.md`
</output>
