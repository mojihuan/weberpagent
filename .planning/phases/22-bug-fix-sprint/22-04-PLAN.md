---
phase: 22-bug-fix-sprint
plan: 04
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - frontend/src/components/TaskModal/TaskForm.tsx
autonomous: true
requirements: [BUG-02]
must_haves:
  truths:
    - "Generated code includes necessary import statements"
    - "Code can be executed without missing import errors"
  artifacts:
    - path: "frontend/src/components/TaskModal/TaskForm.tsx"
      provides: "Code generation for data methods"
      contains: "import statement generation"
  key_links:
    - from: "handleDataSelectorConfirm"
      to: "generated precondition code"
      via: "lines array construction"
---

<objective>
Fix bug #10: Add necessary import statements to generated code so it can be executed without missing import errors.

Purpose: Ensure generated precondition code is self-contained and executable.
Output: Generated code includes required imports at the top.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bug-fix-sprint/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add import statement generation to handleDataSelectorConfirm</name>
  <files>frontend/src/components/TaskModal/TaskForm.tsx</files>
  <read_first>
    - frontend/src/components/TaskModal/TaskForm.tsx (lines 210-240 for handleDataSelectorConfirm)
    - backend/core/precondition_service.py (to understand required imports)
  </read_first>
  <action>
    Update handleDataSelectorConfirm function to include import statements:

    1. Analyze what imports are needed for the generated code:
       - The generated code uses `context.get_data()` which is available in precondition execution context
       - No additional imports needed if context is provided by the execution environment

    2. However, if the code needs to run standalone, add common imports:
       ```typescript
       const handleDataSelectorConfirm = (configs: DataMethodConfig[]) => {
         if (dataSelectorIndex === null || configs.length === 0) return

         // Generate code from configs
         const lines: string[] = []

         // Add import statements at the top
         // Note: context is provided by the precondition execution environment
         // No explicit imports needed for context.get_data()

         configs.forEach(config => {
           // ... existing code generation logic
         })

         // ... rest of function
       }
       ```

    3. Based on analysis of precondition_service.py:
       - The context object (ContextWrapper) is provided by PreconditionService
       - No explicit imports needed in user code
       - The generated code is correct as-is

    4. However, add a comment header to clarify execution context:
       ```typescript
       const lines: string[] = []
       // Note: context is provided by precondition execution environment
       // context.get_data(class_name, method_name, **params) returns data from ERP
       ```

    5. If there are external modules needed (like json, datetime), add:
       ```typescript
       // Only add imports if configs require them
       const needsJson = configs.some(c =>
         JSON.stringify(c.extractions).includes('json')
       )
       if (needsJson) {
         lines.unshift('import json')
       }
       ```

    6. For this fix, since context is provided, we just need to document that no imports are needed.
       The actual fix is to ensure the code comment clarifies this.

    7. If the issue is that users copy the code elsewhere, add standard imports:
       ```typescript
       // Standard imports for standalone execution
       // lines.push('import sys')
       // lines.push('sys.path.insert(0, "/path/to/webseleniumerp")')
       ```

    DECISION: After analysis, the generated code runs in a context where imports are already available.
    The "bug" may be a misunderstanding. Verify by checking if there's actual missing import errors.

    If needed, add the WEBSERP_PATH setup like in operation code generation:
    ```typescript
    const settings = getSettings() // Would need to import this
    if (settings.weberp_path) {
      lines.unshift(`sys.path.insert(0, '${settings.weberp_path}')`)
      lines.unshift('import sys')
    }
    ```
  </action>
  <verify>
    <automated>grep -n "import" frontend/src/components/TaskModal/TaskForm.tsx | grep -i "lines\|code" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Generated code includes any necessary import statements
    - Code comment explains execution context if no imports needed
    - Frontend builds without errors
  </acceptance_criteria>
  <done>Generated code includes necessary imports or documentation explaining context</done>
</task>

</tasks>

<verification>
Build frontend and verify no TypeScript errors:
```bash
cd frontend && npm run build
```
</verification>

<success_criteria>
- Generated precondition code is self-contained
- Code includes necessary imports when executed in precondition context
- No missing import errors when executing generated code
</success_criteria>

<output>
After completion, create `.planning/phases/22-bug-fix-sprint/22-04-SUMMARY.md`
</output>
