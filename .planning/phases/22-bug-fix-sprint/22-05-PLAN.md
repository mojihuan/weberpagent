---
phase: 22-bug-fix-sprint
plan: 05
type: execute
wave: 3
depends_on: [22-03, 22-04]
files_modified:
  - frontend/src/pages/ReportDetail.tsx
  - frontend/src/components/Report/index.ts
  - frontend/src/components/Report/PreconditionSection.tsx (new)
  - backend/db/schemas.py
  - backend/db/repository.py
autonomous: true
requirements: [BUG-02]
must_haves:
  truths:
    - "Report page displays precondition execution status"
    - "Precondition duration is visible"
    - "Variables extracted from preconditions are displayed with names and values"
    - "Details can be expanded to view full execution information"
  artifacts:
    - path: "frontend/src/pages/ReportDetail.tsx"
      provides: "Report detail page"
      contains: "PreconditionSection component"
    - path: "frontend/src/components/Report/PreconditionSection.tsx"
      provides: "Precondition execution display component"
      min_lines: 50
    - path: "backend/db/schemas.py"
      provides: "SSEPreconditionEvent schema"
      contains: "SSEPreconditionEvent"
    - path: "backend/db/repository.py"
      provides: "Report data fetching logic"
      contains: "precondition_results field"
  key_links:
    - from: "ReportDetail.tsx"
      to: "precondition_results API data"
      via: "getReport API response"
    - from: "PreconditionSection"
      to: "variables display"
      via: "props passing"
---

<objective>
Fix bug #15: Add precondition execution information to report page including status, duration, variables, and expandable details.

Purpose: Provide visibility into precondition execution results in the test report.
Output: Report page with precondition section showing execution details.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bug-fix-sprint/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check API response structure for precondition data</name>
  <files>frontend/src/api/reports.ts, backend/db/schemas.py, backend/db/repository.py</files>
  <read_first>
    - frontend/src/api/reports.ts (API client)
    - backend/db/schemas.py (response schemas,    - backend/db/repository.py (ReportRepository class)
  </read_first>
  <action>
    Analyze the API response structure to understand what precondition data is available:

    1. Check frontend/src/api/reports.ts for ReportDetailResponse interface:
       - Add `precondition_results?: PreconditionResult[]` field if not already present

    2. Check backend/db/schemas.py for SSEPreconditionEvent:
       - Already has the fields we need: index, code, status, error, duration_ms, variables

    3. Check backend/db/repository.py for the report detail query method
       - Look for how steps and preconditions are stored
       - If not stored, we need to add storage

    4. Determine if precondition_results should be added to ReportDetailResponse

    This task is primarily investigation - no code changes needed if data is already available.
  </action>
  <verify>
    <automated>grep -n "precondition" backend/db/schemas.py | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Identified SSEPreconditionEvent schema structure
    - Know what fields are available (status, duration, variables)
    - Determined if API changes are needed
  </acceptance_criteria>
  <done>API response structure analyzed for precondition data availability</done>
</task>

<task type="auto">
  <name>Task 2: Create PreconditionSection component</name>
  <files>frontend/src/components/Report/PreconditionSection.tsx, frontend/src/components/Report/index.ts</files>
  <read_first>
    - frontend/src/components/Report/StepItem.tsx (reference for styling pattern)
    - frontend/src/components/Report/AssertionResults.tsx (reference for collapsible section)
  </read_first>
  <action>
    Create a new PreconditionSection component to display precondition execution results:

    1. Create file: frontend/src/components/Report/PreconditionSection.tsx

    2. Component interface:
       ```typescript
       interface PreconditionResult {
         index: number
         code: string
         status: 'success' | 'failed'
         duration_ms?: number
         error?: string
         variables?: Record<string, any>
       }

       interface PreconditionSectionProps {
         results: PreconditionResult[]
       }

       export function PreconditionSection({ results }: PreconditionSectionProps) {
         // Implementation
       }
       ```

    3. Component structure:
       ```tsx
       <div className="mb-6">
         <h2 className="text-lg font-medium text-gray-900 mb-3">
           Precondition Execution
         </h2>
         <div className="space-y-3">
           {results.map((result, index) => (
             <div key={index} className="border rounded-lg p-4">
               <div className="flex items-center justify-between">
                 <div className="flex items-center gap-2">
                   {result.status === 'success'
                     ? <CheckCircle className="w-5 h-5 text-green-500" />
                     : <XCircle className="w-5 h-5 text-red-500" />
                   }
                   <span className="font-medium">Precondition {index + 1}</span>
                 </div>
                 {result.duration_ms && (
                   <span className="text-sm text-gray-500">
                     {formatDuration(result.duration_ms)}
                   </span>
                 )}
               </div>

               {/* Variables section */}
               {result.status === 'success' && result.variables && (
                 <div className="mt-3 pt-3 border-t">
                   <div className="text-sm font-medium text-gray-700 mb-2">
                     Extracted Variables:
                   </div>
                   <div className="bg-gray-50 rounded p-2 font-mono text-sm">
                     {Object.entries(result.variables).map(([key, value]) => (
                       <div key={key} className="flex gap-2">
                         <span className="text-blue-600">{key}</span>
                         <span className="text-gray-400">=</span>
                         <span className="text-green-600">
                           {typeof value === 'object' ? JSON.stringify(value) : String(value)}
                         </span>
                       </div>
                     ))}
                   </div>
                 </div>
               )}

               {/* Error message */}
               {result.status === 'failed' && result.error && (
                 <div className="mt-3 pt-3 border-t text-red-600 text-sm">
                   Error: {result.error}
                 </div>
               )}

               {/* Expandable code */}
               <details className="mt-3">
                 <summary className="text-sm text-blue-500 cursor-pointer">
                   View Code
                 </summary>
                 <pre className="mt-2 p-2 bg-gray-100 rounded text-xs overflow-auto">
                   {result.code}
                 </pre>
               </details>
             </div>
           ))}
         </div>
       </div>
       ```

    4. Export from index.ts:
       ```typescript
       export { PreconditionSection } from './PreconditionSection'
       ```

    5. Import icons from lucide-react: CheckCircle, XCircle, ChevronDown
  </action>
  <verify>
    <automated>test -f frontend/src/components/Report/PreconditionSection.tsx && grep -q "PreconditionSection" frontend/src/components/Report/index.ts && echo "Component created"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/Report/PreconditionSection.tsx` file exists
    - Component accepts `results: PreconditionResult[]` prop
    - Component displays status, duration, variables, and expandable code
    - Exported from `frontend/src/components/Report/index.ts`
  </acceptance_criteria>
  <done>PreconditionSection component created and exported</done>
</task>

<task type="auto">
  <name>Task 3: Add precondition results to ReportDetailResponse and integrate component</name>
  <files>frontend/src/pages/ReportDetail.tsx, frontend/src/api/reports.ts, backend/db/schemas.py, backend/db/repository.py</files>
  <read_first>
    - frontend/src/pages/ReportDetail.tsx (current implementation)
    - frontend/src/api/reports.ts (ReportDetailResponse interface)
    - backend/db/schemas.py (ReportDetailResponse schema)
    - backend/db/repository.py (ReportRepository.get_report_detail)
  </read_first>
  <action>
    Add precondition results to the API response and integrate PreconditionSection in ReportDetail:

    1. Update frontend/src/api/reports.ts to include precondition_results:
       ```typescript
       export interface PreconditionResult {
         index: number
         code: string
         status: 'success' | 'failed'
         duration_ms?: number
         error?: string
         variables?: Record<string, any>
       }

       export interface ReportDetailResponse {
         // ... existing fields
         id: string
         run_id: string
         task_id: string
         task_name: string
         status: string
         total_steps: number
         success_steps: number
         failed_steps: number
         duration_ms: number
         created_at: string
         steps: StepResponse[]
         assertion_results: AssertionResultResponse[]
         ui_assertion_results?: AssertionResultResponse[]
         api_assertion_results?: AssertionResultResponse[]
         pass_rate?: string
         api_pass_rate?: string
         precondition_results?: PreconditionResult[]
       }
       ```

    2. Update backend/db/schemas.py to add precondition_results field:
       ```python
       class ReportDetailResponse(ReportResponse):
           """Report detail response (includes steps and assertion_results)"""
           steps: List[StepResponse] = []
           assertion_results: List["AssertionResultResponse"] = []
           ui_assertion_results: Optional[List["AssertionResultResponse"]] = None
           api_assertion_results: Optional[List["AssertionResultResponse"]] = None
           pass_rate: Optional[str] = None
           api_pass_rate: Optional[str] = None
           precondition_results: Optional[List[SSEPreconditionEvent]] = None
       ```

    3. Update backend/db/repository.py to add precondition_results to the report detail query:
       - Check if precondition results are stored in the database
        - If stored in run metadata, extract and include in response
        - May need to add storage for precondition results

    4. Update frontend/src/pages/ReportDetail.tsx to import and render PreconditionSection:
       ```tsx
       import { PreconditionSection } from '../components/Report'

       // In the component, after summary cards:
       {data.precondition_results && data.precondition_results.length > 0 && (
         <PreconditionSection results={data.precondition_results} />
       )}
       ```

    5. If precondition data is not currently stored in database:
        - The data is sent via SSE during execution (SSEPreconditionEvent)
        - Need to store these events in the database
        - Or retrieve from step logs if logged there

    For now, add the UI component and make it work with available data structure.
  </action>
  <verify>
    <automated>grep -n "PreconditionSection" frontend/src/pages/ReportDetail.tsx | head -3</automated>
  </verify>
  <acceptance_criteria>
    - ReportDetailResponse includes `precondition_results` field
    - ReportDetail.tsx imports and renders PreconditionSection
    - Precondition section displays before step list
    - Frontend builds without errors
  </acceptance_criteria>
  <done>PreconditionSection integrated into ReportDetail page</done>
</task>

</tasks>

<verification>
Build frontend and verify no TypeScript errors:
```bash
cd frontend && npm run build
```
</verification>

<success_criteria>
- PreconditionSection component exists and is styled consistently
- ReportDetail page includes precondition execution information
- Status, duration, and variables are displayed
- Code is expandable for detailed view
- Frontend builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-bug-fix-sprint/22-05-SUMMARY.md`
</output>
