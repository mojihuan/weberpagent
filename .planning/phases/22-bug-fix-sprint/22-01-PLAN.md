---
phase: 22-bug-fix-sprint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_external_bridge.py
  - backend/tests/unit/test_browser_cleanup.py
  - backend/tests/unit/test_config/test_settings.py
  - backend/tests/unit/test_config/test_validators.py
autonomous: true
requirements: [BUG-01]
must_haves:
  truths:
    - "All 16 failing tests pass when run with pytest"
    - "Test isolation is maintained via monkeypatch for environment variables"
    - "Mock signatures match current API contracts"
  artifacts:
    - path: "backend/tests/unit/test_external_bridge.py"
      provides: "ExternalPreconditionBridge tests with proper isolation"
    - path: "backend/tests/unit/test_browser_cleanup.py"
      provides: "Browser cleanup tests with updated mock signatures"
    - path: "backend/tests/unit/test_config/test_settings.py"
      provides: "Settings tests with monkeypatch isolation"
    - path: "backend/tests/unit/test_config/test_validators.py"
      provides: "Validator tests with monkeypatch isolation"
  key_links:
    - from: "test_external_bridge.py"
      to: "WEBSERP_PATH env var"
      via: "monkeypatch.delenv/setenv"
    - from: "test_browser_cleanup.py"
      to: "run_agent_background signature"
      via: "mock parameter matching"
---

<objective>
Fix 16 failing tests by ensuring proper test isolation via monkeypatch and updating mock signatures to match current API contracts.

Purpose: Establish a healthy test suite that can verify bug fixes correctly.
Output: All 16 previously failing tests now pass.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bug-fix-sprint/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix test_external_bridge.py isolation (7 tests)</name>
  <files>backend/tests/unit/test_external_bridge.py</files>
  <read_first>
    - backend/tests/unit/test_external_bridge.py (current test file)
    - backend/core/external_precondition_bridge.py (module under test)
  </read_first>
  <action>
    Fix 7 failing tests in test_external_bridge.py by using pytest monkeypatch for environment isolation:

    1. Add `monkeypatch` parameter to tests that check `is_available()` behavior
    2. For tests expecting unavailable state, use `monkeypatch.delenv('WEBSERP_PATH', raising=False)` at test start
    3. Reset bridge cache via `reset_cache()` after environment manipulation
    4. Tests to fix:
       - `test_is_available_returns_false_when_not_configured` - use monkeypatch.delenv
       - `test_get_unavailable_reason_returns_message` - use monkeypatch.delenv
       - `test_get_operations_grouped_returns_empty_when_unavailable` - use monkeypatch.delenv
       - `test_get_available_operations_returns_empty_when_unavailable` - use monkeypatch.delenv
       - `test_load_base_params_class_unavailable` - use monkeypatch.delenv
       - `test_get_data_methods_grouped_returns_empty_when_unavailable` - use monkeypatch.delenv
       - Ensure reset_cache() fixture runs AFTER monkeypatch setup

    Pattern to use:
    ```python
    def test_is_available_returns_false_when_not_configured(self, monkeypatch):
        monkeypatch.delenv('WEBSERP_PATH', raising=False)
        reset_cache()  # Clear cached state after env change
        result = is_available()
        assert result is False
    ```
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_external_bridge.py -v --tb=short 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest backend/tests/unit/test_external_bridge.py -v` exits with code 0
    - All test class methods show PASSED status
    - No test uses environment variables without monkeypatch isolation
  </acceptance_criteria>
  <done>All 7 test_external_bridge.py tests pass with proper isolation</done>
</task>

<task type="auto">
  <name>Task 2: Fix test_browser_cleanup.py mock signatures (2 tests)</name>
  <files>backend/tests/unit/test_browser_cleanup.py</files>
  <read_first>
    - backend/tests/unit/test_browser_cleanup.py (current test file)
    - backend/api/routes/runs.py (lines 55-64 for run_agent_background signature)
    - backend/core/agent_service.py (run_with_cleanup method signature)
  </read_first>
  <action>
    Fix 2 failing tests by updating mock signatures to match current API:

    1. Check `run_agent_background` function signature in runs.py:
       - Parameters: run_id, task_id, task_name, task_description, max_steps, preconditions, api_assertions, target_url

    2. Update test_run_agent_background_uses_cleanup_pattern in test_browser_cleanup.py:
       - Add missing parameters to the mock call: task_id, preconditions, api_assertions, target_url
       - Current test only passes: run_id, task_name, task_description, max_steps
       - Add: task_id="test-task-id", preconditions=None, api_assertions=None, target_url=None

    3. Ensure mock assertions verify correct parameters are passed
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_browser_cleanup.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest backend/tests/unit/test_browser_cleanup.py -v` exits with code 0
    - Both test classes show all tests PASSED
  </acceptance_criteria>
  <done>Both test_browser_cleanup.py tests pass with updated mock signatures</done>
</task>

<task type="auto">
  <name>Task 3: Fix test_config/test_settings.py isolation (5 tests)</name>
  <files>backend/tests/unit/test_config/test_settings.py</files>
  <read_first>
    - backend/tests/unit/test_config/test_settings.py (current test file)
    - backend/config/settings.py (Settings class)
  </read_first>
  <action>
    Fix 5 failing tests by using monkeypatch for proper environment isolation:

    1. Replace `patch.dict(os.environ, {}, clear=True)` with pytest monkeypatch
    2. Pattern change:
       - OLD: `with patch.dict(os.environ, {}, clear=True):`
       - NEW: Use `monkeypatch` fixture parameter

    3. For each test method:
       - Add `monkeypatch` parameter
       - Use `monkeypatch.delenv('WEBERP_PATH', raising=False)` to clear
       - Use `monkeypatch.setenv('WEBERP_PATH', value)` to set

    4. Tests to update:
       - test_weberp_path_default_none
       - test_weberp_path_from_env
       - test_weberp_path_optional_string

    5. Also check test_validators.py for similar issues
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_config/ -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest backend/tests/unit/test_config/ -v` exits with code 0
    - All test_config tests show PASSED status
  </acceptance_criteria>
  <done>All test_config tests pass with monkeypatch isolation</done>
</task>

<task type="auto">
  <name>Task 4: Fix test_config/test_validators.py isolation (2 tests)</name>
  <files>backend/tests/unit/test_config/test_validators.py</files>
  <read_first>
    - backend/tests/unit/test_config/test_validators.py (current test file)
    - backend/config/validators.py (validators module)
  </read_first>
  <action>
    Fix any remaining test_config failures by checking test_validators.py for similar isolation issues:

    1. Run the tests first to identify specific failures:
       ```bash
       uv run pytest backend/tests/unit/test_config/test_validators.py -v --tb=short
       ```

    2. If tests fail due to environment variable issues, apply the same monkeypatch pattern:
       - Add `monkeypatch` parameter to affected test methods
       - Use `monkeypatch.delenv('WEBERP_PATH', raising=False)` for tests expecting unconfigured state

    3. Specific tests that may need fixing:
       - Tests checking WEBERP_PATH validation behavior
       - Tests that assume specific environment state

    4. Run full test_config suite to verify all tests pass after fixes
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_config/test_validators.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest backend/tests/unit/test_config/test_validators.py -v` exits with code 0
    - All test_validators tests show PASSED status
  </acceptance_criteria>
  <done>All test_config/test_validators.py tests pass with proper isolation</done>
</task>

</tasks>

<verification>
Run all fixed tests together to verify no regressions:
```bash
uv run pytest backend/tests/unit/test_external_bridge.py backend/tests/unit/test_browser_cleanup.py backend/tests/unit/test_config/ -v
```
All tests must pass.
</verification>

<success_criteria>
- All 16 previously failing tests now pass
- Test isolation via monkeypatch is properly implemented
- Mock signatures match current API contracts
- No new test failures introduced
</success_criteria>

<output>
After completion, create `.planning/phases/22-bug-fix-sprint/22-01-SUMMARY.md`
</output>
