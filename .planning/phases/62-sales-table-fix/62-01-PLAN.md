---
phase: 62-sales-table-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/agent/dom_patch.py
  - backend/agent/prompts.py
autonomous: false
requirements:
  - DOM-PATCH-01
  - PROMPT-01
  - E2E-01

must_haves:
  truths:
    - "dom_patch.apply_dom_patch() calls 4 patches (existing 3 + new _patch_assign_interactive_indices)"
    - "Inputs with placeholder='销售金额' inside <td> cells get clickable indices in DOM snapshot"
    - "ENHANCED_SYSTEM_MESSAGE has Section 9 with ERP 表格单元格填写 guidance"
    - "Prompt guidance explicitly warns about 物流费用 vs 销售金额 confusion"
  artifacts:
    - path: "backend/agent/dom_patch.py"
      provides: "Extended DOM patch with _patch_assign_interactive_indices and 3 helper functions"
      min_lines: 60
    - path: "backend/agent/prompts.py"
      provides: "ENHANCED_SYSTEM_MESSAGE with Section 9 ERP table cell filling guidance"
      min_lines: 20
---

<objective>
Extend DOM Patch to make ERP table cell inputs visible in browser-use DOM snapshot, and add Section 9 to prompts for sales outbound table filling guidance.

Purpose: Fix the AI agent's inability to locate and fill the "销售金额" input field in the sales outbound page. Root cause: inputs inside table cells lack snapshot_node and are skipped during interactive index assignment. Fix: patch _assign_interactive_indices_and_mark_new_nodes to force visibility for table cell inputs with ERP-specific placeholders.
Output: Extended dom_patch.py with 4th patch + prompts.py with Section 9
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-sales-table-fix/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend dom_patch.py with 4th patch</name>
  <files>backend/agent/dom_patch.py</files>
  <read_first>
    - backend/agent/dom_patch.py (existing 3 patches)
    - .venv/lib/python3.11/site-packages/browser_use/dom/serializer/serializer.py lines 603-615, 653-670, 700-715
  </read_first>
  <action>
Add a 4th patch to `backend/agent/dom_patch.py` that forces visibility for ERP table cell inputs in `_assign_interactive_indices_and_mark_new_nodes`.

**Step 1: Add new constants**

```python
# ERP table cell input placeholder substrings
_ERP_TABLE_CELL_PLACEHOLDERS = frozenset({"销售金额", "物流费用", "备注", "单号", "数量"})
```

**Step 2: Add `_is_inside_table_cell` helper**

```python
def _is_inside_table_cell(node) -> bool:
    """Check if a SimplifiedNode is inside a table cell (<td> or <th>).

    Walks the original_node's parent chain looking for td/th tags.
    Uses original_node.parent_node (AccessibilityNode) not SimplifiedNode.parent.

    Args:
        node: A SimplifiedNode instance.

    Returns:
        True if the node is a descendant of <td> or <th>.
    """
    from browser_use.dom.service import NodeType
    current = getattr(node.original_node, "parent_node", None)
    while current is not None:
        tag_name = getattr(current, "tag_name", None)
        if tag_name and tag_name.lower() in ("td", "th"):
            return True
        current = getattr(current, "parent_node", None)
    return False
```

**Step 3: Add `_is_erp_table_cell_input` helper**

```python
def _is_erp_table_cell_input(node) -> bool:
    """Check if a SimplifiedNode is an ERP table cell input with relevant placeholder.

    Conditions: (1) inside <td>/<th>, (2) input tag, (3) has placeholder matching ERP fields.

    Args:
        node: A SimplifiedNode instance.

    Returns:
        True if the node is an ERP table cell input.
    """
    if not _is_inside_table_cell(node):
        return False

    original = getattr(node, "original_node", None)
    if original is None:
        return False

    tag_name = getattr(original, "tag_name", None)
    if not tag_name or tag_name.lower() != "input":
        return False

    attributes = getattr(original, "attributes", None)
    if not attributes:
        return False

    placeholder = attributes.get("placeholder", "")
    for erp_placeholder in _ERP_TABLE_CELL_PLACEHOLDERS:
        if erp_placeholder in placeholder:
            return True
    return False
```

**Step 4: Add `_patch_assign_interactive_indices` function**

```python
def _patch_assign_interactive_indices() -> None:
    """Patch _assign_interactive_indices_and_mark_new_nodes.

    Adds 'is_erp_table_cell_input' as a visibility exception in the
    should_make_interactive condition (line 704 of serializer.py).
    Without this, inputs inside <td> cells with sales/logistics placeholders
    are skipped because they lack snapshot_node in the AX tree.

    Pattern follows existing exceptions for file inputs (is_file_input) and
    shadow DOM elements (is_shadow_dom_element).
    """
    from browser_use.dom.serializer.serializer import DOMTreeSerializer

    original_method = DOMTreeSerializer._assign_interactive_indices_and_mark_new_nodes

    def patched_method(self, node) -> None:
        # Call original first to handle all standard cases
        original_method(self, node)

        # Skip if already marked interactive by original method
        if getattr(node, "is_interactive", False):
            return

        # Skip if not an ERP table cell input
        if not _is_erp_table_cell_input(node):
            return

        # Force interactive assignment for ERP table cell inputs
        node.is_interactive = True
        self._selector_map[node.original_node.backend_node_id] = node.original_node
        self._interactive_counter = getattr(self, "_interactive_counter", 0) + 1
        node.is_new = True
        logger.debug(
            "dom_patch: forced interactive for ERP table cell input "
            "placeholder=%s",
            getattr(node.original_node, "attributes", {}).get("placeholder", "")
        )

    DOMTreeSerializer._assign_interactive_indices_and_mark_new_nodes = patched_method
    logger.debug("dom_patch: patched _assign_interactive_indices_and_mark_new_nodes")
```

**Step 5: Update `apply_dom_patch()` to call new patch**

In the `apply_dom_patch()` function's try block (after `_patch_paint_order_remover()` and `_patch_should_exclude_child()`), add:
```python
_patch_assign_interactive_indices()
```

And update the logger.info to say "successfully applied 4 patches".

**Key implementation notes:**
- Use `node.original_node.parent_node` (AccessibilityNode, not SimplifiedNode) for parent chain walking
- Import `NodeType` inside the function (or use string comparison `"td"`/`"th"`)
- Call `original_method` first to preserve all existing behavior
- Only force interactive for nodes NOT already marked interactive by original
- Follow the exact pattern of existing file_input and shadow_dom_element exceptions
- Use `backend_node_id` from `node.original_node` for selector_map
- Handle `_interactive_counter` safely with `getattr(..., 0)` in case it doesn't exist
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && uv run python -c "
from backend.agent.dom_patch import apply_dom_patch, _is_erp_table_cell_input, _is_inside_table_cell
apply_dom_patch()
print('dom_patch imported and applied OK')

# Check the module has the new functions
import backend.agent.dom_patch as dp
assert hasattr(dp, '_is_erp_table_cell_input'), 'missing _is_erp_table_cell_input'
assert hasattr(dp, '_is_inside_table_cell'), 'missing _is_inside_table_cell'
assert hasattr(dp, '_patch_assign_interactive_indices'), 'missing _patch_assign_interactive_indices'
print('All new functions present: OK')
"</automated>
  </verify>
  <done>dom_patch.py extended with 4th patch and 3 new helper functions. All functions importable and apply_dom_patch() succeeds.</done>
  <acceptance_criteria>
    - dom_patch.py contains _is_erp_table_cell_input, _is_inside_table_cell, _patch_assign_interactive_indices
    - apply_dom_patch() calls all 4 patches and logs "4 patches"
    - No import errors when importing dom_patch
    - New functions have correct docstrings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Section 9 to ENHANCED_SYSTEM_MESSAGE</name>
  <files>backend/agent/prompts.py</files>
  <read_first>
    - backend/agent/prompts.py (existing 8 sections)
  </read_first>
  <action>
Add a 9th section to ENHANCED_SYSTEM_MESSAGE in `backend/agent/prompts.py` specifically for ERP table cell input filling.

**Location:** After the existing Section 8 (文件上传) closing.

**Section 9 content:**

```python
## 9. ERP 表格单元格填写
销售出库等页面的表格中，每个商品行有多个可编辑单元格（销售金额、物流费用等）。
这些单元格的 `<input>` 在 DOM 快照中可能不易被识别，需要特殊策略：

**单元格定位：**
- 用 placeholder 精确匹配目标输入框：`placeholder="销售金额"`, `placeholder="物流费用"`
- 不要用 DOM index 定位（同一列可能有多个相同 placeholder 的输入框）
- 添加商品后，该商品的行会出现新的可编辑单元格，找到对应 placeholder 的 input

**row定位技巧：**
- 商品名称（I01781131295568）所在行 → 该行的"销售金额"单元格
- 如果不确定，先点击包含商品名称的单元格激活编辑，再在附近找 input

**禁止行为：**
- 不要点击 `<td>` 本身（td 元素不是交互元素，会误命中）
- 不要混淆"物流费用"和"销售金额"——两者是不同字段
- 不要对非当前商品的行进行操作（先确认商品名称列）

**evaluate JS fallback：**
- 如果标准 input action 失败，可用 `document.querySelector('input[placeholder="销售金额"]').value = '150'`
- 填写后必须用 `input.value` 验证值已写入，不要假设成功
```

**Placement:** Insert before the closing `"""` of ENHANCED_SYSTEM_MESSAGE, after Section 8 content.

**Verify:** Check that `CHINESE_ENHANCEMENT` alias still works (it's the same as ENHANCED_SYSTEM_MESSAGE).
  </action>
  <verify>
    <automated>cd /Users/huhu/project/weberpagent && uv run python -c "
from backend.agent.prompts import ENHANCED_SYSTEM_MESSAGE, CHINESE_ENHANCEMENT
assert ENHANCED_SYSTEM_MESSAGE is CHINESE_ENHANCEMENT, 'alias broken'
assert '## 9.' in ENHANCED_SYSTEM_MESSAGE, 'Section 9 missing'
assert '销售金额' in ENHANCED_SYSTEM_MESSAGE, '销售金额 guidance missing'
assert '物流费用' in ENHANCED_SYSTEM_MESSAGE, '物流费用 warning missing'
assert 'placeholder' in ENHANCED_SYSTEM_MESSAGE, 'placeholder guidance missing'
print('prompts.py Section 9: OK')
print('Total sections:', ENHANCED_SYSTEM_MESSAGE.count('## '))
"</automated>
  </verify>
  <done>prompts.py ENHANCED_SYSTEM_MESSAGE extended with Section 9 ERP table cell filling guidance. Alias CHINESE_ENHANCEMENT still works.</done>
  <acceptance_criteria>
    - ENHANCED_SYSTEM_MESSAGE contains "## 9. ERP 表格单元格填写"
    - Guidance mentions 销售金额, 物流费用, placeholder matching, row targeting
    - CHINESE_ENHANCEMENT alias still valid (same object)
    - Section 9 has negative examples (don't click td, don't confuse fields)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: E2E verification — sales outbound sales amount filling</name>
  <read_first>
    - docs/test-steps/v0.8.0-综合验证结果.md (reference format)
  </read_first>
  <action>
**Prerequisites (Claude automates):**
1. Start backend server: `cd /Users/huhu/project/weberpagent && uv run uvicorn backend.api.main:app --reload --port 8080`
2. Start frontend dev server: `cd frontend && npm run dev`

**User verification steps:**

Create a new test task with this description:
"进入销售管理模块，点击新建销售出库单，选择客户，输入商品 I01781131295568，填写销售金额为 150，填写物流单号为 TEST123，提交并验证提交成功"

1. **执行任务**: 点击"执行"开始任务
2. **观察关键步骤**:
   - 商品 I01781131295568 是否成功添加到表格
   - Agent 是否能定位到该行的"销售金额"输入框（观察 DOM 查询或 placeholder 定位）
   - 销售金额是否从 0.00 变为 150（不再出现"填写失败"或"值未改变"）
   - Agent 是否误填"物流费用"为 150（应该填物流单号 TEST123）
3. **记录结果**: PASS（销售金额=150，物流单号=TEST123）/ FAIL（任一步骤失败）
  </action>
  <how-to-verify>
    A. Create and execute sales outbound task
    B. Watch execution monitor for:
       - Product addition: "商品 I01781131295568" → appears in table
       - Sales amount fill: "input placeholder=销售金额" → value=150
       - Logistics fill: "input placeholder=物流单号" → value=TEST123
       - Submit: Click "确认" or "提交"
    C. Check final report: Did sales amount show 150? Did logistics show TEST123?
  </how-to-verify>
  <resume-signal>
Type "PASS" if the sales amount was correctly filled to 150 and the logistics number was set to TEST123.
Type "FAIL" followed by which step failed and error details.
  </resume-signal>
  <acceptance_criteria>
    - User has manually executed sales outbound ERP scenario through platform UI
    - User reports PASS/FAIL for the key steps (sales amount fill, logistics fill, submit)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. dom_patch.py: apply_dom_patch() applies all 4 patches, no import errors
2. prompts.py: Section 9 present, ENHANCED_SYSTEM_MESSAGE and CHINESE_ENHANCEMENT valid
3. E2E: User manually verified sales outbound scenario PASS
</verification>

<success_criteria>
- DOM Patch extended with 4th patch targeting _assign_interactive_indices_and_mark_new_nodes
- _is_erp_table_cell_input and _is_inside_table_cell helper functions working
- ENHANCED_SYSTEM_MESSAGE has Section 9 with ERP table cell filling guidance
- Sales outbound E2E: sales amount correctly filled to 150, no field confusion
- Phase 62 complete — v0.8.1 milestone goal achieved
</success_criteria>

<output>
After completion, create `.planning/phases/62-sales-table-fix/62-SUMMARY.md`
</output>
