---
phase: 46-code-simplification-and-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_scroll_table_tool.py
  - backend/tests/e2e/test_scroll_table_e2e.py
autonomous: true
requirements:
  - TEST-01
user_setup: []
must_haves:
  truths:
    - "test_scroll_table_tool.py does not exist in backend/tests/unit/"
    - "test_scroll_table_e2e.py does not exist in backend/tests/e2e/"
    - "All remaining tests pass without import errors"
  artifacts:
    - path: "backend/tests/unit/test_scroll_table_tool.py"
      provides: "File should not exist"
      exists: false
    - path: "backend/tests/e2e/test_scroll_table_e2e.py"
      provides: "File should not exist"
      exists: false
  key_links:
    - from: "pytest collection"
      to: "backend/tests/"
      via: "uv run pytest --collect-only"
      pattern: "no import errors"
---

<objective>
Delete obsolete scroll_table test files that reference the deleted backend.agent.tools module.

Purpose: Remove test files that were deferred from Phase 45. These files import from `backend.agent.tools.scroll_table_tool` which was deleted in Phase 45, causing import errors during test collection.

Output: Two test files deleted, all remaining tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-code-simplification-and-testing/46-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete obsolete scroll_table test files</name>
  <files>
    - backend/tests/unit/test_scroll_table_tool.py
    - backend/tests/e2e/test_scroll_table_e2e.py
  </files>
  <read_first>
    - backend/tests/unit/test_scroll_table_tool.py (to confirm it imports from deleted module)
    - backend/tests/e2e/test_scroll_table_e2e.py (to confirm it imports from deleted module)
  </read_first>
  <action>
    Delete the following two files that reference the deleted `backend.agent.tools.scroll_table_tool` module:

    1. `backend/tests/unit/test_scroll_table_tool.py` (177 lines)
       - Contains TestScrollTableInputParams, TestScrollTableAndInput, TestToolRegistration classes
       - Line 11: `from backend.agent.tools.scroll_table_tool import ScrollTableInputParams`
       - Line 55-58: imports scroll_table_and_input

    2. `backend/tests/e2e/test_scroll_table_e2e.py` (175 lines)
       - Contains TestScrollTableE2EDocumentation class
       - Line 81-84: imports from deleted module

    These files were deferred from Phase 45 cleanup per plan 45-05 decision.
    Execute: `rm backend/tests/unit/test_scroll_table_tool.py backend/tests/e2e/test_scroll_table_e2e.py`
  </action>
  <verify>
    <automated>test ! -f backend/tests/unit/test_scroll_table_tool.py && test ! -f backend/tests/e2e/test_scroll_table_e2e.py && echo "Files deleted successfully"</automated>
  </verify>
  <done>
    - File backend/tests/unit/test_scroll_table_tool.py does not exist
    - File backend/tests/e2e/test_scroll_table_e2e.py does not exist
    - Command `ls backend/tests/unit/test_scroll_table_tool.py` returns "No such file or directory"
    - Command `ls backend/tests/e2e/test_scroll_table_e2e.py` returns "No such file or directory"
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all remaining tests pass</name>
  <files>
    - backend/tests/
  </files>
  <read_first>
    - backend/tests/test_agent_service.py (to confirm it doesn't import deleted modules)
    - backend/tests/unit/test_agent_service.py (to confirm it doesn't import deleted modules)
  </read_first>
  <action>
    Run the full test suite to verify no import errors and all tests pass:

    1. First, verify test collection succeeds without errors:
       `uv run pytest backend/tests/ --collect-only`

    2. Then run all tests:
       `uv run pytest backend/tests/ -v --tb=short`

    The test files that should remain and pass:
    - backend/tests/test_agent_service.py - AgentService basic tests
    - backend/tests/unit/test_agent_service.py - LLM temperature configuration tests (TestLLMTemperature class)

    Expected result: All tests collected and pass without ModuleNotFoundError.
  </action>
  <verify>
    <automated>uv run pytest backend/tests/ --collect-only 2>&1 | grep -c "ERROR" | xargs -I {} sh -c 'test {} -eq 0 && echo "No collection errors"'</automated>
  </verify>
  <done>
    - `uv run pytest backend/tests/ --collect-only` shows 0 errors
    - `uv run pytest backend/tests/ -v` exits with code 0
    - No "ModuleNotFoundError: No module named 'backend.agent.tools'" in output
  </done>
</task>

</tasks>

<verification>
1. Both scroll_table test files are deleted
2. Test collection succeeds without import errors
3. All remaining unit tests pass
</verification>

<success_criteria>
1. test_scroll_table_tool.py file does not exist in backend/tests/unit/
2. test_scroll_table_e2e.py file does not exist in backend/tests/e2e/
3. uv run pytest backend/tests/ --collect-only shows 0 errors
4. uv run pytest backend/tests/ exits with code 0
</success_criteria>

<output>
After completion, create `.planning/phases/46-code-simplification-and-testing/46-01-SUMMARY.md`
</output>
