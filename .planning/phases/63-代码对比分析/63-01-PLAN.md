---
phase: 63-代码对比分析
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/63-代码对比分析/63-01-comparison-result.md
autonomous: true
requirements:
  - DIFF-01
  - DIFF-02

must_haves:
  truths:
    - "v0.4.0 和当前版本的 Agent 构造参数逐项对比结果已记录，每项一行"
    - "Playwright headless/headed 配置差异已明确标识，headless=True 的引入提交已标注"
    - "run_simple 和 run_with_streaming 两个方法的参数差异分别列出"
    - "Chrome 启动参数差异（SERVER_BROWSER_ARGS + browser-use 内部 CHROME_HEADLESS_ARGS）已记录"
    - "headless vs headed 模式下 Ant Design 表格 DOM 渲染差异的分析结论已记录"
  artifacts:
    - path: ".planning/phases/63-代码对比分析/63-01-comparison-result.md"
      provides: "Agent/Playwright 配置逐项对比表 + headless DOM 渲染差异分析"
      contains: "run_simple"
  key_links:
    - from: "63-01-comparison-result.md"
      to: "git show v0.4.0:backend/core/agent_service.py"
      via: "git snapshot comparison"
      pattern: "v0.4.0"
    - from: "63-01-comparison-result.md"
      to: "backend/core/agent_service.py"
      via: "current HEAD file comparison"
      pattern: "create_browser_session"
---

<objective>
逐项对比 v0.4.0 与当前版本（HEAD）的 browser-use Agent 构造参数和 Playwright 配置，产出结构化对比表。同时分析 headless vs headed 模式下 DOM 渲染差异。

Purpose: 为 Phase 64 报告提供精确的配置差异数据，识别 headless=True 为根因（per D-01, D-02, D-03）。
Output: 63-01-comparison-result.md，包含 run_simple/run_with_streaming 对比表、Playwright 配置表、headless DOM 渲染分析。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-代码对比分析/63-CONTEXT.md
@.planning/phases/63-代码对比分析/63-RESEARCH.md

<interfaces>
<!-- Key code patterns executor needs for comparison -->

v0.4.0 Agent creation pattern (from git show v0.4.0:backend/core/agent_service.py):
```python
# run_simple
agent = Agent(task=task, llm=llm, max_actions_per_step=5)

# run_with_streaming
agent = Agent(task=actual_task, llm=llm, max_actions_per_step=5, register_new_step_callback=step_callback)
```

Current HEAD Agent creation pattern (from backend/core/agent_service.py):
```python
# run_simple
browser_session = create_browser_session()  # BrowserProfile(headless=True, ...)
agent = Agent(task=task, llm=llm, browser_session=browser_session, max_actions_per_step=5)

# run_with_streaming
browser_session = create_browser_session()
agent = MonitoredAgent(task=..., llm=..., browser_session=browser_session,
    max_actions_per_step=5, register_new_step_callback=step_callback,
    extend_system_message=ENHANCED_SYSTEM_MESSAGE, loop_detection_window=10,
    max_failures=4, planning_replan_on_stall=2, enable_planning=True,
    stall_detector=..., pre_submit_guard=..., task_progress_tracker=..., run_logger=...)
```

create_browser_session() current (from backend/core/agent_service.py):
```python
SERVER_BROWSER_ARGS = [
    '--no-sandbox', '--disable-setuid-sandbox', '--disable-dev-shm-usage',
    '--disable-gpu', '--disable-software-rasterizer', '--disable-extensions',
]
def create_browser_session() -> BrowserSession:
    from browser_use.browser.profile import ViewportSize
    browser_profile = BrowserProfile(
        headless=True, args=SERVER_BROWSER_ARGS,
        viewport=ViewportSize(width=1920, height=1080),
    )
    return BrowserSession(browser_profile=browser_profile)
```

browser-use internal headless resolution (from .venv/lib/.../browser_use/browser/profile.py):
```python
# line 1176-1178: if no headless preference, prefer headful if display available
if self.headless is None:
    self.headless = not has_screen_available

# line 80-82: CHROME_HEADLESS_ARGS applied when headless=True
CHROME_HEADLESS_ARGS = ['--headless=new']
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run git commands and produce Agent/Playwright configuration comparison tables</name>
  <files>.planning/phases/63-代码对比分析/63-01-comparison-result.md</files>
  <read_first>
    backend/core/agent_service.py
    .planning/phases/63-代码对比分析/63-CONTEXT.md
    .planning/phases/63-代码对比分析/63-RESEARCH.md
  </read_first>
  <action>
    Create the file `.planning/phases/63-代码对比分析/63-01-comparison-result.md` with the following structured content.

    **Step 1: Retrieve v0.4.0 snapshot**
    Run: `git show v0.4.0:backend/core/agent_service.py`
    Extract the Agent constructor calls in `run_simple` and `run_with_streaming` methods.

    **Step 2: Read current HEAD**
    Read `backend/core/agent_service.py` (already in read_first).
    Extract the current Agent constructor calls in both methods.

    **Step 3: Produce run_simple comparison table (per D-01)**
    Format: `| 配置项 | v0.4.0 值 | 当前值 | 变更提交 |`
    Rows MUST include:
    - Agent class: `Agent` vs `Agent`
    - task: `task` vs `task`
    - llm: `create_llm(config)` vs `create_llm(config)`
    - browser_session: `None (未传递)` vs `create_browser_session()`
    - max_actions_per_step: `5` vs `5`
    - **Headless 模式**: `Auto-detect (macOS 下为 headed)` vs `显式 True`
    - Chrome args: `None (browser-use 默认)` vs `6 个 SERVER_BROWSER_ARGS + --headless=new`
    - Viewport: `None (自动)` vs `1920x1080`

    **Step 4: Produce run_with_streaming comparison table (per D-01)**
    Same format, rows MUST include all run_simple rows PLUS:
    - Agent class: `Agent` vs `MonitoredAgent`
    - register_new_step_callback: `step_callback` vs `step_callback`
    - extend_system_message: `None` vs `ENHANCED_SYSTEM_MESSAGE`
    - loop_detection_window: `20 (browser-use 默认)` vs `10`
    - max_failures: `5 (browser-use 默认)` vs `4`
    - planning_replan_on_stall: `3 (browser-use 默认)` vs `2`
    - enable_planning: `True (browser-use 默认)` vs `True (显式传递)`
    - stall_detector: `N/A` vs `StallDetector()`
    - pre_submit_guard: `N/A` vs `PreSubmitGuard()`
    - task_progress_tracker: `N/A` vs `TaskProgressTracker()`
    - run_logger: `N/A` vs `RunLogger(run_id)`
    - available_file_paths: `N/A` vs `scan_test_files()`
    - DOM patch: `N/A` vs `apply_dom_patch()`
    - self._browser_session: `N/A` vs `stored for callback access`

    **Step 5: Produce Playwright/Browser 配置对比表 (DIFF-02)**
    Format: `| 配置项 | v0.4.0 | 当前 | 来源 |`
    Rows MUST include:
    - headless: `None (auto-detect)` vs `True (显式)` — 来源 `f951791`
    - BrowserProfile class: `未使用` vs `BrowserProfile(headless=True, ...)` — 来源 `f951791`
    - BrowserSession class: `未使用` vs `BrowserSession(browser_profile=...)` — 来源 `f951791`
    - Chrome args (项目): `无` vs `--no-sandbox, --disable-setuid-sandbox, --disable-dev-shm-usage, --disable-gpu, --disable-software-rasterizer, --disable-extensions` — 来源 `f951791`
    - Chrome args (browser-use): `无` vs `--headless=new` (CHROME_HEADLESS_ARGS, 当 headless=True 时自动附加)` — 来源 `browser-use 内部`
    - ViewportSize: `None (自动)` vs `1920x1080` — 来源 `c84f4e1`
    - 最终 Chrome 启动参数总计: `0 个` vs `7 个` (6 项目 + 1 browser-use)

    **Step 6: Add root cause summary**
    Write a "## 根因分析" section stating:
    - 核心变更提交: `f951791` (2026-03-24) — 为 Linux 服务器部署添加 BrowserSession(headless=True)
    - 影响: `create_browser_session()` 被所有调用路径共享，本地开发也使用 headless=True
    - browser-use 默认行为: `headless=None` 时自动检测环境（macOS=headed, Linux server=headless），但显式 `headless=True` 覆盖了自动检测
    - 次要变更: MonitoredAgent/监控参数/viewport 均为增量变更，不影响浏览器模式

    Use change commit hashes from RESEARCH.md:
    - f951791: BrowserSession(headless=True) introduced
    - c84f4e1: ViewportSize added
    - e2157a1: Agent -> MonitoredAgent
    - 9fc9f44: ENHANCED_SYSTEM_MESSAGE + tuned params
    - 380be48: RunLogger
    - 20efd65: detector wiring
    - b586b54: apply_dom_patch
    - 7ba3f6b: scan_test_files + available_file_paths
  </action>
  <verify>
    <automated>test -f ".planning/phases/63-代码对比分析/63-01-comparison-result.md" && grep -c "配置项" ".planning/phases/63-代码对比分析/63-01-comparison-result.md"</automated>
  </verify>
  <done>
    - 63-01-comparison-result.md exists
    - Contains "## run_simple 方法对比" section with table
    - Contains "## run_with_streaming 方法对比" section with table
    - Contains "## Playwright/Browser 配置对比" section with table
    - Contains "## 根因分析" section identifying f951791 as root cause
    - All table rows use format: 配置项 | v0.4.0 值 | 当前值 | 变更提交
    - headless 行明确标注 f951791 为根因提交
  </done>
</task>

<task type="auto">
  <name>Task 2: Analyze headless vs headed DOM rendering differences for Ant Design tables</name>
  <files>.planning/phases/63-代码对比分析/63-01-comparison-result.md</files>
  <read_first>
    backend/agent/dom_patch.py
    .planning/phases/63-代码对比分析/63-RESEARCH.md
  </read_first>
  <action>
    **APPEND** to the existing `63-01-comparison-result.md` file (do NOT overwrite Task 1 content).

    Add a "## Headless vs Headed DOM 渲染差异分析 (D-03)" section with the following subsections:

    **### Chromium --headless=new 渲染引擎**
    State:
    - Chromium 112+ 的 "new headless" 模式使用与 headed Chrome 相同的 Blink 渲染引擎
    - 静态内容的 DOM 树结构完全相同
    - 引用: Chromium 官方文档

    **### 交互状态差异**
    List:
    1. **焦点/悬停状态:** Headless 模式无真实鼠标光标，Playwright/browser-use 通过程序模拟点击事件。CSS :hover 和 :focus 伪类触发方式可能不同。
    2. **Ant Design click-to-edit 行为:** Ant Design 可编辑表格通常在 `<td>` 单元格显示文本内容，点击后显示 `<input>` 元素。这是 JavaScript 驱动的交互。在 headless 模式下:
       - 点击事件会被分发 (Playwright 处理)
       - input 应该出现在 DOM 中 (JavaScript 正常运行)
       - 但: 时序差异可能导致 browser-use DOM 序列化器在 input 完全渲染前捕获状态
    3. **可访问性树差异:** Headless Chromium 可能构建略有不同的可访问性树。browser-use 使用可访问性树进行元素索引。"屏幕外" 或尺寸为零的元素可能在 headless 模式下被排除在 AX 树之外。

    **### DOM Patch 评估**
    Read `backend/agent/dom_patch.py` and describe what each of the 5 patches addresses:
    - Patch 1: 恢复 ERP 元素的绘制顺序
    - Patch 2: 标记含文本内容的 `<td>` 单元格为可交互
    - Patch 3: 防止 ERP 节点被边界框过滤排除
    - Patch 4: 强制为 ERP 表格单元格 input 分配交互属性
    - Patch 5: (describe based on actual code)

    **### 评估结论**
    Write a summary assessment:
    - v0.8.1 的 DOM Patch 是合理的绕行方案 (MEDIUM confidence)
    - 在 DOM 序列化层面操作，不依赖 headed 模式特性
    - 确保元素在 DOM dump 中可见，无论渲染模式
    - 建议: 在 headed 模式下重新测试以确认根因（超出本阶段范围，记录在 Phase 64 建议）

    **### 置信度表**
    Include a confidence table:
    | 声明 | 置信度 | 来源 |
    |------|--------|------|
    | Headless 使用相同渲染引擎 | HIGH | Chromium 官方文档 |
    | 焦点/悬停模拟有差异 | HIGH | Playwright 文档 + Chromium 架构 |
    | 静态内容 DOM 树相同 | HIGH | Chromium 官方文档 |
    | Click-to-edit input 时序可能有差异 | MEDIUM | 渲染管线行为推断 |
    | DOM Patch 是合理绕行方案 | MEDIUM | dom_patch.py 代码分析 |
  </action>
  <verify>
    <automated>grep -c "Headless vs Headed" ".planning/phases/63-代码对比分析/63-01-comparison-result.md" && grep -c "DOM Patch" ".planning/phases/63-代码对比分析/63-01-comparison-result.md"</automated>
  </verify>
  <done>
    - 63-01-comparison-result.md contains "## Headless vs Headed DOM 渲染差异分析 (D-03)" section
    - Section covers: rendering engine, interaction state differences, Ant Design click-to-edit analysis
    - DOM Patch 评估 lists all 5 patches from dom_patch.py
    - Contains confidence table with at least 5 rows
    - Assessment conclusion states DOM Patch is reasonable workaround with MEDIUM confidence
  </done>
</task>

</tasks>

<verification>
1. `test -f .planning/phases/63-代码对比分析/63-01-comparison-result.md` — file exists
2. `grep -c "run_simple" .planning/phases/63-代码对比分析/63-01-comparison-result.md` — contains run_simple comparison
3. `grep -c "run_with_streaming" .planning/phases/63-代码对比分析/63-01-comparison-result.md` — contains run_with_streaming comparison
4. `grep -c "f951791" .planning/phases/63-代码对比分析/63-01-comparison-result.md` — root cause commit identified
5. `grep -c "DOM" .planning/phases/63-代码对比分析/63-01-comparison-result.md` — D-03 analysis included
</verification>

<success_criteria>
- 63-01-comparison-result.md exists with 4 main sections: run_simple table, run_with_streaming table, Playwright config table, headless DOM analysis
- All tables use D-01 format: 配置项 | v0.4.0 值 | 当前值 | 变更提交
- Root cause commit f951791 clearly identified
- DOM rendering analysis covers Chromium --headless=new behavior, Ant Design specifics, and DOM Patch assessment
</success_criteria>

<output>
After completion, create `.planning/phases/63-代码对比分析/63-01-SUMMARY.md`
</output>
