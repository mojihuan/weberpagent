---
phase: 03-service-layer-restoration
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/unit/test_assertion_service.py
  - backend/tests/unit/test_assertion_result_repo.py
  - backend/tests/unit/test_report_service.py
  - backend/tests/unit/test_event_manager.py
  - backend/tests/unit/test_llm_retry.py
  - backend/tests/integration/test_runs_background.py
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "All test stub files exist and are importable"
    - "Each test file has skip markers pointing to implementing plans"
  artifacts:
    - path: "backend/tests/unit/test_assertion_service.py"
      provides: "Test stubs for AssertionService with ORM"
    - path: "backend/tests/unit/test_assertion_result_repo.py"
      provides: "Test stubs for AssertionResultRepository"
    - path: "backend/tests/unit/test_report_service.py"
      provides: "Test stubs for ReportService"
    - path: "backend/tests/unit/test_event_manager.py"
      provides: "Test stubs for SSE heartbeat"
    - path: "backend/tests/unit/test_llm_retry.py"
      provides: "Test stubs for LLM retry logic"
    - path: "backend/tests/integration/test_runs_background.py"
      provides: "Test stubs for background task status updates"
  key_links: []
---

<objective>
Create Wave 0 test scaffolding for Phase 3 service layer components.

Purpose: Establish test stubs before implementation to enable TDD workflow.
Output: Six test stub files with skip markers pointing to implementing plans.
</objective>

<execution_context>
@/Users/huhu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/huhu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types from codebase that tests will reference -->

From backend/db/models.py:
```python
class AssertionResult(Base):
    id: str
    run_id: str  # ForeignKey to runs.id
    assertion_id: str  # ForeignKey to assertions.id
    status: str  # "pass" or "fail"
    message: Optional[str]
    actual_value: Optional[str]
    created_at: datetime

class Report(Base):
    id: str
    run_id: str
    task_id: str
    task_name: str
    status: str
    total_steps: int
    success_steps: int
    failed_steps: int
    duration_ms: int
    created_at: datetime
```

From backend/db/repository.py (existing pattern):
```python
class TaskRepository:
    def __init__(self, session: AsyncSession): ...
    async def create(...) -> Task: ...
    async def get(...) -> Optional[Task]: ...

class RunRepository:
    async def get_steps(run_id: str) -> List[Step]: ...
    async def update_status(run_id: str, status: str) -> Optional[Run]: ...
```

From backend/core/event_manager.py:
```python
class EventManager:
    async def publish(run_id: str, event: str | None) -> None: ...
    async def subscribe(run_id: str) -> AsyncGenerator[str | None, None]: ...
    def set_status(run_id: str, status: str) -> None: ...
    def is_finished(run_id: str) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AssertionService test stub</name>
  <files>backend/tests/unit/test_assertion_service.py</files>
  <action>
    Create test stub file with skip markers for SVC-01 tests.

    Include test stubs for:
    - test_evaluate_assertions_returns_orm_objects: Skip with "Implementing in 03-01"
    - test_url_contains_assertion: Skip with "Implementing in 03-01"
    - test_text_exists_assertion: Skip with "Implementing in 03-01"
    - test_no_errors_assertion: Skip with "Implementing in 03-01"
    - test_element_exists_assertion: Skip with "Implementing in 03-01"
    - test_failed_assertion_records_message: Skip with "Implementing in 03-01"

    Follow existing test patterns from backend/tests/unit/test_models.py.
    Use pytest.mark.skip decorator with reason pointing to plan 03-01.
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_assertion_service.py --collect-only -q 2>&1 | grep -c "test_" | head -1</automated>
  </verify>
  <done>Test file exists with 6 skipped test functions</done>
</task>

<task type="auto">
  <name>Task 2: Create AssertionResultRepository test stub</name>
  <files>backend/tests/unit/test_assertion_result_repo.py</files>
  <action>
    Create test stub file for AssertionResultRepository (NEW class to be added to repository.py).

    Include test stubs for:
    - test_create_assertion_result: Skip with "Implementing in 03-01"
    - test_list_by_run: Skip with "Implementing in 03-01"

    Follow existing repository test patterns from backend/tests/unit/test_repository.py.
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_assertion_result_repo.py --collect-only -q 2>&1 | grep -c "test_" | head -1</automated>
  </verify>
  <done>Test file exists with 2 skipped test functions</done>
</task>

<task type="auto">
  <name>Task 3: Create ReportService test stub</name>
  <files>backend/tests/unit/test_report_service.py</files>
  <action>
    Create test stub file for NEW ReportService class.

    Include test stubs for:
    - test_generate_report_with_all_details: Skip with "Implementing in 03-02"
    - test_report_includes_assertion_results: Skip with "Implementing in 03-02"
    - test_report_includes_steps: Skip with "Implementing in 03-02"
    - test_report_pass_rate_calculation: Skip with "Implementing in 03-02"

    Follow existing service test patterns.
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_report_service.py --collect-only -q 2>&1 | grep -c "test_" | head -1</automated>
  </verify>
  <done>Test file exists with 4 skipped test functions</done>
</task>

<task type="auto">
  <name>Task 4: Add SSE heartbeat test stub to existing test file</name>
  <files>backend/tests/unit/test_event_manager.py</files>
  <action>
    Add heartbeat test stub to existing test_event_manager.py file.

    Add test stub:
    - test_heartbeat_sent_periodically: Skip with "Implementing in 03-04"

    The existing test file already has other tests. Only add the heartbeat test stub.
  </action>
  <verify>
    <automated>grep -c "test_heartbeat" backend/tests/unit/test_event_manager.py</automated>
  </verify>
  <done>Heartbeat test stub added to existing test file</done>
</task>

<task type="auto">
  <name>Task 5: Create LLM retry test stub</name>
  <files>backend/tests/unit/test_llm_retry.py</files>
  <action>
    Create test stub file for LLM retry logic in factory.py.

    Include test stubs for:
    - test_retry_on_timeout: Skip with "Implementing in 03-04"
    - test_retry_on_rate_limit: Skip with "Implementing in 03-04"
    - test_no_retry_on_auth_error: Skip with "Implementing in 03-04"
    - test_exponential_backoff: Skip with "Implementing in 03-04"
    - test_max_retries_exceeded: Skip with "Implementing in 03-04"

    Note: LLM retry is bundled with SSE heartbeat in plan 03-04 per RESEARCH.md grouping.
  </action>
  <verify>
    <automated>uv run pytest backend/tests/unit/test_llm_retry.py --collect-only -q 2>&1 | grep -c "test_" | head -1</automated>
  </verify>
  <done>Test file exists with 5 skipped test functions</done>
</task>

<task type="auto">
  <name>Task 6: Create background task status test stub</name>
  <files>backend/tests/integration/test_runs_background.py</files>
  <action>
    Create integration test stub for background task status updates (SVC-05).

    Include test stubs for:
    - test_run_status_updated_on_success: Skip with "Implementing in 03-05"
    - test_run_status_updated_on_error: Skip with "Implementing in 03-05"
    - test_error_event_published_on_failure: Skip with "Implementing in 03-05"

    Follow integration test patterns from backend/tests/integration/ directory.
  </action>
  <verify>
    <automated>uv run pytest backend/tests/integration/test_runs_background.py --collect-only -q 2>&1 | grep -c "test_" | head -1</automated>
  </verify>
  <done>Test file exists with 3 skipped test functions</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `uv run pytest backend/tests/unit/ --collect-only -q` to verify all test stubs are collected
2. Run `uv run pytest backend/tests/integration/ --collect-only -q` to verify integration test stubs
3. Verify skip reasons reference correct plan numbers (03-01, 03-02, 03-04, 03-05)
</verification>

<success_criteria>
- 6 test stub files created (5 new, 1 modified)
- Total of 21 skipped test functions across all files
- All tests collectable by pytest
- Skip markers reference correct implementing plans
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-layer-restoration/03-00-SUMMARY.md`
</output>
